---
read_when:
    - Ви хочете безпечно оновити source checkout
    - Вам потрібно зрозуміти скорочену поведінку `--update`
summary: Довідник CLI для `openclaw update` (відносно безпечне оновлення вихідного коду + автоматичний перезапуск gateway)
title: Оновлення
x-i18n:
    generated_at: "2026-04-23T20:49:00Z"
    model: gpt-5.4
    provider: openai
    source_hash: 1f5dcd97c9bc426c8d6ef7979976a27ad90729c692435d5716e09069d7d08266
    source_path: cli/update.md
    workflow: 15
---

# `openclaw update`

Безпечно оновлюйте OpenClaw і перемикайтеся між каналами stable/beta/dev.

Якщо ви встановили через **npm/pnpm/bun** (глобальне встановлення, без метаданих git),
оновлення відбуваються через потік менеджера пакетів у [Оновлення](/uk/install/updating).

## Використання

```bash
openclaw update
openclaw update status
openclaw update wizard
openclaw update --channel beta
openclaw update --channel dev
openclaw update --tag beta
openclaw update --tag main
openclaw update --dry-run
openclaw update --no-restart
openclaw update --yes
openclaw update --json
openclaw --update
```

## Параметри

- `--no-restart`: пропустити перезапуск служби Gateway після успішного оновлення.
- `--channel <stable|beta|dev>`: встановити канал оновлення (git + npm; зберігається в конфігурації).
- `--tag <dist-tag|version|spec>`: перевизначити ціль пакета лише для цього оновлення. Для пакетних встановлень `main` відображається в `github:openclaw/openclaw#main`.
- `--dry-run`: попередньо переглянути заплановані дії оновлення (канал/теґ/ціль/потік перезапуску) без запису конфігурації, встановлення, синхронізації Plugin або перезапуску.
- `--json`: вивести машиночитний JSON `UpdateRunResult`, зокрема
  `postUpdate.plugins.integrityDrifts`, коли під час післяоновлювальної синхронізації Plugin
  виявлено розходження цілісності артефактів npm Plugin.
- `--timeout <seconds>`: тайм-аут для кожного кроку (типово 1200 с).
- `--yes`: пропустити запити на підтвердження (наприклад, підтвердження зниження версії)

Примітка: зниження версії вимагає підтвердження, оскільки старіші версії можуть зламати конфігурацію.

## `update status`

Показує активний канал оновлення + git tag/branch/SHA (для source checkout), а також доступність оновлення.

```bash
openclaw update status
openclaw update status --json
openclaw update status --timeout 10
```

Параметри:

- `--json`: вивести машиночитний JSON стану.
- `--timeout <seconds>`: тайм-аут для перевірок (типово 3 с).

## `update wizard`

Інтерактивний потік для вибору каналу оновлення та підтвердження, чи потрібно перезапускати Gateway
після оновлення (типово виконується перезапуск). Якщо ви виберете `dev` без git checkout, він
запропонує створити його.

Параметри:

- `--timeout <seconds>`: тайм-аут для кожного кроку оновлення (типово `1200`)

## Що це робить

Коли ви явно перемикаєте канали (`--channel ...`), OpenClaw також підтримує
узгодженість методу встановлення:

- `dev` → забезпечує git checkout (типово: `~/openclaw`, перевизначається через `OPENCLAW_GIT_DIR`),
  оновлює його та встановлює глобальний CLI з цього checkout.
- `stable` → встановлює з npm, використовуючи `latest`.
- `beta` → надає перевагу npm dist-tag `beta`, але використовує запасний варіант `latest`, якщо `beta`
  відсутній або старіший за поточний stable release.

Автооновлювач ядра Gateway (коли він увімкнений через конфігурацію) повторно використовує цей самий шлях оновлення.

Для встановлень через менеджер пакетів `openclaw update` визначає цільову
версію пакета перед викликом менеджера пакетів. Якщо встановлена версія точно
збігається з цільовою і не потрібно зберігати зміну каналу оновлення,
команда завершується як пропущена ще до встановлення пакета, синхронізації Plugin, оновлення completion
або перезапуску gateway.

## Потік git checkout

Канали:

- `stable`: checkout останнього теґа без beta, потім build + doctor.
- `beta`: надає перевагу останньому теґу `-beta`, але використовує запасний варіант — останній stable tag,
  якщо beta відсутній або старіший.
- `dev`: checkout `main`, потім fetch + rebase.

На високому рівні:

1. Потребує чистого worktree (без незакомічених змін).
2. Перемикається на вибраний канал (теґ або гілку).
3. Виконує fetch upstream (лише для dev).
4. Лише для dev: виконує preflight lint + TypeScript build у тимчасовому worktree; якщо вершина не проходить перевірку, відступає назад до 10 комітів, щоб знайти найновішу чисту збірку.
5. Виконує rebase на вибраний коміт (лише для dev).
6. Встановлює залежності через менеджер пакетів репозиторію. Для pnpm checkout засіб оновлення за потреби ініціалізує `pnpm` (спочатку через `corepack`, потім через тимчасовий запасний варіант `npm install pnpm@10`) замість запуску `npm run build` усередині pnpm workspace.
7. Виконує build + build Control UI.
8. Запускає `openclaw doctor` як фінальну перевірку “безпечного оновлення”.
9. Синхронізує Plugins з активним каналом (dev використовує вбудовані Plugins; stable/beta використовує npm) і оновлює Plugins, встановлені через npm.

Якщо точне оновлення закріпленого npm Plugin визначається в артефакт, цілісність
якого відрізняється від збереженого запису встановлення, `openclaw update` перериває це оновлення
артефакту Plugin замість його встановлення. Перевстановлюйте або оновлюйте Plugin
явно лише після того, як переконаєтеся, що довіряєте новому артефакту.

Якщо ініціалізація pnpm усе ще не вдається, засіб оновлення тепер зупиняється раніше з помилкою, специфічною для менеджера пакетів, замість спроби виконати `npm run build` усередині checkout.

## Скорочення `--update`

`openclaw --update` переписується на `openclaw update` (корисно для оболонок і скриптів запуску).

## Див. також

- `openclaw doctor` (пропонує спочатку запустити оновлення для git checkout)
- [Канали розробки](/uk/install/development-channels)
- [Оновлення](/uk/install/updating)
- [Довідник CLI](/uk/cli)
