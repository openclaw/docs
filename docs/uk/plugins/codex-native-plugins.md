---
read_when:
    - Ви хочете, щоб агенти OpenClaw у режимі Codex використовували нативні плагіни Codex
    - Ви переносите встановлені з вихідного коду плагіни Codex, curated OpenAI
    - Ви усуваєте неполадки з codexPlugins, інвентаризацією застосунків, деструктивними діями або діагностикою застосунку Plugin
summary: Налаштуйте перенесені нативні Plugin Codex для агентів OpenClaw у режимі Codex
title: Нативні плагіни Codex
x-i18n:
    generated_at: "2026-05-11T20:46:54Z"
    model: gpt-5.5
    provider: openai
    source_hash: 64e8f552e65b3f1c1c62bc1ba1abfc1bf592d1bdc7fbbe2a484f3eb9955159f0
    source_path: plugins/codex-native-plugins.md
    workflow: 16
---

Нативна підтримка плагінів Codex дає змогу агенту OpenClaw у режимі Codex використовувати власні можливості застосунків і плагінів Codex
app-server у тому самому потоці Codex, який
обробляє хід OpenClaw.

OpenClaw не перетворює плагіни Codex на синтетичні динамічні інструменти
OpenClaw `codex_plugin_*`. Виклики плагінів залишаються в нативній транскрипції Codex, а
Codex app-server відповідає за виконання MCP на базі застосунків.

Використовуйте цю сторінку після того, як базовий [harness Codex](/uk/plugins/codex-harness) уже працює.

## Вимоги

- Вибране середовище виконання агента OpenClaw має бути нативним harness Codex.
- `plugins.entries.codex.enabled` має бути true.
- `plugins.entries.codex.config.codexPlugins.enabled` має бути true.
- V1 підтримує лише плагіни `openai-curated`, які міграція виявила як
  встановлені з вихідного коду у вихідній домашній директорії Codex.
- Цільовий Codex app-server має бачити очікуваний маркетплейс,
  плагін та інвентар застосунків.

`codexPlugins` не впливає на запуски PI, звичайні запуски провайдера OpenAI, прив’язки розмов ACP
або інші harness, оскільки ці шляхи не створюють
потоки Codex app-server з нативною конфігурацією `apps`.

## Швидкий старт

Перегляньте міграцію з вихідної домашньої директорії Codex:

```bash
openclaw migrate codex --dry-run
```

Застосуйте міграцію, коли план виглядає правильним:

```bash
openclaw migrate apply codex --yes
```

Міграція записує явні записи `codexPlugins` для відповідних плагінів і викликає
Codex app-server `plugin/install` для вибраних плагінів. Типова перенесена
конфігурація має такий вигляд:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          codexPlugins: {
            enabled: true,
            allow_destructive_actions: false,
            plugins: {
              "google-calendar": {
                enabled: true,
                marketplaceName: "openai-curated",
                pluginName: "google-calendar",
              },
            },
          },
        },
      },
    },
  },
}
```

Після зміни `codexPlugins` використайте `/new`, `/reset` або перезапустіть gateway, щоб
майбутні сесії harness Codex запускалися з оновленим набором застосунків.

## Як працює нативне налаштування плагінів

Інтеграція має три окремі стани:

- Встановлено: Codex має локальний пакет плагіна в цільовому середовищі виконання app-server.
- Увімкнено: конфігурація OpenClaw дозволяє зробити плагін доступним для ходів
  harness Codex.
- Доступно: Codex app-server підтверджує, що записи застосунків плагіна доступні
  для активного облікового запису й можуть бути зіставлені з перенесеною ідентичністю плагіна.

Міграція є довговічним кроком встановлення та перевірки відповідності. Інвентар застосунків під час виконання є
перевіркою доступності. Потім налаштування сесії harness Codex обчислює обмежувальну
конфігурацію застосунків потоку для ввімкнених і доступних застосунків плагіна.

Конфігурація застосунків потоку обчислюється, коли OpenClaw встановлює сесію harness Codex
або замінює застарілу прив’язку потоку Codex. Вона не переобчислюється на кожному ході.

## Межі підтримки V1

V1 навмисно вузька:

- Лише плагіни `openai-curated`, які вже були встановлені в інвентарі вихідного Codex
  app-server, придатні для міграції.
- Міграція записує явні ідентичності плагінів із `marketplaceName` і
  `pluginName`; вона не записує локальні шляхи кешу `marketplacePath`.
- `codexPlugins.enabled` є глобальним перемикачем увімкнення.
- Немає wildcard `plugins["*"]` і немає ключа конфігурації, який надає довільні
  повноваження на встановлення.
- Непідтримувані маркетплейси, кешовані пакети плагінів, хуки та файли конфігурації Codex
  зберігаються у звіті міграції для ручного перегляду.

## Інвентар застосунків і власність

OpenClaw читає інвентар застосунків Codex через app-server `app/list`, кешує його на
одну годину й асинхронно оновлює застарілі або відсутні записи.

Застосунок плагіна надається лише тоді, коли OpenClaw може зіставити його назад із перенесеним
плагіном через стабільну власність:

- точний ідентифікатор застосунку з деталей плагіна
- відома назва сервера MCP
- унікальні стабільні метадані

Власність лише за відображуваною назвою або неоднозначна власність виключається, доки наступне оновлення інвентарю
не підтвердить власність.

## Конфігурація застосунків потоку

OpenClaw вставляє обмежувальний патч `config.apps` для потоку Codex:
`_default` вимкнено, і ввімкнено лише застосунки, що належать увімкненим перенесеним плагінам.

OpenClaw задає `destructive_enabled` на рівні застосунку з ефективної глобальної або
плагінової політики `allow_destructive_actions` і дозволяє Codex застосовувати
метадані деструктивних інструментів з його нативних анотацій інструментів застосунку. Конфігурацію застосунку `_default`
вимкнено з `open_world_enabled: false`. Увімкнені застосунки плагінів
виводяться з `open_world_enabled: true`; OpenClaw не надає окремого
регулятора політики відкритого світу для плагінів і не підтримує списки заборон
назв деструктивних інструментів для окремих плагінів.

Режим схвалення інструментів за замовчуванням є автоматичним для застосунків плагінів, щоб недеструктивні
інструменти читання могли працювати без UI схвалення в тому самому потоці. Деструктивні інструменти залишаються
під контролем політики `destructive_enabled` кожного застосунку.

## Політика деструктивних дій

Деструктивні запити плагінів за замовчуванням відхиляються безпечно:

- Глобальне `allow_destructive_actions` за замовчуванням має значення `false`.
- Плагінове `allow_destructive_actions` перевизначає глобальну політику для цього
  плагіна.
- Коли політика має значення `false`, OpenClaw повертає детерміновану відмову.
- Коли політика має значення `true`, OpenClaw автоматично приймає лише безпечні схеми, які може зіставити з
  відповіддю схвалення, наприклад булеве поле approve.
- Відсутня ідентичність плагіна, неоднозначна власність, відсутній id ходу, неправильний id ходу
  або небезпечна схема запиту призводять до відмови замість запиту підтвердження.

## Усунення несправностей

**`auth_required`:** міграція встановила плагін, але один із його застосунків усе ще
потребує автентифікації. Явний запис плагіна записується вимкненим, доки ви
повторно не авторизуєте його й не ввімкнете.

**`marketplace_missing` або `plugin_missing`:** цільовий Codex app-server
не бачить очікуваний маркетплейс або плагін `openai-curated`. Повторно запустіть міграцію
для цільового середовища виконання або перевірте стан плагінів Codex app-server.

**`app_inventory_missing` або `app_inventory_stale`:** готовність застосунку надійшла з
порожнього або застарілого кешу. OpenClaw планує асинхронне оновлення й виключає застосунки
плагінів, доки власність і готовність не стануть відомими.

**`app_ownership_ambiguous`:** інвентар застосунків збігся лише за відображуваною назвою, тому
застосунок не надається потоку Codex.

**Конфігурація змінилася, але агент не бачить плагін:** використайте `/new`, `/reset` або
перезапустіть gateway. Наявні прив’язки потоків Codex зберігають конфігурацію застосунків, з якою
вони стартували, доки OpenClaw не встановить нову сесію harness або не замінить
застарілу прив’язку.

**Деструктивну дію відхилено:** перевірте глобальні та плагінові
значення `allow_destructive_actions`. Навіть коли політика має значення true, небезпечні схеми запиту
та неоднозначна ідентичність плагіна все одно відхиляються безпечно.

## Пов’язане

- [harness Codex](/uk/plugins/codex-harness)
- [Довідка harness Codex](/uk/plugins/codex-harness-reference)
- [Середовище виконання harness Codex](/uk/plugins/codex-harness-runtime)
- [Довідка з конфігурації](/uk/gateway/configuration-reference#codex-harness-plugin-config)
- [CLI міграції](/uk/cli/migrate)
