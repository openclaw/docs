---
read_when:
    - Ви підключаєте синтетичний QA-транспорт до локального або CI-запуску тестів
    - Вам потрібна вбудована поверхня конфігурації qa-channel
    - Ви вдосконалюєте наскрізну автоматизацію QA
summary: Синтетичний канальний Plugin класу Slack для детермінованих сценаріїв QA OpenClaw
title: QA-канал
x-i18n:
    generated_at: "2026-05-01T05:11:15Z"
    model: gpt-5.5
    provider: openai
    source_hash: efe057812de1fbc6d89d2b6d5860cd6af4648c3e86913efa3a69267c4e8c57b4
    source_path: channels/qa-channel.md
    workflow: 16
---

`qa-channel` — це вбудований синтетичний транспорт повідомлень для автоматизованого QA OpenClaw. Це не production-канал — він існує, щоб перевіряти ту саму межу Plugin каналу, яку використовують реальні транспорти, водночас зберігаючи стан детермінованим і повністю доступним для інспекції.

## Що він робить

- Синтаксис цілей класу Slack:
  - `dm:<user>`
  - `channel:<room>`
  - `group:<room>`
  - `thread:<room>/<thread>`
- Спільні розмови `channel:` і `group:` передаються агентам як ходи кімнат групи/каналу, тож вони перевіряють ту саму політику видимих відповідей і маршрутизації інструментів повідомлень, яку використовують Discord, Slack, Telegram і подібні транспорти.
- Синтетична шина на базі HTTP для ін’єкції вхідних повідомлень, захоплення вихідних транскриптів, створення тредів, реакцій, редагувань, видалень і дій пошуку/читання.
- Засіб самоперевірки на стороні хоста, який записує Markdown-звіт у `.artifacts/qa-e2e/`.

## Конфігурація

```json
{
  "channels": {
    "qa-channel": {
      "baseUrl": "http://127.0.0.1:43123",
      "botUserId": "openclaw",
      "botDisplayName": "OpenClaw QA",
      "allowFrom": ["*"],
      "pollTimeoutMs": 1000
    }
  }
}
```

Ключі облікового запису:

- `enabled` — головний перемикач для цього облікового запису.
- `name` — необов’язкова відображувана мітка.
- `baseUrl` — URL синтетичної шини.
- `botUserId` — ідентифікатор користувача бота в стилі Matrix, який використовується в синтаксисі цілей.
- `botDisplayName` — відображуване ім’я для вихідних повідомлень.
- `pollTimeoutMs` — вікно очікування long-poll. Ціле число від 100 до 30000.
- `allowFrom` — список дозволених відправників (ідентифікатори користувачів або `"*"`).
- `defaultTo` — резервна ціль, якщо жодної не задано.
- `actions.messages` / `actions.reactions` / `actions.search` / `actions.threads` — керування доступом до інструментів для кожної дії.

Ключі для кількох облікових записів на верхньому рівні:

- `accounts` — запис іменованих перевизначень для кожного облікового запису, індексований за ідентифікатором облікового запису.
- `defaultAccount` — бажаний ідентифікатор облікового запису, коли налаштовано кілька облікових записів.

## Засоби запуску

Самоперевірка на стороні хоста (записує Markdown-звіт у `.artifacts/qa-e2e/`):

```bash
pnpm qa:e2e
```

Це проходить через `qa-lab`, запускає QA-шину в репозиторії, завантажує вбудований runtime-зріз `qa-channel` і виконує детерміновану самоперевірку.

Повний набір сценаріїв на базі репозиторію:

```bash
pnpm openclaw qa suite
```

Запускає сценарії паралельно на QA lane Gateway. Див. [огляд QA](/uk/concepts/qa-e2e-automation) щодо сценаріїв, профілів і режимів провайдерів.

QA-сайт на базі Docker (Gateway + UI налагоджувача QA Lab в одному стеку):

```bash
pnpm qa:lab:up
```

Збирає QA-сайт, запускає стек Gateway + QA Lab на базі Docker і виводить URL QA Lab. Звідти можна вибирати сценарії, обирати lane моделі, запускати окремі прогони й переглядати результати наживо. Налагоджувач QA Lab окремий від поставлюваного пакета Control UI.

## Пов’язане

- [Огляд QA](/uk/concepts/qa-e2e-automation) — загальний стек, адаптери транспорту, створення сценаріїв
- [Matrix QA](/uk/concepts/qa-matrix) — приклад засобу запуску live-транспорту, який керує реальним каналом
- [Сполучення](/uk/channels/pairing)
- [Групи](/uk/channels/groups)
- [Огляд каналів](/uk/channels)
