---
read_when:
    - Зміна правил для групових повідомлень або згадок
summary: Поведінка та конфігурація для обробки групових повідомлень у WhatsApp (`mentionPatterns` спільні для всіх поверхонь)
title: Групові повідомлення
x-i18n:
    generated_at: "2026-04-27T06:22:21Z"
    model: gpt-5.4
    provider: openai
    source_hash: 46a2d69a5209421e224cd4f5462f74e7222553d3d84fbdc0e04c05583fc233a9
    source_path: channels/group-messages.md
    workflow: 15
---

Мета: дозволити Clawd бути в групах WhatsApp, прокидатися лише коли його згадують, і тримати цей потік окремо від особистої сесії в DM.

<Note>
`agents.list[].groupChat.mentionPatterns` також використовується в Telegram, Discord, Slack та iMessage. Цей документ зосереджений на специфічній для WhatsApp поведінці. Для конфігурацій із кількома агентами задавайте `agents.list[].groupChat.mentionPatterns` для кожного агента окремо або використовуйте `messages.groupChat.mentionPatterns` як глобальний запасний варіант.
</Note>

## Поточна реалізація (2025-12-03)

- Режими активації: `mention` (типово) або `always`. `mention` вимагає згадки (справжні WhatsApp @-згадки через `mentionedJids`, безпечні regex-шаблони або номер E.164 бота будь-де в тексті). `always` пробуджує агента на кожне повідомлення, але він має відповідати лише тоді, коли може додати змістовну цінність; інакше він повертає точний тихий токен `NO_REPLY` / `no_reply`. Типові значення можна задати в конфігурації (`channels.whatsapp.groups`) і перевизначити для кожної групи через `/activation`. Якщо задано `channels.whatsapp.groups`, воно також працює як список дозволених груп (додайте `"*"`, щоб дозволити всі).
- Політика груп: `channels.whatsapp.groupPolicy` керує тим, чи приймаються групові повідомлення (`open|disabled|allowlist`). `allowlist` використовує `channels.whatsapp.groupAllowFrom` (запасний варіант: явний `channels.whatsapp.allowFrom`). Типове значення — `allowlist` (заблоковано, доки ви не додасте відправників).
- Сесії для кожної групи: ключі сесій мають вигляд `agent:<agentId>:whatsapp:group:<jid>`, тому команди на кшталт `/verbose on`, `/trace on` або `/think high` (надіслані як окремі повідомлення) обмежуються цією групою; стан особистого DM не змінюється. Heartbeat для групових потоків пропускається.
- Впровадження контексту: **лише очікуючі** групові повідомлення (типово 50), які _не_ запустили виконання, додаються на початку під `[Chat messages since your last reply - for context]`, а рядок, що спричинив запуск, додається під `[Current message - respond to this]`. Повідомлення, які вже є в сесії, повторно не додаються.
- Відображення відправника: кожен пакет групових повідомлень тепер завершується рядком `[from: Sender Name (+E164)]`, щоб Pi знав, хто говорить.
- Ефемерні/view-once: ми розгортаємо їх перед витягуванням тексту/згадок, тому згадки всередині них теж спричиняють запуск.
- Системний промпт для груп: на першому ході групової сесії (і щоразу, коли `/activation` змінює режим) ми додаємо короткий фрагмент до системного промпта, наприклад: `You are replying inside the WhatsApp group "<subject>". Group members: Alice (+44...), Bob (+43...), … Activation: trigger-only … Address the specific sender noted in the message context.` Якщо метадані недоступні, ми все одно повідомляємо агенту, що це груповий чат.

## Приклад конфігурації (WhatsApp)

Додайте блок `groupChat` до `~/.openclaw/openclaw.json`, щоб згадки за відображуваним іменем працювали, навіть коли WhatsApp прибирає візуальний `@` з тексту повідомлення:

```json5
{
  channels: {
    whatsapp: {
      groups: {
        "*": { requireMention: true },
      },
    },
  },
  agents: {
    list: [
      {
        id: "main",
        groupChat: {
          historyLimit: 50,
          mentionPatterns: ["@?openclaw", "\\+?15555550123"],
        },
      },
    ],
  },
}
```

Примітки:

- Regex-и є нечутливими до регістру й використовують ті самі захисні обмеження safe-regex, що й інші поверхні regex у конфігурації; недійсні шаблони й небезпечні вкладені повторення ігноруються.
- WhatsApp усе одно надсилає канонічні згадки через `mentionedJids`, коли хтось торкається контакту, тому запасний варіант із номером потрібен рідко, але є корисною підстраховкою.

### Команда активації (лише для власника)

Використовуйте команду групового чату:

- `/activation mention`
- `/activation always`

Лише номер власника (з `channels.whatsapp.allowFrom` або власний E.164 номер бота, якщо не задано) може змінювати це. Надішліть `/status` як окреме повідомлення в групі, щоб побачити поточний режим активації.

## Як використовувати

1. Додайте свій акаунт WhatsApp (той, на якому працює OpenClaw) до групи.
2. Напишіть `@openclaw …` (або вкажіть номер). Лише відправники зі списку дозволених можуть це активувати, якщо тільки ви не встановите `groupPolicy: "open"`.
3. Промпт агента міститиме недавній груповий контекст плюс кінцевий маркер `[from: …]`, щоб він міг звернутися до правильної людини.
4. Директиви рівня сесії (`/verbose on`, `/trace on`, `/think high`, `/new` або `/reset`, `/compact`) застосовуються лише до сесії цієї групи; надсилайте їх як окремі повідомлення, щоб вони зареєструвалися. Ваша особиста DM-сесія залишається незалежною.

## Тестування / перевірка

- Ручна перевірка:
  - Надішліть у групу згадку `@openclaw` і підтвердьте, що у відповіді є посилання на ім’я відправника.
  - Надішліть другу згадку й перевірте, що блок історії додається, а потім очищується на наступному ході.
- Перевірте логи Gateway (запустіть із `--verbose`), щоб побачити записи `inbound web message` із `from: <groupJid>` і суфіксом `[from: …]`.

## Відомі особливості

- Heartbeat навмисно пропускається для груп, щоб уникнути шумних широкомовних повідомлень.
- Придушення еха використовує об’єднаний рядок пакета; якщо ви двічі надсилаєте однаковий текст без згадок, відповідь буде лише на перше повідомлення.
- Записи у сховищі сесій з’являтимуться як `agent:<agentId>:whatsapp:group:<jid>` у сховищі сесій (`~/.openclaw/agents/<agentId>/sessions/sessions.json` типово); відсутній запис просто означає, що група ще не спричинила запуск.
- Індикатори набору тексту в групах дотримуються `agents.defaults.typingMode` (типово: `message`, коли немає згадки).

## Пов’язане

- [Групи](/uk/channels/groups)
- [Маршрутизація каналів](/uk/channels/channel-routing)
- [Групи розсилки](/uk/channels/broadcast-groups)
