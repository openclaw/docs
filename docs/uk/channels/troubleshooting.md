---
read_when:
    - Транспорт каналу показує, що підключений, але відповіді не надсилаються
    - Потрібно виконати перевірки, специфічні для каналу, перш ніж переходити до поглибленої документації провайдера
summary: Швидке усунення несправностей на рівні каналів із характерними ознаками збоїв і способами виправлення для кожного каналу
title: Усунення несправностей каналів
x-i18n:
    generated_at: "2026-04-27T10:57:54Z"
    model: gpt-5.4
    provider: openai
    source_hash: efc35bf7e1c0829142c62cba009780791ac43c0bc658f1c0f7abe243e0e4afa5
    source_path: channels/troubleshooting.md
    workflow: 15
---

Використовуйте цю сторінку, коли канал підключається, але працює некоректно.

## Послідовність команд

Спочатку виконайте їх у такому порядку:

```bash
openclaw status
openclaw gateway status
openclaw logs --follow
openclaw doctor
openclaw channels status --probe
```

Базові ознаки справного стану:

- `Runtime: running`
- `Connectivity probe: ok`
- `Capability: read-only`, `write-capable` або `admin-capable`
- Перевірка каналу показує, що транспорт підключений і, де підтримується, `works` або `audit ok`

## WhatsApp

### Характерні ознаки збоїв у WhatsApp

| Симптом                        | Найшвидша перевірка                               | Виправлення                                               |
| ------------------------------ | ------------------------------------------------- | --------------------------------------------------------- |
| Підключено, але немає відповідей у DM | `openclaw pairing list whatsapp`                    | Підтвердьте відправника або змініть політику DM/список дозволених. |
| Повідомлення в групі ігноруються | Перевірте `requireMention` і шаблони згадок у конфігурації | Згадайте бота або послабте політику згадок для цієї групи. |
| Вхід через QR завершується тайм-аутом із 408 | Перевірте змінні середовища `HTTPS_PROXY` / `HTTP_PROXY` Gateway | Налаштуйте доступний проксі; використовуйте `NO_PROXY` лише для обходів. |
| Випадкові відключення/цикли повторного входу | `openclaw channels status --probe` + журнали           | Увійдіть знову та переконайтеся, що каталог облікових даних справний. |

Повне усунення несправностей: [Усунення несправностей WhatsApp](/uk/channels/whatsapp#troubleshooting)

## Telegram

### Характерні ознаки збоїв у Telegram

| Симптом                            | Найшвидша перевірка                             | Виправлення                                                                                                               |
| ---------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `/start`, але немає придатного сценарію відповіді | `openclaw pairing list telegram`                 | Підтвердьте pairing або змініть політику DM.                                                                              |
| Бот онлайн, але в групі мовчить    | Перевірте вимогу згадки та режим privacy бота   | Вимкніть режим privacy для видимості в групі або згадайте бота.                                                           |
| Збій надсилання з мережевими помилками | Перегляньте журнали на предмет збоїв викликів Telegram API | Виправте маршрутизацію DNS/IPv6/проксі до `api.telegram.org`.                                                             |
| Polling зависає або повільно перепідключається | `openclaw logs --follow` для діагностики polling | Оновіться; якщо перезапуски є хибними спрацьовуваннями, налаштуйте `pollingStallThresholdMs`. Стійкі зависання все ще вказують на проблеми з проксі/DNS/IPv6. |
| `setMyCommands` відхиляється під час запуску | Перегляньте журнали на `BOT_COMMANDS_TOO_MUCH`   | Зменште кількість команд Telegram від плагінів/Skills/користувацьких команд або вимкніть нативні меню.                   |
| Після оновлення вас блокує список дозволених | `openclaw security audit` і списки дозволених у конфігурації | Виконайте `openclaw doctor --fix` або замініть `@username` на числові ID відправників.                                   |

Повне усунення несправностей: [Усунення несправностей Telegram](/uk/channels/telegram#troubleshooting)

## Discord

### Характерні ознаки збоїв у Discord

| Симптом                        | Найшвидша перевірка              | Виправлення                                                |
| ----------------------------- | -------------------------------- | ---------------------------------------------------------- |
| Бот онлайн, але немає відповідей у guild | `openclaw channels status --probe`  | Дозвольте guild/канал і перевірте intent вмісту повідомлень. |
| Повідомлення в групі ігноруються | Перевірте журнали на відхилення через gating згадок | Згадайте бота або встановіть `requireMention: false` для guild/каналу. |
| Відсутні відповіді в DM        | `openclaw pairing list discord`     | Підтвердьте DM pairing або скоригуйте політику DM.         |

Повне усунення несправностей: [Усунення несправностей Discord](/uk/channels/discord#troubleshooting)

## Slack

### Характерні ознаки збоїв у Slack

| Симптом                               | Найшвидша перевірка                      | Виправлення                                                                                                                                             |
| ------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Socket mode підключений, але немає відповідей | `openclaw channels status --probe`        | Перевірте токен застосунку, токен бота та потрібні області доступу; зверніть увагу на `botTokenStatus` / `appTokenStatus = configured_unavailable` у налаштуваннях на базі SecretRef. |
| DM заблоковані                        | `openclaw pairing list slack`             | Підтвердьте pairing або послабте політику DM.                                                                                                           |
| Повідомлення в каналі ігнорується     | Перевірте `groupPolicy` і список дозволених каналів | Дозвольте канал або змініть політику на `open`.                                                                                                         |

Повне усунення несправностей: [Усунення несправностей Slack](/uk/channels/slack#troubleshooting)

## iMessage і BlueBubbles

### Характерні ознаки збоїв у iMessage і BlueBubbles

| Симптом                         | Найшвидша перевірка                                                        | Виправлення                                           |
| ------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------- |
| Немає вхідних подій             | Перевірте доступність Webhook/сервера та дозволи застосунку                | Виправте URL Webhook або стан сервера BlueBubbles.    |
| Можна надсилати, але не отримувати на macOS | Перевірте дозволи конфіденційності macOS для автоматизації Messages         | Повторно надайте дозволи TCC і перезапустіть процес каналу. |
| Відправника DM заблоковано      | `openclaw pairing list imessage` або `openclaw pairing list bluebubbles` | Підтвердьте pairing або оновіть список дозволених.    |

Повне усунення несправностей:

- [Усунення несправностей iMessage](/uk/channels/imessage#troubleshooting)
- [Усунення несправностей BlueBubbles](/uk/channels/bluebubbles#troubleshooting)

## Signal

### Характерні ознаки збоїв у Signal

| Симптом                        | Найшвидша перевірка               | Виправлення                                               |
| ----------------------------- | --------------------------------- | --------------------------------------------------------- |
| Демон доступний, але бот мовчить | `openclaw channels status --probe` | Перевірте URL/обліковий запис демона `signal-cli` і режим отримання. |
| DM заблоковано                | `openclaw pairing list signal`     | Підтвердьте відправника або скоригуйте політику DM.       |
| Відповіді в групі не спрацьовують | Перевірте список дозволених груп і шаблони згадок | Додайте відправника/групу або послабте gating.            |

Повне усунення несправностей: [Усунення несправностей Signal](/uk/channels/signal#troubleshooting)

## QQ Bot

### Характерні ознаки збоїв у QQ Bot

| Симптом                         | Найшвидша перевірка                        | Виправлення                                                        |
| ------------------------------- | ------------------------------------------ | ------------------------------------------------------------------ |
| Бот відповідає "gone to Mars"   | Перевірте `appId` і `clientSecret` у конфігурації | Налаштуйте облікові дані або перезапустіть Gateway.                |
| Немає вхідних повідомлень       | `openclaw channels status --probe`         | Перевірте облікові дані на QQ Open Platform.                       |
| Голос не транскрибується        | Перевірте конфігурацію провайдера STT      | Налаштуйте `channels.qqbot.stt` або `tools.media.audio`.           |
| Проактивні повідомлення не надходять | Перевірте вимоги платформи QQ до взаємодії | QQ може блокувати ініційовані ботом повідомлення без недавньої взаємодії. |

Повне усунення несправностей: [Усунення несправностей QQ Bot](/uk/channels/qqbot#troubleshooting)

## Matrix

### Характерні ознаки збоїв у Matrix

| Симптом                              | Найшвидша перевірка                   | Виправлення                                                                |
| ------------------------------------ | ------------------------------------- | -------------------------------------------------------------------------- |
| Виконано вхід, але повідомлення в кімнаті ігноруються | `openclaw channels status --probe`     | Перевірте `groupPolicy`, список дозволених кімнат і gating згадок.         |
| DM не обробляються                   | `openclaw pairing list matrix`         | Підтвердьте відправника або скоригуйте політику DM.                        |
| Зашифровані кімнати не працюють      | `openclaw matrix verify status`        | Повторно підтвердьте пристрій, потім перевірте `openclaw matrix verify backup status`. |
| Відновлення резервної копії очікується/пошкоджене | `openclaw matrix verify backup status` | Виконайте `openclaw matrix verify backup restore` або повторіть із ключем відновлення. |
| Cross-signing/bootstrap виглядає неправильно | `openclaw matrix verify bootstrap`     | Відновіть secret storage, cross-signing і стан резервної копії за один прохід. |

Повне налаштування та конфігурація: [Matrix](/uk/channels/matrix)

## Пов’язані матеріали

- [Pairing](/uk/channels/pairing)
- [Маршрутизація каналів](/uk/channels/channel-routing)
- [Усунення несправностей Gateway](/uk/gateway/troubleshooting)
