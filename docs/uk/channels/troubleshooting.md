---
read_when:
    - Транспорт каналу показує, що підключення встановлено, але відповіді не надсилаються
    - Вам потрібні перевірки, специфічні для каналу, перш ніж переходити до поглибленої документації provider-а
summary: Швидке усунення несправностей на рівні каналів із характерними ознаками збоїв для кожного каналу та способами виправлення
title: Усунення несправностей каналів
x-i18n:
    generated_at: "2026-04-23T20:45:07Z"
    model: gpt-5.4
    provider: openai
    source_hash: ce24e5f113aa31c9c652fabb1ba6b97827627c6e263b1b4c93ffa230a3bdd9fe
    source_path: channels/troubleshooting.md
    workflow: 15
---

Використовуйте цю сторінку, коли канал підключається, але поводиться неправильно.

## Набір команд

Спочатку виконайте їх у такому порядку:

```bash
openclaw status
openclaw gateway status
openclaw logs --follow
openclaw doctor
openclaw channels status --probe
```

Ознаки здорового стану:

- `Runtime: running`
- `Connectivity probe: ok`
- `Capability: read-only`, `write-capable` або `admin-capable`
- Перевірка каналу показує, що транспорт підключено і, де це підтримується, `works` або `audit ok`

## WhatsApp

### Характерні ознаки збоїв WhatsApp

| Симптом                         | Найшвидша перевірка                                | Виправлення                                           |
| ------------------------------- | -------------------------------------------------- | ----------------------------------------------------- |
| Підключено, але немає відповідей у DM | `openclaw pairing list whatsapp`                   | Схваліть відправника або змініть DM policy/allowlist. |
| Групові повідомлення ігноруються | Перевірте `requireMention` і шаблони згадок у конфігурації | Згадайте бота або пом’якште політику згадок для цієї групи. |
| Випадкові цикли відключення/повторного входу | `openclaw channels status --probe` + журнали       | Увійдіть повторно й перевірте, що каталог облікових даних у нормальному стані. |

Повне усунення несправностей: [усунення несправностей WhatsApp](/uk/channels/whatsapp#troubleshooting)

## Telegram

### Характерні ознаки збоїв Telegram

| Симптом                             | Найшвидша перевірка                              | Виправлення                                                                                                               |
| ----------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| `/start`, але немає придатного сценарію відповіді | `openclaw pairing list telegram`                 | Схваліть pairing або змініть DM policy.                                                                                  |
| Бот онлайн, але в групі мовчить     | Перевірте вимогу згадки та режим конфіденційності бота | Вимкніть privacy mode для видимості в групі або згадайте бота.                                                           |
| Помилки надсилання з мережевими збоями | Перегляньте журнали на предмет помилок викликів Telegram API | Виправте маршрутизацію DNS/IPv6/proxy до `api.telegram.org`.                                                             |
| Polling зависає або повільно перепідключається | `openclaw logs --follow` для діагностики polling | Оновіться; якщо перезапуски є хибнопозитивними, налаштуйте `pollingStallThresholdMs`. Стійкі зависання все одно вказують на proxy/DNS/IPv6. |
| `setMyCommands` відхилено під час запуску | Перегляньте журнали на `BOT_COMMANDS_TOO_MUCH`   | Зменште кількість Telegram-команд plugin/Skills/користувацьких команд або вимкніть рідні меню.                          |
| Після оновлення вас блокує allowlist | `openclaw security audit` і allowlist у конфігурації | Виконайте `openclaw doctor --fix` або замініть `@username` на числові ID відправників.                                  |

Повне усунення несправностей: [усунення несправностей Telegram](/uk/channels/telegram#troubleshooting)

## Discord

### Характерні ознаки збоїв Discord

| Симптом                         | Найшвидша перевірка                | Виправлення                                             |
| ------------------------------- | ---------------------------------- | ------------------------------------------------------- |
| Бот онлайн, але не відповідає в guild | `openclaw channels status --probe` | Дозвольте guild/channel і перевірте дозвіл message content intent. |
| Групові повідомлення ігноруються | Перевірте журнали на drops через gating згадок | Згадайте бота або встановіть `requireMention: false` для guild/channel. |
| Немає відповідей у DM           | `openclaw pairing list discord`    | Схваліть DM pairing або змініть DM policy.              |

Повне усунення несправностей: [усунення несправностей Discord](/uk/channels/discord#troubleshooting)

## Slack

### Характерні ознаки збоїв Slack

| Симптом                                | Найшвидша перевірка                      | Виправлення                                                                                                                                            |
| -------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Socket mode підключено, але немає відповідей | `openclaw channels status --probe`       | Перевірте app token + bot token і потрібні scope; зверніть увагу на `botTokenStatus` / `appTokenStatus = configured_unavailable` у конфігураціях на основі SecretRef. |
| DM заблоковані                         | `openclaw pairing list slack`            | Схваліть pairing або пом’якште DM policy.                                                                                                             |
| Повідомлення каналу ігнорується        | Перевірте `groupPolicy` і allowlist каналу | Дозвольте канал або перемкніть policy на `open`.                                                                                                      |

Повне усунення несправностей: [усунення несправностей Slack](/uk/channels/slack#troubleshooting)

## iMessage і BlueBubbles

### Характерні ознаки збоїв iMessage і BlueBubbles

| Симптом                          | Найшвидша перевірка                                                      | Виправлення                                           |
| -------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------- |
| Немає вхідних подій              | Перевірте доступність Webhook/сервера та дозволи застосунку             | Виправте URL Webhook або стан сервера BlueBubbles.    |
| Можна надсилати, але немає отримання на macOS | Перевірте дозволи конфіденційності macOS для автоматизації Messages | Повторно надайте дозволи TCC і перезапустіть процес каналу. |
| Відправника DM заблоковано       | `openclaw pairing list imessage` або `openclaw pairing list bluebubbles` | Схваліть pairing або оновіть allowlist.               |

Повне усунення несправностей:

- [усунення несправностей iMessage](/uk/channels/imessage#troubleshooting)
- [усунення несправностей BlueBubbles](/uk/channels/bluebubbles#troubleshooting)

## Signal

### Характерні ознаки збоїв Signal

| Симптом                         | Найшвидша перевірка                      | Виправлення                                            |
| ------------------------------- | ---------------------------------------- | ------------------------------------------------------ |
| Daemon доступний, але бот мовчить | `openclaw channels status --probe`       | Перевірте URL/account daemon `signal-cli` і режим receive. |
| DM заблоковано                  | `openclaw pairing list signal`           | Схваліть відправника або змініть DM policy.            |
| Не спрацьовують відповіді в групі | Перевірте груповий allowlist і шаблони згадок | Додайте відправника/групу або послабте gating.         |

Повне усунення несправностей: [усунення несправностей Signal](/uk/channels/signal#troubleshooting)

## QQ Bot

### Характерні ознаки збоїв QQ Bot

| Симптом                         | Найшвидша перевірка                                 | Виправлення                                                     |
| ------------------------------- | --------------------------------------------------- | --------------------------------------------------------------- |
| Бот відповідає «gone to Mars»   | Перевірте `appId` і `clientSecret` у конфігурації   | Укажіть облікові дані або перезапустіть gateway.                |
| Немає вхідних повідомлень       | `openclaw channels status --probe`                  | Перевірте облікові дані на QQ Open Platform.                    |
| Голос не транскрибується        | Перевірте конфігурацію provider-а STT               | Налаштуйте `channels.qqbot.stt` або `tools.media.audio`.        |
| Проактивні повідомлення не надходять | Перевірте вимоги QQ platform до взаємодії       | QQ може блокувати ініційовані ботом повідомлення без недавньої взаємодії. |

Повне усунення несправностей: [усунення несправностей QQ Bot](/uk/channels/qqbot#troubleshooting)

## Matrix

### Характерні ознаки збоїв Matrix

| Симптом                             | Найшвидша перевірка                    | Виправлення                                                              |
| ----------------------------------- | -------------------------------------- | ------------------------------------------------------------------------ |
| Вхід виконано, але повідомлення кімнат ігноруються | `openclaw channels status --probe`     | Перевірте `groupPolicy`, allowlist кімнати та gating згадок.             |
| DM не обробляються                  | `openclaw pairing list matrix`         | Схваліть відправника або змініть DM policy.                              |
| Зашифровані кімнати не працюють     | `openclaw matrix verify status`        | Повторно верифікуйте пристрій, потім перевірте `openclaw matrix verify backup status`. |
| Відновлення резервної копії очікується/пошкоджене | `openclaw matrix verify backup status` | Виконайте `openclaw matrix verify backup restore` або повторіть із recovery key. |
| Cross-signing/bootstrap виглядає неправильно | `openclaw matrix verify bootstrap`     | Виправте secret storage, cross-signing і стан резервної копії за один прохід. |

Повне налаштування та конфігурація: [Matrix](/uk/channels/matrix)
