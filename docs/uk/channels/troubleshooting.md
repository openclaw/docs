---
read_when:
    - Транспорт каналу повідомляє, що підключено, але відповіді не вдаються
    - Перш ніж переходити до докладної документації провайдера, потрібні перевірки, специфічні для каналу
summary: Швидке усунення несправностей на рівні каналів із сигнатурами збоїв і виправленнями для кожного каналу
title: Усунення несправностей каналів
x-i18n:
    generated_at: "2026-06-27T17:14:23Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 56b64030ec56553b4c2e156195806029f91bc8cc449588a242b0f45f8bbddb6e
    source_path: channels/troubleshooting.md
    workflow: 16
---

Використовуйте цю сторінку, коли канал підключається, але поведінка неправильна.

## Драбина команд

Спочатку виконайте їх у такому порядку:

```bash
openclaw status
openclaw gateway status
openclaw logs --follow
openclaw doctor
openclaw channels status --probe
```

Здорова базова лінія:

- `Runtime: running`
- `Connectivity probe: ok`
- `Capability: read-only`, `write-capable` або `admin-capable`
- Перевірка каналу показує, що транспорт підключено і, де підтримується, `works` або `audit ok`

## Після оновлення

Використовуйте це, коли Telegram, iMessage, конфіги епохи BlueBubbles або інший канал Plugin
зникає після оновлення.

```bash
openclaw status --all
openclaw doctor --fix
openclaw gateway restart
openclaw status --all
```

Шукайте `plugin load failed: dependency tree corrupted; run openclaw doctor
--fix` у `openclaw status --all`. Це означає, що канал налаштовано, але
шлях налаштування/завантаження Plugin натрапив на пошкоджене дерево залежностей замість реєстрації
каналу. `openclaw doctor --fix` видаляє застарілі проміжні
каталоги залежностей Plugin і застарілі тіні автентифікації, після чого `openclaw gateway restart` перезавантажує
чистий стан.

## WhatsApp

### Сигнатури збоїв WhatsApp

| Симптом                             | Найшвидша перевірка                                       | Виправлення                                                                                                                              |
| ----------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Підключено, але немає відповідей у DM         | `openclaw pairing list whatsapp`                    | Схваліть відправника або змініть політику/список дозволених для DM.                                                                                    |
| Повідомлення групи ігноруються              | Перевірте `requireMention` + шаблони згадок у конфігу | Згадайте бота або послабте політику згадок для цієї групи.                                                                          |
| Вхід через QR завершується тайм-аутом із 408         | Перевірте змінні середовища Gateway `HTTPS_PROXY` / `HTTP_PROXY`      | Налаштуйте доступний проксі; використовуйте `NO_PROXY` лише для обходів.                                                                         |
| Випадкові цикли відключення/повторного входу     | `openclaw channels status --probe` + журнали           | Нещодавні повторні підключення позначаються навіть за поточного підключення; стежте за журналами, перезапустіть Gateway, потім повторно прив’яжіть, якщо нестабільність триває. |
| Цикл `status=408 Request Time-out`  | Перевірка, журнали, doctor, потім статус Gateway            | Спочатку виправте підключення/таймінги хоста; створіть резервну копію автентифікації і повторно прив’яжіть обліковий запис, якщо цикл не зникає.                                   |
| Відповіді надходять із затримкою в секунди/хвилини | `openclaw doctor --fix`                             | Doctor зупиняє перевірені застарілі локальні клієнти TUI, коли вони погіршують цикл подій Gateway.                                    |

Повне усунення несправностей: [Усунення несправностей WhatsApp](/uk/channels/whatsapp#troubleshooting)

## Telegram

### Сигнатури збоїв Telegram

| Симптом                              | Найшвидша перевірка                                    | Виправлення                                                                                                                        |
| ------------------------------------ | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| `/start`, але немає придатного потоку відповідей    | `openclaw pairing list telegram`                 | Схваліть прив’язування або змініть політику DM.                                                                                       |
| Бот онлайн, але група мовчить    | Перевірте вимогу згадки та режим приватності бота  | Вимкніть режим приватності для видимості в групі або згадайте бота.                                                                  |
| Помилки надсилання з мережевими помилками    | Перегляньте журнали щодо збоїв викликів Telegram API      | Виправте маршрутизацію DNS/IPv6/проксі до `api.telegram.org`.                                                                          |
| Запуск повідомляє `getMe returned 401` | Перевірте налаштоване джерело токена                    | Скопіюйте повторно або згенеруйте наново токен BotFather і оновіть `botToken`, `tokenFile` або `TELEGRAM_BOT_TOKEN` облікового запису за замовчуванням.     |
| Опитування зависає або повільно перепідключається  | `openclaw logs --follow` для діагностики опитування | Оновіть; якщо перезапуски є хибними спрацюваннями, налаштуйте `pollingStallThresholdMs`. Постійні зависання все ще вказують на проксі/DNS/IPv6. |
| `setMyCommands` відхилено під час запуску  | Перегляньте журнали щодо `BOT_COMMANDS_TOO_MUCH`         | Зменште кількість команд Telegram від Plugin/skill/користувацьких команд або вимкніть нативні меню.                                                      |
| Після оновлення список дозволених блокує вас    | `openclaw security audit` і списки дозволених у конфігу  | Запустіть `openclaw doctor --fix` або замініть `@username` на числові ID відправників.                                                |

Повне усунення несправностей: [Усунення несправностей Telegram](/uk/channels/telegram#troubleshooting)

## Discord

### Сигнатури збоїв Discord

| Симптом                                   | Найшвидша перевірка                                                                                                                | Виправлення                                                                                                                                                                                                                                                                   |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Бот онлайн, але немає відповідей у гільдії           | `openclaw channels status --probe`                                                                                           | Дозвольте гільдію/канал і перевірте intent вмісту повідомлень.                                                                                                                                                                                                                |
| Повідомлення групи ігноруються                    | Перевірте журнали щодо відкидань через шлюз згадок                                                                                          | Згадайте бота або встановіть для гільдії/каналу `requireMention: false`.                                                                                                                                                                                                             |
| Є введення/використання токенів, але немає повідомлення Discord | Перевірте, чи це подія ambient-кімнати або кімната `message_tool` з явною згодою, де модель пропустила `message(action=send)` | Перегляньте детальний журнал Gateway щодо придушених метаданих фінального payload, перевірте `messages.groupChat.unmentionedInbound`, прочитайте [Події ambient-кімнати](/uk/channels/ambient-room-events) або залиште `messages.groupChat.visibleReplies: "automatic"` для звичайних групових запитів. |
| Відсутні відповіді в DM                        | `openclaw pairing list discord`                                                                                              | Схваліть прив’язування DM або скоригуйте політику DM.                                                                                                                                                                                                                               |

Повне усунення несправностей: [Усунення несправностей Discord](/uk/channels/discord#troubleshooting)

## Slack

### Сигнатури збоїв Slack

| Симптом                                | Найшвидша перевірка                             | Виправлення                                                                                                                                                  |
| -------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Socket mode підключено, але немає відповідей | `openclaw channels status --probe`        | Перевірте app token + bot token і потрібні scopes; стежте за `botTokenStatus` / `appTokenStatus = configured_unavailable` у налаштуваннях на базі SecretRef. |
| DM заблоковані                            | `openclaw pairing list slack`             | Схваліть прив’язування або послабте політику DM.                                                                                                                  |
| Повідомлення каналу ігнорується                | Перевірте `groupPolicy` і список дозволених каналів | Дозвольте канал або змініть політику на `open`.                                                                                                        |

Повне усунення несправностей: [Усунення несправностей Slack](/uk/channels/slack#troubleshooting)

## iMessage

### Сигнатури збоїв iMessage

| Симптом                              | Найшвидша перевірка                                           | Виправлення                                                                   |
| ------------------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------- |
| `imsg` відсутній або завершується помилкою не на macOS | `openclaw channels status --probe --channel imessage`   | Запустіть OpenClaw на Mac із Messages або використовуйте SSH-обгортку для `cliPath`. |
| Можна надсилати, але немає отримання на macOS     | Перевірте дозволи приватності macOS для автоматизації Messages | Надайте дозволи TCC повторно і перезапустіть процес каналу.                 |
| Відправника DM заблоковано                    | `openclaw pairing list imessage`                        | Схваліть прив’язування або оновіть список дозволених.                                  |

Повне усунення несправностей:

- [Усунення несправностей iMessage](/uk/channels/imessage#troubleshooting)

## Signal

### Сигнатури збоїв Signal

| Симптом                         | Найшвидша перевірка                              | Виправлення                                                      |
| ------------------------------- | ------------------------------------------ | -------------------------------------------------------- |
| Daemon доступний, але бот мовчить | `openclaw channels status --probe`         | Перевірте URL/обліковий запис daemon `signal-cli` і режим отримання. |
| DM заблоковано                      | `openclaw pairing list signal`             | Схваліть відправника або скоригуйте політику DM.                      |
| Відповіді групи не запускаються    | Перевірте список дозволених груп і шаблони згадок | Додайте відправника/групу або послабте обмеження.                       |

Повне усунення несправностей: [Усунення несправностей Signal](/uk/channels/signal#troubleshooting)

## QQ Bot

### Сигнатури збоїв QQ Bot

| Симптом                         | Найшвидша перевірка                               | Виправлення                                                             |
| ------------------------------- | ------------------------------------------- | --------------------------------------------------------------- |
| Бот відповідає "gone to Mars"      | Перевірте `appId` і `clientSecret` у конфігу | Налаштуйте облікові дані або перезапустіть Gateway.                         |
| Немає вхідних повідомлень             | `openclaw channels status --probe`          | Перевірте облікові дані на QQ Open Platform.                     |
| Голос не транскрибується           | Перевірте конфіг провайдера STT                   | Налаштуйте `channels.qqbot.stt` або `tools.media.audio`.          |
| Проактивні повідомлення не надходять | Перевірте вимоги взаємодії платформи QQ  | QQ може блокувати повідомлення, ініційовані ботом, без нещодавньої взаємодії. |

Повне усунення несправностей: [Усунення несправностей QQ Bot](/uk/channels/qqbot#troubleshooting)

## Matrix

### Ознаки збоїв Matrix

| Симптом                             | Найшвидша перевірка                    | Виправлення                                                               |
| ----------------------------------- | -------------------------------------- | ------------------------------------------------------------------------- |
| Увійшов у систему, але ігнорує повідомлення кімнати | `openclaw channels status --probe`     | Перевірте `groupPolicy`, allowlist кімнат і обмеження за згадками.        |
| DM не обробляються                  | `openclaw pairing list matrix`         | Схваліть відправника або налаштуйте політику DM.                          |
| Зашифровані кімнати не працюють     | `openclaw matrix verify status`        | Повторно підтвердьте пристрій, потім перевірте `openclaw matrix verify backup status`. |
| Відновлення резервної копії очікує або зламане | `openclaw matrix verify backup status` | Запустіть `openclaw matrix verify backup restore` або повторіть із ключем відновлення. |
| Cross-signing/bootstrap виглядає неправильно | `openclaw matrix verify bootstrap`     | Відновіть сховище секретів, cross-signing і стан резервної копії за один прохід. |

Повне налаштування й конфігурація: [Matrix](/uk/channels/matrix)

## Пов’язане

- [Pairing](/uk/channels/pairing)
- [Маршрутизація каналів](/uk/channels/channel-routing)
- [Усунення несправностей Gateway](/uk/gateway/troubleshooting)
