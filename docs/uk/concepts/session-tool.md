---
read_when:
    - Ви хочете зрозуміти, які інструменти сесії має агент
    - Ви хочете налаштувати міжсесійний доступ або створення під-агентів
    - Ви хочете перевірити статус або керувати створеними підагентами
summary: Інструменти агентів для міжсеансового статусу, пригадування, обміну повідомленнями та оркестрації субагентів
title: Інструменти сеансу
x-i18n:
    generated_at: "2026-04-28T19:38:58Z"
    model: gpt-5.5
    provider: openai
    source_hash: 0464116d42e271da12cbe90529e06e9f51605981be85b54bb5850ee9b8fb7824
    source_path: concepts/session-tool.md
    workflow: 16
---

OpenClaw надає агентам інструменти для роботи між сеансами, перевірки статусу та
оркестрування субагентів.

## Доступні інструменти

| Інструмент        | Що він робить                                                                |
| ----------------- | ---------------------------------------------------------------------------- |
| `sessions_list`    | Перелічує сеанси з необов'язковими фільтрами (тип, мітка, агент, давність, попередній перегляд) |
| `sessions_history` | Читає транскрипт певного сеансу                                      |
| `sessions_send`    | Надсилає повідомлення до іншого сеансу та, за потреби, очікує               |
| `sessions_spawn`   | Запускає ізольований сеанс субагента для фонової роботи                     |
| `sessions_yield`   | Завершує поточний хід і очікує подальших результатів субагента              |
| `subagents`        | Перелічує, спрямовує або зупиняє створених субагентів для цього сеансу      |
| `session_status`   | Показує картку у стилі `/status` і, за потреби, встановлює перевизначення моделі для окремого сеансу |

На ці інструменти все ще поширюються активний профіль інструментів і політика
дозволу/заборони. `tools.profile: "coding"` включає повний набір оркестрування
сеансів, зокрема `sessions_spawn`, `sessions_yield` і `subagents`.
`tools.profile: "messaging"` включає інструменти обміну повідомленнями між
сеансами (`sessions_list`, `sessions_history`, `sessions_send`, `session_status`), але
не включає створення субагентів. Щоб зберегти профіль обміну повідомленнями й усе одно
дозволити нативне делегування, додайте:

```json5
{
  tools: {
    profile: "messaging",
    alsoAllow: ["sessions_spawn", "sessions_yield", "subagents"],
  },
}
```

Політики групи, провайдера, пісочниці та окремого агента все ще можуть вилучати ці інструменти
після етапу профілю. Використайте `/tools` з відповідного сеансу, щоб перевірити
ефективний список інструментів.

## Переліки та читання сеансів

`sessions_list` повертає сеанси з їхнім ключем, agentId, типом, каналом, моделлю,
кількістю токенів і часовими позначками. Фільтруйте за типом (`main`, `group`, `cron`, `hook`,
`node`), точним `label`, точним `agentId`, пошуковим текстом або давністю
(`activeMinutes`). Коли потрібне сортування у стилі поштової скриньки, він також може запитувати
похідний заголовок у межах видимості, фрагмент попереднього перегляду останнього повідомлення або обмежені
нещодавні повідомлення в кожному рядку. Похідні заголовки та попередні перегляди створюються лише для
сеансів, які викликач уже може бачити відповідно до налаштованої політики видимості
інструментів сеансу, тому непов'язані сеанси залишаються прихованими.

`sessions_history` отримує транскрипт розмови для певного сеансу.
За замовчуванням результати інструментів виключено -- передайте `includeTools: true`, щоб побачити їх.
Повернуте подання навмисно обмежене й відфільтроване для безпеки:

- текст асистента нормалізується перед пригадуванням:
  - теги міркувань вилучаються
  - службові блоки `<relevant-memories>` / `<relevant_memories>` вилучаються
  - текстові XML-блоки payload викликів інструментів, як-от `<tool_call>...</tool_call>`,
    `<function_call>...</function_call>`, `<tool_calls>...</tool_calls>` і
    `<function_calls>...</function_calls>`, вилучаються, зокрема обрізані
    payload, які ніколи коректно не закриваються
  - знижені службові блоки викликів/результатів інструментів, як-от `[Tool Call: ...]`,
    `[Tool Result ...]` і `[Historical context ...]`, вилучаються
  - витіклі токени керування моделлю, як-от `<|assistant|>`, інші ASCII
    токени `<|...|>` і повноширинні варіанти `<｜...｜>`, вилучаються
  - некоректний XML виклику інструмента MiniMax, як-от `<invoke ...>` /
    `</minimax:tool_call>`, вилучається
- текст, схожий на облікові дані/токени, редагується перед поверненням
- довгі текстові блоки обрізаються
- дуже великі історії можуть відкидати старіші рядки або замінювати завеликий рядок на
  `[sessions_history omitted: message too large]`
- інструмент повідомляє прапорці підсумку, як-от `truncated`, `droppedMessages`,
  `contentTruncated`, `contentRedacted` і `bytes`

Обидва інструменти приймають або **ключ сеансу** (наприклад, `"main"`), або **ID сеансу**
з попереднього виклику списку.

Якщо потрібен точний побайтовий транскрипт, перевірте файл транскрипту на
диску, а не розглядайте `sessions_history` як сирий дамп.

## Надсилання повідомлень між сеансами

`sessions_send` доставляє повідомлення до іншого сеансу та, за потреби, очікує
відповіді:

- **Надіслати й не чекати:** встановіть `timeoutSeconds: 0`, щоб поставити в чергу й повернутися
  негайно.
- **Очікувати відповіді:** встановіть час очікування й отримайте відповідь inline.

Повідомлення та подальші відповіді A2A позначаються як міжсеансові дані в
отримувальному prompt (`[Inter-session message ... isUser=false]`) і в походженні
транскрипту. Отримувальний агент має розглядати їх як дані, маршрутизовані інструментом, а не як
пряму інструкцію, написану кінцевим користувачем.

Після відповіді цілі OpenClaw може запустити **цикл зворотної відповіді**, де
агенти чергують повідомлення (до 5 ходів). Цільовий агент може відповісти
`REPLY_SKIP`, щоб зупинитися раніше.

## Помічники статусу та оркестрування

`session_status` — це легкий інструмент, еквівалентний `/status`, для поточного
або іншого видимого сеансу. Він повідомляє використання, час, стан моделі/середовища виконання та
контекст пов'язаного фонового завдання, якщо він є. Як і `/status`, він може дозаповнювати
розріджені лічильники токенів/кешу з останнього запису використання в транскрипті, а
`model=default` очищає перевизначення для окремого сеансу. Використовуйте `sessionKey="current"` для
поточного сеансу викликача; видимі клієнтські мітки, як-от `openclaw-tui`, не є
ключами сеансів.

`sessions_yield` навмисно завершує поточний хід, щоб наступне повідомлення могло бути
подальшою подією, на яку ви очікуєте. Використовуйте його після створення субагентів, коли
хочете, щоб результати завершення надійшли як наступне повідомлення, замість побудови
циклів опитування.

`subagents` — це помічник площини керування для вже створених субагентів
OpenClaw. Він підтримує:

- `action: "list"` для перевірки активних/нещодавніх запусків
- `action: "steer"` для надсилання подальших вказівок запущеній дочірній сутності
- `action: "kill"` для зупинення однієї дочірньої сутності або `all`

## Створення субагентів

`sessions_spawn` за замовчуванням створює ізольований сеанс для фонового завдання.
Він завжди неблокувальний -- одразу повертає `runId` і
`childSessionKey`.

Ключові параметри:

- `runtime: "subagent"` (за замовчуванням) або `"acp"` для зовнішніх агентів harness.
- Перевизначення `model` і `thinking` для дочірнього сеансу.
- `thread: true` для прив'язування spawn до потоку чату (Discord, Slack тощо).
- `sandbox: "require"` для примусового застосування пісочниці до дочірньої сутності.
- `context: "fork"` для нативних субагентів, коли дочірній сутності потрібен поточний
  транскрипт запитувача; опустіть його або використайте `context: "isolated"` для чистої дочірньої сутності.

Типові кінцеві субагенти не отримують інструменти сеансу. Коли
`maxSpawnDepth >= 2`, субагенти-оркестратори глибини 1 додатково отримують
`sessions_spawn`, `subagents`, `sessions_list` і `sessions_history`, щоб вони
могли керувати власними дочірніми сутностями. Кінцеві запуски все одно не отримують рекурсивні
інструменти оркестрування.

Після завершення крок оголошення публікує результат у каналі запитувача.
Доставка завершення зберігає прив'язану маршрутизацію потоку/теми, коли вона доступна, і якщо
джерело завершення ідентифікує лише канал, OpenClaw все одно може повторно використати
збережений маршрут сеансу запитувача (`lastChannel` / `lastTo`) для прямої
доставки.

Для поведінки, специфічної для ACP, див. [Агенти ACP](/uk/tools/acp-agents).

## Видимість

Інструменти сеансу обмежені за областю, щоб контролювати, що агент може бачити:

| Рівень  | Область                                  |
| ------- | ---------------------------------------- |
| `self`  | Лише поточний сеанс                      |
| `tree`  | Поточний сеанс + створені субагенти      |
| `agent` | Усі сеанси для цього агента              |
| `all`   | Усі сеанси (між агентами, якщо налаштовано) |

За замовчуванням використовується `tree`. Сеанси в пісочниці обмежуються до `tree` незалежно від
конфігурації.

## Додаткові матеріали

- [Керування сеансами](/uk/concepts/session) -- маршрутизація, життєвий цикл, обслуговування
- [Агенти ACP](/uk/tools/acp-agents) -- створення зовнішніх harness
- [Мультиагентність](/uk/concepts/multi-agent) -- мультиагентна архітектура
- [Конфігурація Gateway](/uk/gateway/configuration) -- параметри конфігурації інструментів сеансу

## Пов'язане

- [Керування сеансами](/uk/concepts/session)
- [Обрізання сеансів](/uk/concepts/session-pruning)
