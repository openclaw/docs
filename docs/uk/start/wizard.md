---
read_when:
    - Запуск або налаштування онбордингу CLI
    - Налаштування нового комп’ютера
sidebarTitle: 'Onboarding: CLI'
summary: 'Онбординг CLI: кероване налаштування gateway, робочого простору, каналів і skills'
title: Онбординг (CLI)
x-i18n:
    generated_at: "2026-06-27T18:22:17Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 77bbf3d1f953ea2fca148090377f9537b00b657b2d7201c21aea902800815fd2
    source_path: start/wizard.md
    workflow: 16
---

CLI-онбординг є **рекомендованим** шляхом налаштування OpenClaw у терміналі на
macOS, Linux або Windows. Користувачі Windows desktop також можуть почати з
[Windows Hub](/uk/platforms/windows).
Він налаштовує локальний Gateway або підключення до віддаленого Gateway, а також канали, skills
і типові параметри робочої області в одному керованому процесі.

```bash
openclaw onboard
```

## Локаль

Майстер CLI локалізує фіксований текст онбордингу. Він визначає локаль з
`OPENCLAW_LOCALE`, потім `LC_ALL`, потім `LC_MESSAGES`, потім `LANG`, і
повертається до англійської. Підтримувані локалі майстра: `en`, `zh-CN` і `zh-TW`.

```bash
OPENCLAW_LOCALE=zh-CN openclaw onboard
```

Імена та стабільні ідентифікатори лишаються буквальними: `OpenClaw`, `Gateway`, `Tailscale`,
команди, ключі конфігурації, URL-адреси, ідентифікатори провайдерів, ідентифікатори моделей і мітки plugin/каналів
не перекладаються.

<Info>
Найшвидший перший чат: відкрийте Control UI (налаштування каналу не потрібне). Запустіть
`openclaw dashboard` і спілкуйтеся в браузері. Документація: [Dashboard](/uk/web/dashboard).
</Info>

Щоб переналаштувати пізніше:

```bash
openclaw configure
openclaw agents add <name>
```

<Note>
`--json` не означає неінтерактивний режим. Для скриптів використовуйте `--non-interactive`.
</Note>

<Tip>
CLI-онбординг містить крок вебпошуку, де можна вибрати провайдера,
наприклад Brave, DuckDuckGo, Exa, Firecrawl, Gemini, Grok, Kimi, MiniMax Search,
Ollama Web Search, Perplexity, SearXNG або Tavily. Деякі провайдери потребують
API-ключ, тоді як інші працюють без ключа. Це також можна налаштувати пізніше за допомогою
`openclaw configure --section web`. Документація: [Вебінструменти](/uk/tools/web).
</Tip>

## QuickStart проти Advanced

Онбординг починається з вибору **QuickStart** (типові параметри) або **Advanced** (повний контроль).

<Tabs>
  <Tab title="QuickStart (типові параметри)">
    - Локальний gateway (loopback)
    - Типова робоча область (або наявна робоча область)
    - Порт Gateway **18789**
    - Автентифікація Gateway **Token** (генерується автоматично, навіть на loopback)
    - Типова політика інструментів для нових локальних налаштувань: `tools.profile: "coding"` (наявний явно заданий профіль зберігається)
    - Типова ізоляція DM: локальний онбординг записує `session.dmScope: "per-channel-peer"`, якщо не задано. Докладніше: [Довідник із налаштування CLI](/uk/start/wizard-cli-reference#outputs-and-internals)
    - Експозиція Tailscale **Вимкнено**
    - Telegram + WhatsApp DM за замовчуванням використовують **allowlist** (вам буде запропоновано ввести номер телефону)

  </Tab>
  <Tab title="Advanced (повний контроль)">
    - Відкриває кожен крок (режим, робоча область, gateway, канали, демон, skills).

  </Tab>
</Tabs>

## Що налаштовує онбординг

**Локальний режим (типовий)** проводить вас через такі кроки:

1. **Модель/автентифікація** — виберіть будь-якого підтримуваного провайдера/потік автентифікації (API-ключ, OAuth або ручну автентифікацію, специфічну для провайдера), включно з Custom Provider
   (сумісний з OpenAI, сумісний з Anthropic або Unknown із автовизначенням). Виберіть типову модель.
   Примітка з безпеки: якщо цей агент запускатиме інструменти або оброблятиме вміст webhook/hooks, віддавайте перевагу найсильнішій доступній моделі останнього покоління й тримайте політику інструментів суворою. Слабші/старіші рівні легше піддаються prompt-injection.
   Для неінтерактивних запусків `--secret-input-mode ref` зберігає посилання на основі env у профілях автентифікації замість відкритих значень API-ключів.
   У неінтерактивному режимі `ref` змінна середовища провайдера має бути задана; передавання inline-прапорів ключа без цієї змінної середовища швидко завершується помилкою.
   В інтерактивних запусках вибір режиму посилання на секрет дає змогу вказати або змінну середовища, або налаштоване посилання провайдера (`file` чи `exec`), із швидкою попередньою валідацією перед збереженням.
   Для Anthropic інтерактивний онбординг/configure пропонує **Anthropic Claude CLI** як бажаний локальний шлях і **Anthropic API key** як рекомендований виробничий шлях. Anthropic setup-token також лишається доступним як підтримуваний шлях автентифікації за токеном.
2. **Робоча область** — розташування для файлів агента (типово `~/.openclaw/workspace`). Засіває bootstrap-файли.
3. **Gateway** — порт, адреса прив’язки, режим автентифікації, експозиція Tailscale.
   В інтерактивному режимі токена виберіть типове зберігання токена відкритим текстом або увімкніть SecretRef.
   Неінтерактивний шлях SecretRef для токена: `--gateway-token-ref-env <ENV_VAR>`.
4. **Канали** — вбудовані та офіційні plugin-канали чату, такі як iMessage, Discord, Feishu, Google Chat, Mattermost, Microsoft Teams, QQ Bot, Signal, Slack, Telegram, WhatsApp тощо.
5. **Демон** — встановлює LaunchAgent (macOS), користувацький модуль systemd (Linux/WSL2) або нативне Windows Scheduled Task із резервним варіантом Startup-folder для кожного користувача.
   Якщо автентифікація токеном потребує токен і `gateway.auth.token` керується SecretRef, встановлення демона перевіряє його, але не зберігає розв’язаний токен у метаданих середовища служби supervisor.
   Якщо автентифікація токеном потребує токен, а налаштований token SecretRef не розв’язується, встановлення демона блокується з практичними підказками.
   Якщо налаштовано і `gateway.auth.token`, і `gateway.auth.password`, а `gateway.auth.mode` не задано, встановлення демона блокується, доки режим не буде задано явно.
6. **Перевірка стану** — запускає Gateway і перевіряє, що він працює.
7. **Skills** — встановлює рекомендовані skills і необов’язкові залежності.

<Note>
Повторний запуск онбордингу **не** стирає нічого, якщо ви явно не виберете **Reset** (або не передасте `--reset`).
CLI `--reset` типово застосовується до конфігурації, облікових даних і сеансів; використовуйте `--reset-scope full`, щоб включити робочу область.
Якщо конфігурація недійсна або містить застарілі ключі, онбординг попросить спочатку запустити `openclaw doctor`.
</Note>

**Віддалений режим** лише налаштовує локальний клієнт для підключення до Gateway в іншому місці.
Він **не** встановлює й не змінює нічого на віддаленому хості.

## Додати ще одного агента

Використовуйте `openclaw agents add <name>`, щоб створити окремого агента з власною робочою областю,
сеансами та профілями автентифікації. Запуск без `--workspace` запускає онбординг.

Що він задає:

- `agents.list[].name`
- `agents.list[].workspace`
- `agents.list[].agentDir`

Примітки:

- Типові робочі області відповідають `~/.openclaw/workspace-<agentId>`.
- Додайте `bindings`, щоб маршрутизувати вхідні повідомлення (онбординг може це зробити).
- Неінтерактивні прапори: `--model`, `--agent-dir`, `--bind`, `--non-interactive`.

## Повний довідник

Докладні покрокові розбори та вихідні дані конфігурації див. у
[Довіднику з налаштування CLI](/uk/start/wizard-cli-reference).
Неінтерактивні приклади див. у [Автоматизації CLI](/uk/start/wizard-cli-automation).
Глибший технічний довідник, включно з деталями RPC, див. у
[Довіднику з онбордингу](/uk/reference/wizard).

## Пов’язані документи

- Довідник команд CLI: [`openclaw onboard`](/uk/cli/onboard)
- Огляд онбордингу: [Огляд онбордингу](/uk/start/onboarding-overview)
- Онбординг застосунку macOS: [Онбординг](/uk/start/onboarding)
- Ритуал першого запуску агента: [Ініціалізація агента](/uk/start/bootstrapping)
