---
read_when:
    - Зміна рендерингу виводу асистента в Control UI
    - Налагодження директив представлення `[embed ...]`, `MEDIA:`, reply або audio
summary: Протокол shortcode для розширеного виводу для вбудовувань, медіа, підказок аудіо та відповідей
title: Протокол розширеного виводу
x-i18n:
    generated_at: "2026-04-25T17:33:49Z"
    model: gpt-5.4
    provider: openai
    source_hash: 89e01037a8cb80c9de36effd4642701dcc86131a2b8fb236d61c687845e64189
    source_path: reference/rich-output-protocol.md
    workflow: 15
---

Вивід асистента може містити невеликий набір директив доставки/рендерингу:

- `MEDIA:` для доставки вкладень
- `[[audio_as_voice]]` для підказок представлення аудіо
- `[[reply_to_current]]` / `[[reply_to:<id>]]` для метаданих відповіді
- `[embed ...]` для розширеного рендерингу в Control UI

Ці директиви є окремими. Теги `MEDIA:` і reply/voice залишаються метаданими доставки; `[embed ...]` — це шлях розширеного рендерингу лише для вебінтерфейсу.
Довірені медіа з результатів інструментів використовують той самий парсер `MEDIA:` / `[[audio_as_voice]]` перед доставкою, тож текстові виводи інструментів усе ще можуть позначати аудіовкладення як голосове повідомлення.

Коли ввімкнено потокове передавання блоків, `MEDIA:` залишається метаданими одноразової доставки для
ходу. Якщо ту саму URL-адресу медіа надіслано в потоковому блоці та повторено у фінальному
payload асистента, OpenClaw доставляє вкладення один раз і прибирає дублікат
із фінального payload.

## `[embed ...]`

`[embed ...]` — це єдиний доступний агенту синтаксис розширеного рендерингу для Control UI.

Приклад самозакривного тега:

```text
[embed ref="cv_123" title="Status" /]
```

Правила:

- `[view ...]` більше не є дійсним для нового виводу.
- Shortcode вбудовувань рендеряться лише в поверхні повідомлення асистента.
- Рендеряться лише вбудовування, підкріплені URL. Використовуйте `ref="..."` або `url="..."`.
- Shortcode вбудовувань у формі блоків з inline HTML не рендеряться.
- Вебінтерфейс прибирає shortcode з видимого тексту й рендерить вбудовування inline.
- `MEDIA:` не є псевдонімом для embed і не має використовуватися для розширеного рендерингу embed.

## Збережена форма рендерингу

Нормалізований/збережений блок вмісту асистента — це структурований елемент `canvas`:

```json
{
  "type": "canvas",
  "preview": {
    "kind": "canvas",
    "surface": "assistant_message",
    "render": "url",
    "viewId": "cv_123",
    "url": "/__openclaw__/canvas/documents/cv_123/index.html",
    "title": "Status",
    "preferredHeight": 320
  }
}
```

Збережені/відрендерені розширені блоки безпосередньо використовують цю форму `canvas`. `present_view` не розпізнається.

## Пов’язане

- [RPC-адаптери](/uk/reference/rpc)
- [Typebox](/uk/concepts/typebox)
