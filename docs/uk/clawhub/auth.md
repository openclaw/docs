---
read_when:
    - Вхід у ClawHub
    - Використання ClawHub CLI
    - Налагодження помилок 401
summary: Вхід у ClawHub, токени API, вхід у CLI, зберігання токенів і відкликання.
x-i18n:
    generated_at: "2026-05-12T08:44:07Z"
    model: gpt-5.5
    provider: openai
    source_hash: 261f5a93200db8415e3bc8f35251c3486110ce8e076c482e846ad11f2ccd517f
    source_path: clawhub/auth.md
    workflow: 16
---

# Автентифікація

ClawHub використовує GitHub для входу у вебі. CLI використовує API-токени ClawHub, створені
через цей обліковий запис, у який виконано вхід.

## Вхід у вебі

Використовуйте GitHub, щоб увійти на [clawhub.ai](https://clawhub.ai).

Видалені, заблоковані або вимкнені облікові записи не можуть завершити звичайний вхід у ClawHub.
Якщо після входу ви повертаєтеся до стану без виконаного входу, ваш обліковий запис може бути
не в належному стані.

## Вхід у CLI

Типовий потік входу в CLI відкриває ваш браузер:

```bash
clawhub login
clawhub whoami
```

Що відбувається:

1. CLI запускає тимчасовий сервер зворотного виклику на `127.0.0.1`.
2. Ваш браузер відкриває сторінку входу ClawHub.
3. Після входу через GitHub ClawHub створює API-токен.
4. Браузер переспрямовує назад до локального зворотного виклику.
5. CLI зберігає токен у вашому конфігураційному файлі ClawHub.

Якщо ваш браузер не може дістатися до локального зворотного виклику через правила firewall, VPN або
proxy, використовуйте потік токена без графічного інтерфейсу.

## Вхід без графічного інтерфейсу

Створіть токен у вебінтерфейсі ClawHub, а потім передайте його CLI:

```bash
clawhub login --token clh_...
```

Використовуйте цей потік для серверів, завдань CI або середовищ лише з терміналом.

Для віддалених оболонок, де ви можете відкрити браузер в іншому місці, виконайте:

```bash
clawhub login --device
```

CLI виводить одноразовий код і чекає, доки ви авторизуєте його на
`https://clawhub.ai/cli/device`.

## Зберігання токенів

Типові шляхи конфігурації:

- macOS: `~/Library/Application Support/clawhub/config.json`
- Linux/XDG: `$XDG_CONFIG_HOME/clawhub/config.json` або `~/.config/clawhub/config.json`
- Windows: `%APPDATA%\\clawhub\\config.json`

Перевизначте шлях за допомогою:

```bash
export CLAWHUB_CONFIG_PATH=/path/to/config.json
```

## Відкликання

Ви можете відкликати API-токени у вебінтерфейсі ClawHub.

Відкликані, недійсні або відсутні токени повертають `401 Unauthorized`. Увійдіть знову
за допомогою `clawhub login` або надайте свіжий токен за допомогою `clawhub login --token`.

Видалені, заблоковані або вимкнені облікові записи не можуть продовжувати використовувати наявні API-токени.
