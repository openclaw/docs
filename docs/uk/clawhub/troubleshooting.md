---
read_when:
    - Не вдається виконати команди ClawHub CLI або реєстру OpenClaw
    - Пакет неможливо встановити, опублікувати або оновити
summary: Усунення проблем із входом у ClawHub, встановленням, публікацією, оновленням і API.
x-i18n:
    generated_at: "2026-06-28T00:12:41Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: fc789fcc891cf8c44b5d1a10d38a4e6dd4dec9474d8d13f8058ea1c3392a9f91
    source_path: clawhub/troubleshooting.md
    workflow: 16
---

# Усунення несправностей

## `clawhub login` відкриває браузер, але ніколи не завершується

CLI запускає короткочасний локальний сервер зворотного виклику під час входу через браузер.

- Переконайтеся, що ваш браузер може відкрити `http://127.0.0.1:<port>/callback`.
- Перевірте локальний брандмауер, VPN і правила проксі, якщо зворотний виклик не надходить.
- У середовищах без графічного інтерфейсу створіть API-токен у вебінтерфейсі ClawHub і виконайте:

```bash
clawhub login --token clh_...
```

## `whoami` або `publish` повертає `Unauthorized` (401)

- Увійдіть знову за допомогою `clawhub login`.
- Якщо ви використовуєте власний шлях конфігурації, переконайтеся, що `CLAWHUB_CONFIG_PATH` вказує на
  файл, який містить ваш поточний токен.
- Якщо ви використовуєте API-токен, переконайтеся, що його не було відкликано у вебінтерфейсі.

## Пошук або встановлення повертає `Rate limit exceeded` (429)

Прочитайте інформацію про повторну спробу у відповіді:

- `Retry-After`: кількість секунд, які потрібно зачекати перед повторною спробою.
- `RateLimit-Limit`: обмеження, застосоване до цього запиту.
- `RateLimit-Remaining`: ваш точний залишковий бюджет, коли заголовок присутній. Для `429` це `0`.
- `RateLimit-Reset` або `X-RateLimit-Reset`: час скидання.

Якщо багато користувачів спільно використовують одну вихідну IP-адресу, анонімні IP-обмеження можуть спрацювати, навіть якщо кожна
людина надсилає лише кілька запитів. Увійдіть, де це можливо, і повторіть спробу після
повідомленої затримки.

## Пошук або встановлення не працює за проксі

CLI враховує стандартні змінні проксі:

```bash
export HTTPS_PROXY=http://proxy.example.com:3128
clawhub search "my query"
```

Підтримувані назви включають `HTTPS_PROXY`, `HTTP_PROXY`, `https_proxy` і
`http_proxy`.

## Skill не з’являється в пошуку

- Перевірте точний slug або сторінку власника, якщо ви їх знаєте.
- Переконайтеся, що реліз є публічним і не затриманий скануванням або модерацією.
- Якщо ви власник Skill, увійдіть і перегляньте його:

```bash
clawhub inspect @openclaw/demo
```

Діагностика, видима власнику, може пояснити стан сканування, шлюзу завантаження або модерації.

## Публікація не вдається, бо бракує обов’язкових метаданих

Для Skills перевірте frontmatter у `SKILL.md`. Обов’язкові змінні середовища й
інструменти слід оголосити, щоб користувачі та сканери могли зрозуміти пакет.

Для plugins перевірте метадані сумісності в `package.json`. Публікації code-plugin
потребують полів сумісності OpenClaw, як-от `openclaw.compat.pluginApi` і
`openclaw.build.openclawVersion`.

Спершу перегляньте payload публікації:

```bash
clawhub package publish <source> --family code-plugin --dry-run
```

## Публікація не вдається через помилку власника GitHub або джерела

ClawHub використовує ідентичність GitHub і атрибуцію джерела, щоб пов’язувати пакети з їхніми
видавцями.

- Переконайтеся, що ви ввійшли з обліковим записом GitHub, який володіє пакетом або може його публікувати.
- Перевірте, що URL джерела є публічним або доступним для ClawHub.
- Для джерел GitHub використовуйте `owner/repo`, `owner/repo@ref` або повну URL-адресу GitHub.

## Публікація не вдається, бо namespace заявлений або зарезервований

Якщо публікація не вдається, бо handle власника, namespace організації, scope пакета, slug Skill
або назва пакета вже заявлені чи зарезервовані, спершу переконайтеся, що ви
публікуєте від власника, який відповідає namespace. Для пакетів plugin
scoped-імена, як-от `@example-org/example-plugin`, потрібно публікувати від
відповідного власника `example-org`.

Якщо ви вважаєте, що ваша організація, проєкт або бренд є правомірним власником namespace, але
ви не можете керувати поточним власником ClawHub, відкрийте
[issue щодо заявки на організацію / namespace](https://github.com/openclaw/clawhub/issues/new?template=org-namespace-claim.yml)
з публічним, нечутливим доказом. Див.
[Заявки на організації та namespace](/uk/clawhub/namespace-claims) для рекомендацій щодо доказів і того, що
не слід додавати до публічних issues.

## `sync` повідомляє, що Skills не знайдено

`sync` шукає папки, що містять `SKILL.md` або `skill.md`.

Спрямуйте його на корені, які потрібно сканувати:

```bash
clawhub sync --root /path/to/skills
```

Спершу перегляньте попередній результат, якщо не впевнені, що буде опубліковано:

```bash
clawhub sync --all --dry-run --no-input
```

## `update` відмовляється через локальні зміни

Локальні файли не відповідають жодній версії, про яку знає ClawHub. Виберіть один варіант:

- Залишити локальні правки й пропустити оновлення.
- Перезаписати опублікованою версією:

```bash
clawhub update @openclaw/demo --force
```

- Опублікувати відредаговану копію як новий slug або fork.

## Встановлення plugin в OpenClaw не вдається

- Використайте явне джерело ClawHub:

```bash
openclaw plugins install clawhub:<package>
```

- Перевірте сторінку з деталями пакета щодо стану сканування та метаданих сумісності.
- Переконайтеся, що ваша версія OpenClaw відповідає заявленому пакетом
  діапазону сумісності.
- Якщо пакет прихований, затриманий або заблокований, його може бути неможливо встановити, доки
  власник не розв’яже проблему.

## Запити Public API не вдаються

- Дотримуйтеся заголовків повторної спроби `429` і кешуйте публічні відповіді списків/пошуку.
- Спрямовуйте користувачів назад до канонічного списку ClawHub.
- Не дзеркальте прихований, приватний, затриманий або заблокований модерацією вміст поза
  поверхнею Public API.

Див. [HTTP API](/uk/clawhub/http-api) для подробиць про endpoints.
