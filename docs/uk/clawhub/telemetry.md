---
read_when:
    - Працюємо над засобами керування телеметрією / приватністю
    - Питання про те, які дані збираються
summary: Телеметрія встановлення, яку збирає ClawHub CLI, і як відмовитися від неї.
x-i18n:
    generated_at: "2026-06-28T22:33:03Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# Телеметрія

ClawHub використовує мінімальну телеметрію CLI для обчислення сукупної кількості встановлень.

## Коли збирається телеметрія

Телеметрія надсилається лише коли:

- Ви ввійшли в систему в CLI.
- Ви запускаєте `clawhub install <slug>`.
- Телеметрію **не вимкнено** (див. «Як вимкнути» нижче).

Якщо ви не ввійшли в систему, нічого не надсилається.

## Що ми збираємо

Під час кожного зареєстрованого `clawhub install` CLI надсилає одну подію встановлення за принципом максимально можливих зусиль.

Подія містить:

- `slug`: slug встановленого skill.
- `version`: встановлена версія, якщо відома.

### Що ми _не_ збираємо

- Жодних шляхів до папок або ідентифікаторів, отриманих із папок.
- Жодного вмісту файлів.
- Жодних журналів окремих запусків, запитів або іншого виводу CLI.

## Кількість встановлень

ClawHub підтримує сукупні лічильники для кожного skill:

- `installsAllTime`: унікальні користувачі, які повідомили принаймні про одне встановлення skill через CLI.
- `installsCurrent`: унікальні користувачі, які повідомили про встановлення й не видалили свої
  телеметричні дані.

## Прозорість і керування користувачем

Усі бачать лише **сукупні лічильники встановлень**.

Видалення вашого облікового запису також видаляє ваші телеметричні дані.

## Як вимкнути телеметрію

Установіть змінну середовища:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

Коли її встановлено, CLI не надсилатиме телеметрію встановлень.
