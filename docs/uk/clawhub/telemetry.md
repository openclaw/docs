---
read_when:
    - Робота над елементами керування телеметрією / конфіденційністю
    - Питання про те, які дані збираються
summary: Телеметрія встановлення, яку збирає ClawHub CLI, і як відмовитися від неї.
x-i18n:
    generated_at: "2026-06-28T00:12:36Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# Телеметрія

ClawHub використовує мінімальну телеметрію CLI, щоб обчислювати агреговану кількість встановлень.

## Коли збирається телеметрія

Телеметрія надсилається лише тоді, коли:

- Ви ввійшли в обліковий запис у CLI.
- Ви виконуєте `clawhub install <slug>`.
- Телеметрію **не вимкнено** (див. «Як вимкнути» нижче).

Якщо ви не ввійшли в обліковий запис, нічого не повідомляється.

## Що ми збираємо

Під час кожного повідомленого `clawhub install` CLI надсилає одну подію встановлення за принципом найкращої спроби.

Подія містить:

- `slug`: slug встановленої навички.
- `version`: встановлену версію, якщо вона відома.

### Чого ми _не_ збираємо

- Жодних шляхів до папок або ідентифікаторів, похідних від папок.
- Жодного вмісту файлів.
- Жодних журналів окремих запусків, запитів або іншого виводу CLI.

## Кількість встановлень

ClawHub підтримує агреговані лічильники для кожної навички:

- `installsAllTime`: унікальні користувачі, які повідомили принаймні про одне встановлення навички через CLI.
- `installsCurrent`: унікальні користувачі, які повідомили про встановлення й не видалили свою
  телеметрію.

## Прозорість + елементи керування для користувачів

Усі бачать лише **агреговані лічильники встановлень**.

Видалення вашого облікового запису також видаляє ваші дані телеметрії.

## Як вимкнути телеметрію

Установіть змінну середовища:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

Коли її встановлено, CLI не надсилатиме телеметрію встановлень.
