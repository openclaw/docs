---
read_when:
    - Ви хочете керувати Gateway з браузера
    - Вам потрібен доступ до Tailnet без SSH-тунелів
sidebarTitle: Control UI
summary: Браузерний інтерфейс керування для Gateway (чат, активність, вузли, конфігурація)
title: Інтерфейс керування
x-i18n:
    generated_at: "2026-07-03T09:58:27Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: b23d0e2aeefc3b746f1ab51cd9049135e2695ab77cf5cbb5eab6ec0df90f011d
    source_path: web/control-ui.md
    workflow: 16
---

The Control UI — це невеликий односторінковий застосунок **Vite + Lit**, який обслуговує Gateway:

- стандартно: `http://<host>:18789/`
- необов’язковий префікс: задайте `gateway.controlUi.basePath` (наприклад, `/openclaw`)

Він взаємодіє **безпосередньо з Gateway WebSocket** на тому самому порту.

## Швидке відкриття (локально)

Якщо Gateway запущено на тому самому комп’ютері, відкрийте:

- [http://127.0.0.1:18789/](http://127.0.0.1:18789/) (або [http://localhost:18789/](http://localhost:18789/))

Якщо сторінка не завантажується, спочатку запустіть Gateway: `openclaw gateway`.

<Note>
У нативних LAN-прив’язках Windows, Windows Firewall або керована організацією Group Policy усе ще можуть блокувати оголошену LAN-URL, навіть коли `127.0.0.1` працює на хості Gateway. Запустіть `openclaw gateway status --deep` на хості Windows; команда повідомляє про ймовірно заблоковані порти, невідповідності профілів і локальні правила firewall, які політика може ігнорувати.
</Note>

Автентифікація передається під час WebSocket handshake через:

- `connect.params.auth.token`
- `connect.params.auth.password`
- заголовки ідентичності Tailscale Serve, коли `gateway.auth.allowTailscale: true`
- заголовки ідентичності trusted-proxy, коли `gateway.auth.mode: "trusted-proxy"`

Панель налаштувань dashboard зберігає токен для поточної сесії вкладки браузера та вибраної URL Gateway; паролі не зберігаються. Onboarding зазвичай генерує токен Gateway для автентифікації shared-secret під час першого підключення, але автентифікація паролем також працює, коли `gateway.auth.mode` має значення `"password"`.

## Сполучення пристрою (перше підключення)

Коли ви підключаєтеся до Control UI з нового браузера або пристрою, Gateway зазвичай вимагає **одноразового підтвердження сполучення**. Це захід безпеки для запобігання несанкціонованому доступу.

**Що ви побачите:** "disconnected (1008): pairing required"

<Steps>
  <Step title="List pending requests">
    ```bash
    openclaw devices list
    ```
  </Step>
  <Step title="Approve by request ID">
    ```bash
    openclaw devices approve <requestId>
    ```
  </Step>
</Steps>

Якщо браузер повторює спробу сполучення зі зміненими даними автентифікації (роль/області доступу/публічний ключ), попередній запит у стані очікування замінюється, і створюється новий `requestId`. Перед підтвердженням повторно запустіть `openclaw devices list`.

Якщо браузер уже сполучено, і ви змінюєте його доступ із читання на запис/адміністрування, це обробляється як підвищення рівня підтвердження, а не як непомітне повторне підключення. OpenClaw залишає старе підтвердження активним, блокує повторне підключення з ширшими правами й просить вас явно підтвердити новий набір областей доступу.

Після підтвердження пристрій запам’ятовується й не потребуватиме повторного підтвердження, доки ви не відкличете його командою `openclaw devices revoke --device <id> --role <role>`. Див. [Devices CLI](/uk/cli/devices) щодо ротації та відкликання токенів.

Агенти Paperclip, які підключаються через адаптер `openclaw_gateway`, використовують той самий потік підтвердження першого запуску. Після початкової спроби підключення запустіть `openclaw devices approve --latest`, щоб переглянути запит у стані очікування, а потім повторно запустіть надруковану команду `openclaw devices approve <requestId>`, щоб підтвердити його. Передавайте явні значення `--url` і `--token` для віддаленого Gateway. Щоб підтвердження залишалися стабільними між перезапусками, налаштуйте постійний `adapterConfig.devicePrivateKeyPem` у Paperclip замість того, щоб дозволяти йому генерувати нову тимчасову ідентичність пристрою під час кожного запуску.

<Note>
- Прямі браузерні підключення через local loopback (`127.0.0.1` / `localhost`) підтверджуються автоматично.
- Tailscale Serve може пропустити цикл сполучення для операторських сесій Control UI, коли `gateway.auth.allowTailscale: true`, ідентичність Tailscale перевіряється, а браузер надає свою ідентичність пристрою.
- Прямі прив’язки Tailnet, браузерні підключення через LAN і профілі браузера без ідентичності пристрою все одно потребують явного підтвердження.
- Кожен профіль браузера генерує унікальний ID пристрою, тому зміна браузера або очищення даних браузера вимагатиме повторного сполучення.

</Note>

## Особиста ідентичність (локальна для браузера)

Control UI підтримує особисту ідентичність для кожного браузера (відображуване ім’я та аватар), яка додається до вихідних повідомлень для атрибуції у спільних сесіях. Вона зберігається в сховищі браузера, обмежена поточним профілем браузера й не синхронізується з іншими пристроями та не зберігається на сервері, окрім звичайних метаданих авторства стенограми для повідомлень, які ви фактично надсилаєте. Очищення даних сайту або зміна браузера скидає її до порожнього стану.

Такий самий локальний для браузера шаблон застосовується до перевизначення аватара асистента. Завантажені аватари асистента накладаються на ідентичність, визначену Gateway, лише в локальному браузері й ніколи не проходять туди-назад через `config.patch`. Спільне поле конфігурації `ui.assistant.avatar` досі доступне для клієнтів не з UI, які записують це поле напряму (наприклад, scripted gateways або власні dashboards).

## Ендпоінт конфігурації runtime

Control UI отримує свої runtime-налаштування з `/control-ui-config.json`, розв’язаного відносно базового шляху Control UI у Gateway (наприклад, `/__openclaw__/control-ui-config.json`, коли UI обслуговується під `/__openclaw__/`). Цей ендпоінт захищений тією самою автентифікацією Gateway, що й решта HTTP-поверхні: неавтентифіковані браузери не можуть його отримати, а успішне отримання вимагає або вже чинного токена/пароля Gateway, або ідентичності Tailscale Serve, або ідентичності trusted-proxy.

## Підтримка мов

Control UI може локалізувати себе під час першого завантаження на основі локалі вашого браузера. Щоб перевизначити її пізніше, відкрийте **Overview -> Gateway Access -> Language**. Вибір локалі розміщено в картці Gateway Access, а не в Appearance.

- Підтримувані локалі: `en`, `zh-CN`, `zh-TW`, `pt-BR`, `de`, `es`, `ja-JP`, `ko`, `fr`, `ar`, `it`, `tr`, `uk`, `id`, `pl`, `th`, `vi`, `nl`, `fa`
- Неанглійські переклади завантажуються в браузері відкладено.
- Вибрана локаль зберігається в сховищі браузера й повторно використовується під час майбутніх відвідувань.
- Відсутні ключі перекладу повертаються до англійської.

Переклади документації генеруються для того самого набору неанглійських локалей, але вбудований вибір мови сайту документації Mintlify обмежений кодами локалей, які приймає Mintlify. Документація тайською (`th`) і перською (`fa`) усе ще генерується в репозиторії публікації; вона може не з’являтися в цьому виборі, доки Mintlify не підтримуватиме ці коди.

## Теми вигляду

Панель Appearance зберігає вбудовані теми Claw, Knot і Dash, а також один локальний для браузера слот імпорту tweakcn. Щоб імпортувати тему, відкрийте [tweakcn editor](https://tweakcn.com/editor/theme), виберіть або створіть тему, натисніть **Share** і вставте скопійоване посилання на тему в Appearance. Імпортер також приймає registry-URL `https://tweakcn.com/r/themes/<id>`, URL редактора на кшталт `https://tweakcn.com/editor/theme?theme=amethyst-haze`, відносні шляхи `/themes/<id>`, сирі ID тем і назви стандартних тем, як-от `amethyst-haze`.

Appearance також містить локальне для браузера налаштування розміру тексту. Налаштування зберігається разом з іншими параметрами Control UI, застосовується до тексту чату, тексту composer, карток інструментів і бічних панелей чату, а також утримує текстові поля щонайменше 16px, щоб mobile Safari не виконував автоматичне масштабування під час фокуса.

Імпортовані теми зберігаються лише в поточному профілі браузера. Вони не записуються в конфігурацію Gateway і не синхронізуються між пристроями. Заміна імпортованої теми оновлює один локальний слот; очищення перемикає активну тему назад на Claw, якщо було вибрано імпортовану тему.

## Що він може робити (сьогодні)

<AccordionGroup>
  <Accordion title="Chat and Talk">
    - Спілкуватися в чаті з моделлю через Gateway WS (`chat.history`, `chat.send`, `chat.abort`, `chat.inject`).
    - Оновлення історії чату запитує обмежене нещодавнє вікно з лімітами тексту для кожного повідомлення, щоб великі сесії не змушували браузер рендерити повне навантаження стенограми до того, як чат стане придатним до використання.
    - Розмовляти через browser realtime sessions. OpenAI використовує прямий WebRTC, Google Live використовує обмежений одноразовий браузерний токен через WebSocket, а realtime voice plugins, доступні лише на backend, використовують relay-транспорт Gateway. Сесії провайдера, якими володіє клієнт, починаються з `talk.client.create`; relay-сесії Gateway починаються з `talk.session.create`. Relay зберігає облікові дані провайдера на Gateway, поки браузер транслює мікрофонний PCM через `talk.session.appendAudio`, пересилає provider tool calls `openclaw_agent_consult` через `talk.client.toolCall` для політики Gateway і більшої налаштованої моделі OpenClaw, а також маршрутизує voice steering активного запуску через `talk.client.steer` або `talk.session.steer`.
    - Транслювати tool calls + live-картки виводу інструментів у Chat (події агента).
    - Вкладка Activity з локальними для браузера, redaction-first підсумками live-активності інструментів із наявної доставки `session.tool` / подій інструментів.

  </Accordion>
  <Accordion title="Channels, instances, sessions, dreams">
    - Канали: статус вбудованих, а також bundled/external plugin channels, QR-вхід і конфігурація для кожного каналу (`channels.status`, `web.login.*`, `config.patch`).
    - Оновлення channel probe залишають попередній snapshot видимим, поки повільні перевірки провайдера завершуються, а partial snapshots позначаються, коли probe або audit перевищує свій UI-бюджет.
    - Екземпляри: список присутності + оновлення (`system-presence`).
    - Сесії: за замовчуванням показувати список сесій налаштованого агента, виконувати fallback зі stale unconfigured agent session keys і застосовувати per-session перевизначення model/thinking/fast/verbose/trace/reasoning (`sessions.list`, `sessions.patch`).
    - Dreams: статус dreaming, перемикач увімкнення/вимкнення та reader Dream Diary (`doctor.memory.status`, `doctor.memory.dreamDiary`, `config.patch`).

  </Accordion>
  <Accordion title="Cron, skills, nodes, exec approvals">
    - Завдання Cron: список/додавання/редагування/запуск/увімкнення/вимкнення + історія запусків (`cron.*`).
    - Skills: статус, увімкнення/вимкнення, встановлення, оновлення API-ключів (`skills.*`).
    - Вузли: список + обмеження (`node.list`).
    - Підтвердження exec: редагування allowlists Gateway або вузлів + політика запиту для `exec host=gateway/node` (`exec.approvals.*`).

  </Accordion>
  <Accordion title="Config">
    - Перегляд/редагування `~/.openclaw/openclaw.json` (`config.get`, `config.set`).
    - MCP має окрему сторінку налаштувань для налаштованих серверів, enablement, підсумків OAuth/filter/parallel, поширених операторських команд і scoped редактора конфігурації `mcp`.
    - Застосування + перезапуск із валідацією (`config.apply`) і пробудження останньої активної сесії.
    - Записи містять захист base-hash, щоб запобігти перезапису паралельних редагувань.
    - Записи (`config.set`/`config.apply`/`config.patch`) виконують preflight active SecretRef resolution для refs у надісланому payload конфігурації; unresolved active submitted refs відхиляються перед записом.
    - Збереження форми відкидає застарілі редаговані placeholders, які неможливо відновити зі збереженої конфігурації, зберігаючи редаговані значення, які все ще відповідають збереженим secret.
    - Schema + form rendering (`config.schema` / `config.schema.lookup`, включно з field `title` / `description`, matched UI hints, immediate child summaries, docs metadata на вкладених object/wildcard/array/composition nodes, а також plugin + channel schemas, коли доступні); Raw JSON editor доступний лише тоді, коли snapshot має safe raw round-trip.
    - Якщо snapshot не може безпечно виконати round-trip raw text, Control UI примусово вмикає Form mode і вимикає Raw mode для цього snapshot.
    - Raw JSON editor "Reset to saved" зберігає raw-authored форму (formatting, comments, `$include` layout) замість повторного рендерингу flattened snapshot, щоб зовнішні редагування переживали reset, коли snapshot може безпечно виконати round-trip.
    - Структуровані значення об’єктів SecretRef рендеряться лише для читання в текстових полях форми, щоб запобігти випадковому пошкодженню object-to-string.

  </Accordion>
  <Accordion title="Debug, logs, update">
    - Налагодження: snapshots status/health/models + event log + ручні RPC-виклики (`status`, `health`, `models.list`).
    - Event log містить timings оновлення/RPC Control UI, timings повільного render чату/конфігурації та записи responsiveness браузера для long animation frames або long tasks, коли браузер надає ці типи записів PerformanceObserver.
    - Логи: live tail файлових логів Gateway із filter/export (`logs.tail`).
    - Оновлення: запуск оновлення package/git + перезапуск (`update.run`) зі звітом про перезапуск, потім опитування `update.status` після повторного підключення для перевірки запущеної версії Gateway.

  </Accordion>
  <Accordion title="Примітки до панелі завдань Cron">
    - Для ізольованих завдань доставка за замовчуванням оголошує підсумок. Ви можете перемкнути на none, якщо потрібні лише внутрішні запуски.
    - Поля каналу/цілі з’являються, коли вибрано оголошення.
    - Режим Webhook використовує `delivery.mode = "webhook"` із `delivery.to`, заданим як чинна HTTP(S) URL-адреса webhook.
    - Для завдань основного сеансу доступні режими доставки webhook і none.
    - Розширені елементи редагування включають видалення після запуску, очищення перевизначення агента, точні/рознесені параметри cron, перевизначення моделі/мислення агента та перемикачі доставки за принципом найкращої спроби.
    - Перевірка форми вбудована з помилками на рівні полів; некоректні значення вимикають кнопку збереження, доки їх не буде виправлено.
    - Задайте `cron.webhookToken`, щоб надсилати окремий bearer-токен; якщо його пропущено, webhook надсилається без заголовка автентифікації.
    - Застарілий fallback: запустіть `openclaw doctor --fix`, щоб мігрувати збережені застарілі завдання з `notify: true` з `cron.webhook` на явну доставку webhook або завершення для кожного завдання.

  </Accordion>
</AccordionGroup>

## Сторінка MCP

Окрема сторінка MCP — це операторський перегляд MCP-серверів, керованих OpenClaw, у `mcp.servers`. Вона сама не запускає транспорти MCP; використовуйте її, щоб переглядати й редагувати збережену конфігурацію, а потім використовуйте `openclaw mcp doctor --probe`, коли потрібне live-підтвердження сервера.

Типовий робочий процес:

1. Відкрийте **MCP** з бічної панелі.
2. Перевірте картки підсумку для загальної кількості, увімкнених, OAuth і відфільтрованих серверів.
3. Перегляньте кожен рядок сервера щодо транспорту, увімкнення, автентифікації, фільтрів, тайм-аутів і підказок команд.
4. Перемикайте увімкнення, коли сервер має залишатися налаштованим, але не брати участі у виявленні під час виконання.
5. Редагуйте scoped-розділ конфігурації `mcp` для визначень серверів, заголовків, шляхів TLS/mTLS, метаданих OAuth, фільтрів інструментів і метаданих проєкції Codex.
6. Використовуйте **Зберегти** для запису конфігурації або **Зберегти й опублікувати**, коли запущений Gateway має застосувати змінену конфігурацію.
7. Запустіть `openclaw mcp status --verbose`, `openclaw mcp doctor --probe` або `openclaw mcp reload` з термінала, коли відредагованому процесу потрібна статична діагностика, live-підтвердження або скидання кешованого runtime.

Сторінка редагує URL-подібні значення, що містять облікові дані, перед відтворенням і бере імена серверів у лапки у фрагментах команд, щоб скопійовані команди й надалі працювали з пробілами або shell-метасимволами. Повна довідка CLI та конфігурації міститься в [MCP](/uk/cli/mcp).

## Вкладка активності

Вкладка Activity — це ефемерний локальний для браузера спостерігач за live-активністю інструментів. Вона походить з того самого потоку подій Gateway `session.tool` / tool, який живить картки інструментів Chat; вона не додає іншої сім’ї подій Gateway, endpoint, довготривалого сховища активності, стрічки метрик або зовнішнього потоку спостерігача.

Записи активності зберігають лише очищені підсумки та відредаговані, обрізані попередні перегляди виводу. Значення аргументів інструментів не зберігаються у стані Activity; інтерфейс показує, що аргументи приховано, і записує лише кількість полів аргументів. Список у пам’яті прив’язаний до поточної вкладки браузера, переживає навігацію в межах Control UI та скидається під час перезавантаження сторінки, перемикання сеансу або натискання **Очистити**.

## Поведінка чату

<AccordionGroup>
  <Accordion title="Семантика надсилання та історії">
    - `chat.send` є **неблокувальним**: він одразу підтверджує з `{ runId, status: "started" }`, а відповідь транслюється через події `chat`. Довірені клієнти Control UI також можуть отримувати необов’язкові метадані часу ACK для локальної діагностики.
    - Завантаження в чат приймають зображення плюс невідеофайли. Зображення зберігають нативний шлях зображення; інші файли зберігаються як керовані медіа й показуються в історії як посилання на вкладення.
    - Повторне надсилання з тим самим `idempotencyKey` повертає `{ status: "in_flight" }` під час виконання та `{ status: "ok" }` після завершення.
    - Відповіді `chat.history` обмежені за розміром для безпеки інтерфейсу. Коли записи стенограми надто великі, Gateway може обрізати довгі текстові поля, пропускати важкі блоки метаданих і замінювати завеликі повідомлення placeholder-ом (`[chat.history omitted: message too large]`).
    - Коли видиме повідомлення асистента було обрізано в `chat.history`, бічний читач може за потреби отримати повний нормалізований для показу запис стенограми через `chat.message.get` за `sessionKey`, активним `agentId`, коли потрібно, і `messageId` стенограми. Якщо Gateway усе ще не може повернути більше, читач показує явний стан недоступності замість мовчазного повторення обрізаного попереднього перегляду.
    - Зображення, створені асистентом, зберігаються як керовані медіапосилання й повертаються через автентифіковані медіа-URL Gateway, тому перезавантаження не залежать від того, чи залишаються raw base64 payload-и зображень у відповіді історії чату.
    - Під час відтворення `chat.history` Control UI прибирає з видимого тексту асистента inline-теги директив лише для показу (наприклад `[[reply_to_*]]` і `[[audio_as_voice]]`), XML payload-и викликів інструментів у plain text (включно з `<tool_call>...</tool_call>`, `<function_call>...</function_call>`, `<tool_calls>...</tool_calls>`, `<function_calls>...</function_calls>` і обрізаними блоками викликів інструментів), а також leaked ASCII/full-width токени керування моделлю, і пропускає записи асистента, чий увесь видимий текст є лише точним silent-токеном `NO_REPLY` / `no_reply` або токеном підтвердження Heartbeat `HEARTBEAT_OK`.
    - Під час активного надсилання та фінального оновлення історії подання чату залишає локальні оптимістичні повідомлення користувача/асистента видимими, якщо `chat.history` короткочасно повертає старіший snapshot; канонічна стенограма замінює ці локальні повідомлення, щойно історія Gateway наздоганяє.
    - Live-події `chat` є станом доставки, тоді як `chat.history` перебудовується з довготривалої стенограми сеансу. Після фінальних подій інструментів Control UI перезавантажує історію й об’єднує лише невеликий оптимістичний хвіст; межа стенограми задокументована у [WebChat](/uk/web/webchat).
    - `chat.inject` додає нотатку асистента до стенограми сеансу й транслює подію `chat` для оновлень лише інтерфейсу (без запуску агента, без доставки каналом).
    - Бічна панель показує нещодавні сеанси з дією Новий сеанс, посиланням Усі сеанси та кнопкою пошуку сеансів, яка відкриває повний вибір сеансів (обмежений вибраним агентом, з пошуком і пагінацією). Перемикання агентів показує лише сеанси, прив’язані до цього агента, і fallback до основного сеансу цього агента, коли в нього ще немає збережених dashboard-сеансів.
    - На desktop-ширинах елементи керування чатом залишаються в одному компактному рядку й згортаються під час прокручування стенограми вниз; прокручування вгору, повернення до початку або досягнення кінця відновлює елементи керування.
    - Послідовні дублікати текстових повідомлень відтворюються як одна бульбашка з бейджем кількості. Повідомлення, що містять зображення, вкладення, вивід інструментів або попередні перегляди canvas, не згортаються.
    - Пікери моделі та thinking у заголовку чату негайно патчать активний сеанс через `sessions.patch`; це постійні перевизначення сеансу, а не параметри надсилання лише для одного ходу.
    - Якщо ви надсилаєте повідомлення, поки зміна пікера моделі для того самого сеансу ще зберігається, composer чекає на цей patch сеансу перед викликом `chat.send`, щоб надсилання використовувало вибрану модель.
    - Введення `/new` у Control UI створює той самий новий dashboard-сеанс, що й Новий чат, і перемикається на нього, крім випадку, коли налаштовано `session.dmScope: "main"` і поточний parent є основним сеансом агента; у такому разі він скидає основний сеанс на місці. Введення `/reset` зберігає явне in-place скидання Gateway для поточного сеансу.
    - Пікер моделі чату запитує налаштоване подання моделей Gateway. Якщо присутній `agents.defaults.models`, цей allowlist керує пікером, включно з записами `provider/*`, які зберігають provider-scoped каталоги динамічними. Інакше пікер показує явні записи `models.providers.*.models` плюс провайдерів із придатною автентифікацією. Повний каталог залишається доступним через debug RPC `models.list` з `view: "all"`.
    - Коли свіжі звіти про використання сеансу Gateway містять поточні context tokens, панель інструментів chat composer показує невелике кільце використання контексту з відсотком використання; повні відомості про токени містяться в tooltip. Кільце перемикається на стиль попередження за високого тиску контексту і, на рекомендованих рівнях Compaction, показує компактну кнопку, яка запускає звичайний шлях Compaction сеансу. Застарілі token snapshot-и приховуються, доки Gateway знову не повідомить свіже використання.

  </Accordion>
  <Accordion title="Режим Talk (браузерний realtime)">
    Режим Talk використовує зареєстрованого провайдера realtime-голосу. Налаштуйте OpenAI з `talk.realtime.provider: "openai"` плюс профілем автентифікації API-ключа `openai`, `talk.realtime.providers.openai.apiKey` або `OPENAI_API_KEY`; OAuth-профілі OpenAI не налаштовують Realtime-голос. Налаштуйте Google з `talk.realtime.provider: "google"` плюс `talk.realtime.providers.google.apiKey`. Браузер ніколи не отримує стандартний API-ключ провайдера. OpenAI отримує ефемерний Realtime client secret для WebRTC. Google Live отримує одноразовий обмежений Live API auth token для браузерного WebSocket-сеансу, з інструкціями та деклараціями інструментів, зафіксованими в токені Gateway. Провайдери, які надають лише backend realtime bridge, працюють через relay-транспорт Gateway, тому облікові дані та vendor socket-и залишаються на серверному боці, а браузерне аудіо проходить через автентифіковані RPC Gateway. Prompt Realtime-сеансу збирається Gateway; `talk.client.create` не приймає надані caller-ом перевизначення інструкцій.

    Chat composer містить кнопку параметрів Talk поруч із кнопкою запуску/зупинки Talk. Параметри застосовуються до наступного сеансу Talk і можуть перевизначати провайдера, транспорт, модель, голос, reasoning effort, поріг VAD, тривалість тиші та prefix padding. Коли параметр порожній, Gateway використовує налаштовані defaults, де доступно, або default провайдера. Вибір Gateway relay примусово використовує backend relay path; вибір WebRTC залишає сеанс client-owned і завершується помилкою замість мовчазного fallback до relay, якщо провайдер не може створити браузерний сеанс.

    У Chat composer елемент керування Talk — це кнопка з хвилями поруч із кнопкою диктування через мікрофон. Коли Talk запускається, рядок стану composer показує `Connecting Talk...`, потім `Talk live`, доки аудіо під’єднано, або `Asking OpenClaw...`, доки realtime-виклик інструмента консультується з налаштованою більшою моделлю через `talk.client.toolCall`.

    Maintainer live smoke: `OPENAI_API_KEY=... GEMINI_API_KEY=... node --import tsx scripts/dev/realtime-talk-live-smoke.ts` перевіряє backend WebSocket bridge OpenAI, браузерний WebRTC SDP exchange OpenAI, браузерне WebSocket-налаштування Google Live з constrained-token і браузерний adapter Gateway relay з фейковим медіа мікрофона. Команда виводить лише стан провайдера й не логує секрети.

  </Accordion>
  <Accordion title="Зупинка та переривання">
    - Натисніть **Зупинити** (викликає `chat.abort`).
    - Поки запуск активний, звичайні follow-up-и стають у чергу. Натисніть **Спрямувати** на повідомленні в черзі, щоб вставити цей follow-up у поточний хід.
    - Введіть `/stop` (або окремі фрази переривання, як-от `stop`, `stop action`, `stop run`, `stop openclaw`, `please stop`), щоб перервати поза основним потоком.
    - `chat.abort` підтримує `{ sessionKey }` (без `runId`) для переривання всіх активних запусків цього сеансу.

  </Accordion>
  <Accordion title="Збереження часткового виводу після переривання">
    - Коли запуск перервано, частковий текст асистента все ще може показуватися в інтерфейсі.
    - Gateway зберігає перерваний частковий текст асистента в історії стенограми, коли існує буферизований вивід.
    - Збережені записи містять метадані переривання, щоб споживачі стенограми могли відрізняти перервані часткові записи від звичайного виводу завершення.

  </Accordion>
</AccordionGroup>

## Встановлення PWA та web push

Control UI постачається з `manifest.webmanifest` і service worker, тож сучасні браузери можуть встановити його як standalone PWA. Web Push дає Gateway змогу будити встановлену PWA сповіщеннями, навіть коли вкладка або вікно браузера не відкриті.

Якщо сторінка показує **Невідповідність протоколу** одразу після оновлення OpenClaw, спершу повторно відкрийте панель керування за допомогою `openclaw dashboard` і примусово оновіть сторінку. Якщо помилка не зникає, очистьте дані сайту для origin панелі керування або перевірте в приватному вікні браузера; стара вкладка або кеш service worker браузера може продовжувати запускати пакет Control UI до оновлення проти новішого Gateway.

| Поверхня                                             | Що робить                                                        |
| ---------------------------------------------------- | ---------------------------------------------------------------- |
| `ui/public/manifest.webmanifest`                     | Маніфест PWA. Браузери пропонують «Установити застосунок», коли він стає доступним. |
| `ui/public/sw.js`                                    | Service worker, який обробляє події `push` і кліки сповіщень. |
| `push/vapid-keys.json` (у каталозі стану OpenClaw)   | Автоматично згенерована пара ключів VAPID, що використовується для підписування payload Web Push. |
| `push/web-push-subscriptions.json`                   | Збережені endpoint підписок браузера.                           |

Перевизначайте пару ключів VAPID через змінні середовища в процесі Gateway, коли потрібно зафіксувати ключі (для багатохостових розгортань, ротації секретів або тестів):

- `OPENCLAW_VAPID_PUBLIC_KEY`
- `OPENCLAW_VAPID_PRIVATE_KEY`
- `OPENCLAW_VAPID_SUBJECT` (типово `https://openclaw.ai`)

Control UI використовує ці методи Gateway, обмежені scope, щоб реєструвати й тестувати підписки браузера:

- `push.web.vapidPublicKey` — отримує активний публічний ключ VAPID.
- `push.web.subscribe` — реєструє `endpoint` разом із `keys.p256dh`/`keys.auth`.
- `push.web.unsubscribe` — видаляє зареєстрований endpoint.
- `push.web.test` — надсилає тестове сповіщення до підписки викликача.

<Note>
Web Push не залежить від шляху relay iOS APNS (див. [Конфігурація](/uk/gateway/configuration) для push на базі relay) і наявного методу `push.test`, які націлені на native mobile pairing.
</Note>

## Вбудовані hosted-елементи

Повідомлення асистента можуть рендерити hosted web content inline за допомогою shortcode `[embed ...]`. Політика iframe sandbox контролюється `gateway.controlUi.embedSandbox`:

<Tabs>
  <Tab title="strict">
    Вимикає виконання скриптів усередині hosted embeds.
  </Tab>
  <Tab title="scripts (default)">
    Дозволяє інтерактивні embeds, зберігаючи ізоляцію origin; це стандартний режим, і зазвичай його достатньо для самодостатніх браузерних ігор/віджетів.
  </Tab>
  <Tab title="trusted">
    Додає `allow-same-origin` поверх `allow-scripts` для документів того самого сайту, яким навмисно потрібні сильніші привілеї.
  </Tab>
</Tabs>

Приклад:

```json5
{
  gateway: {
    controlUi: {
      embedSandbox: "scripts",
    },
  },
}
```

<Warning>
Використовуйте `trusted` лише тоді, коли вбудованому документу справді потрібна поведінка same-origin. Для більшості згенерованих агентом ігор та інтерактивних canvas `scripts` є безпечнішим вибором.
</Warning>

Абсолютні зовнішні URL embed `http(s)` типово залишаються заблокованими. Якщо ви навмисно хочете, щоб `[embed url="https://..."]` завантажував сторонні сторінки, задайте `gateway.controlUi.allowExternalEmbedUrls: true`.

## Ширина повідомлень чату

Згруповані повідомлення чату використовують читабельну стандартну максимальну ширину. Розгортання на широких моніторах можуть перевизначити її без патчення вбудованого CSS, задавши `gateway.controlUi.chatMessageMaxWidth`:

```json5
{
  gateway: {
    controlUi: {
      chatMessageMaxWidth: "min(1280px, 82%)",
    },
  },
}
```

Значення перевіряється до того, як потрапляє в браузер. Підтримувані значення включають прості довжини й відсотки, як-от `960px` або `82%`, а також обмежені вирази ширини `min(...)`, `max(...)`, `clamp(...)`, `calc(...)` і `fit-content(...)`.

## Доступ tailnet (рекомендовано)

<Tabs>
  <Tab title="Integrated Tailscale Serve (preferred)">
    Тримайте Gateway на loopback і дозвольте Tailscale Serve проксувати його через HTTPS:

    ```bash
    openclaw gateway --tailscale serve
    ```

    Відкрийте:

    - `https://<magicdns>/` (або ваш налаштований `gateway.controlUi.basePath`)

    Типово запити Control UI/WebSocket Serve можуть автентифікуватися через заголовки ідентичності Tailscale (`tailscale-user-login`), коли `gateway.auth.allowTailscale` дорівнює `true`. OpenClaw перевіряє ідентичність, розв’язуючи адресу `x-forwarded-for` через `tailscale whois` і зіставляючи її із заголовком, та приймає їх лише тоді, коли запит приходить на loopback із заголовками `x-forwarded-*` від Tailscale. Для операторських сесій Control UI з ідентичністю браузерного пристрою цей перевірений шлях Serve також пропускає цикл device-pairing; браузери без пристрою та з’єднання з роллю вузла й далі проходять звичайні перевірки пристрою. Задайте `gateway.auth.allowTailscale: false`, якщо хочете вимагати явні облікові дані shared-secret навіть для трафіку Serve. Потім використовуйте `gateway.auth.mode: "token"` або `"password"`.

    Для цього асинхронного шляху ідентичності Serve невдалі спроби автентифікації для тієї самої IP-адреси клієнта й auth scope серіалізуються перед записами rate-limit. Тому конкурентні невдалі повтори з того самого браузера можуть показати `retry later` у другому запиті замість двох простих невідповідностей, що змагаються паралельно.

    <Warning>
    Автентифікація Serve без токена передбачає, що хост gateway є довіреним. Якщо на цьому хості може виконуватися недовірений локальний код, вимагайте автентифікацію token/password.
    </Warning>

  </Tab>
  <Tab title="Bind to tailnet + token">
    ```bash
    openclaw gateway --bind tailnet --token "$(openssl rand -hex 32)"
    ```

    Потім відкрийте:

    - `http://<tailscale-ip>:18789/` (або ваш налаштований `gateway.controlUi.basePath`)

    Вставте відповідний shared secret у налаштування UI (надсилається як `connect.params.auth.token` або `connect.params.auth.password`).

  </Tab>
</Tabs>

## Небезпечний HTTP

Якщо відкрити панель керування через звичайний HTTP (`http://<lan-ip>` або `http://<tailscale-ip>`), браузер працює в **небезпечному контексті** й блокує WebCrypto. Типово OpenClaw **блокує** з’єднання Control UI без ідентичності пристрою.

Задокументовані винятки:

- сумісність небезпечного HTTP лише для localhost із `gateway.controlUi.allowInsecureAuth=true`
- успішна операторська автентифікація Control UI через `gateway.auth.mode: "trusted-proxy"`
- аварійний режим `gateway.controlUi.dangerouslyDisableDeviceAuth=true`

**Рекомендоване виправлення:** використовуйте HTTPS (Tailscale Serve) або відкрийте UI локально:

- `https://<magicdns>/` (Serve)
- `http://127.0.0.1:18789/` (на хості gateway)

<AccordionGroup>
  <Accordion title="Insecure-auth toggle behavior">
    ```json5
    {
      gateway: {
        controlUi: { allowInsecureAuth: true },
        bind: "tailnet",
        auth: { mode: "token", token: "replace-me" },
      },
    }
    ```

    `allowInsecureAuth` — це лише локальний перемикач сумісності:

    - Він дозволяє сесіям Control UI на localhost продовжуватися без ідентичності пристрою в небезпечних HTTP-контекстах.
    - Він не обходить перевірки pairing.
    - Він не послаблює вимоги до ідентичності віддалених (не localhost) пристроїв.

  </Accordion>
  <Accordion title="Break-glass only">
    ```json5
    {
      gateway: {
        controlUi: { dangerouslyDisableDeviceAuth: true },
        bind: "tailnet",
        auth: { mode: "token", token: "replace-me" },
      },
    }
    ```

    <Warning>
    `dangerouslyDisableDeviceAuth` вимикає перевірки ідентичності пристрою Control UI і є серйозним зниженням рівня безпеки. Швидко поверніть налаштування назад після аварійного використання.
    </Warning>

  </Accordion>
  <Accordion title="Trusted-proxy note">
    - Успішна автентифікація trusted-proxy може допускати **операторські** сесії Control UI без ідентичності пристрою.
    - Це **не** поширюється на сесії Control UI з роллю вузла.
    - Reverse proxy через loopback на тому самому хості все одно не задовольняють автентифікацію trusted-proxy; див. [Автентифікація trusted proxy](/uk/gateway/trusted-proxy-auth).

  </Accordion>
</AccordionGroup>

Див. [Tailscale](/uk/gateway/tailscale) для порад із налаштування HTTPS.

## Політика безпеки контенту

Control UI постачається зі строгою політикою `img-src`: дозволені лише ресурси **same-origin**, URL `data:` і локально згенеровані URL `blob:`. Віддалені URL зображень `http(s)` і protocol-relative відхиляються браузером і не спричиняють мережевих fetch-запитів.

Що це означає на практиці:

- Аватари й зображення, що подаються за відносними шляхами (наприклад `/avatars/<id>`), усе ще рендеряться, зокрема автентифіковані маршрути аватарів, які UI отримує та перетворює на локальні URL `blob:`.
- Inline URL `data:image/...` усе ще рендеряться (корисно для payload у протоколі).
- Локальні URL `blob:`, створені Control UI, усе ще рендеряться.
- Віддалені URL аватарів, які видаються метаданими каналу, відсікаються helper-ами аватарів Control UI і замінюються вбудованим логотипом/бейджем, тому скомпрометований або зловмисний канал не може змусити браузер оператора виконувати довільні віддалені fetch-запити зображень.

Вам не потрібно нічого змінювати, щоб отримати цю поведінку — вона завжди ввімкнена й не налаштовується.

## Автентифікація маршруту аватарів

Коли автентифікацію gateway налаштовано, endpoint аватарів Control UI вимагає той самий токен gateway, що й решта API:

- `GET /avatar/<agentId>` повертає зображення аватара лише автентифікованим викликачам. `GET /avatar/<agentId>?meta=1` повертає метадані аватара за тим самим правилом.
- Неавтентифіковані запити до будь-якого з цих маршрутів відхиляються (відповідно до sibling route assistant-media). Це не дає маршруту аватарів розкривати ідентичність агента на хостах, які інакше захищені.
- Сам Control UI пересилає токен gateway як bearer header під час отримання аватарів і використовує автентифіковані blob URL, щоб зображення й надалі рендерилося в панелях керування.

Якщо вимкнути автентифікацію gateway (не рекомендовано на shared hosts), маршрут аватарів також стає неавтентифікованим, відповідно до решти gateway.

## Автентифікація маршруту media асистента

Коли автентифікацію gateway налаштовано, локальні media previews асистента використовують двоетапний маршрут:

- `GET /__openclaw__/assistant-media?meta=1&source=<path>` вимагає звичайну операторську автентифікацію Control UI. Браузер надсилає токен gateway як bearer header під час перевірки доступності.
- Успішні відповіді metadata містять короткоживучий `mediaTicket`, обмежений саме цим source path.
- URL зображень, аудіо, відео та документів, що рендеряться браузером, використовують `mediaTicket=<ticket>` замість активного токена або пароля gateway. Ticket швидко спливає й не може авторизувати інше джерело.

Це зберігає сумісність звичайного рендерингу media з нативними media-елементами браузера, не розміщуючи багаторазові облікові дані gateway у видимих URL media.

## Збирання UI

Gateway подає статичні файли з `dist/control-ui`. Зберіть їх за допомогою:

```bash
pnpm ui:build
```

Необов’язкова абсолютна база (коли потрібні фіксовані URL ресурсів):

```bash
OPENCLAW_CONTROL_UI_BASE_PATH=/openclaw/ pnpm ui:build
```

Для локальної розробки (окремий dev server):

```bash
pnpm ui:dev
```

Потім спрямуйте UI на URL Gateway WS (наприклад `ws://127.0.0.1:18789`).

## Порожня сторінка Control UI

Якщо браузер завантажує порожню панель керування, а DevTools не показує корисної помилки, розширення або ранній content script могли завадити JavaScript module app виконатися. Статична сторінка містить просту HTML-панель відновлення, яка з’являється, коли `<openclaw-app>` не зареєстровано після запуску.

Скористайтеся дією панелі **Спробувати ще раз** після зміни середовища браузера або перезавантажте вручну після цих перевірок:

- Вимкніть розширення, які інжектять код на всі сторінки, особливо розширення з content scripts `<all_urls>`.
- Спробуйте приватне вікно, чистий профіль браузера або інший браузер.
- Тримайте Gateway запущеним і перевірте той самий URL панелі керування після зміни браузера.

## Налагодження/тестування: dev server + віддалений Gateway

Control UI — це статичні файли; ціль WebSocket налаштовується й може відрізнятися від HTTP origin. Це зручно, коли Vite dev server потрібен локально, а Gateway працює деінде.

<Steps>
  <Step title="Start the UI dev server">
    ```bash
    pnpm ui:dev
    ```
  </Step>
  <Step title="Open with gatewayUrl">
    ```text
    http://localhost:5173/?gatewayUrl=ws%3A%2F%2F<gateway-host>%3A18789
    ```

    Необов’язкова одноразова автентифікація (якщо потрібно):

    ```text
    http://localhost:5173/?gatewayUrl=wss%3A%2F%2F<gateway-host>%3A18789#token=<gateway-token>
    ```

  </Step>
</Steps>

<AccordionGroup>
  <Accordion title="Notes">
    - `gatewayUrl` зберігається в localStorage після завантаження та видаляється з URL.
    - Якщо ви передаєте повну кінцеву точку `ws://` або `wss://` через `gatewayUrl`, URL-кодуйте значення `gatewayUrl`, щоб браузер правильно розібрав рядок запиту.
    - `token` слід передавати через фрагмент URL (`#token=...`) щоразу, коли це можливо. Фрагменти не надсилаються на сервер, що запобігає витоку через журнали запитів і Referer. Застарілі параметри запиту `?token=` усе ще одноразово імпортуються для сумісності, але лише як резервний варіант, і негайно видаляються після початкового завантаження.
    - `password` зберігається лише в пам'яті.
    - Коли `gatewayUrl` задано, інтерфейс користувача не повертається до облікових даних із конфігурації або середовища. Надайте `token` (або `password`) явно. Відсутність явних облікових даних є помилкою.
    - Використовуйте `wss://`, коли Gateway розміщений за TLS (Tailscale Serve, HTTPS-проксі тощо).
    - `gatewayUrl` приймається лише у вікні верхнього рівня (не вбудованому), щоб запобігти clickjacking.
    - Публічні нелокальні розгортання Control UI мають явно задавати `gateway.controlUi.allowedOrigins` (повні origins). Приватні завантаження з тієї самої origin у LAN/Tailnet із local loopback, RFC1918/link-local, `.local`, `.ts.net` або хостів Tailscale CGNAT приймаються без увімкнення резервного режиму Host-header.
    - Під час запуску Gateway може засівати локальні origins, як-от `http://localhost:<port>` і `http://127.0.0.1:<port>`, з ефективної runtime-прив'язки та порту, але віддалені origins браузера все одно потребують явних записів.
    - Не використовуйте `gateway.controlUi.allowedOrigins: ["*"]`, крім ретельно контрольованого локального тестування. Це означає дозволити будь-яку origin браузера, а не «зіставити будь-який хост, який я використовую».
    - `gateway.controlUi.dangerouslyAllowHostHeaderOriginFallback=true` вмикає резервний режим origin на основі Host-header, але це небезпечний режим безпеки.

  </Accordion>
</AccordionGroup>

Приклад:

```json5
{
  gateway: {
    controlUi: {
      allowedOrigins: ["http://localhost:5173"],
    },
  },
}
```

Докладніше про налаштування віддаленого доступу: [Віддалений доступ](/uk/gateway/remote).

## Пов'язане

- [Панель керування](/uk/web/dashboard) — панель керування Gateway
- [Перевірки працездатності](/uk/gateway/health) — моніторинг працездатності Gateway
- [TUI](/uk/web/tui) — термінальний інтерфейс користувача
- [WebChat](/uk/web/webchat) — браузерний інтерфейс чату
