---
read_when:
    - Вам потрібна фонова або паралельна робота через агента
    - Ви змінюєте sessions_spawn або політику інструментів під-агентів
    - Ви впроваджуєте або усуваєте неполадки в сеансах субагентів, прив’язаних до потоку
sidebarTitle: Sub-agents
summary: Запускайте ізольовані фонові виконання агентів, які повідомляють результати назад у чат запитувача
title: Субагенти
x-i18n:
    generated_at: "2026-05-07T15:15:19Z"
    model: gpt-5.5
    provider: openai
    source_hash: 5b112f9c45bcb9cdc5d3b856f2fe2a36617606ad278b0ccc3db8830f0e847ba9
    source_path: tools/subagents.md
    workflow: 16
---

Підагентів запускають у фоні з наявного запуску агента.
Вони працюють у власному сеансі (`agent:<agentId>:subagent:<uuid>`) і,
коли завершуються, **оголошують** свій результат назад у канал чату
запитувача. Кожен запуск підагента відстежується як
[фонове завдання](/uk/automation/tasks).

Основні цілі:

- Паралелити роботу "дослідження / довге завдання / повільний інструмент", не блокуючи основний запуск.
- Тримати підагентів ізольованими за замовчуванням (розділення сеансів + необов’язкова ізоляція в sandbox).
- Зробити поверхню інструментів складною для неправильного використання: підагенти за замовчуванням **не** отримують інструменти сеансу.
- Підтримувати налаштовувану глибину вкладення для шаблонів оркестраторів.

<Note>
**Примітка щодо вартості:** за замовчуванням кожен підагент має власний контекст і власне використання токенів.
Для важких або повторюваних завдань задайте дешевшу модель для підагентів
і залиште основного агента на якіснішій моделі. Налаштовуйте через
`agents.defaults.subagents.model` або перевизначення для окремого агента. Коли дочірньому запуску
    справді потрібен поточний transcript запитувача, агент може запросити
    `context: "fork"` для цього одного запуску. Сеанси підагентів, прив’язані до thread,
    за замовчуванням використовують `context: "fork"`, бо вони відгалужують поточну розмову в
    follow-up thread.
</Note>

## Slash-команда

Використовуйте `/subagents`, щоб переглядати або керувати запусками підагентів для **поточного
сеансу**:

```text
/subagents list
/subagents kill <id|#|all>
/subagents log <id|#> [limit] [tools]
/subagents info <id|#>
/subagents send <id|#> <message>
/subagents steer <id|#> <message>
/subagents spawn <agentId> <task> [--model <model>] [--thinking <level>]
```

Використовуйте верхньорівневу [`/steer <message>`](/uk/tools/steer), щоб спрямувати активний запуск поточного сеансу запитувача. Використовуйте `/subagents steer <id|#> <message>`, коли ціль — дочірній запуск.

`/subagents info` показує метадані запуску (статус, часові позначки, id сеансу,
шлях до transcript, cleanup). Використовуйте `sessions_history` для обмеженого,
відфільтрованого з міркувань безпеки перегляду попередніх даних; перевіряйте шлях до transcript на диску, коли
потрібен необроблений повний transcript.

### Елементи керування прив’язкою до thread

Ці команди працюють у каналах, що підтримують постійні прив’язки до thread.
Див. [Канали з підтримкою thread](#thread-supporting-channels) нижче.

```text
/focus <subagent-label|session-key|session-id|session-label>
/unfocus
/agents
/session idle <duration|off>
/session max-age <duration|off>
```

### Поведінка запуску

`/subagents spawn` запускає фоновий підагент як команду користувача (не як
внутрішню ретрансляцію) і надсилає одне фінальне оновлення про завершення назад у
чат запитувача, коли запуск завершується.

<AccordionGroup>
  <Accordion title="Неблокувальне завершення на основі push">
    - Команда запуску неблокувальна; вона одразу повертає id запуску.
    - Після завершення підагент оголошує підсумок/результат повідомленням назад у канал чату запитувача.
    - Завершення працює на основі push. Після запуску **не** опитуйте `/subagents list`, `sessions_list` або `sessions_history` у циклі лише для очікування завершення; перевіряйте статус лише за потреби для налагодження або втручання.
    - Після завершення OpenClaw за принципом best-effort закриває відстежувані вкладки браузера/процеси, відкриті цим сеансом підагента, перш ніж продовжиться потік announce cleanup.

  </Accordion>
  <Accordion title="Стійкість доставлення ручного запуску">
    - OpenClaw передає завершення назад у сеанс запитувача через хід `agent` зі стабільним ключем ідемпотентності.
    - Якщо запуск запитувача ще активний, OpenClaw спершу намагається розбудити/спрямувати цей запуск замість запуску другого видимого шляху відповіді.
    - Якщо передавання завершення агенту-запитувачу завершується невдало або не створює видимого виводу, OpenClaw вважає доставлення невдалим і переходить до маршрутизації через чергу/повторної спроби. Він не надсилає необроблений результат дочірнього запуску напряму в зовнішній чат.
    - Якщо пряме передавання не можна використати, воно переходить до маршрутизації через чергу.
    - Якщо маршрутизація через чергу все ще недоступна, оголошення повторюється з короткою експоненційною затримкою перед остаточною відмовою.
    - Доставлення завершення зберігає розв’язаний маршрут запитувача: маршрути завершення, прив’язані до thread або розмови, мають пріоритет, коли доступні; якщо джерело завершення надає лише канал, OpenClaw заповнює відсутню ціль/обліковий запис із розв’язаного маршруту сеансу запитувача (`lastChannel` / `lastTo` / `lastAccountId`), щоб пряме доставлення все одно працювало.

  </Accordion>
  <Accordion title="Метадані передавання завершення">
    Передавання завершення в сеанс запитувача — це згенерований runtime
    внутрішній контекст (не текст, написаний користувачем) і містить:

    - `Result` — найновіший видимий текст відповіді `assistant`, інакше очищений найновіший текст tool/toolResult. Завершені з помилкою terminal-запуски не використовують повторно захоплений текст відповіді.
    - `Status` — `completed successfully` / `failed` / `timed out` / `unknown`.
    - Компактну статистику runtime/токенів.
    - Інструкцію доставлення, яка наказує агенту-запитувачу переписати у звичайному голосі assistant (не пересилати необроблені внутрішні метадані).

  </Accordion>
  <Accordion title="Режими та ACP runtime">
    - `--model` і `--thinking` перевизначають значення за замовчуванням для цього конкретного запуску.
    - Використовуйте `info`/`log`, щоб переглянути подробиці та вивід після завершення.
    - `/subagents spawn` — це одноразовий режим (`mode: "run"`). Для постійних сеансів, прив’язаних до thread, використовуйте `sessions_spawn` з `thread: true` і `mode: "session"`.
    - Для сеансів ACP harness (Claude Code, Gemini CLI, OpenCode або явний Codex ACP/acpx) використовуйте `sessions_spawn` з `runtime: "acp"`, коли інструмент оголошує цей runtime. Див. [Модель доставлення ACP](/uk/tools/acp-agents#delivery-model), коли налагоджуєте завершення або цикли агент-до-агента. Коли plugin `codex` увімкнено, керування чатом/thread Codex має надавати перевагу `/codex ...` замість ACP, якщо користувач явно не просить ACP/acpx.
    - OpenClaw приховує `runtime: "acp"`, доки ACP не ввімкнено, запитувач не sandboxed і не завантажено backend plugin, як-от `acpx`. `runtime: "acp"` очікує зовнішній id ACP harness або запис `agents.list[]` з `runtime.type="acp"`; використовуйте стандартний runtime підагентів для звичайних config-агентів OpenClaw з `agents_list`.

  </Accordion>
</AccordionGroup>

## Режими контексту

Нативні підагенти стартують ізольовано, якщо caller явно не просить fork
поточного transcript.

| Режим      | Коли його використовувати                                                                                                              | Поведінка                                                                         |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `isolated` | Свіже дослідження, незалежна реалізація, робота з повільним інструментом або будь-що, що можна коротко описати в тексті завдання      | Створює чистий дочірній transcript. Це значення за замовчуванням і воно зменшує використання токенів. |
| `fork`     | Робота, що залежить від поточної розмови, попередніх результатів інструментів або нюансованих інструкцій, уже наявних у transcript запитувача | Відгалужує transcript запитувача в дочірній сеанс перед стартом дочірнього запуску. |

Використовуйте `fork` ощадливо. Він призначений для делегування, чутливого до контексту, а не як
заміна написанню чіткого prompt завдання.

## Інструмент: `sessions_spawn`

Запускає підагент із `deliver: false` на глобальній лінії `subagent`,
потім виконує крок оголошення і публікує відповідь-оголошення в канал чату
запитувача.

Доступність залежить від ефективної політики інструментів caller. Профілі `coding` і
`full` за замовчуванням надають `sessions_spawn`. Профіль `messaging`
ні; додайте `tools.alsoAllow: ["sessions_spawn", "sessions_yield",
"subagents"]` або використовуйте `tools.profile: "coding"` для агентів, які мають делегувати
роботу. Канал/група, provider, sandbox і політики allow/deny для окремого агента можуть
усе ще прибрати інструмент після етапу профілю. Використовуйте `/tools` з того самого
сеансу, щоб підтвердити ефективний список інструментів.

**Значення за замовчуванням:**

- **Модель:** успадковує caller, якщо ви не задасте `agents.defaults.subagents.model` (або `agents.list[].subagents.model` для окремого агента); явне `sessions_spawn.model` усе одно має пріоритет.
- **Thinking:** успадковує caller, якщо ви не задасте `agents.defaults.subagents.thinking` (або `agents.list[].subagents.thinking` для окремого агента); явне `sessions_spawn.thinking` усе одно має пріоритет.
- **Таймаут запуску:** якщо `sessions_spawn.runTimeoutSeconds` пропущено, OpenClaw використовує `agents.defaults.subagents.runTimeoutSeconds`, коли задано; інакше повертається до `0` (без таймауту).

### Параметри інструмента

<ParamField path="task" type="string" required>
  Опис завдання для підагента.
</ParamField>
<ParamField path="label" type="string">
  Необов’язкова зрозуміла для людини мітка.
</ParamField>
<ParamField path="agentId" type="string">
  Запуск під іншим id агента, коли це дозволено `subagents.allowAgents`.
</ParamField>
<ParamField path="runtime" type='"subagent" | "acp"' default="subagent">
  `acp` призначено лише для зовнішніх ACP harness (`claude`, `droid`, `gemini`, `opencode` або явно запитаного Codex ACP/acpx) і для записів `agents.list[]`, у яких `runtime.type` дорівнює `acp`.
</ParamField>
<ParamField path="resumeSessionId" type="string">
  Лише ACP. Відновлює наявний сеанс ACP harness, коли `runtime: "acp"`; ігнорується для нативних запусків підагентів.
</ParamField>
<ParamField path="streamTo" type='"parent"'>
  Лише ACP. Транслює вивід запуску ACP у батьківський сеанс, коли `runtime: "acp"`; пропустіть для нативних запусків підагентів.
</ParamField>
<ParamField path="model" type="string">
  Перевизначає модель підагента. Некоректні значення пропускаються, і підагент запускається на моделі за замовчуванням із попередженням у результаті інструмента.
</ParamField>
<ParamField path="thinking" type="string">
  Перевизначає рівень thinking для запуску підагента.
</ParamField>
<ParamField path="runTimeoutSeconds" type="number">
  За замовчуванням дорівнює `agents.defaults.subagents.runTimeoutSeconds`, коли задано, інакше `0`. Коли задано, запуск підагента переривається через N секунд.
</ParamField>
<ParamField path="thread" type="boolean" default="false">
  Коли `true`, запитує прив’язку thread каналу для цього сеансу підагента.
</ParamField>
<ParamField path="mode" type='"run" | "session"' default="run">
  Якщо `thread: true` і `mode` пропущено, типовим стає `session`. `mode: "session"` вимагає `thread: true`.
</ParamField>
<ParamField path="cleanup" type='"delete" | "keep"' default="keep">
  `"delete"` архівує одразу після оголошення (все одно зберігає transcript через rename).
</ParamField>
<ParamField path="sandbox" type='"inherit" | "require"' default="inherit">
  `require` відхиляє запуск, якщо цільовий дочірній runtime не sandboxed.
</ParamField>
<ParamField path="context" type='"isolated" | "fork"' default="isolated">
  `fork` відгалужує поточний transcript запитувача в дочірній сеанс. Лише нативні підагенти. Запуски, прив’язані до thread, за замовчуванням використовують `fork`; запуски без thread за замовчуванням використовують `isolated`.
</ParamField>

<Warning>
`sessions_spawn` **не** приймає параметри доставлення каналу (`target`,
`channel`, `to`, `threadId`, `replyTo`, `transport`). Для доставлення використовуйте
`message`/`sessions_send` зі створеного запуску.
</Warning>

## Сеанси, прив’язані до thread

Коли для каналу ввімкнено прив’язки thread, підагент може залишатися прив’язаним
до thread, щоб подальші повідомлення користувача в цьому thread продовжували маршрутизуватися в той самий
сеанс підагента.

### Канали з підтримкою thread

**Discord** наразі є єдиним підтримуваним каналом. Він підтримує
постійні сеанси підагентів, прив’язані до thread (`sessions_spawn` з
`thread: true`), ручні елементи керування thread (`/focus`, `/unfocus`, `/agents`,
`/session idle`, `/session max-age`) і ключі адаптера
`channels.discord.threadBindings.enabled`,
`channels.discord.threadBindings.idleHours`,
`channels.discord.threadBindings.maxAgeHours` і
`channels.discord.threadBindings.spawnSessions`.

### Швидкий потік

<Steps>
  <Step title="Spawn">
    `sessions_spawn` з `thread: true` (і за бажанням `mode: "session"`).
  </Step>
  <Step title="Bind">
    OpenClaw створює або прив’язує гілку до цієї цілі сеансу в активному каналі.
  </Step>
  <Step title="Route follow-ups">
    Відповіді та подальші повідомлення в цій гілці спрямовуються до прив’язаного сеансу.
  </Step>
  <Step title="Inspect timeouts">
    Використовуйте `/session idle`, щоб переглянути/оновити автоматичне зняття фокуса після неактивності, і
    `/session max-age`, щоб керувати жорстким лімітом.
  </Step>
  <Step title="Detach">
    Використовуйте `/unfocus`, щоб від’єднати вручну.
  </Step>
</Steps>

### Ручне керування

| Команда            | Ефект                                                                |
| ------------------ | --------------------------------------------------------------------- |
| `/focus <target>`  | Прив’язати поточну гілку (або створити її) до цілі під-агента/сеансу |
| `/unfocus`         | Видалити прив’язку для поточної прив’язаної гілки                       |
| `/agents`          | Показати активні запуски та стан прив’язки (`thread:<id>` або `unbound`)       |
| `/session idle`    | Переглянути/оновити автоматичне зняття фокуса через бездіяльність (лише сфокусовані прив’язані гілки)         |
| `/session max-age` | Переглянути/оновити жорсткий ліміт (лише сфокусовані прив’язані гілки)                  |

### Перемикачі конфігурації

- **Глобальне значення за замовчуванням:** `session.threadBindings.enabled`, `session.threadBindings.idleHours`, `session.threadBindings.maxAgeHours`.
- **Перевизначення каналу та ключі автоматичної прив’язки під час створення** залежать від адаптера. Див. [Канали з підтримкою гілок](#thread-supporting-channels) вище.

Див. [Довідник із конфігурації](/uk/gateway/configuration-reference) і
[Slash-команди](/uk/tools/slash-commands), щоб отримати актуальні відомості про адаптери.

### Список дозволених

<ParamField path="agents.list[].subagents.allowAgents" type="string[]">
  Список ідентифікаторів агентів, які можна вибирати через явний `agentId` (`["*"]` дозволяє будь-який). За замовчуванням: лише агент-запитувач. Якщо ви задаєте список і все одно хочете, щоб запитувач міг створювати себе з `agentId`, додайте ідентифікатор запитувача до списку.
</ParamField>
<ParamField path="agents.defaults.subagents.allowAgents" type="string[]">
  Стандартний список дозволених цільових агентів, який використовується, коли агент-запитувач не задає власний `subagents.allowAgents`.
</ParamField>
<ParamField path="agents.defaults.subagents.requireAgentId" type="boolean" default="false">
  Блокує виклики `sessions_spawn`, які не вказують `agentId` (змушує явно вибрати профіль). Перевизначення для окремого агента: `agents.list[].subagents.requireAgentId`.
</ParamField>

Якщо сеанс запитувача працює в пісочниці, `sessions_spawn` відхиляє цілі,
які запускалися б без пісочниці.

### Виявлення

Використовуйте `agents_list`, щоб побачити, які ідентифікатори агентів зараз дозволені для
`sessions_spawn`. Відповідь містить ефективну модель кожного переліченого агента
та вбудовані метадані середовища виконання, щоб викликачі могли відрізняти PI, сервер застосунку Codex
та інші налаштовані нативні середовища виконання.

### Автоархівація

- Сеанси під-агентів автоматично архівуються після `agents.defaults.subagents.archiveAfterMinutes` (за замовчуванням `60`).
- Архівація використовує `sessions.delete` і перейменовує транскрипт на `*.deleted.<timestamp>` (у тій самій теці).
- `cleanup: "delete"` архівує одразу після оголошення (транскрипт усе одно зберігається через перейменування).
- Автоархівація виконується за можливості; очікувані таймери втрачаються, якщо gateway перезапускається.
- `runTimeoutSeconds` **не** виконує автоархівацію; він лише зупиняє запуск. Сеанс залишається до автоархівації.
- Автоархівація однаково застосовується до сеансів глибини 1 і глибини 2.
- Очищення браузера окреме від архівного очищення: відстежувані вкладки/процеси браузера за можливості закриваються після завершення запуску, навіть якщо запис транскрипту/сеансу зберігається.

## Вкладені під-агенти

За замовчуванням під-агенти не можуть створювати власних під-агентів
(`maxSpawnDepth: 1`). Установіть `maxSpawnDepth: 2`, щоб увімкнути один рівень
вкладення — **шаблон оркестратора**: основний → під-агент-оркестратор →
робочі під-під-агенти.

```json5
{
  agents: {
    defaults: {
      subagents: {
        maxSpawnDepth: 2, // allow sub-agents to spawn children (default: 1)
        maxChildrenPerAgent: 5, // max active children per agent session (default: 5)
        maxConcurrent: 8, // global concurrency lane cap (default: 8)
        runTimeoutSeconds: 900, // default timeout for sessions_spawn when omitted (0 = no timeout)
      },
    },
  },
}
```

### Рівні глибини

| Глибина | Форма ключа сеансу                            | Роль                                          | Може створювати?                   |
| ----- | -------------------------------------------- | --------------------------------------------- | ---------------------------- |
| 0     | `agent:<id>:main`                            | Основний агент                                    | Завжди                       |
| 1     | `agent:<id>:subagent:<uuid>`                 | Під-агент (оркестратор, коли дозволено глибину 2) | Лише якщо `maxSpawnDepth >= 2` |
| 2     | `agent:<id>:subagent:<uuid>:subagent:<uuid>` | Під-під-агент (кінцевий виконавець)                   | Ніколи                        |

### Ланцюг оголошень

Результати передаються назад угору ланцюгом:

1. Виконавець глибини 2 завершує роботу → оголошує своєму батьківському елементу (оркестратору глибини 1).
2. Оркестратор глибини 1 отримує оголошення, узагальнює результати, завершує роботу → оголошує основному агенту.
3. Основний агент отримує оголошення та доставляє його користувачу.

Кожен рівень бачить лише оголошення від своїх безпосередніх дочірніх елементів.

<Note>
**Операційні настанови:** запускайте дочірню роботу один раз і чекайте подій
завершення замість того, щоб будувати цикли опитування навколо `sessions_list`,
`sessions_history`, `/subagents list` або команд сну `exec`.
`sessions_list` і `/subagents list` утримують зв’язки дочірніх сеансів
зосередженими на живій роботі — живі дочірні елементи залишаються приєднаними, завершені дочірні елементи
залишаються видимими протягом короткого недавнього вікна, а застарілі посилання лише зі сховища на дочірні елементи
ігноруються після завершення їхнього вікна актуальності. Це запобігає відновленню старих метаданих `spawnedBy` /
`parentSessionKey` у вигляді примарних дочірніх елементів після
перезапуску. Якщо подія завершення дочірнього елемента надходить після того, як ви вже надіслали
фінальну відповідь, правильна подальша дія — точний мовчазний токен
`NO_REPLY` / `no_reply`.
</Note>

### Політика інструментів за глибиною

- Роль і область керування записуються в метадані сеансу під час створення. Це не дає плоским або відновленим ключам сеансу випадково знову отримати привілеї оркестратора.
- **Глибина 1 (оркестратор, коли `maxSpawnDepth >= 2`):** отримує `sessions_spawn`, `subagents`, `sessions_list`, `sessions_history`, щоб керувати своїми дочірніми елементами. Інші інструменти сеансу/системи залишаються забороненими.
- **Глибина 1 (кінцевий елемент, коли `maxSpawnDepth == 1`):** без інструментів сеансу (поточна поведінка за замовчуванням).
- **Глибина 2 (кінцевий виконавець):** без інструментів сеансу — `sessions_spawn` завжди заборонено на глибині 2. Не може створювати подальші дочірні елементи.

### Ліміт створення для агента

Кожен сеанс агента (на будь-якій глибині) може мати не більше `maxChildrenPerAgent`
(за замовчуванням `5`) активних дочірніх елементів одночасно. Це запобігає неконтрольованому розгалуженню
від одного оркестратора.

### Каскадна зупинка

Зупинка оркестратора глибини 1 автоматично зупиняє всі його дочірні елементи глибини 2:

- `/stop` в основному чаті зупиняє всіх агентів глибини 1 і каскадно зупиняє їхні дочірні елементи глибини 2.
- `/subagents kill <id>` зупиняє конкретного під-агента та каскадно зупиняє його дочірні елементи.
- `/subagents kill all` зупиняє всіх під-агентів для запитувача та виконує каскадну зупинку.

## Автентифікація

Автентифікація під-агента визначається за **ідентифікатором агента**, а не за типом сеансу:

- Ключ сеансу під-агента — `agent:<agentId>:subagent:<uuid>`.
- Сховище автентифікації завантажується з `agentDir` цього агента.
- Профілі автентифікації основного агента об’єднуються як **резервні**; профілі агента перевизначають основні профілі в разі конфліктів.

Об’єднання є додатковим, тож основні профілі завжди доступні як
резервні. Повністю ізольована автентифікація для кожного агента поки не підтримується.

## Оголошення

Під-агенти звітують через крок оголошення:

- Крок оголошення виконується всередині сеансу під-агента (не в сеансі запитувача).
- Якщо під-агент відповідає точно `ANNOUNCE_SKIP`, нічого не публікується.
- Якщо останній текст асистента — точний мовчазний токен `NO_REPLY` / `no_reply`, вивід оголошення пригнічується, навіть якщо раніше був видимий прогрес.

Доставка залежить від глибини запитувача:

- Сеанси запитувача верхнього рівня використовують подальший виклик `agent` із зовнішньою доставкою (`deliver=true`).
- Вкладені сеанси під-агента-запитувача отримують внутрішнє подальше впровадження (`deliver=false`), щоб оркестратор міг узагальнювати дочірні результати в межах сеансу.
- Якщо вкладений сеанс під-агента-запитувача зник, OpenClaw повертається до запитувача цього сеансу, коли він доступний.

Для сеансів запитувача верхнього рівня пряма доставка в режимі завершення спочатку
визначає будь-який прив’язаний маршрут розмови/гілки та перевизначення hook, потім заповнює
відсутні поля цілі каналу зі збереженого маршруту сеансу запитувача.
Це зберігає завершення в правильному чаті/темі, навіть коли джерело завершення
ідентифікує лише канал.

Агрегація завершень дочірніх елементів обмежується поточним запуском запитувача під час
побудови вкладених знахідок завершення, запобігаючи потраплянню застарілих виводів дочірніх елементів
із попередніх запусків у поточне оголошення. Відповіді оголошень зберігають
маршрутизацію гілки/теми, коли вона доступна в адаптерах каналів.

### Контекст оголошення

Контекст оголошення нормалізується до стабільного внутрішнього блоку події:

| Поле          | Джерело                                                                                                        |
| -------------- | ------------------------------------------------------------------------------------------------------------- |
| Джерело         | `subagent` або `cron`                                                                                          |
| Ідентифікатори сеансу    | Ключ/ідентифікатор дочірнього сеансу                                                                                          |
| Тип           | Тип оголошення + мітка завдання                                                                                    |
| Статус         | Виведено з результату середовища виконання (`success`, `error`, `timeout` або `unknown`) — **не** виводиться з тексту моделі |
| Вміст результату | Останній видимий текст асистента, інакше очищений останній текст tool/toolResult                                |
| Подальша дія      | Інструкція, що описує, коли відповідати, а коли залишатися мовчазним                                                           |

Кінцеві невдалі запуски повідомляють статус помилки без повторного відтворення захопленого
тексту відповіді. У разі тайм-ауту, якщо дочірній елемент встиг лише пройти виклики інструментів, оголошення
може згорнути цю історію в короткий підсумок часткового прогресу замість
повторного відтворення сирого виводу інструменту.

### Рядок статистики

Корисні навантаження оголошень містять рядок статистики в кінці (навіть коли обгорнуті):

- Час виконання (наприклад, `runtime 5m12s`).
- Використання токенів (вхідні/вихідні/загалом).
- Орієнтовна вартість, коли налаштовано ціни моделей (`models.providers.*.models[].cost`).
- `sessionKey`, `sessionId` і шлях транскрипту, щоб основний агент міг отримати історію через `sessions_history` або перевірити файл на диску.

Внутрішні метадані призначені лише для оркестрації; відповіді для користувача
слід переписувати звичайним голосом асистента.

### Чому варто надавати перевагу `sessions_history`

`sessions_history` — безпечніший шлях оркестрації:

- Спогади асистента спочатку нормалізуються: теги мислення вилучаються; каркас `<relevant-memories>` / `<relevant_memories>` вилучається; текстові блоки XML-навантаження викликів інструментів (`<tool_call>`, `<function_call>`, `<tool_calls>`, `<function_calls>`) вилучаються, включно з обрізаними навантаженнями, які ніколи не закриваються коректно; понижений каркас викликів/результатів інструментів і маркери історичного контексту вилучаються; витіклі керівні токени моделі (`<|assistant|>`, інші ASCII `<|...|>`, повної ширини `<｜...｜>`) вилучаються; некоректний XML викликів інструментів MiniMax вилучається.
- Текст, схожий на облікові дані/токени, редагується.
- Довгі блоки можуть обрізатися.
- Дуже великі історії можуть відкидати старіші рядки або замінювати надмірно великий рядок на `[sessions_history omitted: message too large]`.
- Перевірка сирого транскрипту на диску — резервний варіант, коли вам потрібен повний побайтовий транскрипт.

## Політика інструментів

Субагенти спочатку використовують той самий профіль і конвеєр політики інструментів, що й батьківський або цільовий агент. Після цього OpenClaw застосовує шар обмежень для субагентів.

Без обмежувального `tools.profile` субагенти отримують **усі інструменти, крім інструментів сеансів** і системних інструментів:

- `sessions_list`
- `sessions_history`
- `sessions_send`
- `sessions_spawn`

`sessions_history` тут також залишається обмеженим, очищеним поданням пригадування — це не сире вивантаження транскрипту.

Коли `maxSpawnDepth >= 2`, субагенти-оркестратори глибини 1 додатково отримують `sessions_spawn`, `subagents`, `sessions_list` і `sessions_history`, щоб вони могли керувати своїми дочірніми елементами.

### Перевизначення через конфігурацію

```json5
{
  agents: {
    defaults: {
      subagents: {
        maxConcurrent: 1,
      },
    },
  },
  tools: {
    subagents: {
      tools: {
        // deny wins
        deny: ["gateway", "cron"],
        // if allow is set, it becomes allow-only (deny still wins)
        // allow: ["read", "exec", "process"]
      },
    },
  },
}
```

`tools.subagents.tools.allow` — це фінальний фільтр лише для дозволених інструментів. Він може звузити вже визначений набір інструментів, але не може **додати назад** інструмент, видалений через `tools.profile`. Наприклад, `tools.profile: "coding"` включає `web_search`/`web_fetch`, але не інструмент `browser`. Щоб дозволити субагентам із профілем кодування використовувати автоматизацію браузера, додайте browser на етапі профілю:

```json5
{
  tools: {
    profile: "coding",
    alsoAllow: ["browser"],
  },
}
```

Використовуйте `agents.list[].tools.alsoAllow: ["browser"]` для окремого агента, коли лише один агент має отримати автоматизацію браузера.

## Паралельність

Субагенти використовують окрему внутрішньопроцесну чергу:

- **Назва черги:** `subagent`
- **Паралельність:** `agents.defaults.subagents.maxConcurrent` (за замовчуванням `8`)

## Активність і відновлення

OpenClaw не вважає відсутність `endedAt` постійним доказом того, що субагент досі активний. Незавершені запуски, старші за вікно застарілого запуску, перестають враховуватися як активні/очікувані в `/subagents list`, зведеннях статусу, блокуванні завершення нащадків і перевірках паралельності для кожного сеансу.

Після перезапуску Gateway застарілі незавершені відновлені запуски обрізаються, якщо їхній дочірній сеанс не позначено `abortedLastRun: true`. Такі перервані перезапуском дочірні сеанси залишаються відновлюваними через потік відновлення осиротілого субагента, який надсилає синтетичне повідомлення відновлення перед очищенням маркера переривання.

Автоматичне відновлення після перезапуску обмежене для кожного дочірнього сеансу. Якщо той самий дочірній субагент неодноразово приймається для відновлення сироти в межах вікна швидкого повторного застрягання, OpenClaw зберігає recovery tombstone у цьому сеансі та припиняє автоматично відновлювати його під час наступних перезапусків. Запустіть `openclaw tasks maintenance --apply`, щоб узгодити запис завдання, або `openclaw doctor --fix`, щоб очистити застарілі прапорці перерваного відновлення в сеансах із tombstone.

<Note>
Якщо створення субагента завершується помилкою Gateway `PAIRING_REQUIRED` / `scope-upgrade`, перевірте RPC-викликача перед редагуванням стану спарювання. Внутрішня координація `sessions_spawn` має підключатися як `client.id: "gateway-client"` з `client.mode: "backend"` через пряму автентифікацію спільним токеном/паролем через loopback; цей шлях не залежить від базової області парного пристрою CLI. Віддаленим викликачам, явному `deviceIdentity`, явним шляхам device-token і клієнтам browser/node все ще потрібне звичайне схвалення пристрою для підвищення області.
</Note>

## Зупинення

- Надсилання `/stop` у чаті запитувача перериває сеанс запитувача та зупиняє всі активні запуски субагентів, створені з нього, з каскадним поширенням на вкладені дочірні елементи.
- `/subagents kill <id>` зупиняє конкретного субагента та каскадно поширюється на його дочірні елементи.

## Обмеження

- Оголошення субагента виконується **за принципом найкращої спроби**. Якщо Gateway перезапускається, очікувана робота "оголосити назад" втрачається.
- Субагенти все ще спільно використовують ресурси того самого процесу Gateway; розглядайте `maxConcurrent` як запобіжний клапан.
- `sessions_spawn` завжди неблокувальний: він негайно повертає `{ status: "accepted", runId, childSessionKey }`.
- Контекст субагента інʼєктує лише `AGENTS.md` + `TOOLS.md` (без `SOUL.md`, `IDENTITY.md`, `USER.md`, `HEARTBEAT.md` або `BOOTSTRAP.md`).
- Максимальна глибина вкладеності — 5 (діапазон `maxSpawnDepth`: 1–5). Для більшості сценаріїв використання рекомендовано глибину 2.
- `maxChildrenPerAgent` обмежує кількість активних дочірніх елементів на сеанс (за замовчуванням `5`, діапазон `1–20`).

## Пов’язане

- [Агенти ACP](/uk/tools/acp-agents)
- [Надсилання агенту](/uk/tools/agent-send)
- [Фонові завдання](/uk/automation/tasks)
- [Інструменти пісочниці для кількох агентів](/uk/tools/multi-agent-sandbox-tools)
