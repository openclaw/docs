---
read_when:
    - Генерування зображень через агента
    - Налаштування постачальників і моделей для генерації зображень
    - Розуміння параметрів інструмента image_generate
summary: Генеруйте та редагуйте зображення за допомогою налаштованих постачальників (OpenAI, Google Gemini, fal, MiniMax, ComfyUI)
title: Генерація зображень
x-i18n:
    generated_at: "2026-04-06T00:47:49Z"
    model: gpt-5.4
    provider: openai
    source_hash: ab69a02eea97b1d77ddb2127f3db01c660401974c53fad79a7f4a5649b0efc1b
    source_path: tools/image-generation.md
    workflow: 15
---

# Генерація зображень

Інструмент `image_generate` дає змогу агенту створювати та редагувати зображення за допомогою ваших налаштованих постачальників. Згенеровані зображення автоматично доставляються як медіавкладення у відповіді агента.

<Note>
Інструмент з’являється, лише коли доступний принаймні один постачальник генерації зображень. Якщо ви не бачите `image_generate` серед інструментів вашого агента, налаштуйте `agents.defaults.imageGenerationModel` або задайте API-ключ постачальника.
</Note>

## Швидкий старт

1. Задайте API-ключ принаймні для одного постачальника (наприклад, `OPENAI_API_KEY` або `GEMINI_API_KEY`).
2. За бажанням задайте бажану модель:

```json5
{
  agents: {
    defaults: {
      imageGenerationModel: {
        primary: "openai/gpt-image-1",
      },
    },
  },
}
```

3. Попросіть агента: _"Згенеруй зображення дружнього маскота-лобстера."_

Агент автоматично викликає `image_generate`. Додавати інструмент до списку дозволених не потрібно — його ввімкнено типово, коли постачальник доступний.

## Підтримувані постачальники

| Постачальник | Типова модель                    | Підтримка редагування              | API-ключ                                              |
| ------------ | -------------------------------- | ---------------------------------- | ----------------------------------------------------- |
| OpenAI       | `gpt-image-1`                    | Так (до 5 зображень)               | `OPENAI_API_KEY`                                      |
| Google       | `gemini-3.1-flash-image-preview` | Так                                | `GEMINI_API_KEY` або `GOOGLE_API_KEY`                 |
| fal          | `fal-ai/flux/dev`                | Так                                | `FAL_KEY`                                             |
| MiniMax      | `image-01`                       | Так (посилання на об’єкт)          | `MINIMAX_API_KEY` або MiniMax OAuth (`minimax-portal`) |
| ComfyUI      | `workflow`                       | Так (1 зображення, визначається workflow) | `COMFY_API_KEY` або `COMFY_CLOUD_API_KEY` для хмари   |

Використовуйте `action: "list"`, щоб переглянути доступних постачальників і моделі під час виконання:

```
/tool image_generate action=list
```

## Параметри інструмента

| Параметр      | Тип      | Опис                                                                                  |
| ------------- | -------- | ------------------------------------------------------------------------------------- |
| `prompt`      | string   | Запит для генерації зображення (обов’язковий для `action: "generate"`)                |
| `action`      | string   | `"generate"` (типово) або `"list"` для перегляду постачальників                       |
| `model`       | string   | Перевизначення постачальника/моделі, наприклад `openai/gpt-image-1`                   |
| `image`       | string   | Шлях або URL одного еталонного зображення для режиму редагування                      |
| `images`      | string[] | Кілька еталонних зображень для режиму редагування (до 5)                              |
| `size`        | string   | Підказка щодо розміру: `1024x1024`, `1536x1024`, `1024x1536`, `1024x1792`, `1792x1024` |
| `aspectRatio` | string   | Співвідношення сторін: `1:1`, `2:3`, `3:2`, `3:4`, `4:3`, `4:5`, `5:4`, `9:16`, `16:9`, `21:9` |
| `resolution`  | string   | Підказка щодо роздільної здатності: `1K`, `2K` або `4K`                               |
| `count`       | number   | Кількість зображень для генерації (1–4)                                               |
| `filename`    | string   | Підказка щодо назви вихідного файлу                                                   |

Не всі постачальники підтримують усі параметри. Інструмент передає те, що підтримує кожен постачальник, ігнорує решту та повідомляє про відкинуті перевизначення в результаті інструмента.

## Налаштування

### Вибір моделі

```json5
{
  agents: {
    defaults: {
      imageGenerationModel: {
        primary: "openai/gpt-image-1",
        fallbacks: ["google/gemini-3.1-flash-image-preview", "fal/fal-ai/flux/dev"],
      },
    },
  },
}
```

### Порядок вибору постачальників

Під час генерації зображення OpenClaw пробує постачальників у такому порядку:

1. Параметр **`model`** з виклику інструмента (якщо агент його вказує)
2. **`imageGenerationModel.primary`** з конфігурації
3. **`imageGenerationModel.fallbacks`** у вказаному порядку
4. **Автовизначення** — використовує лише типові значення постачальників, підтверджені автентифікацією:
   - спочатку поточний типовий постачальник
   - далі решта зареєстрованих постачальників генерації зображень у порядку ідентифікаторів постачальників

Якщо постачальник не спрацьовує (помилка автентифікації, ліміт запитів тощо), автоматично пробується наступний кандидат. Якщо не спрацьовують усі, помилка містить подробиці кожної спроби.

Примітки:

- Автовизначення враховує автентифікацію. Типове значення постачальника потрапляє до списку кандидатів
  лише тоді, коли OpenClaw справді може автентифікувати цього постачальника.
- Використовуйте `action: "list"`, щоб переглянути поточно зареєстрованих постачальників, їхні
  типові моделі та підказки щодо змінних середовища для автентифікації.

### Редагування зображень

OpenAI, Google, fal, MiniMax і ComfyUI підтримують редагування еталонних зображень. Передайте шлях або URL еталонного зображення:

```
"Згенеруй акварельну версію цього фото" + image: "/path/to/photo.jpg"
```

OpenAI і Google підтримують до 5 еталонних зображень через параметр `images`. fal, MiniMax і ComfyUI підтримують 1.

Генерація зображень MiniMax доступна через обидва вбудовані шляхи автентифікації MiniMax:

- `minimax/image-01` для налаштувань з API-ключем
- `minimax-portal/image-01` для налаштувань з OAuth

## Можливості постачальників

| Можливість            | OpenAI               | Google               | fal                 | MiniMax                    | ComfyUI                            |
| --------------------- | -------------------- | -------------------- | ------------------- | -------------------------- | ---------------------------------- |
| Генерація             | Так (до 4)           | Так (до 4)           | Так (до 4)          | Так (до 9)                 | Так (визначені workflow виходи)    |
| Редагування/еталон    | Так (до 5 зображень) | Так (до 5 зображень) | Так (1 зображення)  | Так (1 зображення, посилання на об’єкт) | Так (1 зображення, визначається workflow) |
| Керування розміром    | Так                  | Так                  | Так                 | Ні                         | Ні                                 |
| Співвідношення сторін | Ні                   | Так                  | Так (лише генерація) | Так                       | Ні                                 |
| Роздільна здатність (1K/2K/4K) | Ні         | Так                  | Так                 | Ні                         | Ні                                 |

## Пов’язане

- [Огляд інструментів](/uk/tools) — усі доступні інструменти агента
- [fal](/uk/providers/fal) — налаштування постачальника зображень і відео fal
- [ComfyUI](/providers/comfy) — налаштування локального ComfyUI і Comfy Cloud workflow
- [Google (Gemini)](/uk/providers/google) — налаштування постачальника зображень Gemini
- [MiniMax](/uk/providers/minimax) — налаштування постачальника зображень MiniMax
- [OpenAI](/uk/providers/openai) — налаштування постачальника OpenAI Images
- [Довідник з конфігурації](/uk/gateway/configuration-reference#agent-defaults) — конфігурація `imageGenerationModel`
- [Моделі](/uk/concepts/models) — конфігурація моделей і перемикання на резервні варіанти
