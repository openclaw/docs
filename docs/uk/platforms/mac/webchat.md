---
read_when:
    - Налагодження подання WebChat на Mac або порту зворотного зв’язку
summary: Як застосунок для Mac вбудовує Gateway WebChat і як його налагоджувати
title: Вебчат (macOS)
x-i18n:
    generated_at: "2026-05-05T23:38:01Z"
    model: gpt-5.5
    provider: openai
    source_hash: b53eda688ff8786da4a4a615927a640090a1ecc71af8c08469c3a3c98a32af41
    source_path: platforms/mac/webchat.md
    workflow: 16
---

Застосунок рядка меню macOS вбудовує інтерфейс WebChat як нативне подання SwiftUI. Він
підключається до Gateway і за замовчуванням використовує **основний сеанс** для вибраного
агента (з перемикачем сеансів для інших сеансів).

- **Локальний режим**: підключається безпосередньо до локального WebSocket Gateway.
- **Віддалений режим**: перенаправляє керівний порт Gateway через SSH і використовує цей
  тунель як площину даних.

## Запуск і налагодження

- Вручну: меню Lobster → “Відкрити чат”.
- Автоматичне відкриття для тестування:

  ```bash
  dist/OpenClaw.app/Contents/MacOS/OpenClaw --webchat
  ```

- Журнали: `./scripts/clawlog.sh` (підсистема `ai.openclaw`, категорія `WebChatSwiftUI`).

## Як це пов’язано

- Площина даних: методи Gateway WS `chat.history`, `chat.send`, `chat.abort`,
  `chat.inject` і події `chat`, `agent`, `presence`, `tick`, `health`.
- `chat.history` повертає рядки транскрипту, нормалізовані для відображення: вбудовані теги
  директив вилучаються з видимого тексту, XML-навантаження викликів інструментів у звичайному тексті
  (зокрема `<tool_call>...</tool_call>`,
  `<function_call>...</function_call>`, `<tool_calls>...</tool_calls>`,
  `<function_calls>...</function_calls>` і скорочені блоки викликів інструментів) та
  просочені ASCII/повноширинні керівні токени моделі вилучаються, суто
  беззвучні рядки асистента з токенами, як-от точні `NO_REPLY` / `no_reply`, не
  включаються, а надмірно великі рядки можуть замінюватися заповнювачами.
- Сеанс: за замовчуванням використовується основний сеанс (`main` або `global`, коли область дії є
  глобальною). Інтерфейс може перемикатися між сеансами.
- Онбординг використовує окремий сеанс, щоб відокремити початкове налаштування.

## Поверхня безпеки

- Віддалений режим перенаправляє через SSH лише керівний WebSocket-порт Gateway.

## Відомі обмеження

- Інтерфейс оптимізовано для чат-сеансів (це не повна браузерна пісочниця).

## Пов’язане

- [WebChat](/uk/web/webchat)
- [застосунок macOS](/uk/platforms/macos)
