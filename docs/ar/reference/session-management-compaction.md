---
read_when:
    - تحتاج إلى استكشاف أخطاء معرّفات الجلسات أو سجل المحادثة بصيغة JSONL أو حقول sessions.json وإصلاحها
    - أنت تغيّر سلوك Compaction التلقائي أو تضيف أعمال صيانة «ما قبل Compaction»
    - تريد تنفيذ عمليات تفريغ الذاكرة أو أدوار النظام الصامتة
summary: 'نظرة معمقة: مخزن الجلسات + السجلات النصية، ودورة الحياة، والتفاصيل الداخلية لـ Compaction (التلقائي)'
title: نظرة متعمقة على إدارة الجلسات
x-i18n:
    generated_at: "2026-05-05T07:31:27Z"
    model: gpt-5.5
    provider: openai
    source_hash: 3161dd9c98bff7ea24266f44a9261693d8a9ee2b47d9af2d152de7057016748b
    source_path: reference/session-management-compaction.md
    workflow: 16
---

يدير OpenClaw الجلسات من البداية إلى النهاية عبر هذه المجالات:

- **توجيه الجلسة** (كيفية ربط الرسائل الواردة بـ `sessionKey`)
- **مخزن الجلسات** (`sessions.json`) وما يتتبعه
- **استمرارية النص الحواري** (`*.jsonl`) وبنيته
- **نظافة النص الحواري** (تصحيحات خاصة بالمزوّد قبل التشغيلات)
- **حدود السياق** (نافذة السياق مقابل الرموز المتتبعة)
- **Compaction** (Compaction اليدوي والتلقائي) وأين تربط عمل ما قبل Compaction
- **صيانة صامتة** (كتابات الذاكرة التي يجب ألا تنتج مخرجات مرئية للمستخدم)

إذا أردت نظرة عامة أعلى مستوى أولاً، فابدأ بـ:

- [إدارة الجلسات](/ar/concepts/session)
- [Compaction](/ar/concepts/compaction)
- [نظرة عامة على الذاكرة](/ar/concepts/memory)
- [البحث في الذاكرة](/ar/concepts/memory-search)
- [تنظيف الجلسات](/ar/concepts/session-pruning)
- [نظافة النص الحواري](/ar/reference/transcript-hygiene)

---

## مصدر الحقيقة: Gateway

صُمم OpenClaw حول **عملية Gateway** واحدة تملك حالة الجلسة.

- يجب أن تستعلم واجهات المستخدم (تطبيق macOS، واجهة Control UI على الويب، TUI) من Gateway عن قوائم الجلسات وأعداد الرموز.
- في الوضع البعيد، تكون ملفات الجلسات على المضيف البعيد؛ لذلك لن يعكس “فحص ملفات Mac المحلية لديك” ما يستخدمه Gateway.

---

## طبقتا استمرارية

يحفظ OpenClaw الجلسات في طبقتين:

1. **مخزن الجلسات (`sessions.json`)**
   - خريطة مفتاح/قيمة: `sessionKey -> SessionEntry`
   - صغير، قابل للتغيير، وآمن للتحرير (أو حذف الإدخالات)
   - يتتبع بيانات الجلسة الوصفية (معرّف الجلسة الحالي، آخر نشاط، المفاتيح، عدادات الرموز، إلخ)

2. **النص الحواري (`<sessionId>.jsonl`)**
   - نص حواري للإلحاق فقط ببنية شجرية (تحتوي الإدخالات على `id` + `parentId`)
   - يخزّن المحادثة الفعلية + استدعاءات الأدوات + ملخصات Compaction
   - يُستخدم لإعادة بناء سياق النموذج للدورات المستقبلية
   - يتم تخطي نقاط فحص التصحيح الكبيرة قبل Compaction بمجرد أن يتجاوز النص الحواري النشط حد حجم نقطة الفحص، لتجنب نسخة `.checkpoint.*.jsonl` عملاقة ثانية.

يجب أن يتجنب قارئو سجل Gateway تحميل النص الحواري بالكامل في الذاكرة إلا إذا كان السطح يحتاج صراحةً إلى وصول تاريخي عشوائي. تستخدم سجلات الصفحة الأولى، وسجل الدردشة المضمّن، واسترداد إعادة التشغيل، وفحوص الرموز/الاستخدام قراءات ذيلية محدودة. تمر عمليات فحص النص الحواري الكاملة عبر فهرس النص الحواري غير المتزامن، الذي يُخزّن مؤقتاً حسب مسار الملف إضافة إلى `mtimeMs`/`size` ويُشارك بين القراء المتزامنين.

---

## المواقع على القرص

لكل وكيل، على مضيف Gateway:

- المخزن: `~/.openclaw/agents/<agentId>/sessions/sessions.json`
- النصوص الحوارية: `~/.openclaw/agents/<agentId>/sessions/<sessionId>.jsonl`
  - جلسات موضوع Telegram: `.../<sessionId>-topic-<threadId>.jsonl`

يحل OpenClaw هذه المسارات عبر `src/config/sessions.ts`.

---

## صيانة المخزن وضوابط القرص

تحتوي استمرارية الجلسات على ضوابط صيانة تلقائية (`session.maintenance`) لـ `sessions.json`، وتحف النصوص الحوارية، وملفات trajectory الجانبية:

- `mode`: `warn` (الافتراضي) أو `enforce`
- `pruneAfter`: حد عمر الإدخال القديم (الافتراضي `30d`)
- `maxEntries`: حد الإدخالات في `sessions.json` (الافتراضي `500`)
- `resetArchiveRetention`: مدة الاحتفاظ بأرشيفات النصوص الحوارية `*.reset.<timestamp>` (الافتراضي: مثل `pruneAfter`؛ يعطل `false` التنظيف)
- `maxDiskBytes`: ميزانية اختيارية لدليل الجلسات
- `highWaterBytes`: هدف اختياري بعد التنظيف (الافتراضي `80%` من `maxDiskBytes`)

تمر كتابات Gateway العادية عبر كاتب جلسات لكل مخزن يقوم بتسلسل التغييرات داخل العملية دون أخذ قفل ملف وقت التشغيل. تستعير أدوات التصحيح المساعدة في المسار الساخن الذاكرة المؤقتة القابلة للتغيير بعد التحقق أثناء امتلاكها خانة الكاتب تلك، لذلك لا تُنسخ ملفات `sessions.json` الكبيرة أو تُقرأ من جديد لكل تحديث بيانات وصفية. يجب أن يفضّل كود وقت التشغيل `updateSessionStore(...)` أو `updateSessionStoreEntry(...)`؛ أما عمليات حفظ المخزن بالكامل مباشرة فهي أدوات توافق وصيانة دون اتصال. عندما يمكن الوصول إلى Gateway، تفوض `openclaw sessions cleanup` و`openclaw agents delete` دون `--dry-run` تغييرات المخزن إلى Gateway حتى ينضم التنظيف إلى طابور الكاتب نفسه؛ ويكون `--store <path>` هو مسار الإصلاح الصريح دون اتصال لصيانة الملفات المباشرة. لا يزال تنظيف `maxEntries` يُنفذ على دفعات للحدود المناسبة للإنتاج، لذلك قد يتجاوز المخزن الحد المكوّن لفترة وجيزة قبل أن يعيده تنظيف مستوى الماء العالي التالي إلى ما دون الحد. لا تقوم قراءات مخزن الجلسات بتنظيف الإدخالات أو تحديدها أثناء بدء Gateway؛ استخدم الكتابات أو `openclaw sessions cleanup --enforce` للتنظيف. لا يزال `openclaw sessions cleanup --enforce` يطبق الحد المكوّن فوراً وينظف النصوص الحوارية القديمة غير المشار إليها، ونقاط الفحص، وتحف trajectory حتى عند عدم تكوين ميزانية قرص.

تحافظ الصيانة على مؤشرات المحادثات الخارجية الدائمة مثل جلسات المجموعات وجلسات الدردشة محددة الخيط، لكن لا يزال يمكن إزالة إدخالات وقت التشغيل الاصطناعية الخاصة بـ Cron، والخطافات، وHeartbeat، وACP، والوكلاء الفرعيين عندما تتجاوز العمر أو العدد أو ميزانية القرص المكوّنة.

لم يعد OpenClaw ينشئ نسخ تدوير احتياطية تلقائية `sessions.json.bak.*` أثناء كتابات Gateway. يتم تجاهل مفتاح `session.maintenance.rotateBytes` القديم ويزيله `openclaw doctor --fix` من الإعدادات الأقدم.

تستخدم تغييرات النص الحواري قفل كتابة جلسة على ملف النص الحواري. ينتظر الحصول على القفل حتى `session.writeLock.acquireTimeoutMs` قبل إظهار خطأ جلسة مشغولة؛ القيمة الافتراضية هي `60000` مللي ثانية. ارفع هذه القيمة فقط عندما يتنافس عمل مشروع للتحضير أو التنظيف أو Compaction أو مرآة النص الحواري لمدة أطول على أجهزة بطيئة. تبقى سياسات اكتشاف الأقفال العالقة وتحذيرات الحد الأقصى لمدة الإمساك منفصلة.

ترتيب الإنفاذ لتنظيف ميزانية القرص (`mode: "enforce"`):

1. أزل أولاً أقدم التحف المؤرشفة أو النصوص الحوارية اليتيمة أو تحف trajectory اليتيمة.
2. إذا بقي الاستخدام فوق الهدف، فأخلِ أقدم إدخالات الجلسات وملفات النصوص الحوارية/trajectory الخاصة بها.
3. واصل حتى يصبح الاستخدام عند `highWaterBytes` أو دونه.

في `mode: "warn"`، يبلّغ OpenClaw عن عمليات الإخلاء المحتملة لكنه لا يغيّر المخزن/الملفات.

شغّل الصيانة عند الطلب:

```bash
openclaw sessions cleanup --dry-run
openclaw sessions cleanup --enforce
```

---

## جلسات Cron وسجلات التشغيل

تنشئ تشغيلات Cron المعزولة أيضاً إدخالات جلسات ونصوصاً حوارية، ولها ضوابط احتفاظ مخصصة:

- `cron.sessionRetention` (الافتراضي `24h`) ينظف جلسات تشغيل Cron المعزولة القديمة من مخزن الجلسات (يعطل `false` ذلك).
- `cron.runLog.maxBytes` + `cron.runLog.keepLines` ينظفان ملفات `~/.openclaw/cron/runs/<jobId>.jsonl` (القيم الافتراضية: `2_000_000` بايت و`2000` سطر).

عندما ينشئ Cron قسراً جلسة تشغيل معزولة جديدة، فإنه يعقم إدخال الجلسة السابق `cron:<jobId>` قبل كتابة الصف الجديد. يحمل تفضيلات آمنة مثل إعدادات التفكير/السريع/المفصل، والتسميات، وتجاوزات النموذج/المصادقة التي اختارها المستخدم صراحةً. ويسقط سياق المحادثة المحيط مثل توجيه القناة/المجموعة، وسياسة الإرسال أو الطابور، والرفع، والأصل، وربط وقت تشغيل ACP حتى لا ترث تشغيله معزولة جديدة تسليماً قديماً أو صلاحية وقت تشغيل من تشغيل أقدم.

---

## مفاتيح الجلسات (`sessionKey`)

يحدد `sessionKey` _حاوية المحادثة_ التي أنت فيها (التوجيه + العزل).

الأنماط الشائعة:

- دردشة رئيسية/مباشرة (لكل وكيل): `agent:<agentId>:<mainKey>` (الافتراضي `main`)
- مجموعة: `agent:<agentId>:<channel>:group:<id>`
- غرفة/قناة (Discord/Slack): `agent:<agentId>:<channel>:channel:<id>` أو `...:room:<id>`
- Cron: `cron:<job.id>`
- Webhook: `hook:<uuid>` (ما لم يتم تجاوزه)

القواعد المعتمدة موثقة في [/concepts/session](/ar/concepts/session).

---

## معرّفات الجلسات (`sessionId`)

يشير كل `sessionKey` إلى `sessionId` حالي (ملف النص الحواري الذي يواصل المحادثة).

قواعد عملية:

- **إعادة الضبط** (`/new`، `/reset`) تنشئ `sessionId` جديداً لذلك `sessionKey`.
- **إعادة الضبط اليومية** (افتراضياً 4:00 صباحاً بالتوقيت المحلي على مضيف Gateway) تنشئ `sessionId` جديداً عند الرسالة التالية بعد حد إعادة الضبط.
- **انتهاء الخمول** (`session.reset.idleMinutes` أو `session.idleMinutes` القديم) ينشئ `sessionId` جديداً عندما تصل رسالة بعد نافذة الخمول. عند تكوين اليومية + الخمول معاً، يفوز أيهما ينتهي أولاً.
- **أحداث النظام** (Heartbeat، إيقاظات Cron، إشعارات التنفيذ، مسك دفاتر Gateway) قد تغيّر صف الجلسة لكنها لا تمدد حداثة إعادة الضبط اليومية/الخاملة. يتجاهل تدوير إعادة الضبط إشعارات أحداث النظام الموضوعة في الطابور للجلسة السابقة قبل بناء الموجّه الجديد.
- **سياسة تفرع الأصل** تستخدم الفرع النشط لـ Pi عند إنشاء خيط أو تفرع وكيل فرعي. إذا كان ذلك الفرع كبيراً جداً، يبدأ OpenClaw الطفل بسياق معزول بدلاً من الفشل أو وراثة سجل غير قابل للاستخدام. سياسة التحجيم تلقائية؛ وتتم إزالة إعداد `session.parentForkMaxTokens` القديم بواسطة `openclaw doctor --fix`.

تفصيل تنفيذي: يحدث القرار في `initSessionState()` داخل `src/auto-reply/reply/session.ts`.

---

## مخطط مخزن الجلسات (`sessions.json`)

نوع قيمة المخزن هو `SessionEntry` في `src/config/sessions.ts`.

الحقول الرئيسية (ليست حصرية):

- `sessionId`: معرّف النص الحواري الحالي (يُشتق اسم الملف من هذا ما لم يتم تعيين `sessionFile`)
- `sessionStartedAt`: طابع زمني لبداية `sessionId` الحالي؛ تستخدم حداثة إعادة الضبط اليومية هذا الحقل. قد تستمد الصفوف القديمة قيمته من ترويسة جلسة JSONL.
- `lastInteractionAt`: طابع زمني لآخر تفاعل مستخدم/قناة حقيقي؛ تستخدم حداثة إعادة ضبط الخمول هذا الحقل حتى لا تُبقي أحداث Heartbeat وCron والتنفيذ الجلسات حية. تعود الصفوف القديمة التي لا تحتوي هذا الحقل إلى وقت بدء الجلسة المسترد لحداثة الخمول.
- `updatedAt`: طابع زمني لآخر تغيير في صف المخزن، يُستخدم للسرد والتنظيف ومسك الدفاتر. ليس هو المرجع لحداثة إعادة الضبط اليومية/الخاملة.
- `sessionFile`: تجاوز اختياري صريح لمسار النص الحواري
- `chatType`: `direct | group | room` (يساعد واجهات المستخدم وسياسة الإرسال)
- `provider`، `subject`، `room`، `space`، `displayName`: بيانات وصفية لتسمية المجموعة/القناة
- المفاتيح:
  - `thinkingLevel`، `verboseLevel`، `reasoningLevel`، `elevatedLevel`
  - `sendPolicy` (تجاوز لكل جلسة)
- اختيار النموذج:
  - `providerOverride`، `modelOverride`، `authProfileOverride`
- عدادات الرموز (أفضل جهد / تعتمد على المزوّد):
  - `inputTokens`، `outputTokens`، `totalTokens`، `contextTokens`
- `compactionCount`: عدد مرات اكتمال Compaction التلقائي لمفتاح الجلسة هذا
- `memoryFlushAt`: طابع زمني لآخر تفريغ ذاكرة قبل Compaction
- `memoryFlushCompactionCount`: عدد Compaction عند تشغيل آخر تفريغ

المخزن آمن للتحرير، لكن Gateway هو المرجع: قد يعيد كتابة الإدخالات أو إعادة ترطيبها أثناء تشغيل الجلسات.

---

## بنية النص الحواري (`*.jsonl`)

تُدار النصوص الحوارية بواسطة `SessionManager` الخاص بـ `@mariozechner/pi-coding-agent`.

الملف بصيغة JSONL:

- السطر الأول: ترويسة الجلسة (`type: "session"`، تتضمن `id`، و`cwd`، و`timestamp`، و`parentSession` اختياري)
- ثم: إدخالات جلسة مع `id` + `parentId` (شجرة)

أنواع الإدخالات البارزة:

- `message`: رسائل المستخدم/المساعد/نتيجة الأداة
- `custom_message`: رسائل محقونة من إضافة _تدخل_ سياق النموذج (يمكن إخفاؤها من واجهة المستخدم)
- `custom`: حالة إضافة _لا تدخل_ سياق النموذج
- `compaction`: ملخص Compaction محفوظ مع `firstKeptEntryId` و`tokensBefore`
- `branch_summary`: ملخص محفوظ عند التنقل في فرع شجرة

لا يقوم OpenClaw عمداً “بتصحيح” النصوص الحوارية؛ يستخدم Gateway `SessionManager` لقراءتها/كتابتها.

---

## نوافذ السياق مقابل الرموز المتتبعة

هناك مفهومان مختلفان مهمان:

1. **نافذة سياق النموذج**: حد صارم لكل نموذج (الرموز المرئية للنموذج)
2. **عدادات مخزن الجلسات**: إحصاءات متحركة تُكتب في `sessions.json` (تُستخدم في /status ولوحات المعلومات)

إذا كنت تضبط الحدود:

- تأتي نافذة السياق من كتالوج النماذج (ويمكن تجاوزها عبر الإعدادات).
- `contextTokens` في المخزن قيمة تقدير/إبلاغ وقت تشغيل؛ لا تعاملها كضمان صارم.

للمزيد، راجع [/token-use](/ar/reference/token-use).

---

## Compaction: ما هو

يلخص Compaction المحادثة الأقدم في إدخال `compaction` محفوظ داخل النص الحواري ويحافظ على الرسائل الحديثة كما هي.

بعد Compaction، ترى الدورات المستقبلية:

- ملخص Compaction
- الرسائل بعد `firstKeptEntryId`

Compaction **دائم** (على عكس تقليم الجلسات). راجع [/concepts/session-pruning](/ar/concepts/session-pruning).

## حدود مقاطع Compaction واقتران الأدوات

عندما يقسم OpenClaw نصًا طويلًا إلى مقاطع Compaction، فإنه يبقي
استدعاءات أدوات المساعد مقترنة بإدخالات `toolResult` المطابقة لها.

- إذا وقع تقسيم حصة الرموز بين استدعاء أداة ونتيجته، فإن OpenClaw
  ينقل الحد إلى رسالة استدعاء الأداة من المساعد بدلًا من فصل
  الزوج.
- إذا كان من شأن كتلة نتيجة أداة في النهاية أن تدفع المقطع فوق الهدف،
  فإن OpenClaw يحافظ على كتلة الأداة المعلقة تلك ويبقي الذيل غير الملخص
  سليمًا.
- لا تبقي كتل استدعاء الأداة المجهضة/ذات الخطأ تقسيمًا معلقًا مفتوحًا.

---

## متى يحدث Compaction التلقائي (وقت تشغيل Pi)

في وكيل Pi المضمن، يتم تشغيل Compaction التلقائي في حالتين:

1. **استعادة التجاوز**: يعيد النموذج خطأ تجاوز للسياق
   (`request_too_large`, `context length exceeded`, `input exceeds the maximum
number of tokens`, `input token count exceeds the maximum number of input
tokens`, `input is too long for the model`, `ollama error: context length
exceeded`، ومتغيرات مشابهة بصياغة المزوّد) → compact → إعادة المحاولة.
2. **صيانة العتبة**: بعد دورة ناجحة، عندما:

`contextTokens > contextWindow - reserveTokens`

حيث:

- `contextWindow` هي نافذة سياق النموذج
- `reserveTokens` هي مساحة احتياطية محجوزة للمطالبات + خرج النموذج التالي

هذه دلالات وقت تشغيل Pi (يستهلك OpenClaw الأحداث، لكن Pi يقرر متى يجري Compaction).

يمكن لـ OpenClaw أيضًا تشغيل Compaction محلي تمهيدي قبل فتح التشغيل التالي
عند ضبط `agents.defaults.compaction.maxActiveTranscriptBytes` ووصول ملف
النص النشط إلى ذلك الحجم. هذا حارس لحجم الملف لتكلفة إعادة الفتح المحلية،
وليس أرشفة خامًا: ما يزال OpenClaw يشغل Compaction الدلالي المعتاد،
ويتطلب `truncateAfterCompaction` حتى يتمكن الملخص المضغوط من أن يصبح
نصًا لاحقًا جديدًا.

بالنسبة إلى تشغيلات Pi المضمنة، يضيف `agents.defaults.compaction.midTurnPrecheck.enabled: true`
حارس حلقة أدوات اختياريًا. بعد إلحاق نتيجة أداة وقبل استدعاء النموذج
التالي، يقدر OpenClaw ضغط المطالبة باستخدام منطق الميزانية التمهيدية نفسه
المستخدم عند بداية الدورة. إذا لم يعد السياق مناسبًا، لا يجري الحارس
Compaction داخل خطاف `transformContext` الخاص بـ Pi. بل يرفع إشارة
فحص تمهيدي منتصف الدورة منظمة، ويوقف إرسال المطالبة الحالية، ويتيح
لحلقة التشغيل الخارجية استخدام مسار الاسترداد الموجود: اقتطاع نتائج الأدوات
كبيرة الحجم عندما يكون ذلك كافيًا، أو تشغيل وضع Compaction المكوّن
وإعادة المحاولة. الخيار معطل افتراضيًا ويعمل مع وضعي Compaction
`default` و`safeguard`، بما في ذلك Compaction الوقائي المدعوم بمزوّد.
هذا مستقل عن `maxActiveTranscriptBytes`: يعمل حارس حجم البايتات قبل
فتح الدورة، بينما يعمل الفحص التمهيدي منتصف الدورة لاحقًا في حلقة أدوات
Pi المضمنة بعد إلحاق نتائج أدوات جديدة.

---

## إعدادات Compaction (`reserveTokens`, `keepRecentTokens`)

توجد إعدادات Compaction الخاصة بـ Pi في إعدادات Pi:

```json5
{
  compaction: {
    enabled: true,
    reserveTokens: 16384,
    keepRecentTokens: 20000,
  },
}
```

يفرض OpenClaw أيضًا حدًا أدنى للأمان للتشغيلات المضمنة:

- إذا كان `compaction.reserveTokens < reserveTokensFloor`، يرفعه OpenClaw.
- الحد الأدنى الافتراضي هو `20000` رمز.
- اضبط `agents.defaults.compaction.reserveTokensFloor: 0` لتعطيل الحد الأدنى.
- إذا كان أعلى بالفعل، يتركه OpenClaw كما هو.
- يحترم `/compact` اليدوي قيمة `agents.defaults.compaction.keepRecentTokens`
  الصريحة ويحافظ على نقطة قص الذيل الحديث الخاصة بـ Pi. من دون ميزانية إبقاء صريحة،
  يبقى Compaction اليدوي نقطة تفتيش صارمة ويبدأ السياق المعاد بناؤه من
  الملخص الجديد.
- اضبط `agents.defaults.compaction.midTurnPrecheck.enabled: true` لتشغيل
  الفحص التمهيدي الاختياري لحلقة الأدوات بعد نتائج الأدوات الجديدة وقبل
  استدعاء النموذج التالي. هذا مشغل فقط؛ ما يزال إنشاء الملخص يستخدم مسار
  Compaction المكوّن. وهو مستقل عن `maxActiveTranscriptBytes`، الذي يمثل
  حارس حجم بايتات للنص النشط عند بداية الدورة.
- اضبط `agents.defaults.compaction.maxActiveTranscriptBytes` على قيمة بايتات أو
  سلسلة مثل `"20mb"` لتشغيل Compaction محلي قبل دورة عندما يكبر النص
  النشط. يكون هذا الحارس نشطًا فقط عند تمكين `truncateAfterCompaction`
  أيضًا. اتركه غير مضبوط أو اضبطه على `0`
  للتعطيل.
- عند تمكين `agents.defaults.compaction.truncateAfterCompaction`،
  يدوّر OpenClaw النص النشط إلى ملف JSONL لاحق مضغوط بعد
  Compaction. يبقى النص الكامل القديم مؤرشفًا ومرتبطًا من نقطة تفتيش
  Compaction بدلًا من إعادة كتابته في مكانه.

السبب: ترك مساحة احتياطية كافية لمهام "الصيانة" متعددة الدورات (مثل كتابات الذاكرة) قبل أن يصبح Compaction حتميًا.

التنفيذ: `ensurePiCompactionReserveTokens()` في `src/agents/pi-settings.ts`
(يُستدعى من `src/agents/pi-embedded-runner.ts`).

---

## مزودو Compaction القابلون للتوصيل

يمكن لـ Plugins تسجيل مزوّد Compaction عبر `registerCompactionProvider()` على واجهة برمجة تطبيقات Plugin. عند ضبط `agents.defaults.compaction.provider` على معرف مزوّد مسجل، يفوض امتداد safeguard التلخيص إلى ذلك المزوّد بدلًا من خط أنابيب `summarizeInStages` المدمج.

- `provider`: معرف Plugin مزوّد Compaction مسجل. اتركه غير مضبوط لتلخيص LLM الافتراضي.
- ضبط `provider` يفرض `mode: "safeguard"`.
- يتلقى المزوّدون تعليمات Compaction نفسها وسياسة الحفاظ على المعرّفات نفسها مثل المسار المدمج.
- ما يزال safeguard يحافظ على سياق لاحقة الدورة الحديثة والدورة المقسمة بعد خرج المزوّد.
- يعيد التلخيص المدمج في safeguard تكثيف الملخصات السابقة مع الرسائل الجديدة
  بدلًا من الحفاظ على الملخص السابق الكامل حرفيًا.
- يفعّل وضع safeguard تدقيقات جودة الملخص افتراضيًا؛ اضبط
  `qualityGuard.enabled: false` لتخطي سلوك إعادة المحاولة عند الخرج سيئ التكوين.
- إذا فشل المزوّد أو أعاد نتيجة فارغة، يعود OpenClaw تلقائيًا إلى تلخيص LLM المدمج.
- تتم إعادة رمي إشارات الإلغاء/انتهاء المهلة (ولا يتم ابتلاعها) احترامًا لإلغاء المستدعي.

المصدر: `src/plugins/compaction-provider.ts`, `src/agents/pi-hooks/compaction-safeguard.ts`.

---

## الأسطح المرئية للمستخدم

يمكنك مراقبة Compaction وحالة الجلسة عبر:

- `/status` (في أي جلسة دردشة)
- `openclaw status` (CLI)
- `openclaw sessions` / `sessions --json`
- الوضع المفصل: `🧹 Auto-compaction complete` + عدد مرات Compaction

---

## صيانة صامتة (`NO_REPLY`)

يدعم OpenClaw دورات "صامتة" للمهام الخلفية حيث يجب ألا يرى المستخدم خرجًا وسيطًا.

العرف:

- يبدأ المساعد خرجه بالرمز الصامت الدقيق `NO_REPLY` /
  `no_reply` للإشارة إلى "لا تسلم ردًا إلى المستخدم".
- يزيل/يكبت OpenClaw هذا في طبقة التسليم.
- كبت الرمز الصامت الدقيق غير حساس لحالة الأحرف، لذا يُحسب كل من `NO_REPLY` و
  `no_reply` عندما يكون كامل الحمولة هو الرمز الصامت فقط.
- هذا مخصص فقط لدورات الخلفية/عدم التسليم الحقيقية؛ وليس اختصارًا
  لطلبات المستخدم العادية القابلة للتنفيذ.

اعتبارًا من `2026.1.10`، يكبت OpenClaw أيضًا **بث المسودة/الكتابة** عندما تبدأ
قطعة جزئية بـ `NO_REPLY`، حتى لا تسرب العمليات الصامتة خرجًا جزئيًا
منتصف الدورة.

---

## "تفريغ الذاكرة" قبل Compaction (منفذ)

الهدف: قبل حدوث Compaction التلقائي، شغّل دورة وكيلية صامتة تكتب حالة دائمة
إلى القرص (مثل `memory/YYYY-MM-DD.md` في مساحة عمل الوكيل) حتى لا يستطيع Compaction
محو سياق حرج.

يستخدم OpenClaw نهج **التفريغ قبل العتبة**:

1. راقب استخدام سياق الجلسة.
2. عندما يتجاوز "عتبة لينة" (أدنى من عتبة Compaction الخاصة بـ Pi)، شغّل توجيهًا صامتًا
   "اكتب الذاكرة الآن" إلى الوكيل.
3. استخدم الرمز الصامت الدقيق `NO_REPLY` / `no_reply` حتى لا يرى المستخدم
   شيئًا.

الإعداد (`agents.defaults.compaction.memoryFlush`):

- `enabled` (الافتراضي: `true`)
- `model` (تجاوز اختياري دقيق للمزوّد/النموذج لدورة التفريغ، مثل `ollama/qwen3:8b`)
- `softThresholdTokens` (الافتراضي: `4000`)
- `prompt` (رسالة المستخدم لدورة التفريغ)
- `systemPrompt` (مطالبة نظام إضافية تُلحق لدورة التفريغ)

ملاحظات:

- تتضمن مطالبة النظام/المطالبة الافتراضية تلميح `NO_REPLY` لكبت
  التسليم.
- عند ضبط `model`، تستخدم دورة التفريغ ذلك النموذج من دون وراثة سلسلة
  الرجوع الاحتياطي للجلسة النشطة، حتى لا ترجع الصيانة المحلية فقط بصمت
  إلى نموذج محادثة مدفوع.
- يعمل التفريغ مرة واحدة لكل دورة Compaction (يُتتبع في `sessions.json`).
- يعمل التفريغ فقط لجلسات Pi المضمنة (تتخطاه خلفيات CLI).
- يتم تخطي التفريغ عندما تكون مساحة عمل الجلسة للقراءة فقط (`workspaceAccess: "ro"` أو `"none"`).
- راجع [الذاكرة](/ar/concepts/memory) لتخطيط ملفات مساحة العمل وأنماط الكتابة.

يعرض Pi أيضًا خطاف `session_before_compact` في واجهة برمجة تطبيقات الامتداد، لكن منطق
التفريغ في OpenClaw موجود اليوم على جانب Gateway.

---

## قائمة التحقق لاستكشاف الأخطاء وإصلاحها

- مفتاح الجلسة خاطئ؟ ابدأ بـ [/concepts/session](/ar/concepts/session) وأكد `sessionKey` في `/status`.
- عدم تطابق المخزن مع النص؟ أكد مضيف Gateway ومسار المخزن من `openclaw status`.
- رسائل Compaction متكررة؟ تحقق من:
  - نافذة سياق النموذج (صغيرة جدًا)
  - إعدادات Compaction (`reserveTokens` مرتفع جدًا بالنسبة إلى نافذة النموذج قد يسبب Compaction أبكر)
  - تضخم نتائج الأدوات: فعّل/اضبط تقليم الجلسات
- تسرب الدورات الصامتة؟ أكد أن الرد يبدأ بـ `NO_REPLY` (رمز دقيق غير حساس لحالة الأحرف) وأنك على بناء يتضمن إصلاح كبت البث.

## مرتبط

- [إدارة الجلسات](/ar/concepts/session)
- [تقليم الجلسات](/ar/concepts/session-pruning)
- [محرك السياق](/ar/concepts/context-engine)
