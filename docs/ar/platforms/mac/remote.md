---
read_when:
    - إعداد التحكم عن بُعد في Mac أو تصحيح أخطائه
summary: تدفق تطبيق macOS للتحكم في Gateway بعيد لـ OpenClaw عبر SSH
title: التحكم عن بُعد
x-i18n:
    generated_at: "2026-04-30T16:29:33Z"
    model: gpt-5.5
    provider: openai
    source_hash: 2c63f752c3636a253220310c7c8e57a28549704b74b2f0370bac432bae28a7d3
    source_path: platforms/mac/remote.md
    workflow: 16
---

# OpenClaw عن بُعد (macOS ⇄ المضيف البعيد)

يتيح هذا المسار لتطبيق macOS العمل كوحدة تحكم عن بُعد كاملة لـ OpenClaw Gateway يعمل على مضيف آخر (سطح مكتب/خادم). هذه هي ميزة **التشغيل عن بُعد عبر SSH** في التطبيق. جميع الميزات، مثل فحوصات السلامة، وتمرير Voice Wake، وWeb Chat، تعيد استخدام إعداد SSH البعيد نفسه من _الإعدادات → عام_.

## الأوضاع

- **محلي (هذا الـ Mac)**: يعمل كل شيء على الحاسوب المحمول. لا يوجد SSH.
- **عن بُعد عبر SSH (الافتراضي)**: تُنفَّذ أوامر OpenClaw على المضيف البعيد. يفتح تطبيق Mac اتصال SSH باستخدام `-o BatchMode` مع الهوية/المفتاح الذي اخترته وتمرير منفذ محلي.
- **عن بُعد مباشر (ws/wss)**: لا يوجد نفق SSH. يتصل تطبيق Mac بعنوان URL الخاص بـ Gateway مباشرةً (على سبيل المثال، عبر Tailscale Serve أو وكيل عكسي HTTPS عام).

## وسائل النقل البعيدة

يدعم الوضع البعيد وسيلتي نقل:

- **نفق SSH** (الافتراضي): يستخدم `ssh -N -L ...` لتمرير منفذ Gateway إلى localhost. سيرى Gateway عنوان IP الخاص بـ Node على أنه `127.0.0.1` لأن النفق يعمل عبر loopback.
- **مباشر (ws/wss)**: يتصل مباشرة بعنوان URL الخاص بـ Gateway. يرى Gateway عنوان IP الحقيقي للعميل.

في وضع نفق SSH، تُحفَظ أسماء مضيفي LAN/tailnet المكتشفة كـ
`gateway.remote.sshTarget`. يُبقي التطبيق `gateway.remote.url` على نقطة نهاية
النفق المحلية، مثل `ws://127.0.0.1:18789`، بحيث تستخدم CLI وWeb Chat
وخدمة مضيف Node المحلية كلها وسيلة نقل loopback الآمنة نفسها.

تشغيل المتصفح آلياً في الوضع البعيد تملكه CLI Node host، وليس Node الخاص
بتطبيق macOS الأصلي. يبدأ التطبيق خدمة Node host المثبتة عندما يكون ذلك
ممكناً؛ إذا كنت تحتاج إلى التحكم بالمتصفح من ذلك الـ Mac، فثبّتها/ابدأها باستخدام
`openclaw node install ...` و`openclaw node start` (أو شغّل
`openclaw node run ...` في الواجهة الأمامية)، ثم استهدف ذلك الـ Node
القادر على تشغيل المتصفح.

## المتطلبات المسبقة على المضيف البعيد

1. ثبّت Node + pnpm وابنِ/ثبّت OpenClaw CLI (`pnpm install && pnpm build && pnpm link --global`).
2. تأكد من أن `openclaw` موجود في PATH للأصداف غير التفاعلية (أنشئ رابطاً رمزياً داخل `/usr/local/bin` أو `/opt/homebrew/bin` عند الحاجة).
3. افتح SSH باستخدام مصادقة المفتاح. نوصي بعناوين IP الخاصة بـ **Tailscale** لضمان وصول مستقر خارج LAN.

## إعداد تطبيق macOS

1. افتح _الإعدادات → عام_.
2. ضمن **مكان تشغيل OpenClaw**، اختر **عن بُعد عبر SSH** واضبط:
   - **وسيلة النقل**: **نفق SSH** أو **مباشر (ws/wss)**.
   - **هدف SSH**: `user@host` (اختياري `:port`).
     - إذا كان Gateway على LAN نفسها ويعلن عبر Bonjour، فاختره من القائمة المكتشفة لملء هذا الحقل تلقائياً.
   - **عنوان URL الخاص بـ Gateway** (للوضع المباشر فقط): `wss://gateway.example.ts.net` (أو `ws://...` للمحلي/LAN).
   - **ملف الهوية** (متقدم): المسار إلى مفتاحك.
   - **جذر المشروع** (متقدم): مسار checkout البعيد المستخدم للأوامر.
   - **مسار CLI** (متقدم): مسار اختياري إلى نقطة دخول/ملف ثنائي `openclaw` قابل للتشغيل (يُملأ تلقائياً عند الإعلان عنه).
3. اضغط **اختبار الاتصال البعيد**. يشير النجاح إلى أن `openclaw status --json` البعيد يعمل بشكل صحيح. تعني حالات الفشل عادةً مشكلات في PATH/CLI؛ ويعني رمز الخروج 127 أن CLI غير موجود عن بُعد.
4. ستعمل فحوصات السلامة وWeb Chat الآن عبر نفق SSH هذا تلقائياً.

## Web Chat

- **نفق SSH**: يتصل Web Chat بـ Gateway عبر منفذ تحكم WebSocket المُمرَّر (الافتراضي 18789).
- **مباشر (ws/wss)**: يتصل Web Chat مباشرة بعنوان URL الخاص بـ Gateway المُعدّ.
- لم يعد هناك خادم WebChat HTTP منفصل.

## الأذونات

- يحتاج المضيف البعيد إلى موافقات TCC نفسها المطلوبة محلياً (Automation وAccessibility وScreen Recording وMicrophone وSpeech Recognition وNotifications). شغّل onboarding على ذلك الجهاز لمنحها مرة واحدة.
- تعلن Nodes عن حالة أذوناتها عبر `node.list` / `node.describe` حتى يعرف الوكلاء ما هو متاح.

## ملاحظات الأمان

- فضّل ربط loopback على المضيف البعيد والاتصال عبر SSH أو Tailscale.
- يستخدم نفق SSH تحققاً صارماً من مفتاح المضيف؛ ثق بمفتاح المضيف أولاً حتى يكون موجوداً في `~/.ssh/known_hosts`.
- إذا ربطت Gateway بواجهة غير loopback، فاشترط مصادقة Gateway صالحة: رمزاً، أو كلمة مرور، أو وكيلاً عكسياً واعياً بالهوية مع `gateway.auth.mode: "trusted-proxy"`.
- راجع [الأمان](/ar/gateway/security) و[Tailscale](/ar/gateway/tailscale).

## مسار تسجيل الدخول إلى WhatsApp (عن بُعد)

- شغّل `openclaw channels login --verbose` **على المضيف البعيد**. امسح رمز QR باستخدام WhatsApp على هاتفك.
- أعد تشغيل تسجيل الدخول على ذلك المضيف إذا انتهت صلاحية المصادقة. سيُظهر فحص السلامة مشكلات الربط.

## استكشاف الأخطاء وإصلاحها

- **رمز الخروج 127 / غير موجود**: `openclaw` ليس ضمن PATH للأصداف غير الخاصة بتسجيل الدخول. أضفه إلى `/etc/paths`، أو ملف إعدادات الصدفة، أو أنشئ رابطاً رمزياً داخل `/usr/local/bin`/`/opt/homebrew/bin`.
- **فشل فحص السلامة**: تحقق من إمكانية الوصول عبر SSH، وPATH، وأن Baileys مسجّل الدخول (`openclaw status --json`).
- **Web Chat عالق**: تأكد من أن Gateway يعمل على المضيف البعيد وأن المنفذ المُمرَّر يطابق منفذ WS الخاص بـ Gateway؛ تتطلب واجهة المستخدم اتصال WS سليماً.
- **عنوان IP الخاص بـ Node يظهر 127.0.0.1**: هذا متوقع مع نفق SSH. بدّل **وسيلة النقل** إلى **مباشر (ws/wss)** إذا أردت أن يرى Gateway عنوان IP الحقيقي للعميل.
- **لوحة المعلومات تعمل لكن إمكانات Mac غير متصلة**: يعني ذلك أن اتصال المشغّل/التحكم الخاص بالتطبيق سليم، لكن اتصال Node المرافق غير متصل أو يفتقد سطح الأوامر الخاص به. افتح قسم الجهاز في شريط القوائم وتحقق مما إذا كان الـ Mac يظهر كـ `paired · disconnected`. بالنسبة إلى نقاط نهاية Tailscale Serve بنمط `wss://*.ts.net`، يكتشف التطبيق دبابيس أوراق TLS القديمة المتقادمة بعد تدوير الشهادة، ويمسح الدبوس المتقادم عندما يثق macOS بالشهادة الجديدة، ثم يعيد المحاولة تلقائياً. إذا لم تكن الشهادة موثوقة على مستوى النظام أو لم يكن المضيف اسماً من Tailscale Serve، فراجع الشهادة أو انتقل إلى **عن بُعد عبر SSH**.
- **Voice Wake**: تُمرَّر عبارات التشغيل تلقائياً في الوضع البعيد؛ لا حاجة إلى مُمرِّر منفصل.

## أصوات الإشعارات

اختر الأصوات لكل إشعار من السكربتات باستخدام `openclaw` و`node.invoke`، مثل:

```bash
openclaw nodes notify --node <id> --title "Ping" --body "Remote gateway ready" --sound Glass
```

لم يعد هناك تبديل عام لـ “الصوت الافتراضي” في التطبيق؛ يختار المستدعون صوتاً (أو لا يختارون أي صوت) لكل طلب.

## ذو صلة

- [تطبيق macOS](/ar/platforms/macos)
- [الوصول عن بُعد](/ar/gateway/remote)
