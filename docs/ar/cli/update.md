---
read_when:
    - تريد تحديث نسخة عمل من المصدر بأمان
    - أنت تصحّح أخطاء مخرجات `openclaw update` أو خياراته
    - يجب أن تفهم سلوك الاختصار `--update`
summary: مرجع CLI لـ `openclaw update` (تحديث المصدر الآمن نسبيًا + إعادة التشغيل التلقائي لـ Gateway)
title: تحديث
x-i18n:
    generated_at: "2026-05-06T17:55:27Z"
    model: gpt-5.5
    provider: openai
    source_hash: 483e702dfe7f1d1b2f4bcd1037a93ba794fc6a24ff2060afcb3a825c3dc165c7
    source_path: cli/update.md
    workflow: 16
---

# `openclaw update`

حدّث OpenClaw بأمان وبدّل بين قنوات stable/beta/dev.

إذا ثبّت عبر **npm/pnpm/bun** (تثبيت عام، من دون بيانات git وصفية)،
تتم التحديثات عبر مسار مدير الحزم في [التحديث](/ar/install/updating).

## الاستخدام

```bash
openclaw update
openclaw update status
openclaw update wizard
openclaw update --channel beta
openclaw update --channel dev
openclaw update --tag beta
openclaw update --tag main
openclaw update --dry-run
openclaw update --no-restart
openclaw update --yes
openclaw update --json
openclaw --update
```

## الخيارات

- `--no-restart`: تخطَّ إعادة تشغيل خدمة Gateway بعد نجاح التحديث. تحديثات مدير الحزم التي تعيد تشغيل Gateway تتحقق من أن الخدمة المعاد تشغيلها تبلغ عن الإصدار المحدّث المتوقع قبل نجاح الأمر.
- `--channel <stable|beta|dev>`: اضبط قناة التحديث (git + npm؛ تُحفظ في الإعدادات).
- `--tag <dist-tag|version|spec>`: تجاوز هدف الحزمة لهذا التحديث فقط. لتثبيتات الحزم، يطابق `main` القيمة `github:openclaw/openclaw#main`.
- `--dry-run`: عاين إجراءات التحديث المخطط لها (تدفق القناة/الوسم/الهدف/إعادة التشغيل) من دون كتابة الإعدادات أو التثبيت أو مزامنة plugins أو إعادة التشغيل.
- `--json`: اطبع JSON قابلًا للقراءة آليًا لـ `UpdateRunResult`، بما في ذلك
  `postUpdate.plugins.warnings` عندما تحتاج plugins المُدارة التالفة أو غير القابلة للتحميل
  إلى إصلاح بعد نجاح تحديث النواة، و`postUpdate.plugins.integrityDrifts`
  عند اكتشاف انحراف في أثر حزمة npm plugin أثناء مزامنة plugin بعد التحديث.
- `--timeout <seconds>`: مهلة لكل خطوة (الافتراضي 1800 ثانية).
- `--yes`: تخطَّ مطالبات التأكيد (مثل تأكيد الرجوع إلى إصدار أقدم).

لا يحتوي `openclaw update` على علم `--verbose`. استخدم `--dry-run` لمعاينة
إجراءات القناة/الوسم/التثبيت/إعادة التشغيل المخطط لها، و`--json` للنتائج
القابلة للقراءة آليًا، و`openclaw update status --json` عندما تحتاج فقط إلى
تفاصيل القناة والتوفر. إذا كنت تصحح سجلات Gateway حول تحديث ما، فإن إسهاب وحدة
التحكم ومستوى سجل الملفات منفصلان: يؤثر `--verbose` الخاص بـ Gateway في مخرجات
الطرفية/WebSocket، بينما تتطلب سجلات الملفات `logging.level: "debug"` أو
`"trace"` في الإعدادات. راجع [تسجيل Gateway](/ar/gateway/logging).

<Note>
في وضع Nix (`OPENCLAW_NIX_MODE=1`)، تُعطّل عمليات `openclaw update` التي تُجري تغييرات. حدّث مصدر Nix أو مُدخل flake لهذا التثبيت بدلًا من ذلك؛ بالنسبة إلى nix-openclaw، استخدم [البداية السريعة](https://github.com/openclaw/nix-openclaw#quick-start) التي تبدأ بالوكيل. يظل `openclaw update status` و`openclaw update --dry-run` للقراءة فقط.
</Note>

<Warning>
تتطلب عمليات الرجوع إلى إصدار أقدم تأكيدًا لأن الإصدارات الأقدم قد تعطل الإعدادات.
</Warning>

## `update status`

اعرض قناة التحديث النشطة + وسم/فرع/SHA الخاص بـ git (لنسخ المصدر)، إضافة إلى توفر التحديث.

```bash
openclaw update status
openclaw update status --json
openclaw update status --timeout 10
```

الخيارات:

- `--json`: اطبع JSON للحالة قابلًا للقراءة آليًا.
- `--timeout <seconds>`: مهلة الفحوصات (الافتراضي 3 ثوانٍ).

## `update wizard`

تدفق تفاعلي لاختيار قناة تحديث وتأكيد ما إذا كان ينبغي إعادة تشغيل Gateway
بعد التحديث (الافتراضي هو إعادة التشغيل). إذا اخترت `dev` من دون نسخة git، فإنه
يعرض إنشاء واحدة.

الخيارات:

- `--timeout <seconds>`: مهلة كل خطوة تحديث (الافتراضي `1800`)

## ما الذي يفعله

عند تبديل القنوات صراحةً (`--channel ...`)، يحافظ OpenClaw أيضًا على اتساق
طريقة التثبيت:

- `dev` → يضمن وجود نسخة git (الافتراضي: `~/openclaw`، ويمكن التجاوز بـ `OPENCLAW_GIT_DIR`)،
  ويحدثها، ويثبّت CLI العام من تلك النسخة.
- `stable` → يثبّت من npm باستخدام `latest`.
- `beta` → يفضل وسم توزيع npm `beta`، لكنه يعود إلى `latest` عندما يكون beta
  مفقودًا أو أقدم من الإصدار stable الحالي.

يشغّل محدّث نواة Gateway التلقائي (عند تفعيله عبر الإعدادات) مسار تحديث CLI
خارج معالج طلبات Gateway الحي. تفرض تحديثات مدير الحزم `update.run` في مستوى
التحكم إعادة تشغيل تحديث غير مؤجلة ومن دون فترة تهدئة بعد استبدال الحزمة،
لأن عملية Gateway القديمة قد تظل تحمل في الذاكرة أجزاء تشير إلى ملفات أزالتها
الحزمة الجديدة.

بالنسبة إلى تثبيتات مدير الحزم، يحل `openclaw update` إصدار الحزمة الهدف قبل
استدعاء مدير الحزم. تستخدم تثبيتات npm العامة تثبيتًا مرحليًا: يثبّت OpenClaw
الحزمة الجديدة في بادئة npm مؤقتة، ويتحقق من مخزون `dist` المضمّن هناك، ثم
يستبدل شجرة الحزمة النظيفة تلك داخل البادئة العامة الحقيقية. إذا فشل التحقق،
لا تُشغَّل أعمال doctor بعد التحديث، أو مزامنة plugin، أو إعادة التشغيل من
الشجرة المشتبه بها. حتى عندما يطابق الإصدار المثبت الهدف بالفعل، يحدّث الأمر
تثبيت الحزمة العامة، ثم يشغّل مزامنة plugin، وتحديث إكمال أوامر النواة، وأعمال
إعادة التشغيل. يحافظ ذلك على اتساق الملحقات الجانبية المضمّنة وسجلات plugin
المملوكة للقناة مع بناء OpenClaw المثبت، مع ترك عمليات إعادة بناء إكمال أوامر
plugin الكاملة لتشغيلات `openclaw completion --write-state` الصريحة.

عند تثبيت خدمة Gateway مُدارة محليًا وتفعيل إعادة التشغيل، توقف تحديثات مدير
الحزم الخدمة الجارية قبل استبدال شجرة الحزمة، ثم تحدّث بيانات الخدمة الوصفية
من التثبيت المحدّث، وتعيد تشغيل الخدمة، وتتحقق من أن Gateway المعاد تشغيله
يبلغ عن الإصدار المتوقع قبل الإبلاغ عن النجاح. على macOS، يتحقق فحص ما بعد
التحديث أيضًا من أن LaunchAgent محمّل/قيد التشغيل للملف النشط وأن منفذ loopback
المهيأ سليم. إذا كان plist مثبتًا لكن launchd لا يشرف عليه، يعيد OpenClaw
تمهيد LaunchAgent تلقائيًا، ثم يعيد تشغيل فحوصات الجاهزية الخاصة بالصحة/الإصدار/القناة.
يحمّل التمهيد الجديد مهمة RunAtLoad مباشرةً، لذلك لا ينفّذ استرداد التحديث
فورًا `kickstart -k` على Gateway المنشأ حديثًا. إذا ظل Gateway غير سليم، يخرج
الأمر بقيمة غير صفرية ويطبع مسار سجل إعادة التشغيل إضافة إلى تعليمات صريحة
لإعادة التشغيل وإعادة التثبيت والتراجع عن الحزمة. مع `--no-restart`،
يظل استبدال الحزمة قيد التنفيذ، لكن الخدمة المُدارة لا تتوقف ولا يُعاد تشغيلها،
لذلك قد يظل Gateway الجاري يستخدم الشيفرة القديمة إلى أن تعيد تشغيله يدويًا.

## تدفق نسخة git

### اختيار القناة

- `stable`: الانتقال إلى أحدث وسم غير beta، ثم البناء وتشغيل doctor.
- `beta`: تفضيل أحدث وسم `-beta`، مع الرجوع إلى أحدث وسم stable عند غياب beta أو كونه أقدم.
- `dev`: الانتقال إلى `main`، ثم الجلب وإعادة التأسيس.

### خطوات التحديث

<Steps>
  <Step title="التحقق من نظافة شجرة العمل">
    يتطلب عدم وجود تغييرات غير ملتزم بها.
  </Step>
  <Step title="تبديل القناة">
    يبدّل إلى القناة المحددة (وسم أو فرع).
  </Step>
  <Step title="جلب المصدر الأعلى">
    لـ Dev فقط.
  </Step>
  <Step title="بناء تمهيدي (لـ dev فقط)">
    يشغّل بناء TypeScript في شجرة عمل مؤقتة. إذا فشلت القمة، يرجع حتى 10 التزامات للعثور على أحدث التزام قابل للبناء. اضبط `OPENCLAW_UPDATE_PREFLIGHT_LINT=1` لتشغيل الفحص كذلك أثناء هذا التمهيد؛ يعمل الفحص في وضع تسلسلي مقيّد لأن مضيفي تحديث المستخدم غالبًا أصغر من مشغلات CI.
  </Step>
  <Step title="إعادة التأسيس">
    يعيد التأسيس على الالتزام المحدد (لـ dev فقط).
  </Step>
  <Step title="تثبيت الاعتماديات">
    يستخدم مدير حزم المستودع. بالنسبة إلى نسخ pnpm، يمهّد المحدّث `pnpm` عند الطلب (عبر `corepack` أولًا، ثم خيار احتياطي مؤقت `npm install pnpm@10`) بدلًا من تشغيل `npm run build` داخل مساحة عمل pnpm.
  </Step>
  <Step title="بناء واجهة التحكم">
    يبني gateway وواجهة التحكم.
  </Step>
  <Step title="تشغيل doctor">
    يعمل `openclaw doctor` كفحص التحديث الآمن النهائي.
  </Step>
  <Step title="مزامنة plugins">
    يزامن plugins مع القناة النشطة. يستخدم Dev plugins المضمّنة؛ وتستخدم stable وbeta npm. يحدّث تثبيتات plugin المتتبعة.
  </Step>
</Steps>

على قناة تحديث beta، تحاول تثبيتات npm وClawHub plugin المتتبعة التي تتبع خط
default/latest إصدار plugin `@beta` أولًا. إذا لم يكن لدى plugin إصدار beta،
يرجع OpenClaw إلى مواصفة default/latest المسجلة. بالنسبة إلى plugins الخاصة بـ npm،
يرجع OpenClaw أيضًا عندما توجد حزمة beta لكنها تفشل في تحقق التثبيت. لا تُعاد
كتابة الإصدارات الدقيقة والوسوم الصريحة.

<Warning>
إذا أدى تحديث npm plugin مثبت بدقة إلى أثر تختلف سلامته عن سجل التثبيت المخزن، يوقف `openclaw update` تحديث أثر ذلك plugin بدلًا من تثبيته. أعد تثبيت plugin أو حدّثه صراحةً فقط بعد التحقق من أنك تثق بالأثر الجديد.
</Warning>

<Note>
تُبلّغ إخفاقات مزامنة plugin بعد التحديث والمحصورة في plugin مُدار كتحذيرات بعد نجاح تحديث النواة. تحتفظ نتيجة JSON بحالة التحديث العلوية `status: "ok"` وتبلغ عن `postUpdate.plugins.status: "warning"` مع إرشادات `openclaw doctor --fix` و`openclaw plugins inspect <id> --runtime --json`. تظل استثناءات المحدّث أو المزامنة غير المتوقعة تُفشل نتيجة التحديث. أصلح تثبيت plugin أو خطأ التحديث، ثم أعد تشغيل `openclaw doctor --fix` أو `openclaw update`.

عند بدء Gateway المحدّث، يكون تحميل plugin للتحقق فقط: لا يشغّل بدء التشغيل مديري حزم ولا يغيّر أشجار الاعتماديات. تتجاوز عمليات إعادة تشغيل `update.run` الخاصة بمدير الحزم التأجيل الخامل العادي وفترة تهدئة إعادة التشغيل بعد استبدال شجرة الحزمة، كي لا تتمكن العملية القديمة من متابعة التحميل الكسول لأجزاء أزيلت.

إذا ظل تمهيد pnpm يفشل، يتوقف المحدّث مبكرًا مع خطأ خاص بمدير الحزم بدلًا من محاولة `npm run build` داخل النسخة.
</Note>

## اختصار `--update`

يعيد `openclaw --update` الكتابة إلى `openclaw update` (مفيد للصدفات وسكربتات التشغيل).

## ذات صلة

- `openclaw doctor` (يعرض تشغيل التحديث أولًا في نسخ git)
- [قنوات التطوير](/ar/install/development-channels)
- [التحديث](/ar/install/updating)
- [مرجع CLI](/ar/cli)
