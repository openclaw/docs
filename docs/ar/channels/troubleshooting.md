---
read_when:
    - يفيد نقل القناة بأنه متصل، لكن الردود تفشل
    - تحتاج إلى فحوصات خاصة بالقناة قبل وثائق المزوّد المتعمقة
summary: استكشاف أخطاء سريع على مستوى القناة مع بصمات الفشل والإصلاحات لكل قناة
title: استكشاف أخطاء القناة وإصلاحها
x-i18n:
    generated_at: "2026-05-04T02:23:15Z"
    model: gpt-5.5
    provider: openai
    source_hash: a3a0737156ae83897c44d18505e0355a5d8e5700106b984496d94874c270deb2
    source_path: channels/troubleshooting.md
    workflow: 16
---

استخدم هذه الصفحة عندما تتصل قناة لكن يكون السلوك غير صحيح.

## تسلسل الأوامر

شغّل هذه بالترتيب أولاً:

```bash
openclaw status
openclaw gateway status
openclaw logs --follow
openclaw doctor
openclaw channels status --probe
```

خط الأساس السليم:

- `Runtime: running`
- `Connectivity probe: ok`
- `Capability: read-only`، أو `write-capable`، أو `admin-capable`
- يعرض فحص القناة أن النقل متصل، وحيثما يكون ذلك مدعوماً، `works` أو `audit ok`

## WhatsApp

### توقيعات فشل WhatsApp

| العَرَض                         | أسرع فحص                                            | الإصلاح                                                                                                                               |
| ------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| متصل لكن لا توجد ردود في الرسائل المباشرة | `openclaw pairing list whatsapp`                    | وافق على المرسل أو بدّل سياسة الرسائل المباشرة/قائمة السماح.                                                                         |
| يتم تجاهل رسائل المجموعة        | تحقق من `requireMention` وأنماط الإشارة في الإعدادات | أشِر إلى البوت أو خفف سياسة الإشارة لتلك المجموعة.                                                                                   |
| تنتهي مهلة تسجيل الدخول عبر QR بالرمز 408 | تحقق من متغيرات بيئة Gateway `HTTPS_PROXY` / `HTTP_PROXY` | عيّن وكيلاً يمكن الوصول إليه؛ استخدم `NO_PROXY` للتجاوزات فقط.                                                                       |
| حلقات قطع اتصال/إعادة تسجيل دخول عشوائية | `openclaw channels status --probe` + السجلات        | تُعلَّم عمليات إعادة الاتصال الحديثة حتى عند الاتصال حالياً؛ راقب السجلات، وأعد تشغيل Gateway، ثم أعد الربط إذا استمر التذبذب. |

استكشاف الأخطاء الكامل: [استكشاف أخطاء WhatsApp وإصلاحها](/ar/channels/whatsapp#troubleshooting)

## Telegram

### توقيعات فشل Telegram

| العَرَض                              | أسرع فحص                                         | الإصلاح                                                                                                                           |
| ------------------------------------ | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| `/start` لكن لا يوجد مسار رد قابل للاستخدام | `openclaw pairing list telegram`                 | وافق على الاقتران أو غيّر سياسة الرسائل المباشرة.                                                                                 |
| البوت متصل لكن المجموعة تبقى صامتة  | تحقق من متطلب الإشارة ووضع خصوصية البوت          | عطّل وضع الخصوصية لإظهار المجموعة أو أشِر إلى البوت.                                                                              |
| فشل الإرسال مع أخطاء شبكة            | افحص السجلات بحثاً عن فشل استدعاءات Telegram API | أصلح توجيه DNS/IPv6/الوكيل إلى `api.telegram.org`.                                                                                |
| تقارير بدء التشغيل `getMe returned 401` | تحقق من مصدر الرمز المهيأ                         | أعد نسخ رمز BotFather أو أعد توليده وحدّث `botToken` أو `tokenFile` أو `TELEGRAM_BOT_TOKEN` للحساب الافتراضي.                    |
| يتوقف الاستطلاع أو يعيد الاتصال ببطء | `openclaw logs --follow` لتشخيصات الاستطلاع      | رقِّ؛ إذا كانت عمليات إعادة التشغيل نتائج إيجابية كاذبة، فاضبط `pollingStallThresholdMs`. لا تزال التوقفات المستمرة تشير إلى الوكيل/DNS/IPv6. |
| تم رفض `setMyCommands` عند بدء التشغيل | افحص السجلات بحثاً عن `BOT_COMMANDS_TOO_MUCH`     | قلّل أوامر Telegram الخاصة بالـ Plugin/Skill/المخصصة أو عطّل القوائم الأصلية.                                                     |
| بعد الترقية، تمنعك قائمة السماح      | `openclaw security audit` وقوائم السماح في الإعدادات | شغّل `openclaw doctor --fix` أو استبدل `@username` بمعرفات مرسل رقمية.                                                            |

استكشاف الأخطاء الكامل: [استكشاف أخطاء Telegram وإصلاحها](/ar/channels/telegram#troubleshooting)

## Discord

### توقيعات فشل Discord

| العَرَض                                   | أسرع فحص                                                            | الإصلاح                                                                                                                                                         |
| ----------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| البوت متصل لكن لا توجد ردود في الخادم     | `openclaw channels status --probe`                                   | اسمح بالخادم/القناة وتحقق من نية محتوى الرسائل.                                                                                                                 |
| يتم تجاهل رسائل المجموعة                 | تحقق من السجلات بحثاً عن إسقاطات بوابة الإشارة                      | أشِر إلى البوت أو عيّن `requireMention: false` للخادم/القناة.                                                                                                    |
| استخدام للكتابة/الرموز لكن لا توجد رسالة Discord | يعرض سجل الجلسة نص المساعد مع `didSendViaMessagingTool: false` | أجاب النموذج بشكل خاص بدلاً من استدعاء أداة الرسائل. استخدم نموذجاً موثوقاً في استدعاء الأدوات، أو عيّن `messages.groupChat.visibleReplies: "automatic"` للنشر التلقائي. |
| ردود الرسائل المباشرة مفقودة             | `openclaw pairing list discord`                                      | وافق على اقتران الرسائل المباشرة أو عدّل سياسة الرسائل المباشرة.                                                                                                |

استكشاف الأخطاء الكامل: [استكشاف أخطاء Discord وإصلاحها](/ar/channels/discord#troubleshooting)

## Slack

### توقيعات فشل Slack

| العَرَض                                | أسرع فحص                                  | الإصلاح                                                                                                                                                      |
| -------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| وضع Socket متصل لكن لا توجد استجابات   | `openclaw channels status --probe`        | تحقق من رمز التطبيق + رمز البوت والنطاقات المطلوبة؛ راقب `botTokenStatus` / `appTokenStatus = configured_unavailable` في الإعدادات المدعومة بـ SecretRef. |
| الرسائل المباشرة محظورة               | `openclaw pairing list slack`             | وافق على الاقتران أو خفف سياسة الرسائل المباشرة.                                                                                                             |
| يتم تجاهل رسالة القناة                | تحقق من `groupPolicy` وقائمة سماح القناة | اسمح بالقناة أو بدّل السياسة إلى `open`.                                                                                                                      |

استكشاف الأخطاء الكامل: [استكشاف أخطاء Slack وإصلاحها](/ar/channels/slack#troubleshooting)

## iMessage و BlueBubbles

### توقيعات فشل iMessage و BlueBubbles

| العَرَض                          | أسرع فحص                                                               | الإصلاح                                                   |
| -------------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------- |
| لا توجد أحداث واردة              | تحقق من إمكانية الوصول إلى Webhook/الخادم وأذونات التطبيق              | أصلح عنوان URL الخاص بـ Webhook أو حالة خادم BlueBubbles. |
| يمكن الإرسال لكن لا يوجد استقبال على macOS | تحقق من أذونات خصوصية macOS لأتمتة Messages                            | أعد منح أذونات TCC وأعد تشغيل عملية القناة.              |
| مرسل الرسائل المباشرة محظور      | `openclaw pairing list imessage` أو `openclaw pairing list bluebubbles` | وافق على الاقتران أو حدّث قائمة السماح.                  |

استكشاف الأخطاء الكامل:

- [استكشاف أخطاء iMessage وإصلاحها](/ar/channels/imessage#troubleshooting)
- [استكشاف أخطاء BlueBubbles وإصلاحها](/ar/channels/bluebubbles#troubleshooting)

## Signal

### توقيعات فشل Signal

| العَرَض                         | أسرع فحص                                   | الإصلاح                                                      |
| ------------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| يمكن الوصول إلى البرنامج الخفي لكن البوت صامت | `openclaw channels status --probe`         | تحقق من عنوان URL/الحساب الخاص ببرنامج `signal-cli` الخفي ووضع الاستقبال. |
| الرسائل المباشرة محظورة        | `openclaw pairing list signal`             | وافق على المرسل أو عدّل سياسة الرسائل المباشرة.              |
| ردود المجموعة لا تُشغَّل        | تحقق من قائمة سماح المجموعة وأنماط الإشارة | أضف المرسل/المجموعة أو خفف البوابة.                          |

استكشاف الأخطاء الكامل: [استكشاف أخطاء Signal وإصلاحها](/ar/channels/signal#troubleshooting)

## QQ Bot

### توقيعات فشل QQ Bot

| العَرَض                         | أسرع فحص                                    | الإصلاح                                                             |
| ------------------------------- | ------------------------------------------- | ------------------------------------------------------------------- |
| يرد البوت "ذهب إلى المريخ"      | تحقق من `appId` و `clientSecret` في الإعدادات | عيّن بيانات الاعتماد أو أعد تشغيل Gateway.                          |
| لا توجد رسائل واردة             | `openclaw channels status --probe`          | تحقق من بيانات الاعتماد على QQ Open Platform.                       |
| لا يتم نسخ الصوت                | تحقق من إعدادات مزود STT                    | هيّئ `channels.qqbot.stt` أو `tools.media.audio`.                    |
| الرسائل الاستباقية لا تصل       | تحقق من متطلبات تفاعل منصة QQ               | قد يحظر QQ الرسائل التي يبدأها البوت دون تفاعل حديث.                |

استكشاف الأخطاء الكامل: [استكشاف أخطاء QQ Bot وإصلاحها](/ar/channels/qqbot#troubleshooting)

## Matrix

### توقيعات فشل Matrix

| العَرَض                             | أسرع فحص                               | الإصلاح                                                                       |
| ----------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------- |
| مسجّل الدخول لكنه يتجاهل رسائل الغرفة | `openclaw channels status --probe`     | تحقق من `groupPolicy`، وقائمة سماح الغرف، وبوابة الإشارة.                    |
| لا تتم معالجة الرسائل المباشرة      | `openclaw pairing list matrix`         | وافق على المرسل أو عدّل سياسة الرسائل المباشرة.                              |
| تفشل الغرف المشفرة                 | `openclaw matrix verify status`        | أعد التحقق من الجهاز، ثم تحقق من `openclaw matrix verify backup status`.     |
| استعادة النسخة الاحتياطية معلقة/معطلة | `openclaw matrix verify backup status` | شغّل `openclaw matrix verify backup restore` أو أعد التشغيل باستخدام مفتاح استرداد. |
| يبدو التوقيع المتقاطع/التمهيد غير صحيح | `openclaw matrix verify bootstrap`     | أصلح تخزين الأسرار، والتوقيع المتقاطع، وحالة النسخ الاحتياطي في تمريرة واحدة. |

الإعداد والتكوين الكاملان: [Matrix](/ar/channels/matrix)

## ذو صلة

- [الاقتران](/ar/channels/pairing)
- [توجيه القنوات](/ar/channels/channel-routing)
- [استكشاف أخطاء Gateway وإصلاحها](/ar/gateway/troubleshooting)
