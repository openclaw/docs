---
read_when:
    - العمل على عناصر التحكم في القياس عن بُعد / الخصوصية
    - أسئلة حول البيانات التي يتم جمعها
summary: القياسات عن بُعد للتثبيت التي تجمعها ClawHub CLI وكيفية إلغاء الاشتراك.
x-i18n:
    generated_at: "2026-07-02T14:05:58Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# القياسات عن بُعد

يستخدم ClawHub الحد الأدنى من القياسات عن بُعد في CLI لحساب إجمالي عدد عمليات التثبيت.

## متى تُجمع القياسات عن بُعد

لا تُرسل القياسات عن بُعد إلا عندما:

- تكون مسجّل الدخول في CLI.
- تشغّل `clawhub install <slug>`.
- تكون القياسات عن بُعد **غير معطّلة** (راجع “كيفية التعطيل” أدناه).

إذا لم تكن مسجّل الدخول، فلن يتم الإبلاغ عن أي شيء.

## ما نجمعه

عند كل عملية `clawhub install` يتم الإبلاغ عنها، يرسل CLI حدث تثبيت واحدًا بأفضل جهد.

يتضمن الحدث:

- `slug`: slug المهارة المثبّتة.
- `version`: الإصدار المثبّت، عند معرفته.

### ما _لا_ نجمعه

- لا مسارات مجلدات ولا معرّفات مشتقة من المجلدات.
- لا محتويات ملفات.
- لا سجلات لكل تشغيل، ولا مطالبات، ولا أي مخرجات CLI أخرى.

## أعداد التثبيت

يحافظ ClawHub على عدادات إجمالية لكل مهارة:

- `installsAllTime`: المستخدمون الفريدون الذين أبلغوا عن تثبيت CLI واحد على الأقل للمهارة.
- `installsCurrent`: المستخدمون الفريدون الذين أبلغوا عن تثبيت ولم يحذفوا
  قياساتهم عن بُعد.

## الشفافية + عناصر تحكم المستخدم

لا يرى الجميع إلا **عدادات التثبيت الإجمالية**.

يؤدي حذف حسابك أيضًا إلى حذف بيانات القياسات عن بُعد الخاصة بك.

## كيفية تعطيل القياسات عن بُعد

اضبط متغير البيئة:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

عند ضبط هذا المتغير، لن يرسل CLI قياسات التثبيت عن بُعد.
