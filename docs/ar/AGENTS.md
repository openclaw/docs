---
x-i18n:
    generated_at: "2026-05-10T19:20:29Z"
    model: gpt-5.5
    provider: openai
    source_hash: 4fb1075777cead58155336aa27359c8c149748bec8a854ff1de1f75a992b8c8f
    source_path: AGENTS.md
    workflow: 16
---

# دليل الوثائق

يمتلك هذا المجلد تأليف الوثائق، وقواعد روابط Mintlify، وسياسة تدويل الوثائق.

## قواعد Mintlify

- تُستضاف الوثائق على Mintlify (`https://docs.openclaw.ai`).
- يجب أن تبقى روابط الوثائق الداخلية في `docs/**/*.md` نسبية إلى الجذر من دون لاحقة `.md` أو `.mdx` (مثال: `[Config](/gateway/configuration)`).
- ينبغي أن تستخدم الإحالات المرجعية إلى الأقسام المراسي على المسارات النسبية إلى الجذر (مثال: `[Hooks](/gateway/configuration-reference#hooks)`).
- ينبغي أن تتجنب عناوين الوثائق الشرطات الطويلة والفواصل العليا لأن توليد المراسي في Mintlify هش هناك.
- ينبغي أن تُبقي ملفات README وغيرها من الوثائق المعروضة في GitHub عناوين URL المطلقة للوثائق حتى تعمل الروابط خارج Mintlify.
- يجب أن يبقى محتوى الوثائق عامًا: بلا أسماء أجهزة شخصية أو أسماء مضيفين أو مسارات محلية؛ استخدم عناصر نائبة مثل `user@gateway-host`.

## قواعد محتوى الوثائق

- بالنسبة إلى الوثائق ونصوص الواجهة وقوائم الاختيار، رتّب الخدمات/المزوّدين أبجديًا ما لم يكن القسم يصف صراحةً ترتيب وقت التشغيل أو ترتيب الاكتشاف التلقائي.
- حافظ على اتساق تسمية Plugin المضمّنة مع قواعد مصطلحات Plugin على مستوى المستودع في ملف `AGENTS.md` الجذري.

## الوثائق الداخلية

- تنتمي وثائق المشغّلين الخاصة طويلة الأمد إلى `~/Projects/manager/docs/`.
- يمكن أن توجد مسودات/مرايا الوثائق الداخلية المحلية للمستودع تحت `docs/internal/` المتجاهَل.
- لا تُضِف أبدًا صفحات `docs/internal/**` إلى تنقّل `docs/docs.json` ولا تربطها من الوثائق العامة.
- يستثني `scripts/docs-sync-publish.mjs` ويزيل `docs/internal/**` من مستودع النشر العام `openclaw/docs` إذا أُضيفت صفحة قسرًا لاحقًا.
- يجوز للوثائق الداخلية ذكر مسارات المستودع، وأسماء التطبيقات الخاصة، وأسماء عناصر 1Password، وأدلة التشغيل، لكنها لا تتضمن أبدًا قيمًا سرية.

## تدويل الوثائق

- لا تُصان وثائق اللغات الأجنبية في هذا المستودع. توجد مخرجات النشر المولّدة في مستودع `openclaw/docs` المنفصل (غالبًا ما يكون مستنسخًا محليًا باسم `../openclaw-docs`).
- لا تُضِف أو تعدّل وثائق مترجمة تحت `docs/<locale>/**` هنا.
- اعتبر الوثائق الإنجليزية في هذا المستودع مع ملفات المسرد مصدر الحقيقة.
- المسار: حدّث الوثائق الإنجليزية هنا، وحدّث `docs/.i18n/glossary.<locale>.json` حسب الحاجة، ثم اترك مزامنة مستودع النشر و`scripts/docs-i18n` يعملان في `openclaw/docs`.
- قبل إعادة تشغيل `scripts/docs-i18n`، أضف إدخالات مسرد لأي مصطلحات تقنية جديدة، أو عناوين صفحات، أو تسميات تنقّل قصيرة يجب أن تبقى بالإنجليزية أو تستخدم ترجمة ثابتة.
- `pnpm docs:check-i18n-glossary` هو الحارس لعناوين الوثائق الإنجليزية المتغيرة وتسميات الوثائق الداخلية القصيرة.
- توجد ذاكرة الترجمة في ملفات `docs/.i18n/*.tm.jsonl` المولّدة في مستودع النشر.
- راجع `docs/.i18n/README.md`.
