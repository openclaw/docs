---
read_when:
    - تريد تشغيل Gateway من متصفح
    - تريد الوصول إلى Tailnet دون أنفاق SSH
sidebarTitle: Control UI
summary: واجهة تحكم مستندة إلى المتصفح للـ Gateway (الدردشة، النشاط، العُقد، الإعدادات)
title: واجهة التحكم
x-i18n:
    generated_at: "2026-07-03T09:39:39Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: b23d0e2aeefc3b746f1ab51cd9049135e2695ab77cf5cbb5eab6ec0df90f011d
    source_path: web/control-ui.md
    workflow: 16
---

واجهة التحكم هي تطبيق صفحة واحدة صغير مبني بـ **Vite + Lit** ويقدمه Gateway:

- الافتراضي: `http://<host>:18789/`
- بادئة اختيارية: اضبط `gateway.controlUi.basePath` (مثل `/openclaw`)

تتصل **مباشرةً بـ Gateway WebSocket** على المنفذ نفسه.

## فتح سريع (محلي)

إذا كان Gateway يعمل على الكمبيوتر نفسه، فافتح:

- [http://127.0.0.1:18789/](http://127.0.0.1:18789/) (أو [http://localhost:18789/](http://localhost:18789/))

إذا فشل تحميل الصفحة، فابدأ Gateway أولاً: `openclaw gateway`.

<Note>
في ارتباطات LAN الأصلية على Windows، قد يظل Windows Firewall أو Group Policy المُدار من المؤسسة يحظر عنوان URL المعلن على LAN حتى عندما يعمل `127.0.0.1` على مضيف Gateway. شغّل `openclaw gateway status --deep` على مضيف Windows؛ فهو يبلغ عن المنافذ التي يُحتمل أنها محظورة، وعدم تطابق ملفات التعريف، وقواعد الجدار الناري المحلية التي قد تتجاهلها السياسة.
</Note>

تُقدَّم المصادقة أثناء مصافحة WebSocket عبر:

- `connect.params.auth.token`
- `connect.params.auth.password`
- رؤوس هوية Tailscale Serve عند ضبط `gateway.auth.allowTailscale: true`
- رؤوس هوية الوكيل الموثوق عند ضبط `gateway.auth.mode: "trusted-proxy"`

تحتفظ لوحة إعدادات لوحة المعلومات برمز مميز لجلسة تبويب المتصفح الحالية وعنوان URL المحدد لـ gateway؛ ولا يتم الاحتفاظ بكلمات المرور. عادةً ما تنشئ التهيئة الأولية رمز gateway للمصادقة بسر مشترك عند أول اتصال، لكن مصادقة كلمة المرور تعمل أيضاً عندما تكون `gateway.auth.mode` هي `"password"`.

## إقران الجهاز (الاتصال الأول)

عند الاتصال بواجهة التحكم من متصفح أو جهاز جديد، يتطلب Gateway عادةً **موافقة إقران لمرة واحدة**. هذا إجراء أمني لمنع الوصول غير المصرح به.

**ما ستراه:** "غير متصل (1008): يتطلب الإقران"

<Steps>
  <Step title="سرد الطلبات المعلقة">
    ```bash
    openclaw devices list
    ```
  </Step>
  <Step title="الموافقة حسب معرّف الطلب">
    ```bash
    openclaw devices approve <requestId>
    ```
  </Step>
</Steps>

إذا أعاد المتصفح محاولة الإقران بتفاصيل مصادقة متغيرة (الدور/النطاقات/المفتاح العام)، يُستبدل الطلب المعلق السابق ويُنشأ `requestId` جديد. أعد تشغيل `openclaw devices list` قبل الموافقة.

إذا كان المتصفح مقترناً بالفعل وغيّرته من وصول القراءة إلى وصول الكتابة/المسؤول، فيُعامل ذلك كترقية موافقة، وليس إعادة اتصال صامتة. يبقي OpenClaw الموافقة القديمة نشطة، ويحظر إعادة الاتصال الأوسع، ويطلب منك الموافقة صراحةً على مجموعة النطاقات الجديدة.

بعد الموافقة، يتم تذكر الجهاز ولن يتطلب إعادة موافقة إلا إذا ألغيتَه باستخدام `openclaw devices revoke --device <id> --role <role>`. راجع [CLI الأجهزة](/ar/cli/devices) لتدوير الرموز المميزة والإلغاء.

تستخدم وكلاء Paperclip الذين يتصلون عبر محول `openclaw_gateway` مسار موافقة التشغيل الأول نفسه. بعد محاولة الاتصال الأولية، شغّل `openclaw devices approve --latest` لمعاينة الطلب المعلق، ثم أعد تشغيل أمر `openclaw devices approve <requestId>` المطبوع للموافقة عليه. مرّر قيماً صريحة لـ `--url` و`--token` لـ gateway بعيد. للحفاظ على استقرار الموافقات عبر عمليات إعادة التشغيل، اضبط `adapterConfig.devicePrivateKeyPem` دائم في Paperclip بدلاً من تركه ينشئ هوية جهاز مؤقتة جديدة في كل تشغيل.

<Note>
- تتم الموافقة تلقائياً على اتصالات متصفح local loopback المباشرة (`127.0.0.1` / `localhost`).
- يمكن لـ Tailscale Serve تخطي رحلة الإقران ذهاباً وإياباً لجلسات مشغل واجهة التحكم عندما تكون `gateway.auth.allowTailscale: true`، وتتحقق هوية Tailscale، ويعرض المتصفح هوية جهازه.
- لا تزال ارتباطات Tailnet المباشرة، واتصالات متصفح LAN، وملفات تعريف المتصفح بلا هوية جهاز تتطلب موافقة صريحة.
- ينشئ كل ملف تعريف متصفح معرّف جهاز فريداً، لذا سيتطلب تبديل المتصفحات أو مسح بيانات المتصفح إعادة الإقران.

</Note>

## الهوية الشخصية (محلية للمتصفح)

تدعم واجهة التحكم هوية شخصية لكل متصفح (اسم العرض والصورة الرمزية) مرفقة بالرسائل الصادرة للإسناد في الجلسات المشتركة. تعيش في تخزين المتصفح، وتكون مقصورة على ملف تعريف المتصفح الحالي، ولا تتم مزامنتها مع أجهزة أخرى أو الاحتفاظ بها على جانب الخادم خارج بيانات تعريف تأليف السجل العادية على الرسائل التي ترسلها فعلاً. يؤدي مسح بيانات الموقع أو تبديل المتصفحات إلى إعادة ضبطها إلى فارغة.

ينطبق النمط المحلي للمتصفح نفسه على تجاوز صورة المساعد الرمزية. تُركّب صور المساعد الرمزية المرفوعة فوق الهوية التي يحلّها gateway في المتصفح المحلي فقط ولا تمر ذهاباً وإياباً عبر `config.patch`. لا يزال حقل الإعداد المشترك `ui.assistant.avatar` متاحاً للعملاء غير التابعين لواجهة المستخدم الذين يكتبون الحقل مباشرةً (مثل gateways البرمجية أو لوحات المعلومات المخصصة).

## نقطة نهاية إعدادات وقت التشغيل

تجلب واجهة التحكم إعدادات وقت التشغيل من `/control-ui-config.json`، محلولةً بالنسبة إلى مسار الأساس لواجهة التحكم في gateway (على سبيل المثال `/__openclaw__/control-ui-config.json` عندما تُقدَّم الواجهة تحت `/__openclaw__/`). تخضع نقطة النهاية هذه لمصادقة gateway نفسها مثل بقية سطح HTTP: لا يمكن للمتصفحات غير المصادق عليها جلبها، ويتطلب الجلب الناجح إما رمز/كلمة مرور gateway صالحة بالفعل، أو هوية Tailscale Serve، أو هوية وكيل موثوق.

## دعم اللغة

يمكن لواجهة التحكم ترجمة نفسها عند التحميل الأول بناءً على لغة متصفحك. لتجاوز ذلك لاحقاً، افتح **نظرة عامة -> وصول Gateway -> اللغة**. يوجد منتقي اللغة في بطاقة وصول Gateway، وليس ضمن المظهر.

- اللغات المدعومة: `en`, `zh-CN`, `zh-TW`, `pt-BR`, `de`, `es`, `ja-JP`, `ko`, `fr`, `ar`, `it`, `tr`, `uk`, `id`, `pl`, `th`, `vi`, `nl`, `fa`
- تُحمَّل الترجمات غير الإنجليزية تحميلًا كسولًا في المتصفح.
- تُحفظ اللغة المحددة في تخزين المتصفح وتُعاد استخدامها في الزيارات المستقبلية.
- تعود مفاتيح الترجمة المفقودة إلى الإنجليزية.

تُنشأ ترجمات المستندات لمجموعة اللغات غير الإنجليزية نفسها، لكن منتقي اللغة المدمج في موقع المستندات الخاص بـ Mintlify يقتصر على رموز اللغات التي يقبلها Mintlify. لا تزال مستندات التايلاندية (`th`) والفارسية (`fa`) تُنشأ في مستودع النشر؛ وقد لا تظهر في ذلك المنتقي حتى يدعم Mintlify تلك الرموز.

## سمات المظهر

تحتفظ لوحة المظهر بالسمات المضمنة Claw وKnot وDash، بالإضافة إلى خانة استيراد tweakcn محلية للمتصفح واحدة. لاستيراد سمة، افتح [محرر tweakcn](https://tweakcn.com/editor/theme)، واختر سمة أو أنشئ واحدة، وانقر **مشاركة**، والصق رابط السمة المنسوخ في المظهر. يقبل المستورد أيضاً عناوين URL لسجل `https://tweakcn.com/r/themes/<id>`، وعناوين URL للمحرر مثل `https://tweakcn.com/editor/theme?theme=amethyst-haze`، ومسارات `/themes/<id>` النسبية، ومعرفات السمات الخام، وأسماء السمات الافتراضية مثل `amethyst-haze`.

يتضمن المظهر أيضاً إعداداً محلياً للمتصفح لحجم النص. يُخزَّن الإعداد مع بقية تفضيلات واجهة التحكم، وينطبق على نص الدردشة، ونص المؤلف، وبطاقات الأدوات، والأشرطة الجانبية للدردشة، ويحافظ على إدخالات النص بحجم لا يقل عن 16px حتى لا يقوم Safari على الهاتف بالتكبير تلقائياً عند التركيز.

تُخزَّن السمات المستوردة فقط في ملف تعريف المتصفح الحالي. لا تُكتب في إعدادات gateway ولا تتم مزامنتها عبر الأجهزة. يؤدي استبدال السمة المستوردة إلى تحديث الخانة المحلية الواحدة؛ ويعيد مسحها السمة النشطة إلى Claw إذا كانت السمة المستوردة محددة.

## ما الذي يمكنها فعله (اليوم)

<AccordionGroup>
  <Accordion title="الدردشة والتحدث">
    - الدردشة مع النموذج عبر Gateway WS (`chat.history`, `chat.send`, `chat.abort`, `chat.inject`).
    - تطلب تحديثات سجل الدردشة نافذة حديثة محدودة مع حدود نصية لكل رسالة حتى لا تُجبر الجلسات الكبيرة المتصفح على عرض حمولة سجل كاملة قبل أن تصبح الدردشة قابلة للاستخدام.
    - التحدث عبر جلسات المتصفح في الوقت الفعلي. يستخدم OpenAI WebRTC مباشرةً، ويستخدم Google Live رمز متصفح مقيداً لمرة واحدة عبر WebSocket، وتستخدم Plugins الصوت في الوقت الفعلي الخلفية فقط نقل ترحيل Gateway. تبدأ جلسات المزوّد المملوكة للعميل بـ `talk.client.create`؛ وتبدأ جلسات ترحيل Gateway بـ `talk.session.create`. يحتفظ الترحيل ببيانات اعتماد المزوّد على Gateway بينما يبث المتصفح PCM الميكروفون عبر `talk.session.appendAudio`، ويمرر استدعاءات أدوات المزوّد `openclaw_agent_consult` عبر `talk.client.toolCall` لسياسة Gateway ونموذج OpenClaw الأكبر المضبوط، ويوجّه قيادة الصوت للتشغيل النشط عبر `talk.client.steer` أو `talk.session.steer`.
    - بث استدعاءات الأدوات + بطاقات إخراج الأدوات الحية في الدردشة (أحداث الوكيل).
    - تبويب النشاط مع ملخصات محلية للمتصفح وتبدأ بالتنقيح لنشاط الأدوات الحي من تسليم أحداث `session.tool` / الأداة الحالية.

  </Accordion>
  <Accordion title="القنوات، والمثيلات، والجلسات، والأحلام">
    - القنوات: حالة قنوات Plugins المضمنة والمجمعة/الخارجية، وتسجيل الدخول عبر QR، وإعدادات لكل قناة (`channels.status`, `web.login.*`, `config.patch`).
    - تحافظ تحديثات فحص القنوات على اللقطة السابقة مرئية أثناء انتهاء فحوصات المزوّد البطيئة، وتُوسم اللقطات الجزئية عندما يتجاوز فحص أو تدقيق ميزانية واجهة المستخدم الخاصة به.
    - المثيلات: قائمة الحضور + التحديث (`system-presence`).
    - الجلسات: سرد جلسات الوكلاء المضبوطة افتراضياً، والرجوع من مفاتيح جلسات الوكلاء غير المضبوطة القديمة، وتطبيق تجاوزات النموذج/التفكير/السريع/المطول/التتبع/الاستدلال لكل جلسة (`sessions.list`, `sessions.patch`).
    - الأحلام: حالة Dreaming، ومفتاح التفعيل/التعطيل، وقارئ يوميات الأحلام (`doctor.memory.status`, `doctor.memory.dreamDiary`, `config.patch`).

  </Accordion>
  <Accordion title="Cron، وSkills، والعُقد، وموافقات التنفيذ">
    - مهام Cron: سرد/إضافة/تحرير/تشغيل/تفعيل/تعطيل + سجل التشغيل (`cron.*`).
    - Skills: الحالة، التفعيل/التعطيل، التثبيت، تحديثات مفتاح API (`skills.*`).
    - العُقد: سرد + الإمكانات (`node.list`).
    - موافقات التنفيذ: تحرير قوائم السماح لـ gateway أو العقدة + سياسة السؤال لـ `exec host=gateway/node` (`exec.approvals.*`).

  </Accordion>
  <Accordion title="الإعدادات">
    - عرض/تحرير `~/.openclaw/openclaw.json` (`config.get`, `config.set`).
    - لدى MCP صفحة إعدادات مخصصة للخوادم المضبوطة، والتمكين، وملخصات OAuth/التصفية/التوازي، وأوامر المشغل الشائعة، ومحرر إعداد `mcp` المقصور.
    - تطبيق + إعادة التشغيل مع التحقق (`config.apply`) وإيقاظ آخر جلسة نشطة.
    - تتضمن عمليات الكتابة حارس تجزئة أساس لمنع الكتابة فوق تعديلات متزامنة.
    - تجري عمليات الكتابة (`config.set`/`config.apply`/`config.patch`) فحصاً مسبقاً لحل SecretRef النشط للمراجع في حمولة الإعداد المرسلة؛ تُرفض المراجع المرسلة النشطة غير المحلولة قبل الكتابة.
    - تتخلص عمليات حفظ النموذج من العناصر النائبة المنقحة القديمة التي لا يمكن استعادتها من الإعداد المحفوظ مع الحفاظ على القيم المنقحة التي لا تزال ترتبط بالأسرار المحفوظة.
    - عرض المخطط + النموذج (`config.schema` / `config.schema.lookup`، بما في ذلك `title` / `description` للحقل، وتلميحات واجهة المستخدم المطابقة، وملخصات الأبناء المباشرة، وبيانات تعريف المستندات على عُقد الكائنات المتداخلة/أحرف البدل/المصفوفات/التركيب، بالإضافة إلى مخططات Plugin + القنوات عند توفرها)؛ يتوفر محرر JSON الخام فقط عندما تحتوي اللقطة على رحلة ذهاب وإياب خام آمنة.
    - إذا تعذرت على لقطة إجراء رحلة ذهاب وإياب للنص الخام بأمان، تفرض واجهة التحكم وضع النموذج وتعطل الوضع الخام لتلك اللقطة.
    - يحافظ محرر JSON الخام "إعادة الضبط إلى المحفوظ" على الشكل المؤلف خاماً (التنسيق، والتعليقات، وتخطيط `$include`) بدلاً من إعادة عرض لقطة مسطحة، حتى تنجو التعديلات الخارجية من إعادة الضبط عندما تستطيع اللقطة إجراء رحلة ذهاب وإياب بأمان.
    - تُعرض قيم كائن SecretRef المهيكلة للقراءة فقط في إدخالات نص النموذج لمنع تلف عرضي من كائن إلى سلسلة.

  </Accordion>
  <Accordion title="التصحيح، والسجلات، والتحديث">
    - التصحيح: لقطات الحالة/الصحة/النماذج + سجل الأحداث + استدعاءات RPC يدوية (`status`, `health`, `models.list`).
    - يتضمن سجل الأحداث توقيتات تحديث/RPC لواجهة التحكم، وتوقيتات عرض الدردشة/الإعدادات البطيئة، وإدخالات استجابة المتصفح لإطارات الحركة الطويلة أو المهام الطويلة عندما يكشف المتصفح أنواع إدخالات PerformanceObserver تلك.
    - السجلات: متابعة حية لسجلات ملفات gateway مع التصفية/التصدير (`logs.tail`).
    - التحديث: تشغيل تحديث حزمة/git + إعادة تشغيل (`update.run`) مع تقرير إعادة تشغيل، ثم استطلاع `update.status` بعد إعادة الاتصال للتحقق من إصدار gateway قيد التشغيل.

  </Accordion>
  <Accordion title="ملاحظات لوحة مهام Cron">
    - بالنسبة للمهام المعزولة، يكون التسليم الافتراضي هو إعلان الملخص. يمكنك التبديل إلى لا شيء إذا أردت تشغيلات داخلية فقط.
    - تظهر حقول القناة/الهدف عند تحديد الإعلان.
    - يستخدم وضع Webhook القيمة `delivery.mode = "webhook"` مع ضبط `delivery.to` على عنوان URL صالح لـ HTTP(S) webhook.
    - بالنسبة لمهام الجلسة الرئيسية، تتوفر أوضاع تسليم webhook ولا شيء.
    - تتضمن عناصر التحكم المتقدمة في التحرير الحذف بعد التشغيل، ومسح تجاوز الوكيل، وخيارات cron الدقيقة/المتدرجة، وتجاوزات نموذج/تفكير الوكيل، ومفاتيح التسليم بأفضل جهد.
    - يكون التحقق من صحة النموذج مضمنًا مع أخطاء على مستوى الحقول؛ وتؤدي القيم غير الصالحة إلى تعطيل زر الحفظ حتى تُصلح.
    - اضبط `cron.webhookToken` لإرسال رمز bearer token مخصص، وإذا أُهمل فسيُرسل webhook من دون ترويسة مصادقة.
    - الرجوع المهمل: شغّل `openclaw doctor --fix` لترحيل المهام القديمة المخزنة التي تحتوي على `notify: true` من `cron.webhook` إلى webhook صريح لكل مهمة أو تسليم عند الاكتمال.

  </Accordion>
</AccordionGroup>

## صفحة MCP

صفحة MCP المخصصة هي عرض للمشغّل لخوادم MCP التي يديرها OpenClaw ضمن `mcp.servers`. لا تبدأ عمليات نقل MCP بنفسها؛ استخدمها لفحص التكوين المحفوظ وتحريره، ثم استخدم `openclaw mcp doctor --probe` عندما تحتاج إلى إثبات مباشر للخادم.

سير العمل المعتاد:

1. افتح **MCP** من الشريط الجانبي.
2. تحقق من بطاقات الملخص لمعرفة العدد الإجمالي، والمفعّل، وOAuth، وعدد الخوادم المفلترة.
3. راجع كل صف خادم من حيث النقل، والتمكين، والمصادقة، والمرشحات، والمهلات، وتلميحات الأوامر.
4. بدّل التمكين عندما يجب أن يبقى الخادم مكوّنًا لكن خارج اكتشاف وقت التشغيل.
5. حرّر قسم تكوين `mcp` محدود النطاق لتعريفات الخوادم، والترويسات، ومسارات TLS/mTLS، وبيانات OAuth الوصفية، ومرشحات الأدوات، وبيانات إسقاط Codex الوصفية.
6. استخدم **حفظ** لكتابة التكوين، أو **حفظ ونشر** عندما يجب أن يطبق Gateway قيد التشغيل التكوين المعدّل.
7. شغّل `openclaw mcp status --verbose` أو `openclaw mcp doctor --probe` أو `openclaw mcp reload` من طرفية عندما تحتاج العملية المحررة إلى تشخيصات ثابتة، أو إثبات مباشر، أو التخلص من وقت التشغيل المخزّن مؤقتًا.

تحجب الصفحة القيم الشبيهة بعناوين URL التي تحمل بيانات اعتماد قبل العرض وتضع أسماء الخوادم بين علامات اقتباس في مقتطفات الأوامر حتى تظل الأوامر المنسوخة تعمل مع المسافات أو محارف shell الخاصة. يوجد مرجع CLI والتكوين الكامل في [MCP](/ar/cli/mcp).

## تبويب النشاط

تبويب النشاط هو مراقب مؤقت محلي في المتصفح لنشاط الأدوات المباشر. وهو مشتق من تدفق أحداث Gateway نفسه `session.tool` / tool الذي يزوّد بطاقات أدوات Chat؛ ولا يضيف عائلة أحداث Gateway أخرى، أو نقطة نهاية، أو مخزن نشاط دائم، أو تدفق مقاييس، أو تدفق مراقب خارجي.

تحتفظ إدخالات النشاط فقط بملخصات منقّاة ومعاينات إخراج محجوبة ومقتطعة. لا تُخزّن قيم وسيطات الأدوات في حالة النشاط؛ وتعرض واجهة المستخدم أن الوسيطات مخفية وتسجل فقط عدد حقول الوسيطات. تتبع القائمة داخل الذاكرة تبويب المتصفح الحالي، وتبقى أثناء التنقل داخل Control UI، وتُعاد تهيئتها عند إعادة تحميل الصفحة، أو تبديل الجلسة، أو الضغط على **مسح**.

## سلوك Chat

<AccordionGroup>
  <Accordion title="دلالات الإرسال والسجل">
    - `chat.send` هو **غير حاجب**: يقرّ فورًا بـ `{ runId, status: "started" }` ويتدفق الرد عبر أحداث `chat`. قد تتلقى عملاء Control UI الموثوقون أيضًا بيانات وصفية اختيارية لتوقيت ACK للتشخيصات المحلية.
    - تقبل تحميلات Chat الصور بالإضافة إلى الملفات غير المرئية. تحتفظ الصور بمسار الصورة الأصلي؛ وتُخزّن الملفات الأخرى كوسائط مُدارة وتُعرض في السجل كروابط مرفقات.
    - تؤدي إعادة الإرسال باستخدام `idempotencyKey` نفسه إلى إرجاع `{ status: "in_flight" }` أثناء التشغيل، و`{ status: "ok" }` بعد الاكتمال.
    - تكون استجابات `chat.history` محدودة الحجم من أجل سلامة واجهة المستخدم. عندما تكون إدخالات النص كبيرة جدًا، قد يقتطع Gateway حقول النص الطويلة، ويحذف كتل البيانات الوصفية الثقيلة، ويستبدل الرسائل كبيرة الحجم بعنصر نائب (`[chat.history omitted: message too large]`).
    - عندما تُقتطع رسالة مساعد مرئية في `chat.history`، يمكن للقارئ الجانبي جلب إدخال النص الكامل المطبّع للعرض عند الطلب عبر `chat.message.get` باستخدام `sessionKey` و`agentId` النشط عند الحاجة و`messageId` للنص. إذا كان Gateway لا يزال غير قادر على إرجاع المزيد، يعرض القارئ حالة عدم توفر صريحة بدلًا من تكرار المعاينة المقتطعة بصمت.
    - تُحفظ الصور التي أنشأها المساعد/النظام كمراجع وسائط مُدارة وتُقدم مرة أخرى عبر عناوين URL لوسائط Gateway مصادق عليها، لذلك لا تعتمد عمليات إعادة التحميل على بقاء حمولات الصور الخام بصيغة base64 في استجابة سجل Chat.
    - عند عرض `chat.history`، يزيل Control UI وسوم التوجيه المضمنة المخصصة للعرض فقط من نص المساعد المرئي (مثل `[[reply_to_*]]` و`[[audio_as_voice]]`)، وحمولات XML لاستدعاءات الأدوات ذات النص العادي (بما في ذلك `<tool_call>...</tool_call>` و`<function_call>...</function_call>` و`<tool_calls>...</tool_calls>` و`<function_calls>...</function_calls>` وكتل استدعاءات الأدوات المقتطعة)، ورموز تحكم النموذج المسرّبة ASCII/كاملة العرض، ويحذف إدخالات المساعد التي يكون نصها المرئي بالكامل هو فقط الرمز الصامت الدقيق `NO_REPLY` / `no_reply` أو رمز إقرار Heartbeat `HEARTBEAT_OK`.
    - أثناء إرسال نشط وتحديث السجل النهائي، يُبقي عرض المحادثة رسائل المستخدم/المساعد التفاؤلية المحلية مرئية إذا أعاد `chat.history` لفترة وجيزة لقطة أقدم؛ ويستبدل النص القانوني تلك الرسائل المحلية بمجرد أن يلحق سجل Gateway.
    - أحداث `chat` المباشرة هي حالة التسليم، بينما يُعاد بناء `chat.history` من نص الجلسة الدائم. بعد أحداث انتهاء الأداة، يعيد Control UI تحميل السجل ويدمج ذيلًا تفاؤليًا صغيرًا فقط؛ وحد النص موثق في [WebChat](/ar/web/webchat).
    - يضيف `chat.inject` ملاحظة مساعد إلى نص الجلسة ويبث حدث `chat` لتحديثات واجهة المستخدم فقط (من دون تشغيل وكيل، ومن دون تسليم قناة).
    - يسرد الشريط الجانبي الجلسات الحديثة مع إجراء جلسة جديدة، ورابط كل الجلسات، وزر بحث عن جلسة يفتح منتقي الجلسة الكامل (مقيّدًا بالوكيل المحدد، مع البحث والترقيم الصفحي). يعرض تبديل الوكلاء الجلسات المرتبطة بذلك الوكيل فقط، ويرجع إلى الجلسة الرئيسية لذلك الوكيل عندما لا تكون لديه جلسات لوحة محفوظة بعد.
    - في عروض سطح المكتب، تبقى عناصر تحكم Chat في صف مضغوط واحد وتنطوي أثناء التمرير إلى أسفل النص؛ ويؤدي التمرير إلى أعلى، أو الرجوع إلى القمة، أو بلوغ الأسفل إلى استعادة عناصر التحكم.
    - تُعرض الرسائل النصية المتكررة المتتالية كفقاعة واحدة مع شارة عدد. وتُترك الرسائل التي تحمل صورًا، أو مرفقات، أو إخراج أدوات، أو معاينات canvas دون طي.
    - يرقّع منتقيا نموذج Chat والتفكير في الترويسة الجلسة النشطة فورًا عبر `sessions.patch`؛ وهما تجاوزان دائمان للجلسة، وليسا خيارات إرسال لدورة واحدة فقط.
    - إذا أرسلت رسالة بينما لا يزال تغيير منتقي النموذج للجلسة نفسها قيد الحفظ، ينتظر محرر الرسائل رقعة تلك الجلسة قبل استدعاء `chat.send` حتى يستخدم الإرسال النموذج المحدد.
    - تؤدي كتابة `/new` في Control UI إلى إنشاء جلسة لوحة جديدة مماثلة لـ New Chat والتبديل إليها، إلا عندما يكون `session.dmScope: "main"` مكوّنًا ويكون الأصل الحالي هو الجلسة الرئيسية للوكيل؛ في تلك الحالة تعيد ضبط الجلسة الرئيسية في مكانها. وتحافظ كتابة `/reset` على إعادة الضبط الصريحة في المكان نفسه لدى Gateway للجلسة الحالية.
    - يطلب منتقي نموذج Chat عرض النموذج المكوّن في Gateway. إذا كان `agents.defaults.models` موجودًا، فإن قائمة السماح تلك تقود المنتقي، بما في ذلك إدخالات `provider/*` التي تبقي الفهارس محدودة المزوّد ديناميكية. وإلا يعرض المنتقي إدخالات `models.providers.*.models` الصريحة بالإضافة إلى المزوّدين ذوي المصادقة القابلة للاستخدام. يظل الفهرس الكامل متاحًا عبر RPC التصحيحية `models.list` مع `view: "all"`.
    - عندما تتضمن تقارير استخدام جلسة Gateway الجديدة رموز السياق الحالية، يعرض شريط أدوات محرر Chat حلقة صغيرة لاستخدام السياق مع النسبة المستخدمة؛ وتوجد تفاصيل الرموز الكاملة في تلميحها. تتحول الحلقة إلى نمط تحذيري عند ضغط سياق مرتفع، وعند مستويات Compaction الموصى بها، تعرض زرًا مضغوطًا يشغّل مسار Compaction العادي للجلسة. تُخفى لقطات الرموز القديمة حتى يبلغ Gateway عن استخدام جديد مرة أخرى.

  </Accordion>
  <Accordion title="وضع التحدث (الوقت الحقيقي في المتصفح)">
    يستخدم وضع التحدث مزوّد صوت فوري مسجلًا. كوّن OpenAI باستخدام `talk.realtime.provider: "openai"` بالإضافة إلى ملف تعريف مصادقة بمفتاح API من `openai`، أو `talk.realtime.providers.openai.apiKey`، أو `OPENAI_API_KEY`؛ لا تُكوّن ملفات تعريف OAuth لـ OpenAI الصوت الفوري. كوّن Google باستخدام `talk.realtime.provider: "google"` بالإضافة إلى `talk.realtime.providers.google.apiKey`. لا يتلقى المتصفح أبدًا مفتاح API قياسيًا للمزوّد. يتلقى OpenAI سر عميل Realtime مؤقتًا لـ WebRTC. ويتلقى Google Live رمز مصادقة Live API مقيدًا لاستخدام واحد لجلسة WebSocket في المتصفح، مع تثبيت التعليمات وتصريحات الأدوات داخل الرمز بواسطة Gateway. تعمل المزوّدات التي تكشف فقط جسرًا فوريًا في الخلفية عبر نقل ترحيل Gateway، بحيث تبقى بيانات الاعتماد ومآخذ المورّد في جانب الخادم بينما ينتقل صوت المتصفح عبر RPCs Gateway مصادق عليها. يجمع Gateway مطالبة جلسة Realtime؛ ولا يقبل `talk.client.create` تجاوزات تعليمات يقدّمها المستدعي.

    يتضمن محرر Chat زر خيارات التحدث بجانب زر بدء/إيقاف التحدث. تنطبق الخيارات على جلسة التحدث التالية ويمكنها تجاوز المزوّد، والنقل، والنموذج، والصوت، وجهد التفكير، وعتبة VAD، ومدة الصمت، وحشو البادئة. عندما يكون الخيار فارغًا، يستخدم Gateway الإعدادات الافتراضية المكوّنة حيثما توفرت أو القيمة الافتراضية للمزوّد. يفرض تحديد ترحيل Gateway مسار الترحيل الخلفي؛ ويحافظ تحديد WebRTC على أن تكون الجلسة مملوكة للعميل ويفشل بدلًا من الرجوع بصمت إلى الترحيل إذا لم يتمكن المزوّد من إنشاء جلسة متصفح.

    في محرر Chat، عنصر تحكم التحدث هو زر الموجات بجانب زر إملاء الميكروفون. عندما يبدأ التحدث، يعرض صف حالة المحرر `Connecting Talk...`، ثم `Talk live` أثناء اتصال الصوت، أو `Asking OpenClaw...` بينما يستشير استدعاء أداة فوري النموذج الأكبر المكوّن عبر `talk.client.toolCall`.

    فحص مباشر للمشرفين: يتحقق `OPENAI_API_KEY=... GEMINI_API_KEY=... node --import tsx scripts/dev/realtime-talk-live-smoke.ts` من جسر WebSocket الخلفي لدى OpenAI، وتبادل WebRTC SDP في متصفح OpenAI، وإعداد WebSocket في متصفح Google Live برمز مقيّد، ومحوّل متصفح ترحيل Gateway مع وسائط ميكروفون وهمية. يطبع الأمر حالة المزوّد فقط ولا يسجل الأسرار.

  </Accordion>
  <Accordion title="الإيقاف والإجهاض">
    - انقر **إيقاف** (يستدعي `chat.abort`).
    - أثناء نشاط تشغيل، تُصفّ المتابعات العادية في قائمة انتظار. انقر **توجيه** على رسالة في قائمة الانتظار لحقن تلك المتابعة في الدور الجاري.
    - اكتب `/stop` (أو عبارات إجهاض مستقلة مثل `stop`، `stop action`، `stop run`، `stop openclaw`، `please stop`) للإجهاض خارج النطاق.
    - يدعم `chat.abort` البنية `{ sessionKey }` (من دون `runId`) لإجهاض كل التشغيلات النشطة لتلك الجلسة.

  </Accordion>
  <Accordion title="الاحتفاظ الجزئي عند الإجهاض">
    - عندما يُجهض تشغيل، يمكن أن يظل نص المساعد الجزئي معروضًا في واجهة المستخدم.
    - يثبت Gateway نص المساعد الجزئي المُجهض في سجل النص عندما يكون الإخراج المخزن مؤقتًا موجودًا.
    - تتضمن الإدخالات المثبتة بيانات وصفية للإجهاض حتى يتمكن مستهلكو النص من تمييز الأجزاء الناتجة عن الإجهاض من إخراج الاكتمال العادي.

  </Accordion>
</AccordionGroup>

## تثبيت PWA والدفع عبر الويب

تأتي Control UI مع `manifest.webmanifest` وعامل خدمة، لذلك يمكن للمتصفحات الحديثة تثبيتها كتطبيق PWA مستقل. يتيح Web Push لـ Gateway إيقاظ تطبيق PWA المثبت بالإشعارات حتى عندما لا يكون التبويب أو نافذة المتصفح مفتوحًا.

إذا عرضت الصفحة **عدم تطابق البروتوكول** مباشرة بعد تحديث OpenClaw، فأعد أولًا فتح لوحة التحكم باستخدام `openclaw dashboard` ثم حدّث الصفحة تحديثًا قسريًا. إذا استمر الفشل، فامسح بيانات الموقع لأصل لوحة التحكم أو اختبر في نافذة متصفح خاصة؛ فقد يستمر تبويب قديم أو ذاكرة عامل الخدمة في المتصفح بتشغيل حزمة واجهة التحكم السابقة للتحديث مقابل Gateway الأحدث.

| السطح                                                | ما يفعله                                                           |
| ---------------------------------------------------- | ------------------------------------------------------------------ |
| `ui/public/manifest.webmanifest`                     | بيان PWA. تعرض المتصفحات خيار "تثبيت التطبيق" بمجرد أن يصبح قابلًا للوصول. |
| `ui/public/sw.js`                                    | عامل خدمة يتعامل مع أحداث `push` ونقرات الإشعارات.                 |
| `push/vapid-keys.json` (ضمن مجلد حالة OpenClaw)      | زوج مفاتيح VAPID مُنشأ تلقائيًا يُستخدم لتوقيع حمولات Web Push.    |
| `push/web-push-subscriptions.json`                   | نقاط نهاية اشتراكات المتصفح المحفوظة.                              |

تجاوز زوج مفاتيح VAPID عبر متغيرات البيئة في عملية Gateway عندما تريد تثبيت المفاتيح (للنشر متعدد المضيفين، أو تدوير الأسرار، أو الاختبارات):

- `OPENCLAW_VAPID_PUBLIC_KEY`
- `OPENCLAW_VAPID_PRIVATE_KEY`
- `OPENCLAW_VAPID_SUBJECT` (القيمة الافتراضية `https://openclaw.ai`)

تستخدم واجهة التحكم طرق Gateway المقيدة بالنطاق هذه لتسجيل اشتراكات المتصفح واختبارها:

- `push.web.vapidPublicKey` — يجلب مفتاح VAPID العام النشط.
- `push.web.subscribe` — يسجل `endpoint` بالإضافة إلى `keys.p256dh`/`keys.auth`.
- `push.web.unsubscribe` — يزيل نقطة نهاية مسجلة.
- `push.web.test` — يرسل إشعار اختبار إلى اشتراك المستدعي.

<Note>
Web Push مستقل عن مسار ترحيل APNS في iOS (راجع [التكوين](/ar/gateway/configuration) للدفع المدعوم بالترحيل) وعن طريقة `push.test` الحالية، التي تستهدف اقتران الأجهزة المحمولة الأصلية.
</Note>

## التضمينات المستضافة

يمكن لرسائل المساعد عرض محتوى ويب مستضافًا ضمنيًا باستخدام الرمز المختصر `[embed ...]`. تتحكم `gateway.controlUi.embedSandbox` في سياسة صندوق عزل iframe:

<Tabs>
  <Tab title="strict">
    يعطل تنفيذ السكربتات داخل التضمينات المستضافة.
  </Tab>
  <Tab title="scripts (default)">
    يسمح بالتضمينات التفاعلية مع إبقاء عزل الأصل؛ هذا هو الإعداد الافتراضي ويكفي عادةً لألعاب/ودجات المتصفح المستقلة.
  </Tab>
  <Tab title="trusted">
    يضيف `allow-same-origin` فوق `allow-scripts` للمستندات من الموقع نفسه التي تحتاج عمدًا إلى صلاحيات أقوى.
  </Tab>
</Tabs>

مثال:

```json5
{
  gateway: {
    controlUi: {
      embedSandbox: "scripts",
    },
  },
}
```

<Warning>
استخدم `trusted` فقط عندما يحتاج المستند المضمن فعلًا إلى سلوك الأصل نفسه. بالنسبة إلى معظم الألعاب واللوحات التفاعلية التي ينشئها الوكيل، يكون `scripts` هو الخيار الأكثر أمانًا.
</Warning>

تبقى عناوين URL الخارجية المطلقة من نوع `http(s)` محظورة افتراضيًا. إذا كنت تريد عمدًا أن يحمّل `[embed url="https://..."]` صفحات تابعة لجهات خارجية، فعيّن `gateway.controlUi.allowExternalEmbedUrls: true`.

## عرض رسائل الدردشة

تستخدم رسائل الدردشة المجمعة حدًا أقصى افتراضيًا للعرض قابلًا للقراءة. يمكن لعمليات النشر على الشاشات العريضة تجاوزه دون تعديل CSS المضمنة عبر ضبط `gateway.controlUi.chatMessageMaxWidth`:

```json5
{
  gateway: {
    controlUi: {
      chatMessageMaxWidth: "min(1280px, 82%)",
    },
  },
}
```

تُتحقق القيمة قبل أن تصل إلى المتصفح. تشمل القيم المدعومة الأطوال والنسب المئوية العادية مثل `960px` أو `82%`، بالإضافة إلى تعبيرات العرض المقيدة `min(...)` و`max(...)` و`clamp(...)` و`calc(...)` و`fit-content(...)`.

## الوصول عبر Tailnet (موصى به)

<Tabs>
  <Tab title="Integrated Tailscale Serve (preferred)">
    أبقِ Gateway على loopback ودع Tailscale Serve يمرره عبر HTTPS:

    ```bash
    openclaw gateway --tailscale serve
    ```

    افتح:

    - `https://<magicdns>/` (أو `gateway.controlUi.basePath` الذي ضبطته)

    افتراضيًا، يمكن لطلبات Serve الخاصة بواجهة التحكم/WebSocket المصادقة عبر ترويسات هوية Tailscale (`tailscale-user-login`) عندما تكون `gateway.auth.allowTailscale` هي `true`. يتحقق OpenClaw من الهوية عبر حل عنوان `x-forwarded-for` باستخدام `tailscale whois` ومطابقته مع الترويسة، ولا يقبل هذه الترويسات إلا عندما يصل الطلب إلى loopback مع ترويسات `x-forwarded-*` الخاصة بـ Tailscale. بالنسبة إلى جلسات مشغل واجهة التحكم ذات هوية جهاز المتصفح، يتجاوز مسار Serve المتحقق منه هذا أيضًا رحلة اقتران الجهاز؛ أما المتصفحات بلا جهاز واتصالات دور العقدة فما زالت تتبع فحوصات الجهاز العادية. عيّن `gateway.auth.allowTailscale: false` إذا كنت تريد طلب بيانات اعتماد سر مشترك صريحة حتى لحركة Serve. ثم استخدم `gateway.auth.mode: "token"` أو `"password"`.

    بالنسبة إلى مسار هوية Serve غير المتزامن هذا، تُسلسل محاولات المصادقة الفاشلة لعنوان IP العميل نفسه ونطاق المصادقة نفسه قبل كتابات تحديد المعدل. لذلك قد تُظهر إعادة المحاولة السيئة المتزامنة من المتصفح نفسه `retry later` في الطلب الثاني بدل عدم تطابقين عاديين يتسابقان بالتوازي.

    <Warning>
    تفترض مصادقة Serve بلا رمز أن مضيف Gateway موثوق. إذا كان من الممكن تشغيل كود محلي غير موثوق على ذلك المضيف، فاطلب مصادقة الرمز/كلمة المرور.
    </Warning>

  </Tab>
  <Tab title="Bind to tailnet + token">
    ```bash
    openclaw gateway --bind tailnet --token "$(openssl rand -hex 32)"
    ```

    ثم افتح:

    - `http://<tailscale-ip>:18789/` (أو `gateway.controlUi.basePath` الذي ضبطته)

    الصق السر المشترك المطابق في إعدادات الواجهة (يُرسل كـ `connect.params.auth.token` أو `connect.params.auth.password`).

  </Tab>
</Tabs>

## HTTP غير آمن

إذا فتحت لوحة التحكم عبر HTTP عادي (`http://<lan-ip>` أو `http://<tailscale-ip>`)، يعمل المتصفح في **سياق غير آمن** ويحظر WebCrypto. افتراضيًا، **يحظر** OpenClaw اتصالات واجهة التحكم بلا هوية جهاز.

الاستثناءات الموثقة:

- توافق HTTP غير الآمن للمضيف المحلي فقط مع `gateway.controlUi.allowInsecureAuth=true`
- نجاح مصادقة مشغل واجهة التحكم عبر `gateway.auth.mode: "trusted-proxy"`
- خيار الطوارئ `gateway.controlUi.dangerouslyDisableDeviceAuth=true`

**الإصلاح الموصى به:** استخدم HTTPS (Tailscale Serve) أو افتح الواجهة محليًا:

- `https://<magicdns>/` (Serve)
- `http://127.0.0.1:18789/` (على مضيف Gateway)

<AccordionGroup>
  <Accordion title="Insecure-auth toggle behavior">
    ```json5
    {
      gateway: {
        controlUi: { allowInsecureAuth: true },
        bind: "tailnet",
        auth: { mode: "token", token: "replace-me" },
      },
    }
    ```

    `allowInsecureAuth` هو مفتاح توافق محلي فقط:

    - يسمح لجلسات واجهة التحكم على localhost بالمتابعة بلا هوية جهاز في سياقات HTTP غير الآمنة.
    - لا يتجاوز فحوصات الاقتران.
    - لا يخفف متطلبات هوية الجهاز البعيد (غير localhost).

  </Accordion>
  <Accordion title="Break-glass only">
    ```json5
    {
      gateway: {
        controlUi: { dangerouslyDisableDeviceAuth: true },
        bind: "tailnet",
        auth: { mode: "token", token: "replace-me" },
      },
    }
    ```

    <Warning>
    يعطل `dangerouslyDisableDeviceAuth` فحوصات هوية جهاز واجهة التحكم وهو خفض أمني شديد. أعده بسرعة بعد الاستخدام الطارئ.
    </Warning>

  </Accordion>
  <Accordion title="Trusted-proxy note">
    - يمكن لمصادقة trusted-proxy الناجحة أن تسمح بجلسات واجهة التحكم الخاصة بـ **المشغل** بلا هوية جهاز.
    - لا يمتد هذا إلى جلسات واجهة التحكم ذات دور العقدة.
    - ما تزال وكلاء reverse proxy عبر loopback على المضيف نفسه لا تفي بمصادقة trusted-proxy؛ راجع [مصادقة الوكيل الموثوق](/ar/gateway/trusted-proxy-auth).

  </Accordion>
</AccordionGroup>

راجع [Tailscale](/ar/gateway/tailscale) للحصول على إرشادات إعداد HTTPS.

## سياسة أمان المحتوى

تأتي واجهة التحكم بسياسة `img-src` صارمة: يُسمح فقط بالأصول من **الأصل نفسه**، وعناوين URL من نوع `data:`، وعناوين URL من نوع `blob:` المُنشأة محليًا. يرفض المتصفح عناوين صور `http(s)` البعيدة والعناوين النسبية للبروتوكول ولا يُصدر طلبات شبكة لها.

ما يعنيه هذا عمليًا:

- ما تزال الصور الرمزية والصور المقدمة ضمن مسارات نسبية (مثل `/avatars/<id>`) تُعرض، بما في ذلك مسارات الصور الرمزية المصادق عليها التي تجلبها الواجهة وتحولها إلى عناوين URL محلية من نوع `blob:`.
- ما تزال عناوين URL المضمنة `data:image/...` تُعرض (مفيدة لحمولات داخل البروتوكول).
- ما تزال عناوين URL المحلية من نوع `blob:` التي تنشئها واجهة التحكم تُعرض.
- تُزال عناوين URL البعيدة للصور الرمزية الصادرة من بيانات تعريف القناة في مساعدات الصور الرمزية في واجهة التحكم وتُستبدل بالشعار/الشارة المدمجة، لذلك لا يمكن لقناة مخترقة أو خبيثة فرض جلب صور بعيدة عشوائية من متصفح المشغل.

لا تحتاج إلى تغيير أي شيء للحصول على هذا السلوك — فهو مفعل دائمًا وغير قابل للتكوين.

## مصادقة مسار الصورة الرمزية

عند تكوين مصادقة Gateway، تتطلب نقطة نهاية الصورة الرمزية في واجهة التحكم رمز Gateway نفسه كبقية API:

- يعيد `GET /avatar/<agentId>` صورة الصورة الرمزية فقط للمستدعين المصادق عليهم. ويعيد `GET /avatar/<agentId>?meta=1` بيانات تعريف الصورة الرمزية وفق القاعدة نفسها.
- تُرفض الطلبات غير المصادق عليها إلى أي من المسارين (مطابقة لمسار وسائط المساعد الشقيق). يمنع هذا مسار الصورة الرمزية من تسريب هوية الوكيل على المضيفين المحميين بخلاف ذلك.
- تمرر واجهة التحكم نفسها رمز Gateway كترويسة bearer عند جلب الصور الرمزية، وتستخدم عناوين URL من نوع blob مصادقًا عليها بحيث تظل الصورة تُعرض في لوحات التحكم.

إذا عطلت مصادقة Gateway (غير موصى به على المضيفين المشتركين)، يصبح مسار الصورة الرمزية أيضًا غير مصادق عليه، بما يتماشى مع بقية Gateway.

## مصادقة مسار وسائط المساعد

عند تكوين مصادقة Gateway، تستخدم معاينات الوسائط المحلية للمساعد مسارًا من خطوتين:

- يتطلب `GET /__openclaw__/assistant-media?meta=1&source=<path>` مصادقة مشغل واجهة التحكم العادية. يرسل المتصفح رمز Gateway كترويسة bearer عند التحقق من التوفر.
- تتضمن استجابات بيانات التعريف الناجحة `mediaTicket` قصير العمر مقيدًا بمسار المصدر المحدد هذا.
- تستخدم عناوين URL للصور والصوت والفيديو والمستندات المعروضة في المتصفح `mediaTicket=<ticket>` بدل رمز Gateway النشط أو كلمة المرور. تنتهي صلاحية التذكرة سريعًا ولا يمكنها تفويض مصدر مختلف.

يحافظ هذا على توافق عرض الوسائط العادي مع عناصر الوسائط الأصلية في المتصفح دون وضع بيانات اعتماد Gateway قابلة لإعادة الاستخدام في عناوين URL المرئية للوسائط.

## بناء الواجهة

يقدم Gateway الملفات الثابتة من `dist/control-ui`. ابنها باستخدام:

```bash
pnpm ui:build
```

أساس مطلق اختياري (عندما تريد عناوين URL ثابتة للأصول):

```bash
OPENCLAW_CONTROL_UI_BASE_PATH=/openclaw/ pnpm ui:build
```

للتطوير المحلي (خادم تطوير منفصل):

```bash
pnpm ui:dev
```

ثم وجّه الواجهة إلى عنوان URL الخاص بـ Gateway WS لديك (مثل `ws://127.0.0.1:18789`).

## صفحة واجهة تحكم فارغة

إذا حمّل المتصفح لوحة تحكم فارغة ولم تعرض DevTools خطأ مفيدًا، فقد يكون امتداد أو سكربت محتوى مبكر قد منع تطبيق وحدة JavaScript من التقييم. تتضمن الصفحة الثابتة لوحة استرداد HTML بسيطة تظهر عندما لا يُسجل `<openclaw-app>` بعد بدء التشغيل.

استخدم إجراء **حاول مرة أخرى** في اللوحة بعد تغيير بيئة المتصفح، أو أعد التحميل يدويًا بعد هذه الفحوصات:

- عطّل الامتدادات التي تحقن في كل الصفحات، وخصوصًا الامتدادات ذات سكربتات محتوى `<all_urls>`.
- جرّب نافذة خاصة، أو ملف تعريف متصفح نظيفًا، أو متصفحًا آخر.
- أبقِ Gateway قيد التشغيل وتحقق من عنوان URL نفسه للوحة التحكم بعد تغيير المتصفح.

## التصحيح/الاختبار: خادم التطوير + Gateway بعيد

واجهة التحكم هي ملفات ثابتة؛ هدف WebSocket قابل للتكوين ويمكن أن يكون مختلفًا عن أصل HTTP. هذا مفيد عندما تريد خادم تطوير Vite محليًا بينما يعمل Gateway في مكان آخر.

<Steps>
  <Step title="Start the UI dev server">
    ```bash
    pnpm ui:dev
    ```
  </Step>
  <Step title="Open with gatewayUrl">
    ```text
    http://localhost:5173/?gatewayUrl=ws%3A%2F%2F<gateway-host>%3A18789
    ```

    مصادقة اختيارية لمرة واحدة (إذا لزم الأمر):

    ```text
    http://localhost:5173/?gatewayUrl=wss%3A%2F%2F<gateway-host>%3A18789#token=<gateway-token>
    ```

  </Step>
</Steps>

<AccordionGroup>
  <Accordion title="ملاحظات">
    - يُخزَّن `gatewayUrl` في localStorage بعد التحميل ويُزال من عنوان URL.
    - إذا مرّرت نقطة نهاية `ws://` أو `wss://` كاملة عبر `gatewayUrl`، فشفّر قيمة `gatewayUrl` بصيغة URL حتى يفسّر المتصفح سلسلة الاستعلام بشكل صحيح.
    - ينبغي تمرير `token` عبر جزء عنوان URL (`#token=...`) كلما أمكن. لا تُرسَل الأجزاء إلى الخادم، ما يتجنب تسرّبها في سجلات الطلبات وReferer. لا تزال معاملات الاستعلام القديمة `?token=` تُستورَد مرة واحدة للتوافق، لكن كخيار احتياطي فقط، وتُزال مباشرة بعد التمهيد.
    - يُحتفَظ بـ `password` في الذاكرة فقط.
    - عند تعيين `gatewayUrl`، لا تعود واجهة المستخدم إلى بيانات اعتماد الإعدادات أو البيئة. وفّر `token` (أو `password`) صراحةً. غياب بيانات الاعتماد الصريحة يُعد خطأ.
    - استخدم `wss://` عندما يكون Gateway خلف TLS (Tailscale Serve، وكيل HTTPS، وما إلى ذلك).
    - لا يُقبل `gatewayUrl` إلا في نافذة من المستوى الأعلى (غير مضمّنة) لمنع clickjacking.
    - يجب على عمليات نشر واجهة التحكم العامة غير local loopback تعيين `gateway.controlUi.allowedOrigins` صراحةً (مصادر كاملة). تُقبل عمليات التحميل الخاصة من شبكة LAN/Tailnet ذات المصدر نفسه من local loopback أو RFC1918/link-local أو `.local` أو `.ts.net` أو مضيفي Tailscale CGNAT دون تمكين الرجوع الاحتياطي إلى ترويسة Host.
    - قد يزرع بدء تشغيل Gateway مصادر محلية مثل `http://localhost:<port>` و`http://127.0.0.1:<port>` من ربط و منفذ وقت التشغيل الفعّالين، لكن مصادر المتصفحات البعيدة لا تزال تحتاج إلى إدخالات صريحة.
    - لا تستخدم `gateway.controlUi.allowedOrigins: ["*"]` إلا للاختبار المحلي شديد الضبط. فهذا يعني السماح لأي مصدر متصفح، وليس "مطابقة أي مضيف أستخدمه."
    - يفعّل `gateway.controlUi.dangerouslyAllowHostHeaderOriginFallback=true` وضع الرجوع الاحتياطي إلى منشأ ترويسة Host، لكنه وضع أمني خطير.

  </Accordion>
</AccordionGroup>

مثال:

```json5
{
  gateway: {
    controlUi: {
      allowedOrigins: ["http://localhost:5173"],
    },
  },
}
```

تفاصيل إعداد الوصول عن بُعد: [الوصول عن بُعد](/ar/gateway/remote).

## ذو صلة

- [لوحة المعلومات](/ar/web/dashboard) — لوحة معلومات Gateway
- [فحوصات السلامة](/ar/gateway/health) — مراقبة سلامة Gateway
- [TUI](/ar/web/tui) — واجهة مستخدم طرفية
- [WebChat](/ar/web/webchat) — واجهة دردشة مستندة إلى المتصفح
