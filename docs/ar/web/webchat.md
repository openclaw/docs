---
read_when:
    - تصحيح أخطاء الوصول إلى WebChat أو تكوينه
summary: مضيف WebChat الثابت للاسترجاع المحلي واستخدام WS في Gateway لواجهة الدردشة
title: دردشة الويب
x-i18n:
    generated_at: "2026-04-30T08:34:51Z"
    model: gpt-5.5
    provider: openai
    source_hash: d8a4fef0aab37ca82bff249c6b31eb65475f12c16dfb9b86ddd62c1a938a34f3
    source_path: web/webchat.md
    workflow: 16
---

الحالة: تتواصل واجهة دردشة macOS/iOS SwiftUI مباشرة مع Gateway WebSocket.

## ما هي

- واجهة دردشة أصلية للـ Gateway (بلا متصفح مضمّن ولا خادم ثابت محلي).
- تستخدم الجلسات وقواعد التوجيه نفسها التي تستخدمها القنوات الأخرى.
- توجيه حتمي: تعود الردود دائمًا إلى WebChat.

## البدء السريع

1. شغّل الـ Gateway.
2. افتح واجهة WebChat (تطبيق macOS/iOS) أو تبويب الدردشة في Control UI.
3. تأكد من تكوين مسار مصادقة Gateway صالح (سرّ مشترك افتراضيًا،
   حتى على loopback).

## كيف يعمل (السلوك)

- تتصل الواجهة بـ Gateway WebSocket وتستخدم `chat.history` و`chat.send` و`chat.inject`.
- `chat.history` محدود لضمان الاستقرار: قد يقتطع Gateway حقول النص الطويلة، أو يحذف البيانات الوصفية الثقيلة، أو يستبدل الإدخالات الكبيرة جدًا بـ `[chat.history omitted: message too large]`.
- يتبع `chat.history` فرع النص النشط لملفات الجلسات الحديثة ذات الإلحاق فقط، لذلك لا تُعرض فروع إعادة الكتابة المهجورة ونسخ المطالبات المستبدلة في WebChat.
- يدمج Control UI عمليات الإرسال المكررة قيد التنفيذ للجلسة والرسالة والمرفقات نفسها قبل إنشاء معرّف تشغيل `chat.send` جديد؛ ولا يزال Gateway يزيل تكرار الطلبات المتكررة التي تعيد استخدام مفتاح عدم التكرار نفسه.
- يتم أيضًا تطبيع عرض `chat.history`: سياق OpenClaw الخاص بوقت التشغيل فقط،
  وأغلفة المغلفات الواردة، ووسوم توجيهات التسليم المضمنة
  مثل `[[reply_to_*]]` و`[[audio_as_voice]]`، وحمولات XML النصية الصرفة لاستدعاءات الأدوات
  (بما في ذلك `<tool_call>...</tool_call>`،
  و`<function_call>...</function_call>`، و`<tool_calls>...</tool_calls>`،
  و`<function_calls>...</function_calls>`، وكتل استدعاءات الأدوات المقتطعة)،
  ورموز تحكم النموذج المسرّبة بنمط ASCII/العرض الكامل تُزال من النص المرئي،
  وتُحذف إدخالات المساعد التي يكون نصها المرئي بالكامل هو الرمز الصامت الدقيق
  `NO_REPLY` / `no_reply` فقط.
- تُستبعد حمولات الرد الموسومة بالتفكير (`isReasoning: true`) من محتوى مساعد WebChat، ونص إعادة تشغيل النص، وكتل محتوى الصوت، لذلك لا تظهر حمولات التفكير فقط كرسائل مساعد مرئية أو صوت قابل للتشغيل.
- يضيف `chat.inject` ملاحظة مساعد مباشرة إلى النص ويبثها إلى الواجهة (بلا تشغيل وكيل).
- يمكن للتشغيلات المُجهضة أن تُبقي مخرجات المساعد الجزئية مرئية في الواجهة.
- يحفظ Gateway نص المساعد الجزئي المُجهض في سجل النص عندما توجد مخرجات مخزّنة مؤقتًا، ويميز تلك الإدخالات ببيانات وصفية للإجهاض.
- يُجلب السجل دائمًا من الـ Gateway (بلا مراقبة ملفات محلية).
- إذا تعذّر الوصول إلى الـ Gateway، يكون WebChat للقراءة فقط.

## لوحة أدوات وكلاء Control UI

- تحتوي لوحة الأدوات `/agents` في Control UI على عرضين منفصلين:
  - يستخدم **متاح الآن** `tools.effective(sessionKey=...)` ويعرض ما تستطيع الجلسة الحالية
    استخدامه فعليًا وقت التشغيل، بما في ذلك أدوات النواة والـ Plugin والقناة.
  - يستخدم **تكوين الأدوات** `tools.catalog` ويبقى مركّزًا على الملفات الشخصية والتجاوزات
    ودلالات الفهرس.
- يتحدد التوفر وقت التشغيل بنطاق الجلسة. يمكن أن يؤدي تبديل الجلسات على الوكيل نفسه إلى تغيير
  قائمة **متاح الآن**.
- لا يعني محرر التكوين التوفر وقت التشغيل؛ فالوصول الفعّال لا يزال يتبع أسبقية السياسة
  (`allow`/`deny`، وتجاوزات كل وكيل ومزوّد/قناة).

## الاستخدام عن بُعد

- يمرر وضع الاتصال عن بُعد Gateway WebSocket عبر SSH/Tailscale.
- لا تحتاج إلى تشغيل خادم WebChat منفصل.

## مرجع التكوين (WebChat)

التكوين الكامل: [التكوين](/ar/gateway/configuration)

خيارات WebChat:

- `gateway.webchat.chatHistoryMaxChars`: الحد الأقصى لعدد الأحرف لحقول النص في استجابات `chat.history`. عندما يتجاوز إدخال نص هذا الحد، يقتطع Gateway حقول النص الطويلة وقد يستبدل الرسائل الكبيرة جدًا بعنصر نائب. يمكن للعميل أيضًا إرسال `maxChars` لكل طلب لتجاوز هذا الإعداد الافتراضي لاستدعاء `chat.history` واحد.

الخيارات العامة ذات الصلة:

- `gateway.port`, `gateway.bind`: مضيف/منفذ WebSocket.
- `gateway.auth.mode`, `gateway.auth.token`, `gateway.auth.password`:
  مصادقة WebSocket بالسرّ المشترك.
- `gateway.auth.allowTailscale`: يمكن لتبويب دردشة Control UI في المتصفح استخدام ترويسات هوية Tailscale
  Serve عند التفعيل.
- `gateway.auth.mode: "trusted-proxy"`: مصادقة الوكيل العكسي لعملاء المتصفح خلف مصدر وكيل **غير loopback** ومدرك للهوية (راجع [مصادقة الوكيل الموثوق](/ar/gateway/trusted-proxy-auth)).
- `gateway.remote.url`, `gateway.remote.token`, `gateway.remote.password`: هدف Gateway البعيد.
- `session.*`: تخزين الجلسات والقيم الافتراضية للمفتاح الرئيسي.

## ذو صلة

- [Control UI](/ar/web/control-ui)
- [لوحة المعلومات](/ar/web/dashboard)
