---
read_when:
    - تغيير سلوك مؤشرات الكتابة أو إعداداتها الافتراضية
summary: متى يعرض OpenClaw مؤشرات الكتابة وكيفية ضبطها
title: مؤشرات الكتابة
x-i18n:
    generated_at: "2026-04-24T07:39:31Z"
    model: gpt-5.4
    provider: openai
    source_hash: 80f5c3bb79cf87f79db5336978b877f4a01025f59c9e822ab66198f00907123f
    source_path: concepts/typing-indicators.md
    workflow: 15
---

تُرسَل مؤشرات الكتابة إلى قناة الدردشة أثناء نشاط التشغيل. استخدم
`agents.defaults.typingMode` للتحكم في **متى** تبدأ الكتابة، واستخدم `typingIntervalSeconds`
للتحكم في **عدد مرات** تحديثها.

## الإعدادات الافتراضية

عندما يكون `agents.defaults.typingMode` **غير مضبوط**، يحافظ OpenClaw على السلوك القديم:

- **الدردشات المباشرة**: تبدأ الكتابة فورًا بمجرد بدء حلقة النموذج.
- **الدردشات الجماعية مع إشارة**: تبدأ الكتابة فورًا.
- **الدردشات الجماعية بدون إشارة**: تبدأ الكتابة فقط عندما يبدأ تدفق نص الرسالة.
- **تشغيلات Heartbeat**: تبدأ الكتابة عند بدء تشغيل Heartbeat إذا كان
  هدف Heartbeat المحلول دردشة تدعم الكتابة ولم تكن الكتابة معطلة.

## الأوضاع

اضبط `agents.defaults.typingMode` على أحد القيم التالية:

- `never` — لا يوجد مؤشر كتابة إطلاقًا.
- `instant` — ابدأ الكتابة **بمجرد بدء حلقة النموذج**، حتى إذا كان التشغيل
  سيعيد لاحقًا فقط الرمز المميز للرد الصامت.
- `thinking` — ابدأ الكتابة عند **أول delta للتفكير** (يتطلب
  `reasoningLevel: "stream"` لهذا التشغيل).
- `message` — ابدأ الكتابة عند **أول delta نصية غير صامتة** (ويتجاهل
  الرمز الصامت `NO_REPLY`).

ترتيب “مدى مبكرية التشغيل”:
`never` → `message` → `thinking` → `instant`

## التهيئة

```json5
{
  agent: {
    typingMode: "thinking",
    typingIntervalSeconds: 6,
  },
}
```

يمكنك تجاوز الوضع أو الوتيرة لكل جلسة:

```json5
{
  session: {
    typingMode: "message",
    typingIntervalSeconds: 4,
  },
}
```

## ملاحظات

- لن يعرض وضع `message` الكتابة للردود الصامتة فقط عندما تكون الحمولة
  بأكملها هي الرمز الصامت المطابق تمامًا (مثل `NO_REPLY` / `no_reply`,
  مع مطابقة غير حساسة لحالة الأحرف).
- لا يعمل `thinking` إلا إذا كان التشغيل يرسل التفكير بشكل متدفق (`reasoningLevel: "stream"`).
  وإذا لم يُصدر النموذج deltas للتفكير، فلن تبدأ الكتابة.
- تُعد كتابة Heartbeat إشارة حيوية لهدف التسليم المحلول. وهي
  تبدأ عند بدء تشغيل Heartbeat بدلًا من اتباع توقيت التدفق الخاص بـ `message` أو `thinking`.
  اضبط `typingMode: "never"` لتعطيلها.
- لا تعرض Heartbeats الكتابة عندما تكون `target: "none"`، أو عندما يتعذر
  حل الهدف، أو عندما يكون تسليم الدردشة معطلًا لـ Heartbeat، أو عندما
  لا تدعم القناة الكتابة.
- يتحكم `typingIntervalSeconds` في **وتيرة التحديث**، وليس وقت البدء.
  والقيمة الافتراضية هي 6 ثوانٍ.

## ذو صلة

- [الحضور](/ar/concepts/presence)
- [التدفق والتقسيم إلى أجزاء](/ar/concepts/streaming)
