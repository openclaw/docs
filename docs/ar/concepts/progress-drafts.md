---
read_when:
    - إعداد تحديثات التقدم المرئية لجولات الدردشة طويلة الأمد
    - الاختيار بين أوضاع البث الجزئي وبث الكتل وبث التقدم
    - شرح كيفية تحديث OpenClaw لرسالة قناة واحدة أثناء سير العمل
    - استكشاف أخطاء مسودات التقدم، أو رسائل التقدم المستقلة، أو الرجوع الاحتياطي عند الإنهاء وإصلاحها
summary: 'مسودات التقدم: رسالة عمل قيد التنفيذ واحدة مرئية تُحدَّث أثناء تشغيل وكيل'
title: مسودات التقدم
x-i18n:
    generated_at: "2026-05-04T07:04:10Z"
    model: gpt-5.5
    provider: openai
    source_hash: f78c07866cd7f613012a80a40413e5866c1dd2edd477088f9fc141347f5f3788
    source_path: concepts/progress-drafts.md
    workflow: 16
---

تجعل مسودات التقدم أدوار الوكيل طويلة التشغيل تبدو حيّة في المحادثة من دون تحويل
المحادثة إلى كومة من ردود الحالة المؤقتة.

عند تمكين مسودات التقدم، ينشئ OpenClaw رسالة عمل قيد التنفيذ مرئية واحدة
فقط بعد أن يثبت الدور أنه ينجز عملا حقيقيا، ويحدثها بينما يقرأ الوكيل أو يخطط
أو يستدعي الأدوات أو ينتظر الموافقة، ثم يحول تلك المسودة إلى الإجابة النهائية
عندما تستطيع القناة فعل ذلك بأمان.

```text
Shelling...
📖 Read: from docs/concepts/progress-drafts.md
🔎 Web Search: for "discord edit message"
🛠️ Exec: run tests
```

استخدم مسودات التقدم عندما تريد رسالة حالة مرتبة واحدة أثناء العمل المكثف
بالأدوات، ثم الإجابة النهائية عند اكتمال الدور.

## البدء السريع

مكّن مسودات التقدم لكل قناة باستخدام `streaming.mode: "progress"`:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
      },
    },
  },
}
```

هذا يكفي عادة. سيختار OpenClaw تسمية تلقائية من كلمة واحدة، وينتظر إلى أن
يستمر العمل خمس ثوان على الأقل أو يصدر حدث عمل ثانيا، ويضيف سطور تقدم
موجزة أثناء حدوث عمل مفيد، ويمنع رسائل التقدم المستقلة المكررة لذلك الدور.

## ما يراه المستخدمون

تتكون مسودة التقدم من جزأين:

| الجزء        | الغرض                                                                      |
| ------------ | -------------------------------------------------------------------------- |
| التسمية      | عنوان قصير مثل `Thinking...` أو `Shelling...`.                             |
| سطور التقدم | تحديثات تشغيل موجزة تستخدم تسميات الأدوات والأيقونات نفسها كمخرجات الإسهاب. |

تظهر التسمية بعد أن يبدأ الوكيل عملا ذا معنى، وإما أن يبقى مشغولا
لخمس ثوان أو يصدر حدث عمل ثانيا. لا تعرض الردود النصية فقط مسودة تقدم.
تضاف سطور التقدم فقط عندما يصدر الوكيل تحديثات عمل مفيدة، مثلا `🛠️ Exec` أو
`🔎 Web Search` أو `✍️ Write: to /tmp/file`.
بشكل افتراضي تستخدم وضع الشرح الموجز نفسه مثل `/verbose`؛ اضبط
`agents.defaults.toolProgressDetail: "raw"` عند تصحيح الأخطاء وعندما تريد أيضا إلحاق
الأوامر/التفاصيل الخام.
تحل الإجابة النهائية محل المسودة عندما يكون ذلك ممكنا؛ وإلا
يرسل OpenClaw الإجابة النهائية بالطريقة المعتادة وينظف المسودة أو يتوقف عن
تحديثها وفقا لنقل القناة.

## اختيار وضع

يتحكم `channels.<channel>.streaming.mode` في سلوك قيد التنفيذ المرئي:

| الوضع      | الأنسب لـ                         | ما يظهر في المحادثة                                  |
| ---------- | --------------------------------- | ---------------------------------------------------- |
| `off`      | القنوات الهادئة                   | الإجابة النهائية فقط.                                |
| `partial`  | مشاهدة نص الإجابة وهو يظهر        | مسودة واحدة تعدل بأحدث نص للإجابة.                   |
| `block`    | مقاطع معاينة إجابة أكبر           | معاينة واحدة تحدث أو تلحق بمقاطع أكبر.               |
| `progress` | الأدوار الثقيلة بالأدوات أو الطويلة | مسودة حالة واحدة، ثم الإجابة النهائية.               |

اختر `progress` عندما يهتم المستخدمون أكثر بمعرفة "ما الذي يحدث" بدلا من مشاهدة
نص الإجابة يتدفق رمزا برمز.

اختر `partial` عندما تكون الإجابة نفسها هي إشارة التقدم.

اختر `block` عندما تريد تحديثات معاينة المسودة في مقاطع نصية أكبر. على
Discord وTelegram، لا يزال `streaming.mode: "block"` بث معاينة، وليس
تسليم كتل عاديا. استخدم `streaming.block.enabled` أو الخيار القديم
`blockStreaming` عندما تريد ردود كتل عادية.

## تكوين التسميات

توجد تسميات التقدم ضمن `channels.<channel>.streaming.progress`.

التسمية الافتراضية هي `auto`، والتي تختار من مجموعة التسميات المضمنة في
OpenClaw والمؤلفة من كلمة واحدة مع نقاط حذف:

```text
Thinking...
Shelling...
Scuttling...
Clawing...
Pinching...
Molting...
Bubbling...
Tiding...
Reefing...
Cracking...
Sifting...
Brining...
Nautiling...
Krilling...
Barnacling...
Lobstering...
Tidepooling...
Pearling...
Snapping...
Surfacing...
```

استخدم تسمية ثابتة:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
        progress: {
          label: "Investigating",
        },
      },
    },
  },
}
```

استخدم مجموعة تسميات تلقائية خاصة بك:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
        progress: {
          label: "auto",
          labels: ["Checking", "Reading", "Testing", "Finishing"],
        },
      },
    },
  },
}
```

أخف التسمية وأظهر سطور التقدم فقط:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
        progress: {
          label: false,
        },
      },
    },
  },
}
```

## التحكم في سطور التقدم

تكون سطور التقدم ممكنة افتراضيا في وضع التقدم. تأتي من أحداث تشغيل حقيقية:
بدء الأدوات، تحديثات العناصر، خطط المهام، الموافقات، مخرجات الأوامر، ملخصات
التصحيحات، وأنشطة الوكيل المشابهة.

يستخدم OpenClaw المنسق نفسه لمسودات التقدم و`/verbose`:

```json5
{
  agents: {
    defaults: {
      toolProgressDetail: "explain", // explain | raw
    },
  },
}
```

`"explain"` هو الافتراضي ويحافظ على استقرار المسودات بتسميات موجزة مثل
`🛠️ Exec: check JS syntax for /tmp/app.js`. يلحق `"raw"` الأمر/التفصيل الأساسي
عند توفره، وهذا مفيد أثناء تصحيح الأخطاء لكنه أكثر ضجيجا في المحادثة.

على سبيل المثال، يظهر الأمر نفسه بشكل مختلف اعتمادا على وضع التفاصيل:

| الوضع      | سطر التقدم                                                        |
| ---------- | ----------------------------------------------------------------- |
| `explain`  | `🛠️ Exec: check JS syntax for /tmp/app.js`                        |
| `raw`      | `🛠️ Exec: check JS syntax for /tmp/app.js, node --check /tmp/app.js` |

حدد عدد السطور التي تبقى مرئية:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
        progress: {
          maxLines: 4,
        },
      },
    },
  },
}
```

تضغط سطور التقدم تلقائيا لتقليل إعادة تدفق فقاعة المحادثة أثناء تعديل المسودة.

يقصر OpenClaw سطور التقدم الطويلة افتراضيا حتى لا تلتف تعديلات المسودة المتكررة
بشكل مختلف في كل تحديث. تظل البادئة قابلة للقراءة، وتختصر التفاصيل الطويلة
مثل المسارات أو الأوامر الخام بنقاط حذف.

يمكن أن يعرض Slack سطور التقدم كحقول Block Kit منظمة بدلا من متن نصي واحد:

```json5
{
  channels: {
    slack: {
      streaming: {
        mode: "progress",
        progress: {
          render: "rich",
        },
      },
    },
  },
}
```

يحافظ العرض الغني على الاحتياط النصي العادي نفسه، بحيث تستطيع القنوات والعملاء
التي لا تدعم الشكل الأغنى أن تعرض نص التقدم الموجز أيضا.

احتفظ بمسودة التقدم الواحدة لكن أخف سطور الأدوات والمهام:

```json5
{
  channels: {
    discord: {
      streaming: {
        mode: "progress",
        progress: {
          toolProgress: false,
        },
      },
    },
  },
}
```

مع `toolProgress: false`، لا يزال OpenClaw يمنع رسائل تقدم الأدوات المستقلة
الأقدم لذلك الدور. تبقى القناة هادئة بصريا حتى الإجابة النهائية، باستثناء
التسمية إذا كانت مكونة.

## سلوك القنوات

تستخدم كل قناة أنظف نقل تدعمه:

| القناة          | نقل التقدم                               | ملاحظات                                                                 |
| --------------- | ---------------------------------------- | ----------------------------------------------------------------------- |
| Discord         | إرسال رسالة واحدة، ثم تعديلها.           | يعدل النص النهائي في مكانه عندما يلائم رسالة معاينة آمنة واحدة.         |
| Matrix          | إرسال حدث واحد، ثم تعديله.               | يتحكم تكوين البث على مستوى الحساب في المسودات على مستوى الحساب.          |
| Microsoft Teams | بث Teams أصلي في المحادثات الشخصية.      | يطابق `streaming.mode: "block"` تسليم كتل Teams.                         |
| Slack           | بث أصلي أو منشور مسودة قابل للتعديل.      | يؤثر توفر السلسلة في إمكانية استخدام البث الأصلي.                        |
| Telegram        | إرسال رسالة واحدة، ثم تعديلها.           | قد تستبدل المسودات المرئية الأقدم حتى تبقى الطوابع الزمنية النهائية مفيدة. |
| Mattermost      | منشور مسودة قابل للتعديل.                | يطوى نشاط الأدوات داخل المنشور نفسه ذي نمط المسودة.                      |

عادة ما تعود القنوات التي لا تدعم التعديل الآمن إلى مؤشرات الكتابة أو
التسليم النهائي فقط.

## الإنهاء

عندما تصبح الإجابة النهائية جاهزة، يحاول OpenClaw إبقاء المحادثة نظيفة:

- إذا كان يمكن أن تصبح المسودة الإجابة النهائية بأمان، يعدلها OpenClaw في مكانها.
- إذا كانت القناة تستخدم بث تقدم أصليا، ينهي OpenClaw ذلك البث
  عندما يقبل النقل الأصلي النص النهائي.
- إذا كانت الإجابة النهائية تحتوي على وسائط، أو مطالبة موافقة، أو هدف رد صريح،
  أو عددا كبيرا من المقاطع، أو فشل تعديل/إرسال، يرسل OpenClaw الإجابة النهائية عبر
  مسار تسليم القناة العادي.

مسار الاحتياط مقصود. من الأفضل إرسال إجابة نهائية جديدة بدلا من فقدان
النص، أو وضع رد في سلسلة خاطئة، أو استبدال مسودة بحمولة لا تستطيع القناة
تمثيلها بأمان.

## استكشاف الأخطاء وإصلاحها

**لا أرى إلا الإجابة النهائية.**

تحقق من أن `channels.<channel>.streaming.mode` مضبوط على `progress` للحساب
أو القناة التي عالجت الرسالة. قد تعطل بعض مسارات المجموعات أو الردود المقتبسة
معاينات المسودة لدور ما عندما لا تستطيع القناة تعديل الرسالة الصحيحة بأمان.

**أرى التسمية لكن لا أرى سطور أدوات.**

تحقق من `streaming.progress.toolProgress`. إذا كانت قيمته `false`، يحافظ OpenClaw على
سلوك المسودة الواحدة لكنه يخفي سطور تقدم الأدوات والمهام.

**أرى رسالة نهائية جديدة بدلا من مسودة معدلة.**

هذا احتياط للسلامة. يمكن أن يحدث مع ردود الوسائط، أو الإجابات الطويلة،
أو أهداف الرد الصريحة، أو مسودات Telegram القديمة، أو أهداف سلاسل Slack المفقودة،
أو رسائل المعاينة المحذوفة، أو فشل إنهاء البث الأصلي.

**ما زلت أرى رسائل تقدم مستقلة.**

يمنع وضع التقدم رسائل تقدم الأدوات المستقلة الافتراضية عندما تكون المسودة
نشطة. إذا ظلت الرسائل المستقلة تظهر، فتحقق من أن الدور يستخدم وضع التقدم فعلا
وليس `streaming.mode: "off"` أو مسار قناة لا يستطيع إنشاء مسودة لتلك الرسالة.

**يتصرف Teams بشكل مختلف عن Discord أو Telegram.**

يستخدم Microsoft Teams بثا أصليا في المحادثات الشخصية بدلا من نقل المعاينة العام
بالإرسال ثم التعديل. يعامل Teams أيضا `streaming.mode: "block"` على أنه
تسليم كتل Teams لأنه لا يملك وضع كتل معاينة المسودة نفسه المستخدم في
Discord وTelegram.

## ذات صلة

- [البث والتقطيع](/ar/concepts/streaming)
- [الرسائل](/ar/concepts/messages)
- [تكوين القنوات](/ar/gateway/config-channels)
- [Discord](/ar/channels/discord)
- [Matrix](/ar/channels/matrix)
- [Microsoft Teams](/ar/channels/msteams)
- [Slack](/ar/channels/slack)
- [Telegram](/ar/channels/telegram)
