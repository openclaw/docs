---
read_when:
    - تحرير نص مطالبة النظام أو قائمة الأدوات أو أقسام الوقت/نبضات القلب
    - تغيير تهيئة مساحة العمل أو سلوك حقن Skills
summary: ما الذي تتضمنه مطالبة نظام OpenClaw وكيف يتم تجميعها
title: مطالبة النظام
x-i18n:
    generated_at: "2026-04-12T07:15:29Z"
    model: gpt-5.4
    provider: openai
    source_hash: 057f01aac51f7737b5223f61f5d55e552d9011232aebb130426e269d8f6c257f
    source_path: concepts/system-prompt.md
    workflow: 15
---

# مطالبة النظام

يبني OpenClaw مطالبة نظام مخصصة لكل تشغيل وكيل. المطالبة **مملوكة لـ OpenClaw** ولا تستخدم المطالبة الافتراضية لـ pi-coding-agent.

يتم تجميع المطالبة بواسطة OpenClaw وحقنها في كل تشغيل وكيل.

يمكن لإضافات الموفر المساهمة بإرشادات مطالبة تراعي التخزين المؤقت دون استبدال
مطالبة OpenClaw الكاملة المملوكة له. يمكن لوقت تشغيل الموفر أن:

- يستبدل مجموعة صغيرة من الأقسام الأساسية المسمّاة (`interaction_style`,
  `tool_call_style`, `execution_bias`)
- يحقن **بادئة مستقرة** فوق حد التخزين المؤقت للمطالبة
- يحقن **لاحقة ديناميكية** أسفل حد التخزين المؤقت للمطالبة

استخدم المساهمات المملوكة للموفر من أجل الضبط الخاص بعائلات النماذج. أبقِ
تعديل المطالبة القديم `before_prompt_build` للتوافق أو لتغييرات المطالبة
العالمية فعلًا، وليس لسلوك الموفر العادي.

## البنية

المطالبة مضغوطة عمدًا وتستخدم أقسامًا ثابتة:

- **الأدوات**: تذكير بمصدر الحقيقة للأدوات المهيكلة بالإضافة إلى إرشادات وقت التشغيل لاستخدام الأدوات.
- **السلامة**: تذكير قصير بالضوابط لتجنب السلوك الساعي إلى السلطة أو تجاوز الإشراف.
- **Skills** (عند توفرها): يوضح للنموذج كيفية تحميل تعليمات Skill عند الطلب.
- **التحديث الذاتي لـ OpenClaw**: كيفية فحص الإعدادات بأمان باستخدام
  `config.schema.lookup`، وترقيع الإعدادات باستخدام `config.patch`، واستبدال
  الإعدادات كاملة باستخدام `config.apply`، وتشغيل `update.run` فقط عند طلب
  صريح من المستخدم. كما أن أداة `gateway` المخصصة للمالك فقط ترفض أيضًا إعادة
  كتابة `tools.exec.ask` / `tools.exec.security`، بما في ذلك الأسماء
  المستعارة القديمة `tools.bash.*` التي تُطبَّع إلى مسارات التنفيذ المحمية هذه.
- **مساحة العمل**: دليل العمل (`agents.defaults.workspace`).
- **التوثيق**: المسار المحلي إلى مستندات OpenClaw (المستودع أو حزمة npm) ومتى يجب قراءتها.
- **ملفات مساحة العمل (محقونة)**: يشير إلى أن ملفات التهيئة الأولية مضمنة أدناه.
- **Sandbox** (عند التمكين): يشير إلى بيئة تشغيل معزولة، ومسارات العزل، وما إذا كان التنفيذ المرتفع الصلاحيات متاحًا.
- **التاريخ والوقت الحاليان**: الوقت المحلي للمستخدم، والمنطقة الزمنية، وتنسيق الوقت.
- **وسوم الرد**: صياغة اختيارية لوسوم الرد لموفري الخدمة المدعومين.
- **نبضات القلب**: مطالبة نبضات القلب وسلوك الإقرار، عندما تكون نبضات القلب مفعلة للوكيل الافتراضي.
- **وقت التشغيل**: المضيف، ونظام التشغيل، وNode، والنموذج، وجذر المستودع (عند اكتشافه)، ومستوى التفكير (سطر واحد).
- **الاستدلال**: مستوى الرؤية الحالي + تلميح تبديل `/reasoning`.

يتضمن قسم الأدوات أيضًا إرشادات وقت التشغيل للأعمال طويلة التشغيل:

- استخدم cron للمتابعة المستقبلية (`تحقق لاحقًا`، والتذكيرات، والعمل المتكرر)
  بدلًا من حلقات النوم في `exec`، أو حيل التأخير `yieldMs`، أو الاستطلاع
  المتكرر لـ `process`
- استخدم `exec` / `process` فقط للأوامر التي تبدأ الآن وتستمر في العمل في
  الخلفية
- عندما تكون خاصية التنبيه التلقائي عند الاكتمال مفعلة، ابدأ الأمر مرة واحدة
  واعتمد على مسار التنبيه القائم على الدفع عندما يُخرج ناتجًا أو يفشل
- استخدم `process` للسجلات أو الحالة أو الإدخال أو التدخل عندما تحتاج إلى
  فحص أمر قيد التشغيل
- إذا كانت المهمة أكبر، ففضّل `sessions_spawn`؛ إذ إن اكتمال الوكيل الفرعي
  قائم على الدفع ويُعلن تلقائيًا للطالب
- لا تستطلع `subagents list` / `sessions_list` في حلقة لمجرد انتظار
  الاكتمال

عندما تكون أداة `update_plan` التجريبية مفعلة، يوضح قسم الأدوات أيضًا
للنموذج أن يستخدمها فقط للأعمال متعددة الخطوات غير البسيطة، وأن يبقي خطوة
واحدة فقط بحالة `in_progress`، وأن يتجنب تكرار الخطة كاملة بعد كل تحديث.

ضوابط السلامة في مطالبة النظام إرشادية. فهي توجه سلوك النموذج لكنها لا تفرض
السياسة. استخدم سياسة الأدوات، وموافقات التنفيذ، والعزل، وقوائم سماح القنوات
للفرض الصارم؛ إذ يمكن للمشغلين تعطيل هذه الأمور عن قصد.

في القنوات التي تحتوي على بطاقات/أزرار موافقة أصلية، توضح مطالبة وقت التشغيل
الآن للوكيل أن يعتمد أولًا على واجهة الموافقة الأصلية تلك. ويجب ألا يتضمن أمر
`/approve` يدويًا إلا عندما تشير نتيجة الأداة إلى أن موافقات الدردشة غير
متاحة أو أن الموافقة اليدوية هي المسار الوحيد.

## أوضاع المطالبة

يمكن لـ OpenClaw عرض مطالبات نظام أصغر للوكلاء الفرعيين. يضبط وقت التشغيل
`promptMode` لكل تشغيل (وليس إعدادًا ظاهرًا للمستخدم):

- `full` (الافتراضي): يتضمن جميع الأقسام أعلاه.
- `minimal`: يُستخدم للوكلاء الفرعيين؛ ويحذف **Skills** و**استدعاء الذاكرة** و**التحديث الذاتي لـ OpenClaw** و**أسماء النماذج المستعارة** و**هوية المستخدم** و**وسوم الرد** و**المراسلة** و**الردود الصامتة** و**نبضات القلب**. تظل الأدوات و**السلامة** ومساحة العمل وSandbox والتاريخ والوقت الحاليان (عند توفرهما) ووقت التشغيل والسياق المحقون متاحة.
- `none`: يعيد فقط سطر الهوية الأساسي.

عندما يكون `promptMode=minimal`، تُوسم المطالبات الإضافية المحقونة باسم **سياق الوكيل الفرعي** بدلًا من **سياق الدردشة الجماعية**.

## حقن تهيئة مساحة العمل الأولية

تُقص ملفات التهيئة الأولية وتُلحق تحت **سياق المشروع** بحيث يرى النموذج
الهوية وسياق الملف الشخصي من دون الحاجة إلى قراءتها صراحةً:

- `AGENTS.md`
- `SOUL.md`
- `TOOLS.md`
- `IDENTITY.md`
- `USER.md`
- `HEARTBEAT.md`
- `BOOTSTRAP.md` (فقط في مساحات العمل الجديدة تمامًا)
- `MEMORY.md` عند وجوده، وإلا `memory.md` كبديل بأحرف صغيرة

تُحقن جميع هذه الملفات **في نافذة السياق** في كل دور ما لم تنطبق بوابة خاصة
بملف معين. ويُحذف `HEARTBEAT.md` في التشغيلات العادية عندما تكون نبضات القلب
معطلة للوكيل الافتراضي أو عندما تكون
`agents.defaults.heartbeat.includeSystemPromptSection` مساوية لـ false. اجعل
الملفات المحقونة موجزة — وخاصة `MEMORY.md`، الذي قد ينمو بمرور الوقت ويؤدي إلى
استخدام سياق أعلى من المتوقع وإلى ضغط أكثر تكرارًا.

> **ملاحظة:** ملفات `memory/*.md` اليومية **ليست** جزءًا من سياق مشروع
> التهيئة الأولية العادي. ففي الأدوار العادية يُصار إلى الوصول إليها عند
> الطلب عبر أداتي `memory_search` و`memory_get`، لذلك لا تُحتسب ضمن نافذة
> السياق ما لم يقرأها النموذج صراحةً. وتُعد أدوار `/new` و`/reset` المجردة
> الاستثناء: إذ يمكن لوقت التشغيل أن يسبق مؤخرات الذاكرة اليومية كسياق
> بدء لمرة واحدة لذلك الدور الأول.

تُقتطع الملفات الكبيرة مع علامة. ويُتحكم في الحد الأقصى للحجم لكل ملف بواسطة
`agents.defaults.bootstrapMaxChars` (الافتراضي: 20000). كما يُحدد سقف لإجمالي
محتوى التهيئة الأولية المحقون عبر الملفات بواسطة
`agents.defaults.bootstrapTotalMaxChars` (الافتراضي: 150000). وتحقن الملفات
المفقودة علامة قصيرة تشير إلى فقدان الملف. وعند حدوث اقتطاع، يمكن لـ OpenClaw
حقن كتلة تحذير في سياق المشروع؛ تحكم في ذلك باستخدام
`agents.defaults.bootstrapPromptTruncationWarning` (`off` أو `once` أو `always`؛
والافتراضي: `once`).

تقوم جلسات الوكيل الفرعي بحقن `AGENTS.md` و`TOOLS.md` فقط (وتُرشَّح ملفات
التهيئة الأولية الأخرى لإبقاء سياق الوكيل الفرعي صغيرًا).

يمكن للخطافات الداخلية اعتراض هذه الخطوة عبر `agent:bootstrap` لتعديل ملفات
التهيئة الأولية المحقونة أو استبدالها (على سبيل المثال استبدال `SOUL.md`
بشخصية بديلة).

إذا كنت تريد أن تجعل الوكيل يبدو أقل عمومية، فابدأ من
[دليل شخصية SOUL.md](/ar/concepts/soul).

لفحص مقدار مساهمة كل ملف محقون (الخام مقابل المحقون، والاقتطاع، إضافةً إلى
العبء الإضافي لمخطط الأداة)، استخدم `/context list` أو `/context detail`.
راجع [السياق](/ar/concepts/context).

## التعامل مع الوقت

تتضمن مطالبة النظام قسم **التاريخ والوقت الحاليان** مخصصًا عندما تكون المنطقة
الزمنية للمستخدم معروفة. وللحفاظ على ثبات التخزين المؤقت للمطالبة، فهي تتضمن
الآن فقط **المنطقة الزمنية** (من دون ساعة ديناميكية أو تنسيق وقت).

استخدم `session_status` عندما يحتاج الوكيل إلى الوقت الحالي؛ إذ تتضمن بطاقة
الحالة سطرًا للطابع الزمني. ويمكن للأداة نفسها أيضًا تعيين تجاوز نموذج لكل
جلسة اختياريًا (`model=default` يمسحه).

اضبط ذلك باستخدام:

- `agents.defaults.userTimezone`
- `agents.defaults.timeFormat` (`auto` | `12` | `24`)

راجع [التاريخ والوقت](/ar/date-time) للاطلاع على التفاصيل الكاملة للسلوك.

## Skills

عندما تكون هناك Skills مؤهلة، يحقن OpenClaw **قائمة Skills متاحة** مضغوطة
(`formatSkillsForPrompt`) تتضمن **مسار الملف** لكل Skill. وتوجه المطالبة
النموذج إلى استخدام `read` لتحميل SKILL.md من الموقع المدرج (مساحة العمل، أو
المدار، أو المضمن). وإذا لم تكن هناك Skills مؤهلة، فيُحذف قسم Skills.

تشمل الأهلية بوابات بيانات Skill الوصفية، وفحوصات بيئة/إعدادات وقت التشغيل،
وقائمة السماح الفعلية لـ Skills الخاصة بالوكيل عندما تكون
`agents.defaults.skills` أو `agents.list[].skills` مضبوطة.

```
<available_skills>
  <skill>
    <name>...</name>
    <description>...</description>
    <location>...</location>
  </skill>
</available_skills>
```

يحافظ هذا على صغر حجم المطالبة الأساسية مع الاستمرار في تمكين استخدام Skills
المستهدف.

## التوثيق

عند توفره، تتضمن مطالبة النظام قسم **التوثيق** الذي يشير إلى
دليل مستندات OpenClaw المحلي (إما `docs/` في مساحة عمل المستودع أو مستندات
حزمة npm المضمنة)، ويذكر أيضًا النسخة العامة المعكوسة، والمستودع المصدري،
ومجتمع Discord، وClawHub ([https://clawhub.ai](https://clawhub.ai)) لاكتشاف Skills. وتوجه المطالبة النموذج إلى الرجوع إلى المستندات المحلية أولًا
لسلوك OpenClaw أو الأوامر أو الإعدادات أو البنية، وإلى تشغيل
`openclaw status` بنفسه عند الإمكان (ولا يسأل المستخدم إلا عندما يفتقر إلى الوصول).
