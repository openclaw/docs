---
read_when:
    - تغيير مخرجات التسجيل أو تنسيقاته
    - تصحيح أخطاء مخرجات CLI أو Gateway
summary: واجهات التسجيل، وسجلات الملفات، وأنماط سجلات WS، وتنسيق وحدة التحكم
title: تسجيل Gateway
x-i18n:
    generated_at: "2026-04-30T07:59:41Z"
    model: gpt-5.5
    provider: openai
    source_hash: 9ce9c78201d2e26760282b08eacb17826b1eac84e80b99d3a9d5cbff4078b5b3
    source_path: gateway/logging.md
    workflow: 16
---

# التسجيل

للاطلاع على نظرة عامة موجهة للمستخدمين (CLI + واجهة التحكم + الإعدادات)، راجع [/logging](/ar/logging).

لدى OpenClaw سطحان للتسجيل:

- **مخرجات وحدة التحكم** (ما تراه في الطرفية / واجهة التصحيح).
- **سجلات الملفات** (أسطر JSON) التي يكتبها مسجل Gateway.

## المسجل المعتمد على الملفات

- ملف السجل المتداول الافتراضي يكون ضمن `/tmp/openclaw/` (ملف واحد لكل يوم): `openclaw-YYYY-MM-DD.log`
  - يستخدم التاريخ المنطقة الزمنية المحلية لمضيف Gateway.
- تدور ملفات السجل النشطة عند `logging.maxFileBytes` (الافتراضي: 100 MB)، مع الاحتفاظ
  بما يصل إلى خمسة أرشيفات مرقمة ومتابعة الكتابة في ملف نشط جديد.
- يمكن إعداد مسار ملف السجل ومستواه عبر `~/.openclaw/openclaw.json`:
  - `logging.file`
  - `logging.level`

تنسيق الملف هو كائن JSON واحد في كل سطر.

يتابع تبويب السجلات في واجهة التحكم هذا الملف عبر Gateway (`logs.tail`).
يمكن لـ CLI فعل الشيء نفسه:

```bash
openclaw logs --follow
```

**الإسهاب مقابل مستويات السجل**

- **سجلات الملفات** يتحكم بها `logging.level` حصريا.
- يؤثر `--verbose` فقط في **إسهاب وحدة التحكم** (ونمط سجلات WS)؛ ولا يقوم
  برفع مستوى سجل الملف.
- لالتقاط التفاصيل الخاصة بالإسهاب في سجلات الملفات، اضبط `logging.level` على `debug` أو
  `trace`.

## التقاط وحدة التحكم

يلتقط CLI استدعاءات `console.log/info/warn/error/debug/trace` ويكتبها إلى سجلات الملفات،
مع الاستمرار في طباعتها إلى stdout/stderr.

يمكنك ضبط إسهاب وحدة التحكم بشكل مستقل عبر:

- `logging.consoleLevel` (الافتراضي `info`)
- `logging.consoleStyle` (`pretty` | `compact` | `json`)

## التنقيح

يمكن لـ OpenClaw إخفاء الرموز الحساسة قبل أن تغادر مخرجات السجل أو النصوص المسجلة
العملية. تطبق سياسة تنقيح التسجيل هذه على مصارف نص وحدة التحكم وسجل الملفات وسجلات
OTLP وسجل نصوص الجلسات، بحيث يتم إخفاء قيم الأسرار المطابقة قبل كتابة أسطر JSONL أو الرسائل إلى القرص.

- `logging.redactSensitive`: `off` | `tools` (الافتراضي: `tools`)
- `logging.redactPatterns`: مصفوفة من سلاسل regex (تتجاوز الافتراضيات)
  - استخدم سلاسل regex خاما (تطبق `gi` تلقائيا)، أو `/pattern/flags` إذا كنت تحتاج إلى أعلام مخصصة.
  - يتم إخفاء المطابقات مع إبقاء أول 6 + آخر 4 أحرف (الطول >= 18)، وإلا `***`.
  - تغطي الافتراضيات إسنادات المفاتيح الشائعة، وأعلام CLI، وحقول JSON، وترويسات bearer، وكتل PEM، وبوادئ الرموز الشائعة.

بعض حدود السلامة تنقح دائما بغض النظر عن `logging.redactSensitive`.
يشمل ذلك أحداث استدعاء الأدوات في واجهة التحكم، ومخرجات أداة `sessions_history`،
وتصديرات دعم التشخيص، وملاحظات أخطاء المزود، وعرض أمر موافقة التنفيذ،
وسجلات بروتوكول WebSocket في Gateway. قد تستمر هذه الأسطح في استخدام
`logging.redactPatterns` كأنماط إضافية، لكن `redactSensitive: "off"`
لا يجعلها تصدر أسرارا خاما.

## سجلات WebSocket في Gateway

يطبع Gateway سجلات بروتوكول WebSocket بوضعين:

- **الوضع العادي (بدون `--verbose`)**: تتم طباعة نتائج RPC "المهمة" فقط:
  - الأخطاء (`ok=false`)
  - الاستدعاءات البطيئة (الحد الافتراضي: `>= 50ms`)
  - أخطاء التحليل
- **الوضع المسهب (`--verbose`)**: يطبع كل حركة طلبات/استجابات WS.

### نمط سجل WS

يدعم `openclaw gateway` مفتاح تبديل نمط لكل Gateway:

- `--ws-log auto` (الافتراضي): الوضع العادي محسن؛ يستخدم الوضع المسهب مخرجات مضغوطة
- `--ws-log compact`: مخرجات مضغوطة (طلب/استجابة مقترنان) عند الإسهاب
- `--ws-log full`: مخرجات كاملة لكل إطار عند الإسهاب
- `--compact`: اسم بديل لـ `--ws-log compact`

أمثلة:

```bash
# optimized (only errors/slow)
openclaw gateway

# show all WS traffic (paired)
openclaw gateway --verbose --ws-log compact

# show all WS traffic (full meta)
openclaw gateway --verbose --ws-log full
```

## تنسيق وحدة التحكم (تسجيل الأنظمة الفرعية)

منسق وحدة التحكم **واع بحالة TTY** ويطبع أسطرا متسقة ومسبوقة.
تحافظ مسجلات الأنظمة الفرعية على المخرجات مجمعة وسهلة الفحص.

السلوك:

- **بادئات الأنظمة الفرعية** في كل سطر (مثل `[gateway]`، `[canvas]`، `[tailscale]`)
- **ألوان الأنظمة الفرعية** (ثابتة لكل نظام فرعي) بالإضافة إلى تلوين المستوى
- **اللون عندما يكون المخرج TTY أو عندما تبدو البيئة كطرفية غنية** (`TERM`/`COLORTERM`/`TERM_PROGRAM`)، مع احترام `NO_COLOR`
- **بادئات أنظمة فرعية مختصرة**: تسقط `gateway/` + `channels/` البادئتين، وتحتفظ بآخر مقطعين (مثل `whatsapp/outbound`)
- **مسجلات فرعية حسب النظام الفرعي** (بادئة تلقائية + حقل منظم `{ subsystem }`)
- **`logRaw()`** لمخرجات QR/تجربة المستخدم (بلا بادئة وبلا تنسيق)
- **أنماط وحدة التحكم** (مثل `pretty | compact | json`)
- **مستوى سجل وحدة التحكم** منفصل عن مستوى سجل الملف (يحتفظ الملف بالتفاصيل الكاملة عند ضبط `logging.level` على `debug`/`trace`)
- **نصوص رسائل WhatsApp** تسجل عند `debug` (استخدم `--verbose` لرؤيتها)

يحافظ هذا على استقرار سجلات الملفات الحالية مع جعل المخرجات التفاعلية سهلة الفحص.

## ذو صلة

- [التسجيل](/ar/logging)
- [تصدير OpenTelemetry](/ar/gateway/opentelemetry)
- [تصدير التشخيصات](/ar/gateway/diagnostics)
