---
read_when:
    - أنت تنقل OpenClaw إلى حاسوب محمول أو خادم جديد
    - أنت قادم من نظام وكيل آخر وتريد الاحتفاظ بالحالة
    - تقوم بترقية Plugin في مكانه
summary: 'مركز الترحيل: عمليات الاستيراد عبر الأنظمة، وعمليات النقل من آلة إلى آلة، وترقيات Plugin'
title: دليل الترحيل
x-i18n:
    generated_at: "2026-04-30T08:09:03Z"
    model: gpt-5.5
    provider: openai
    source_hash: e2a1dc86ed367a0b92cdc0d5189123bb045d327be944516f564dac723f324c97
    source_path: install/migrating.md
    workflow: 16
---

يدعم OpenClaw ثلاثة مسارات ترحيل: الاستيراد من نظام وكيل آخر، ونقل تثبيت موجود إلى جهاز جديد، وترقية Plugin في مكانه.

## الاستيراد من نظام وكيل آخر

استخدم مزودي الترحيل المضمّنين لجلب التعليمات، وخوادم MCP، وSkills، وإعدادات النموذج، ومفاتيح API (اختياريًا) إلى OpenClaw. تُعايَن الخطط قبل أي تغيير، وتُنقّح الأسرار في التقارير، ويستند التطبيق إلى نسخة احتياطية مُتحقَّق منها.

<CardGroup cols={2}>
  <Card title="الترحيل من Claude" href="/ar/install/migrating-claude" icon="brain">
    استورد حالة Claude Code وClaude Desktop، بما في ذلك `CLAUDE.md`، وخوادم MCP، وSkills، وأوامر المشاريع.
  </Card>
  <Card title="الترحيل من Hermes" href="/ar/install/migrating-hermes" icon="feather">
    استورد إعدادات Hermes والمزودين وخوادم MCP والذاكرة وSkills ومفاتيح `.env` المدعومة.
  </Card>
</CardGroup>

نقطة دخول CLI هي [`openclaw migrate`](/ar/cli/migrate). يمكن للإعداد الأولي أيضًا أن يعرض الترحيل عندما يكتشف مصدرًا معروفًا (`openclaw onboard --flow import`).

## نقل OpenClaw إلى جهاز جديد

انسخ **دليل الحالة** (`~/.openclaw/` افتراضيًا) و**مساحة العمل** لديك للحفاظ على:

- **الإعدادات** — `openclaw.json` وكل إعدادات Gateway.
- **المصادقة** — ملف `auth-profiles.json` لكل وكيل (مفاتيح API بالإضافة إلى OAuth)، إلى جانب أي حالة قناة أو مزود ضمن `credentials/`.
- **الجلسات** — سجل المحادثات وحالة الوكيل.
- **حالة القنوات** — تسجيل دخول WhatsApp، وجلسة Telegram، وما شابه ذلك.
- **ملفات مساحة العمل** — `MEMORY.md` و`USER.md` وSkills والمطالبات.

<Tip>
شغّل `openclaw status` على الجهاز القديم لتأكيد مسار دليل الحالة لديك. تستخدم الملفات الشخصية المخصصة `~/.openclaw-<profile>/` أو مسارًا مضبوطًا عبر `OPENCLAW_STATE_DIR`.
</Tip>

### خطوات الترحيل

<Steps>
  <Step title="إيقاف Gateway وإنشاء نسخة احتياطية">
    على الجهاز **القديم**، أوقف Gateway حتى لا تتغير الملفات أثناء النسخ، ثم أنشئ أرشيفًا:

    ```bash
    openclaw gateway stop
    cd ~
    tar -czf openclaw-state.tgz .openclaw
    ```

    إذا كنت تستخدم عدة ملفات شخصية (مثل `~/.openclaw-work`)، فأرشف كل واحد منها على حدة.

  </Step>

  <Step title="تثبيت OpenClaw على الجهاز الجديد">
    [ثبّت](/ar/install) CLI (وNode إذا لزم الأمر) على الجهاز الجديد. لا مشكلة إذا أنشأ الإعداد الأولي دليل `~/.openclaw/` جديدًا. ستستبدله في الخطوة التالية.
  </Step>

  <Step title="نسخ دليل الحالة ومساحة العمل">
    انقل الأرشيف عبر `scp` أو `rsync -a` أو قرص خارجي، ثم استخرجه:

    ```bash
    cd ~
    tar -xzf openclaw-state.tgz
    ```

    تأكد من تضمين الأدلة المخفية، وأن ملكية الملفات تطابق المستخدم الذي سيشغّل Gateway.

  </Step>

  <Step title="تشغيل doctor والتحقق">
    على الجهاز الجديد، شغّل [Doctor](/ar/gateway/doctor) لتطبيق عمليات ترحيل الإعدادات وإصلاح الخدمات:

    ```bash
    openclaw doctor
    openclaw gateway restart
    openclaw status
    ```

  </Step>
</Steps>

### المشكلات الشائعة

<AccordionGroup>
  <Accordion title="عدم تطابق الملف الشخصي أو دليل الحالة">
    إذا كان Gateway القديم يستخدم `--profile` أو `OPENCLAW_STATE_DIR` بينما الجديد لا يستخدمه، فستظهر القنوات كأنها مسجلة الخروج وستكون الجلسات فارغة. شغّل Gateway باستخدام **نفس** الملف الشخصي أو دليل الحالة الذي رحّلته، ثم أعد تشغيل `openclaw doctor`.
  </Accordion>

  <Accordion title="نسخ openclaw.json فقط">
    ملف الإعدادات وحده لا يكفي. تعيش ملفات مصادقة النماذج ضمن `agents/<agentId>/agent/auth-profiles.json`، وتعيش حالة القنوات والمزودين ضمن `credentials/`. رحّل دائمًا دليل الحالة **بالكامل**.
  </Accordion>

  <Accordion title="الأذونات والملكية">
    إذا نسخت كجذر أو بدّلت المستخدمين، فقد يفشل Gateway في قراءة بيانات الاعتماد. تأكد من أن دليل الحالة ومساحة العمل مملوكان للمستخدم الذي يشغّل Gateway.
  </Accordion>

  <Accordion title="الوضع البعيد">
    إذا كانت واجهة المستخدم لديك تشير إلى Gateway **بعيد**، فإن المضيف البعيد هو مالك الجلسات ومساحة العمل. رحّل مضيف Gateway نفسه، وليس حاسوبك المحمول المحلي. راجع [الأسئلة الشائعة](/ar/help/faq#where-things-live-on-disk).
  </Accordion>

  <Accordion title="الأسرار في النسخ الاحتياطية">
    يحتوي دليل الحالة على ملفات المصادقة وبيانات اعتماد القنوات وحالات مزودين أخرى. خزّن النسخ الاحتياطية مشفرة، وتجنب قنوات النقل غير الآمنة، ودوّر المفاتيح إذا اشتبهت في تعرضها.
  </Accordion>
</AccordionGroup>

### قائمة التحقق

على الجهاز الجديد، تأكد مما يلي:

- [ ] يعرض `openclaw status` أن Gateway يعمل.
- [ ] لا تزال القنوات متصلة (لا حاجة إلى إعادة الاقتران).
- [ ] تفتح لوحة التحكم وتعرض الجلسات الموجودة.
- [ ] ملفات مساحة العمل (الذاكرة، والإعدادات) موجودة.

## ترقية Plugin في مكانه

تحافظ ترقيات Plugin في مكانها على معرف Plugin نفسه ومفاتيح الإعدادات نفسها، لكنها قد تنقل الحالة الموجودة على القرص إلى التخطيط الحالي. توجد أدلة الترقية الخاصة بكل Plugin إلى جانب قنواتها:

- [ترحيل Matrix](/ar/channels/matrix-migration): حدود استعادة الحالة المشفرة، وسلوك اللقطات التلقائية، وأوامر الاستعادة اليدوية.

## ذو صلة

- [`openclaw migrate`](/ar/cli/migrate): مرجع CLI لعمليات الاستيراد بين الأنظمة.
- [نظرة عامة على التثبيت](/ar/install): كل طرق التثبيت.
- [Doctor](/ar/gateway/doctor): فحص السلامة بعد الترحيل.
- [إلغاء التثبيت](/ar/install/uninstall): إزالة OpenClaw بشكل نظيف.
