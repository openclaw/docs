---
read_when:
    - می‌خواهید عامل‌های Pi از یک کاتالوگ بزرگ ابزارها استفاده کنند، بدون اینکه طرح‌وارهٔ هر ابزار را به پرامپت اضافه کنید
    - می‌خواهید ابزارهای OpenClaw، ابزارهای MCP و ابزارهای کلاینت از طریق یک سطح فشردهٔ PI در دسترس باشند
    - در حال پیاده‌سازی یا اشکال‌زدایی کشف ابزار برای اجراهای PI هستید
summary: 'جست‌وجوی ابزار: فهرست‌های بزرگ ابزارهای PI را پشت جست‌وجو، توصیف و فراخوانی فشرده کنید'
title: جست‌وجوی ابزار
x-i18n:
    generated_at: "2026-05-10T20:13:08Z"
    model: gpt-5.5
    provider: openai
    source_hash: 182b850db5a1d6c9a769d5d50ccae914bc65416c1fd9368f0aeeb43663c0c0ae
    source_path: tools/tool-search.md
    workflow: 16
---

جست‌وجوی ابزار به عامل‌های PI یک راه فشرده برای کشف و فراخوانی کاتالوگ‌های بزرگ ابزار می‌دهد. این قابلیت زمانی مفید است که در اجرا ابزارهای زیادی در دسترس باشند اما مدل احتمالا فقط به چند مورد از آن‌ها نیاز داشته باشد.

وقتی برای PI فعال شود، مدل به‌طور پیش‌فرض یک ابزار `tool_search_code` دریافت می‌کند. این ابزار یک بدنه کوتاه JavaScript را در یک زیرفرایند Node ایزوله با پل `openclaw.tools` اجرا می‌کند:

```js
const hits = await openclaw.tools.search("create a GitHub issue");
const tool = await openclaw.tools.describe(hits[0].id);
return await openclaw.tools.call(tool.id, {
  title: "Crash on startup",
  body: "Steps to reproduce...",
});
```

کاتالوگ می‌تواند شامل ابزارهای OpenClaw، ابزارهای plugin، ابزارهای MCP و ابزارهای ارائه‌شده توسط کلاینت باشد. مدل همه schemaهای کامل را از ابتدا نمی‌بیند. در عوض، descriptorهای فشرده را جست‌وجو می‌کند، وقتی به schema دقیق نیاز دارد یک ابزار انتخاب‌شده را توصیف می‌کند، و آن ابزار را از طریق OpenClaw فراخوانی می‌کند.

اجراهای harness مربوط به Codex این کنترل‌های جست‌وجوی ابزار OpenClaw را دریافت نمی‌کنند. OpenClaw قابلیت‌های محصول را به‌صورت ابزارهای پویا به Codex می‌دهد، و Codex مالک حالت کد بومی، جست‌وجوی ابزار بومی، ابزارهای پویای به‌تعویق‌افتاده، و فراخوانی‌های ابزار تو در تو است.

## یک نوبت چگونه اجرا می‌شود

در زمان برنامه‌ریزی، runner تعبیه‌شده PI کاتالوگ مؤثر را برای اجرا می‌سازد:

1. سیاست ابزار فعال را برای agent، profile، sandbox و session حل می‌کند.
2. ابزارهای واجد شرایط OpenClaw و plugin را فهرست می‌کند.
3. ابزارهای واجد شرایط MCP را از طریق runtime مربوط به MCP در session فهرست می‌کند.
4. ابزارهای واجد شرایط کلاینت را که برای اجرای فعلی ارائه شده‌اند اضافه می‌کند.
5. descriptorهای فشرده را برای جست‌وجو index می‌کند.
6. یا پل کد PI یا ابزارهای جایگزین ساختاریافته را در اختیار مدل قرار می‌دهد.

در زمان اجرا، هر فراخوانی واقعی ابزار به OpenClaw برمی‌گردد. runtime ایزوله Node پیاده‌سازی‌های plugin، اشیای کلاینت MCP یا secrets را نگه نمی‌دارد. `openclaw.tools.call(...)` از پل عبور می‌کند و به Gateway برمی‌گردد؛ جایی که سیاست، approval، hook، logging و پردازش نتیجه معمول همچنان اعمال می‌شوند.

## حالت‌ها

`tools.toolSearch` دو حالت قابل مشاهده برای مدل دارد:

- `code`: ابزار `tool_search_code` را نمایش می‌دهد؛ پل فشرده پیش‌فرض JavaScript.
- `tools`: ابزارهای `tool_search`، `tool_describe` و `tool_call` را به‌عنوان ابزارهای ساختاریافته ساده برای providerهایی نمایش می‌دهد که نباید کد دریافت کنند.

هر دو حالت از یک کاتالوگ و مسیر اجرا استفاده می‌کنند. تنها تفاوت، شکلی است که مدل می‌بیند. اگر runtime فعلی نتواند زیرفرایند ایزوله Node برای حالت کد را اجرا کند، حالت پیش‌فرض `code` پیش از فشرده‌سازی کاتالوگ به `tools` fallback می‌کند.

پیکربندی جداگانه‌ای برای انتخاب منبع وجود ندارد. وقتی جست‌وجوی ابزار فعال باشد، کاتالوگ پس از filtering معمول سیاست شامل ابزارهای واجد شرایط OpenClaw، MCP و کلاینت می‌شود.

## چرا این وجود دارد

کاتالوگ‌های بزرگ مفید اما پرهزینه‌اند. ارسال هر schema ابزار به مدل، درخواست را بزرگ‌تر می‌کند، برنامه‌ریزی را کند می‌کند، و انتخاب تصادفی ابزار را افزایش می‌دهد.

جست‌وجوی ابزار شکل را تغییر می‌دهد:

- ابزارهای مستقیم: مدل پیش از اولین token هر schema انتخاب‌شده را می‌بیند
- حالت کد جست‌وجوی ابزار: مدل یک ابزار کد فشرده و یک قرارداد کوتاه API را می‌بیند
- حالت ابزارهای جست‌وجوی ابزار: مدل سه ابزار جایگزین ساختاریافته فشرده را می‌بیند
- در طول نوبت: مدل فقط schemaهای ابزاری را بارگذاری می‌کند که واقعا به آن‌ها نیاز دارد

نمایش مستقیم ابزارها همچنان پیش‌فرض درست برای کاتالوگ‌های کوچک است. جست‌وجوی ابزار زمانی بهترین گزینه است که یک اجرا بتواند ابزارهای زیادی را ببیند، به‌ویژه از سرورهای MCP یا ابزارهای app ارائه‌شده توسط کلاینت.

## API

`openclaw.tools.search(query, options?)`

کاتالوگ مؤثر اجرای فعلی را جست‌وجو می‌کند. نتایج فشرده و امن‌اند و می‌توان آن‌ها را دوباره در context مربوط به prompt قرار داد.

```js
const hits = await openclaw.tools.search("calendar event", { limit: 5 });
```

`openclaw.tools.describe(id)`

metadata کامل یک نتیجه جست‌وجو، از جمله schema دقیق ورودی، را بارگذاری می‌کند.

```js
const calendarCreate = await openclaw.tools.describe("mcp:calendar:create_event");
```

`openclaw.tools.call(id, args)`

یک ابزار انتخاب‌شده را از طریق OpenClaw فراخوانی می‌کند.

```js
await openclaw.tools.call(calendarCreate.id, {
  summary: "Planning",
  start: "2026-05-09T14:00:00Z",
});
```

حالت جایگزین ساختاریافته همان عملیات را به‌صورت ابزارها ارائه می‌کند:

- `tool_search`
- `tool_describe`
- `tool_call`

## مرز runtime

پل کد در یک زیرفرایند کوتاه‌عمر Node اجرا می‌شود. زیرفرایند با حالت permission مربوط به Node فعال، محیط خالی، بدون مجوز filesystem یا network، و بدون مجوز child-process یا worker شروع می‌شود. OpenClaw یک timeout دیواری در فرایند والد اعمال می‌کند و در صورت timeout، زیرفرایند را می‌کشد؛ حتی پس از ادامه‌های async.

runtime فقط این موارد را ارائه می‌کند:

- `console.log`، `console.warn` و `console.error`
- `openclaw.tools.search`
- `openclaw.tools.describe`
- `openclaw.tools.call`

رفتار معمول OpenClaw همچنان برای فراخوانی‌های نهایی اعمال می‌شود:

- سیاست‌های allow و deny ابزار
- محدودیت‌های ابزار برای هر agent و هر sandbox
- gating فقط برای مالک
- hookهای approval
- hookهای `before_tool_call` مربوط به plugin
- هویت session، logs و telemetry

## پیکربندی

جست‌وجوی ابزار را برای اجراهای PI با پل کد پیش‌فرض فعال کنید:

```bash
openclaw config set tools.toolSearch true
```

JSON معادل:

```json5
{
  tools: {
    toolSearch: true,
  },
}
```

به‌جای آن، برای اجراهای PI از ابزارهای جایگزین ساختاریافته استفاده کنید:

```json5
{
  tools: {
    toolSearch: {
      mode: "tools",
    },
  },
}
```

timeout حالت کد و limitهای نتیجه جست‌وجو را تنظیم کنید:

```json5
{
  tools: {
    toolSearch: {
      mode: "code",
      codeTimeoutMs: 10000,
      searchDefaultLimit: 8,
      maxSearchLimit: 20,
    },
  },
}
```

آن را غیرفعال کنید:

```json5
{
  tools: {
    toolSearch: false,
  },
}
```

## Prompt و telemetry

جست‌وجوی ابزار telemetry کافی ثبت می‌کند تا بتوان آن را با نمایش مستقیم ابزارها مقایسه کرد:

- مجموع byteهای serialized ابزار و prompt ارسال‌شده به harness
- اندازه کاتالوگ و breakdown منبع
- شمار جست‌وجو، توصیف و فراخوانی
- فراخوانی‌های نهایی ابزار که از طریق OpenClaw اجرا شده‌اند
- idها و منابع ابزارهای انتخاب‌شده

logهای session باید امکان پاسخ به این موارد را فراهم کنند:

- مدل از ابتدا چند schema ابزار را دید
- چند عملیات search و describe انجام داد
- کدام ابزار نهایی فراخوانی شد
- آیا نتیجه از OpenClaw، MCP یا یک ابزار کلاینت آمده است

## اعتبارسنجی E2E

runner مربوط به E2E در gateway هر دو مسیر را با harness مربوط به PI اثبات می‌کند:

```bash
node --import tsx scripts/tool-search-gateway-e2e.ts
```

این runner یک plugin جعلی موقت با یک کاتالوگ بزرگ ابزار ایجاد می‌کند، provider ساختگی OpenAI را شروع می‌کند، یک Gateway را یک‌بار در حالت مستقیم و یک‌بار با جست‌وجوی ابزار فعال اجرا می‌کند، سپس payloadهای درخواست provider و logهای session را مقایسه می‌کند.

این regression اثبات می‌کند:

1. حالت مستقیم می‌تواند ابزار plugin جعلی را فراخوانی کند.
2. جست‌وجوی ابزار می‌تواند همان ابزار plugin جعلی را فراخوانی کند.
3. حالت مستقیم schemaهای ابزار plugin جعلی را مستقیما در اختیار provider قرار می‌دهد.
4. جست‌وجوی ابزار فقط پل فشرده را نمایش می‌دهد.
5. payload درخواست جست‌وجوی ابزار برای کاتالوگ جعلی بزرگ کوچک‌تر است.
6. logهای session شمار مورد انتظار فراخوانی ابزار و telemetry فراخوانی bridge شده را نشان می‌دهند.

## رفتار در شکست

جست‌وجوی ابزار باید بسته شکست بخورد:

- اگر ابزاری در policy مؤثر نباشد، search نباید آن را برگرداند
- اگر یک ابزار انتخاب‌شده از دسترس خارج شود، `tool_call` باید شکست بخورد
- اگر policy یا approval اجرا را مسدود کند، نتیجه فراخوانی باید به‌جای دور زدن آن، همان block را گزارش کند
- اگر پل کد نتواند یک runtime ایزوله ایجاد کند، از `mode: "tools"` استفاده کنید یا جست‌وجوی ابزار را برای آن deployment غیرفعال کنید

## مرتبط

- [ابزارها و pluginها](/fa/tools)
- [sandbox و ابزارهای چند-agent](/fa/tools/multi-agent-sandbox-tools)
- [ابزار Exec](/fa/tools/exec)
- [راه‌اندازی agentهای ACP](/fa/tools/acp-agents-setup)
- [ساخت pluginها](/fa/plugins/building-plugins)
