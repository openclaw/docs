---
read_when:
    - کار روی کنترل‌های تله‌متری / حریم خصوصی
    - پرسش‌هایی دربارهٔ اینکه چه داده‌هایی جمع‌آوری می‌شود
summary: دورآزمایی نصب که توسط CLI ClawHub جمع‌آوری می‌شود و روش انصراف از آن.
x-i18n:
    generated_at: "2026-06-28T05:08:14Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# تله‌متری

ClawHub از حداقل تله‌متری CLI برای محاسبهٔ شمارش‌های تجمیعی نصب استفاده می‌کند.

## تله‌متری چه زمانی جمع‌آوری می‌شود

تله‌متری فقط زمانی ارسال می‌شود که:

- در CLI وارد شده باشید.
- `clawhub install <slug>` را اجرا کنید.
- تله‌متری **غیرفعال نشده باشد** (بخش «نحوهٔ غیرفعال‌سازی» را در پایین ببینید).

اگر وارد نشده باشید، هیچ چیزی گزارش نمی‌شود.

## چه چیزهایی را جمع‌آوری می‌کنیم

در هر `clawhub install` گزارش‌شده، CLI یک رویداد نصب را تا حد امکان ارسال می‌کند.

این رویداد شامل موارد زیر است:

- `slug`: اسلاگ Skills نصب‌شده.
- `version`: نسخهٔ نصب‌شده، در صورت مشخص بودن.

### چه چیزهایی را جمع‌آوری _نمی‌کنیم_

- هیچ مسیر پوشه یا شناسهٔ مشتق‌شده از پوشه‌ای.
- هیچ محتوای فایلی.
- هیچ لاگ مربوط به هر اجرا، پرامپت، یا خروجی دیگر CLI.

## شمارش‌های نصب

ClawHub شمارنده‌های تجمیعی را برای هر Skills نگه می‌دارد:

- `installsAllTime`: کاربران یکتایی که دست‌کم یک نصب CLI را برای آن Skills گزارش کرده‌اند.
- `installsCurrent`: کاربران یکتایی که یک نصب را گزارش کرده‌اند و تله‌متری خود را حذف نکرده‌اند.

## شفافیت + کنترل‌های کاربر

همه فقط **شمارنده‌های تجمیعی نصب** را می‌بینند.

حذف حساب شما داده‌های تله‌متری شما را نیز حذف می‌کند.

## نحوهٔ غیرفعال‌سازی تله‌متری

متغیر محیطی را تنظیم کنید:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

با این تنظیم، CLI تله‌متری نصب را ارسال نخواهد کرد.
