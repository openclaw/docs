---
read_when:
    - می‌خواهید از هارنس app-server همراه Codex استفاده کنید
    - به نمونه‌های پیکربندی هارنس Codex نیاز دارید
    - می‌خواهید استقرارهای فقط Codex به‌جای بازگشت به PI با شکست مواجه شوند
summary: نوبت‌های عامل تعبیه‌شدهٔ OpenClaw را از طریق هارنس app-server همراه Codex اجرا کنید
title: چارچوب اجرایی Codex
x-i18n:
    generated_at: "2026-05-01T11:51:04Z"
    model: gpt-5.5
    provider: openai
    source_hash: 1e6f25b0e12a5a46f01e4837fa23df900eb0d62845c068d4a2d6298ace2d0449
    source_path: plugins/codex-harness.md
    workflow: 16
---

Plugin همراه `codex` به OpenClaw اجازه می‌دهد نوبت‌های عامل تعبیه‌شده را به‌جای هارنس PI داخلی از طریق سرور برنامه Codex اجرا کند.

وقتی می‌خواهید Codex مالک نشست سطح‌پایین عامل باشد از این استفاده کنید: کشف مدل، ازسرگیری بومی رشته، Compaction بومی، و اجرای سرور برنامه. OpenClaw همچنان مالک کانال‌های گفتگو، فایل‌های نشست، انتخاب مدل، ابزارها، تأییدها، تحویل رسانه، و آینه رونوشت قابل مشاهده است.

اگر می‌خواهید جهت‌گیری اولیه پیدا کنید، از
[زمان‌های اجرای عامل](/fa/concepts/agent-runtimes) شروع کنید. نسخه کوتاه این است:
`openai/gpt-5.5` ارجاع مدل است، `codex` زمان اجرا است، و Telegram،
Discord، Slack، یا کانالی دیگر سطح ارتباطی باقی می‌ماند.

## پیکربندی سریع

برای استفاده از هارنس Codex برای نوبت‌های عامل GPT، ارجاع مدل را به‌شکل متعارف
`openai/gpt-*` نگه دارید، Plugin همراه `codex` را فعال کنید، و
`agentRuntime.id: "codex"` را تنظیم کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
      },
    },
  },
  agents: {
    defaults: {
      model: "openai/gpt-5.5",
      agentRuntime: {
        id: "codex",
        fallback: "none",
      },
    },
  },
}
```

اگر پیکربندی شما از `plugins.allow` استفاده می‌کند، `codex` را آنجا هم قرار دهید:

```json5
{
  plugins: {
    allow: ["codex"],
    entries: {
      codex: {
        enabled: true,
      },
    },
  },
}
```

برای این مسیر از `openai-codex/gpt-*` استفاده نکنید. این کار OAuth مربوط به Codex را از طریق اجراکننده عادی PI انتخاب می‌کند، مگر اینکه جداگانه یک زمان اجرا را اجباری کنید. تغییرات پیکربندی روی نشست‌های جدید یا بازنشانی‌شده اعمال می‌شوند؛ نشست‌های موجود زمان اجرای ثبت‌شده خود را نگه می‌دارند.

## این Plugin چه چیزی را تغییر می‌دهد

Plugin همراه `codex` چند قابلیت جداگانه فراهم می‌کند:

| قابلیت | نحوه استفاده | کاری که انجام می‌دهد |
| --------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------- |
| زمان اجرای تعبیه‌شده بومی | `agentRuntime.id: "codex"` | نوبت‌های عامل تعبیه‌شده OpenClaw را از طریق سرور برنامه Codex اجرا می‌کند. |
| فرمان‌های بومی کنترل گفتگو | `/codex bind`, `/codex resume`, `/codex steer`, ... | رشته‌های سرور برنامه Codex را از یک مکالمه پیام‌رسانی متصل و کنترل می‌کند. |
| ارائه‌دهنده/کاتالوگ سرور برنامه Codex | درونیات `codex`، عرضه‌شده از طریق هارنس | به زمان اجرا اجازه می‌دهد مدل‌های سرور برنامه را کشف و اعتبارسنجی کند. |
| مسیر درک رسانه Codex | مسیرهای سازگاری مدل تصویر `codex/*` | نوبت‌های محدود سرور برنامه Codex را برای مدل‌های پشتیبانی‌شده درک تصویر اجرا می‌کند. |
| بازپخش هوک بومی | هوک‌های Plugin پیرامون رویدادهای بومی Codex | به OpenClaw اجازه می‌دهد رویدادهای ابزار/نهایی‌سازی بومی Codex پشتیبانی‌شده را مشاهده یا مسدود کند. |

فعال‌سازی Plugin این قابلیت‌ها را در دسترس قرار می‌دهد. این کار:

- استفاده از Codex برای هر مدل OpenAI را شروع نمی‌کند
- ارجاع‌های مدل `openai-codex/*` را به زمان اجرای بومی تبدیل نمی‌کند
- ACP/acpx را به مسیر پیش‌فرض Codex تبدیل نمی‌کند
- نشست‌های موجودی را که از قبل یک زمان اجرای PI ثبت کرده‌اند به‌صورت داغ عوض نمی‌کند
- تحویل کانال OpenClaw، فایل‌های نشست، ذخیره‌سازی نمایه احراز هویت، یا
  مسیریابی پیام را جایگزین نمی‌کند

همین Plugin مالک سطح فرمان کنترل گفتگوی بومی `/codex` هم هست. اگر Plugin فعال باشد و کاربر بخواهد رشته‌های Codex را از گفتگو متصل، ازسرگیری، هدایت، متوقف، یا بررسی کند، عامل‌ها باید `/codex ...` را به ACP ترجیح دهند. ACP زمانی که کاربر ACP/acpx را درخواست می‌کند یا در حال آزمودن سازگارکننده Codex مربوط به ACP است، جایگزین صریح باقی می‌ماند.

نوبت‌های بومی Codex هوک‌های Plugin OpenClaw را به‌عنوان لایه سازگاری عمومی نگه می‌دارند. این‌ها هوک‌های درون‌فرایندی OpenClaw هستند، نه هوک‌های فرمان Codex `hooks.json`:

- `before_prompt_build`
- `before_compaction`, `after_compaction`
- `llm_input`, `llm_output`
- `before_tool_call`, `after_tool_call`
- `before_message_write` برای رکوردهای رونوشت آینه‌شده
- `before_agent_finalize` از طریق بازپخش `Stop` در Codex
- `agent_end`

Pluginها همچنین می‌توانند میان‌افزار نتیجه ابزار مستقل از زمان اجرا ثبت کنند تا پس از اجرای ابزار توسط OpenClaw و پیش از بازگرداندن نتیجه به Codex، نتایج ابزار پویای OpenClaw را بازنویسی کنند. این با هوک عمومی Plugin به نام
`tool_result_persist` جدا است، که نوشتن نتایج ابزار در رونوشت‌های تحت مالکیت OpenClaw را تبدیل می‌کند.

برای معناشناسی خود هوک‌های Plugin، [هوک‌های Plugin](/fa/plugins/hooks)
و [رفتار نگهبان Plugin](/fa/tools/plugin) را ببینید.

هارنس به‌طور پیش‌فرض خاموش است. پیکربندی‌های جدید باید ارجاع‌های مدل OpenAI را به‌شکل متعارف `openai/gpt-*` نگه دارند و وقتی اجرای بومی سرور برنامه را می‌خواهند، به‌صراحت
`agentRuntime.id: "codex"` یا `OPENCLAW_AGENT_RUNTIME=codex` را اجباری کنند. ارجاع‌های مدل قدیمی `codex/*` همچنان برای سازگاری به‌طور خودکار هارنس را انتخاب می‌کنند، اما پیشوندهای قدیمی ارائه‌دهنده مبتنی بر زمان اجرا به‌عنوان انتخاب‌های عادی مدل/ارائه‌دهنده نشان داده نمی‌شوند.

اگر Plugin `codex` فعال باشد اما مدل اصلی همچنان
`openai-codex/*` باشد، `openclaw doctor` به‌جای تغییر مسیر هشدار می‌دهد. این عمدی است: `openai-codex/*` مسیر OAuth/اشتراک Codex در PI باقی می‌ماند، و اجرای بومی سرور برنامه یک انتخاب صریح زمان اجرا می‌ماند.

## نقشه مسیر

پیش از تغییر پیکربندی از این جدول استفاده کنید:

| رفتار مطلوب | ارجاع مدل | پیکربندی زمان اجرا | نیازمندی Plugin | برچسب وضعیت مورد انتظار |
| ------------------------------------------- | -------------------------- | -------------------------------------- | --------------------------- | ------------------------------ |
| API مربوط به OpenAI از طریق اجراکننده عادی OpenClaw | `openai/gpt-*` | حذف‌شده یا `runtime: "pi"` | ارائه‌دهنده OpenAI | `Runtime: OpenClaw Pi Default` |
| OAuth/اشتراک Codex از طریق PI | `openai-codex/gpt-*` | حذف‌شده یا `runtime: "pi"` | ارائه‌دهنده OAuth مربوط به OpenAI Codex | `Runtime: OpenClaw Pi Default` |
| نوبت‌های تعبیه‌شده بومی سرور برنامه Codex | `openai/gpt-*` | `agentRuntime.id: "codex"` | Plugin `codex` | `Runtime: OpenAI Codex` |
| ارائه‌دهندگان ترکیبی با حالت خودکار محافظه‌کارانه | ارجاع‌های مخصوص ارائه‌دهنده | `agentRuntime.id: "auto"` | زمان‌های اجرای Plugin اختیاری | به زمان اجرای انتخاب‌شده بستگی دارد |
| نشست صریح سازگارکننده ACP مربوط به Codex | وابسته به اعلان/مدل ACP | `sessions_spawn` با `runtime: "acp"` | پشتانه سالم `acpx` | وضعیت کار/نشست ACP |

تفکیک مهم بین ارائه‌دهنده و زمان اجرا است:

- `openai-codex/*` پاسخ می‌دهد «PI باید از کدام مسیر ارائه‌دهنده/احراز هویت استفاده کند؟»
- `agentRuntime.id: "codex"` پاسخ می‌دهد «کدام حلقه باید این
  نوبت تعبیه‌شده را اجرا کند؟»
- `/codex ...` پاسخ می‌دهد «این گفتگو باید به کدام مکالمه بومی Codex متصل شود
  یا آن را کنترل کند؟»
- ACP پاسخ می‌دهد «acpx باید کدام فرایند هارنس خارجی را راه‌اندازی کند؟»

## پیشوند مدل درست را انتخاب کنید

مسیرهای خانواده OpenAI به پیشوند حساس هستند. وقتی OAuth مربوط به Codex را از طریق PI می‌خواهید از `openai-codex/*` استفاده کنید؛ وقتی دسترسی مستقیم API مربوط به OpenAI را می‌خواهید یا هارنس بومی سرور برنامه Codex را اجباری می‌کنید، از `openai/*` استفاده کنید:

| ارجاع مدل | مسیر زمان اجرا | زمان استفاده |
| --------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------- |
| `openai/gpt-5.4` | ارائه‌دهنده OpenAI از طریق لوله‌کشی OpenClaw/PI | دسترسی فعلی مستقیم به API پلتفرم OpenAI را با `OPENAI_API_KEY` می‌خواهید. |
| `openai-codex/gpt-5.5` | OAuth مربوط به OpenAI Codex از طریق OpenClaw/PI | احراز هویت اشتراک ChatGPT/Codex را با اجراکننده پیش‌فرض PI می‌خواهید. |
| `openai/gpt-5.5` + `agentRuntime.id: "codex"` | هارنس سرور برنامه Codex | اجرای بومی سرور برنامه Codex را برای نوبت عامل تعبیه‌شده می‌خواهید. |

GPT-5.5 در حال حاضر در OpenClaw فقط با اشتراک/OAuth در دسترس است. برای OAuth در PI از
`openai-codex/gpt-5.5` استفاده کنید، یا از `openai/gpt-5.5` همراه با هارنس سرور برنامه Codex استفاده کنید. دسترسی مستقیم با کلید API برای `openai/gpt-5.5` وقتی پشتیبانی می‌شود که OpenAI، GPT-5.5 را روی API عمومی فعال کند.

ارجاع‌های قدیمی `codex/gpt-*` همچنان به‌عنوان نام مستعار سازگاری پذیرفته می‌شوند. مهاجرت سازگاری Doctor ارجاع‌های قدیمی زمان اجرای اصلی را به ارجاع‌های متعارف مدل بازنویسی می‌کند و سیاست زمان اجرا را جداگانه ثبت می‌کند، در حالی که ارجاع‌های قدیمی فقط-جایگزین بدون تغییر باقی می‌مانند، چون زمان اجرا برای کل ظرف عامل پیکربندی می‌شود. پیکربندی‌های جدید OAuth مربوط به PI Codex باید از `openai-codex/gpt-*` استفاده کنند؛ پیکربندی‌های جدید هارنس بومی سرور برنامه باید از `openai/gpt-*` به‌همراه
`agentRuntime.id: "codex"` استفاده کنند.

`agents.defaults.imageModel` از همان تفکیک پیشوند پیروی می‌کند. وقتی درک تصویر باید از مسیر ارائه‌دهنده OAuth مربوط به OpenAI Codex اجرا شود، از
`openai-codex/gpt-*` استفاده کنید. وقتی درک تصویر باید از طریق یک نوبت محدود سرور برنامه Codex اجرا شود، از `codex/gpt-*` استفاده کنید. مدل سرور برنامه Codex باید پشتیبانی از ورودی تصویر را اعلام کند؛ مدل‌های فقط-متن Codex پیش از شروع نوبت رسانه شکست می‌خورند.

برای تأیید هارنس مؤثر برای نشست فعلی از `/status` استفاده کنید. اگر انتخاب غیرمنتظره است، ثبت اشکال‌زدایی را برای زیرسامانه `agents/harness` فعال کنید و رکورد ساختاریافته `agent harness selected` در Gateway را بررسی کنید. این رکورد شامل شناسه هارنس انتخاب‌شده، دلیل انتخاب، سیاست زمان اجرا/جایگزین، و در حالت `auto`، نتیجه پشتیبانی هر نامزد Plugin است.

### هشدارهای Doctor به چه معنا هستند

`openclaw doctor` وقتی همه موارد زیر درست باشند هشدار می‌دهد:

- Plugin همراه `codex` فعال یا مجاز است
- مدل اصلی یک عامل `openai-codex/*` است
- زمان اجرای مؤثر آن عامل `codex` نیست

این هشدار وجود دارد چون کاربران اغلب انتظار دارند «Plugin مربوط به Codex فعال است» به معنای «زمان اجرای بومی سرور برنامه Codex» باشد. OpenClaw چنین پرشی انجام نمی‌دهد. معنی هشدار این است:

- اگر قصد شما OAuth مربوط به ChatGPT/Codex از طریق PI بوده است، **هیچ تغییری لازم نیست**.
- اگر قصد شما اجرای بومی سرور برنامه بوده است، مدل را به `openai/<model>` تغییر دهید و
  `agentRuntime.id: "codex"` را تنظیم کنید.
- نشست‌های موجود پس از تغییر زمان اجرا همچنان به `/new` یا `/reset` نیاز دارند،
  چون پین‌های زمان اجرای نشست چسبنده هستند.

انتخاب هارنس کنترل زنده نشست نیست. وقتی یک نوبت تعبیه‌شده اجرا می‌شود، OpenClaw شناسه هارنس انتخاب‌شده را روی آن نشست ثبت می‌کند و برای نوبت‌های بعدی در همان شناسه نشست از همان استفاده می‌کند. وقتی می‌خواهید نشست‌های آینده از هارنس دیگری استفاده کنند، پیکربندی `agentRuntime` یا
`OPENCLAW_AGENT_RUNTIME` را تغییر دهید؛ برای شروع یک نشست تازه پیش از جابه‌جایی یک مکالمه موجود بین PI و Codex، از `/new` یا `/reset` استفاده کنید. این کار از بازپخش یک رونوشت از طریق دو سامانه نشست بومی ناسازگار جلوگیری می‌کند.

نشست‌های قدیمی ایجادشده پیش از پین‌های هارنس، پس از داشتن تاریخچه رونوشت، به‌عنوان پین‌شده به PI در نظر گرفته می‌شوند. برای وارد کردن آن مکالمه به Codex پس از تغییر پیکربندی، از `/new` یا `/reset` استفاده کنید.

`/status` زمان اجرای مؤثر مدل را نشان می‌دهد. هارنس پیش‌فرض PI به‌صورت
`Runtime: OpenClaw Pi Default` ظاهر می‌شود، و هارنس سرور برنامه Codex به‌صورت
`Runtime: OpenAI Codex` ظاهر می‌شود.

## نیازمندی‌ها

- OpenClaw با Plugin همراه `codex` در دسترس.
- سرور برنامه Codex نسخه `0.125.0` یا جدیدتر. Plugin همراه به‌طور پیش‌فرض یک باینری سازگار سرور برنامه Codex را مدیریت می‌کند، بنابراین فرمان‌های محلی `codex` در `PATH` بر راه‌اندازی عادی هارنس اثر نمی‌گذارند.
- احراز هویت Codex در دسترس فرایند سرور برنامه یا پل احراز هویت Codex در OpenClaw باشد. راه‌اندازی‌های محلی سرور برنامه stdio برای هر عامل از خانه Codex مدیریت‌شده توسط OpenClaw و یک `HOME` فرزند ایزوله استفاده می‌کنند، بنابراین به‌طور پیش‌فرض حساب شخصی
  `~/.codex`، Skills، Pluginها، پیکربندی، وضعیت رشته، یا
  `$HOME/.agents/skills` بومی شما را نمی‌خوانند.

Plugin دست‌دهی‌های قدیمی‌تر یا بدون نسخه سرور برنامه را مسدود می‌کند. این کار OpenClaw را روی سطح پروتکلی نگه می‌دارد که در برابر آن آزموده شده است.

برای آزمون‌های دود زنده و Docker، احراز هویت معمولاً از حساب CLI مربوط به Codex یا یک نمایه احراز هویت `openai-codex` در OpenClaw می‌آید. راه‌اندازی‌های محلی سرور برنامه stdio همچنین وقتی حسابی وجود ندارد می‌توانند به `CODEX_API_KEY` / `OPENAI_API_KEY` برگردند.

## Codex را کنار مدل‌های دیگر اضافه کنید

`agentRuntime.id: "codex"` را به‌صورت سراسری تنظیم نکنید اگر همان عامل باید بتواند آزادانه
بین مدل‌های ارائه‌دهنده Codex و غیر Codex جابه‌جا شود. یک runtime اجباری روی همه
نوبت‌های توکار برای آن عامل یا نشست اعمال می‌شود. اگر در حالی که آن runtime اجباری است
یک مدل Anthropic را انتخاب کنید، OpenClaw همچنان harness مربوط به Codex را امتحان می‌کند و به‌جای
مسیریابی بی‌سروصدا آن نوبت از طریق PI، به‌صورت بسته شکست می‌خورد.

به‌جای آن از یکی از این شکل‌ها استفاده کنید:

- Codex را روی یک عامل اختصاصی با `agentRuntime.id: "codex"` قرار دهید.
- عامل پیش‌فرض را روی `agentRuntime.id: "auto"` و fallback به PI برای استفاده معمول ترکیبی از
  ارائه‌دهندگان نگه دارید.
- از ارجاع‌های قدیمی `codex/*` فقط برای سازگاری استفاده کنید. پیکربندی‌های جدید باید
  `openai/*` را همراه با یک خط‌مشی runtime صریح برای Codex ترجیح دهند.

برای مثال، این مورد عامل پیش‌فرض را روی انتخاب خودکار معمول نگه می‌دارد و
یک عامل Codex جداگانه اضافه می‌کند:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
      },
    },
  },
  agents: {
    defaults: {
      agentRuntime: {
        id: "auto",
        fallback: "pi",
      },
    },
    list: [
      {
        id: "main",
        default: true,
        model: "anthropic/claude-opus-4-6",
      },
      {
        id: "codex",
        name: "Codex",
        model: "openai/gpt-5.5",
        agentRuntime: {
          id: "codex",
        },
      },
    ],
  },
}
```

با این شکل:

- عامل پیش‌فرض `main` از مسیر معمول ارائه‌دهنده و fallback سازگاری PI استفاده می‌کند.
- عامل `codex` از harness سرور برنامه Codex استفاده می‌کند.
- اگر Codex برای عامل `codex` وجود نداشته باشد یا پشتیبانی نشود، نوبت شکست می‌خورد
  به‌جای اینکه بی‌سروصدا از PI استفاده کند.

## مسیریابی فرمان عامل

عامل‌ها باید درخواست‌های کاربر را بر اساس نیت مسیریابی کنند، نه فقط بر اساس واژه "Codex":

| کاربر درخواست می‌کند...                                  | عامل باید استفاده کند از...                      |
| -------------------------------------------------------- | ------------------------------------------------ |
| "این گفتگو را به Codex متصل کن"                          | `/codex bind`                                    |
| "رشته Codex با شناسه `<id>` را اینجا ازسر بگیر"          | `/codex resume <id>`                             |
| "رشته‌های Codex را نشان بده"                             | `/codex threads`                                 |
| "برای یک اجرای بد Codex یک گزارش پشتیبانی ثبت کن"        | `/diagnostics [note]`                            |
| "فقط برای این رشته پیوست‌شده بازخورد Codex بفرست"        | `/codex diagnostics [note]`                      |
| "از Codex به‌عنوان runtime برای این عامل استفاده کن"      | تغییر پیکربندی به `agentRuntime.id`              |
| "از اشتراک ChatGPT/Codex من با OpenClaw معمولی استفاده کن" | ارجاع‌های مدل `openai-codex/*`                   |
| "Codex را از طریق ACP/acpx اجرا کن"                      | ACP `sessions_spawn({ runtime: "acp", ... })`    |
| "Claude Code/Gemini/OpenCode/Cursor را در یک رشته شروع کن" | ACP/acpx، نه `/codex` و نه زیرعامل‌های بومی      |

OpenClaw فقط زمانی راهنمایی spawn مربوط به ACP را برای عامل‌ها تبلیغ می‌کند که ACP فعال،
قابل dispatch، و توسط یک backend runtime بارگذاری‌شده پشتیبانی شده باشد. اگر ACP در دسترس نباشد،
system prompt و Skills مربوط به Plugin نباید به عامل درباره مسیریابی ACP آموزش دهند.

## استقرارهای فقط Codex

وقتی لازم است ثابت کنید که هر نوبت عامل توکار از Codex استفاده می‌کند، harness مربوط به Codex را اجباری کنید.
runtimeهای صریح Plugin به‌صورت پیش‌فرض fallback به PI ندارند، بنابراین
`fallback: "none"` اختیاری است اما اغلب به‌عنوان مستندات مفید است:

```json5
{
  agents: {
    defaults: {
      model: "openai/gpt-5.5",
      agentRuntime: {
        id: "codex",
        fallback: "none",
      },
    },
  },
}
```

نادیده‌گیری محیط:

```bash
OPENCLAW_AGENT_RUNTIME=codex openclaw gateway run
```

وقتی Codex اجباری باشد، اگر Plugin مربوط به Codex غیرفعال باشد، سرور برنامه
بیش از حد قدیمی باشد، یا سرور برنامه نتواند شروع شود، OpenClaw زود شکست می‌خورد. فقط زمانی
`OPENCLAW_AGENT_HARNESS_FALLBACK=pi` را تنظیم کنید که عمداً می‌خواهید PI انتخاب harness
ناموجود را مدیریت کند.

## Codex به‌ازای هر عامل

می‌توانید یک عامل را فقط Codex کنید در حالی که عامل پیش‌فرض انتخاب خودکار
معمول را نگه می‌دارد:

```json5
{
  agents: {
    defaults: {
      agentRuntime: {
        id: "auto",
        fallback: "pi",
      },
    },
    list: [
      {
        id: "main",
        default: true,
        model: "anthropic/claude-opus-4-6",
      },
      {
        id: "codex",
        name: "Codex",
        model: "openai/gpt-5.5",
        agentRuntime: {
          id: "codex",
          fallback: "none",
        },
      },
    ],
  },
}
```

از فرمان‌های معمول نشست برای جابه‌جایی عامل‌ها و مدل‌ها استفاده کنید. `/new` یک نشست تازه
OpenClaw ایجاد می‌کند و harness مربوط به Codex در صورت نیاز رشته سرور برنامه sidecar خود را ایجاد
یا ازسرگیری می‌کند. `/reset` اتصال نشست OpenClaw را برای آن رشته پاک می‌کند
و اجازه می‌دهد نوبت بعدی دوباره harness را از پیکربندی فعلی resolve کند.

## کشف مدل

به‌صورت پیش‌فرض، Plugin مربوط به Codex از سرور برنامه مدل‌های موجود را می‌پرسد. اگر
کشف شکست بخورد یا timeout شود، از یک کاتالوگ fallback همراه‌شده برای موارد زیر استفاده می‌کند:

- GPT-5.5
- GPT-5.4 mini
- GPT-5.2

می‌توانید کشف را زیر `plugins.entries.codex.config.discovery` تنظیم کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          discovery: {
            enabled: true,
            timeoutMs: 2500,
          },
        },
      },
    },
  },
}
```

وقتی می‌خواهید راه‌اندازی از probe کردن Codex اجتناب کند و به کاتالوگ
fallback پایبند بماند، کشف را غیرفعال کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          discovery: {
            enabled: false,
          },
        },
      },
    },
  },
}
```

## اتصال و خط‌مشی سرور برنامه

به‌صورت پیش‌فرض، Plugin باینری مدیریت‌شده Codex در OpenClaw را به‌صورت محلی با این فرمان شروع می‌کند:

```bash
codex app-server --listen stdio://
```

باینری مدیریت‌شده به‌عنوان یک وابستگی runtime همراه‌شده Plugin اعلام شده و
همراه با بقیه وابستگی‌های Plugin مربوط به `codex` آماده‌سازی می‌شود. این کار نسخه سرور برنامه را
به Plugin همراه‌شده گره می‌زند، نه به هر Codex CLI جداگانه‌ای که اتفاقاً
به‌صورت محلی نصب شده است. `appServer.command` را فقط زمانی تنظیم کنید که
عمداً می‌خواهید یک فایل اجرایی متفاوت را اجرا کنید.

به‌صورت پیش‌فرض، OpenClaw نشست‌های محلی harness مربوط به Codex را در حالت YOLO شروع می‌کند:
`approvalPolicy: "never"`، `approvalsReviewer: "user"`، و
`sandbox: "danger-full-access"`. این وضعیت اپراتور محلی مورد اعتماد است که
برای Heartbeatهای خودمختار استفاده می‌شود: Codex می‌تواند از ابزارهای shell و network بدون
توقف روی promptهای تأیید بومی که کسی برای پاسخ دادن به آن‌ها حضور ندارد استفاده کند.

برای انتخاب تأییدهای بازبینی‌شده توسط guardian در Codex، `appServer.mode:
"guardian"` را تنظیم کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          appServer: {
            mode: "guardian",
            serviceTier: "fast",
          },
        },
      },
    },
  },
}
```

حالت Guardian از مسیر تأیید auto-review بومی Codex استفاده می‌کند. وقتی Codex درخواست می‌کند
از sandbox خارج شود، خارج از workspace بنویسد، یا مجوزهایی مانند دسترسی network
اضافه کند، Codex آن درخواست تأیید را به‌جای prompt انسانی به بازبین بومی
مسیریابی می‌کند. بازبین چارچوب ریسک Codex را اعمال می‌کند و درخواست مشخص را تأیید یا رد
می‌کند. وقتی guardrailهای بیشتری نسبت به حالت YOLO می‌خواهید اما همچنان لازم است عامل‌های بی‌ناظر
پیشرفت کنند، از Guardian استفاده کنید.

preset مربوط به `guardian` به `approvalPolicy: "on-request"`،
`approvalsReviewer: "auto_review"`، و `sandbox: "workspace-write"` گسترش می‌یابد.
فیلدهای خط‌مشی منفرد همچنان `mode` را override می‌کنند، بنابراین استقرارهای پیشرفته می‌توانند
preset را با انتخاب‌های صریح ترکیب کنند. مقدار بازبین قدیمی‌تر `guardian_subagent`
همچنان به‌عنوان alias سازگاری پذیرفته می‌شود، اما پیکربندی‌های جدید باید از
`auto_review` استفاده کنند.

برای یک سرور برنامه از قبل در حال اجرا، از انتقال WebSocket استفاده کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          appServer: {
            transport: "websocket",
            url: "ws://127.0.0.1:39175",
            authToken: "${CODEX_APP_SERVER_TOKEN}",
            requestTimeoutMs: 60000,
          },
        },
      },
    },
  },
}
```

راه‌اندازی‌های سرور برنامه stdio به‌صورت پیش‌فرض محیط پردازه OpenClaw را به ارث می‌برند،
اما OpenClaw پل حساب سرور برنامه Codex را مالکیت می‌کند و هر دو
`CODEX_HOME` و `HOME` را به دایرکتوری‌های به‌ازای هر عامل زیر state مربوط به OpenClaw آن عامل
تنظیم می‌کند. loader مربوط به skill خود Codex از `$CODEX_HOME/skills` و
`$HOME/.agents/skills` می‌خواند، بنابراین هر دو مقدار برای راه‌اندازی‌های محلی سرور برنامه
ایزوله هستند. این کار skillهای بومی Codex، plugins، config، حساب‌ها، و state رشته را
به عامل OpenClaw محدود می‌کند، به‌جای اینکه از خانه شخصی Codex CLI اپراتور
نشت کنند.

Pluginهای OpenClaw و snapshotهای skill مربوط به OpenClaw همچنان از طریق registry مربوط به Plugin و
loader مربوط به skill خود OpenClaw جریان پیدا می‌کنند. دارایی‌های شخصی Codex CLI این‌طور نیستند. اگر
skillها یا plugins مفیدی در Codex CLI دارید که باید بخشی از یک عامل OpenClaw شوند،
آن‌ها را صریحاً inventory کنید:

```bash
openclaw migrate codex --dry-run
openclaw migrate apply codex --yes
```

provider مهاجرت Codex، skillها را در workspace عامل فعلی OpenClaw کپی می‌کند.
plugins بومی Codex، hookها، و فایل‌های config به‌جای فعال‌سازی خودکار، گزارش یا archive
می‌شوند تا به‌صورت دستی بازبینی شوند، زیرا می‌توانند
فرمان‌ها را اجرا کنند، سرورهای MCP را آشکار کنند، یا credentials داشته باشند.

Auth به این ترتیب انتخاب می‌شود:

1. یک profile صریح auth مربوط به OpenClaw Codex برای عامل.
2. حساب موجود سرور برنامه در خانه Codex آن عامل.
3. فقط برای راه‌اندازی‌های محلی سرور برنامه stdio، `CODEX_API_KEY`، سپس
   `OPENAI_API_KEY`، زمانی که هیچ حساب سرور برنامه‌ای وجود ندارد و auth مربوط به OpenAI
   همچنان لازم است.

وقتی OpenClaw یک profile auth مربوط به Codex از نوع اشتراک ChatGPT را می‌بیند، آن
`CODEX_API_KEY` و `OPENAI_API_KEY` را از پردازه فرزند Codex ایجادشده حذف می‌کند. این
کلیدهای API در سطح Gateway را برای embeddings یا مدل‌های مستقیم OpenAI در دسترس نگه می‌دارد
بدون اینکه نوبت‌های بومی سرور برنامه Codex به‌طور تصادفی از طریق API صورت‌حساب شوند.
profileهای صریح کلید API مربوط به Codex و fallback کلید محیط محلی stdio به‌جای env پردازه فرزند ارث‌بری‌شده،
از login سرور برنامه استفاده می‌کنند. اتصال‌های سرور برنامه WebSocket
fallback کلید API محیط Gateway را دریافت نمی‌کنند؛ از یک profile auth صریح یا
حساب خود سرور برنامه remote استفاده کنید.

اگر یک استقرار به ایزوله‌سازی محیطی بیشتری نیاز دارد، آن متغیرها را به
`appServer.clearEnv` اضافه کنید:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          appServer: {
            clearEnv: ["CODEX_API_KEY", "OPENAI_API_KEY"],
          },
        },
      },
    },
  },
}
```

`appServer.clearEnv` فقط روی پردازه فرزند سرور برنامه Codex ایجادشده اثر می‌گذارد.

ابزارهای dynamic در Codex به‌صورت پیش‌فرض از profile مربوط به `native-first` استفاده می‌کنند. در آن حالت،
OpenClaw ابزارهای dynamic را که عملیات workspace بومی Codex را تکرار می‌کنند
در معرض قرار نمی‌دهد: `read`، `write`، `edit`، `apply_patch`، `exec`، `process`، و
`update_plan`. ابزارهای یکپارچه‌سازی OpenClaw مانند messaging، sessions، media،
cron، browser، nodes، gateway، و `web_search` همچنان در دسترس می‌مانند.

فیلدهای سطح بالای پشتیبانی‌شده Plugin مربوط به Codex:

| فیلد                       | پیش‌فرض         | معنی                                                                                       |
| -------------------------- | ---------------- | ----------------------------------------------------------------------------------------- |
| `codexDynamicToolsProfile` | `"native-first"` | از `"openclaw-compat"` استفاده کنید تا مجموعه کامل ابزارهای dynamic مربوط به OpenClaw در اختیار سرور برنامه Codex قرار گیرد. |
| `codexDynamicToolsExclude` | `[]`             | نام‌های اضافی ابزار dynamic مربوط به OpenClaw که باید از نوبت‌های سرور برنامه Codex حذف شوند. |

فیلدهای پشتیبانی‌شده `appServer`:

| فیلد               | پیش‌فرض                                  | معنا                                                                                                                                                                                                                              |
| ------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `transport`         | `"stdio"`                                | `"stdio"`، Codex را اجرا می‌کند؛ `"websocket"` به `url` وصل می‌شود.                                                                                                                                                                             |
| `command`           | باینری مدیریت‌شده Codex                     | فایل اجرایی برای انتقال stdio. برای استفاده از باینری مدیریت‌شده آن را تنظیم‌نشده بگذارید؛ فقط برای بازنویسی صریح آن را تنظیم کنید.                                                                                                                         |
| `args`              | `["app-server", "--listen", "stdio://"]` | آرگومان‌های انتقال stdio.                                                                                                                                                                                                       |
| `url`               | تنظیم‌نشده                                    | نشانی app-server WebSocket.                                                                                                                                                                                                            |
| `authToken`         | تنظیم‌نشده                                    | توکن Bearer برای انتقال WebSocket.                                                                                                                                                                                                |
| `headers`           | `{}`                                     | سرآیندهای اضافی WebSocket.                                                                                                                                                                                                             |
| `clearEnv`          | `[]`                                     | نام‌های اضافی متغیرهای محیطی که پس از ساخت محیط ارث‌بری‌شده توسط OpenClaw، از فرایند app-server stdio اجراشده حذف می‌شوند. `CODEX_HOME` و `HOME` برای جداسازی Codex به‌ازای هر عامل در OpenClaw هنگام اجراهای محلی رزرو شده‌اند. |
| `requestTimeoutMs`  | `60000`                                  | مهلت زمانی برای فراخوانی‌های کنترل‌پلین app-server.                                                                                                                                                                                          |
| `mode`              | `"yolo"`                                 | پیش‌تنظیم برای اجرای YOLO یا اجرای بازبینی‌شده توسط guardian.                                                                                                                                                                                      |
| `approvalPolicy`    | `"never"`                                | سیاست تأیید بومی Codex که به شروع/ازسرگیری/نوبت نخ ارسال می‌شود.                                                                                                                                                                       |
| `sandbox`           | `"danger-full-access"`                   | حالت sandbox بومی Codex که به شروع/ازسرگیری نخ ارسال می‌شود.                                                                                                                                                                               |
| `approvalsReviewer` | `"user"`                                 | از `"auto_review"` استفاده کنید تا Codex اعلان‌های تأیید بومی را بازبینی کند. `guardian_subagent` همچنان یک نام مستعار قدیمی است.                                                                                                                         |
| `serviceTier`       | تنظیم‌نشده                                    | رده سرویس اختیاری app-server Codex: `"fast"`، `"flex"` یا `null`. مقادیر قدیمی نامعتبر نادیده گرفته می‌شوند.                                                                                                                            |

فراخوانی‌های ابزار پویا که متعلق به OpenClaw هستند مستقل از
`appServer.requestTimeoutMs` محدود می‌شوند: هر درخواست Codex `item/tool/call` باید
در ۳۰ ثانیه یک پاسخ OpenClaw دریافت کند. هنگام پایان مهلت، OpenClaw در صورت
پشتیبانی سیگنال ابزار را لغو می‌کند و یک پاسخ ابزار پویای ناموفق به Codex
برمی‌گرداند تا نوبت بتواند ادامه یابد، به‌جای اینکه نشست در حالت `processing`
باقی بماند.

پس از اینکه OpenClaw به یک درخواست app-server در محدوده نوبت Codex پاسخ می‌دهد، harness
همچنین انتظار دارد Codex نوبت بومی را با `turn/completed` تمام کند. اگر
app-server پس از آن پاسخ به مدت ۶۰ ثانیه ساکت بماند، OpenClaw به بهترین شکل ممکن
نوبت Codex را interrupt می‌کند، یک timeout تشخیصی ثبت می‌کند، و مسیر نشست
OpenClaw را آزاد می‌کند تا پیام‌های چت بعدی پشت یک نوبت بومی stale در صف نمانند.

بازنویسی‌های محیطی برای آزمون محلی همچنان در دسترس هستند:

- `OPENCLAW_CODEX_APP_SERVER_BIN`
- `OPENCLAW_CODEX_APP_SERVER_ARGS`
- `OPENCLAW_CODEX_APP_SERVER_MODE=yolo|guardian`
- `OPENCLAW_CODEX_APP_SERVER_APPROVAL_POLICY`
- `OPENCLAW_CODEX_APP_SERVER_SANDBOX`

`OPENCLAW_CODEX_APP_SERVER_BIN` وقتی `appServer.command` تنظیم نشده باشد،
باینری مدیریت‌شده را دور می‌زند.

`OPENCLAW_CODEX_APP_SERVER_GUARDIAN=1` حذف شده است. به‌جای آن از
`plugins.entries.codex.config.appServer.mode: "guardian"` استفاده کنید، یا برای
آزمون محلی یک‌باره از `OPENCLAW_CODEX_APP_SERVER_MODE=guardian` استفاده کنید. پیکربندی
برای استقرارهای تکرارپذیر ترجیح داده می‌شود، زیرا رفتار Plugin را در همان فایل
بازبینی‌شده‌ای نگه می‌دارد که بقیه راه‌اندازی harness Codex در آن قرار دارد.

## استفاده از رایانه

استفاده از رایانه در راهنمای راه‌اندازی خودش پوشش داده شده است:
[استفاده از رایانه در Codex](/fa/plugins/codex-computer-use).

خلاصه کوتاه: OpenClaw برنامه کنترل دسکتاپ را vendor نمی‌کند و خودش
اقدام‌های دسکتاپ را اجرا نمی‌کند. app-server Codex را آماده می‌کند، بررسی می‌کند که
سرور MCP `computer-use` در دسترس باشد، و سپس اجازه می‌دهد Codex فراخوانی‌های ابزار
MCP بومی را در طول نوبت‌های حالت Codex مدیریت کند.

برای دسترسی مستقیم به driver TryCua خارج از جریان marketplace Codex،
`cua-driver mcp` را با `openclaw mcp set cua-driver '{"command":"cua-driver","args":["mcp"]}'` ثبت کنید.
برای تمایز بین استفاده از رایانه متعلق به Codex و ثبت مستقیم MCP، به
[استفاده از رایانه در Codex](/fa/plugins/codex-computer-use) مراجعه کنید.

پیکربندی حداقلی:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          computerUse: {
            autoInstall: true,
          },
        },
      },
    },
  },
  agents: {
    defaults: {
      model: "openai/gpt-5.5",
      agentRuntime: {
        id: "codex",
        fallback: "none",
      },
    },
  },
}
```

راه‌اندازی را می‌توان از سطح فرمان بررسی یا نصب کرد:

- `/codex computer-use status`
- `/codex computer-use install`
- `/codex computer-use install --source <marketplace-source>`
- `/codex computer-use install --marketplace-path <path>`

استفاده از رایانه مخصوص macOS است و ممکن است پیش از اینکه سرور MCP Codex بتواند
برنامه‌ها را کنترل کند، به مجوزهای محلی سیستم‌عامل نیاز داشته باشد. اگر
`computerUse.enabled` برابر true باشد و سرور MCP در دسترس نباشد، نوبت‌های حالت
Codex پیش از شروع نخ شکست می‌خورند، به‌جای اینکه بی‌صدا بدون ابزارهای بومی
استفاده از رایانه اجرا شوند. برای گزینه‌های marketplace، محدودیت‌های کاتالوگ
راه‌دور، دلایل وضعیت، و عیب‌یابی، به
[استفاده از رایانه در Codex](/fa/plugins/codex-computer-use) مراجعه کنید.

وقتی `computerUse.autoInstall` برابر true باشد، اگر Codex هنوز marketplace محلی
را کشف نکرده باشد، OpenClaw می‌تواند marketplace استاندارد دسکتاپ Codex همراه‌شده را
از `/Applications/Codex.app/Contents/Resources/plugins/openai-bundled` ثبت کند.
پس از تغییر پیکربندی runtime یا استفاده از رایانه، از `/new` یا `/reset` استفاده کنید
تا نشست‌های موجود یک اتصال نخ PI یا Codex قدیمی را نگه ندارند.

## دستورالعمل‌های رایج

Codex محلی با انتقال stdio پیش‌فرض:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
      },
    },
  },
}
```

اعتبارسنجی harness فقط Codex:

```json5
{
  agents: {
    defaults: {
      model: "openai/gpt-5.5",
      agentRuntime: {
        id: "codex",
      },
    },
  },
  plugins: {
    entries: {
      codex: {
        enabled: true,
      },
    },
  },
}
```

تأییدهای Codex بازبینی‌شده توسط guardian:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          appServer: {
            mode: "guardian",
            approvalPolicy: "on-request",
            approvalsReviewer: "auto_review",
            sandbox: "workspace-write",
          },
        },
      },
    },
  },
}
```

app-server راه‌دور با سرآیندهای صریح:

```json5
{
  plugins: {
    entries: {
      codex: {
        enabled: true,
        config: {
          appServer: {
            transport: "websocket",
            url: "ws://gateway-host:39175",
            headers: {
              "X-OpenClaw-Agent": "main",
            },
          },
        },
      },
    },
  },
}
```

تعویض مدل تحت کنترل OpenClaw باقی می‌ماند. وقتی یک نشست OpenClaw به یک نخ
موجود Codex متصل است، نوبت بعدی مدل OpenAI، provider، سیاست تأیید، sandbox، و رده
سرویس انتخاب‌شده فعلی را دوباره به app-server ارسال می‌کند. تعویض از
`openai/gpt-5.5` به `openai/gpt-5.2` اتصال نخ را حفظ می‌کند، اما از Codex می‌خواهد
با مدل تازه انتخاب‌شده ادامه دهد.

## فرمان Codex

Plugin همراه‌شده، `/codex` را به‌عنوان یک فرمان slash مجاز ثبت می‌کند. این فرمان
عمومی است و روی هر کانالی که از فرمان‌های متنی OpenClaw پشتیبانی کند کار می‌کند.

شکل‌های رایج:

- `/codex status` اتصال زنده app-server، مدل‌ها، حساب، محدودیت‌های نرخ، سرورهای MCP، و skills را نشان می‌دهد.
- `/codex models` مدل‌های زنده app-server Codex را فهرست می‌کند.
- `/codex threads [filter]` نخ‌های اخیر Codex را فهرست می‌کند.
- `/codex resume <thread-id>` نشست فعلی OpenClaw را به یک نخ موجود Codex متصل می‌کند.
- `/codex compact` از app-server Codex می‌خواهد نخ متصل را compact کند.
- `/codex review` بازبینی بومی Codex را برای نخ متصل شروع می‌کند.
- `/codex diagnostics [note]` پیش از ارسال بازخورد تشخیصی Codex برای نخ متصل درخواست تأیید می‌کند.
- `/codex computer-use status` Plugin پیکربندی‌شده استفاده از رایانه و سرور MCP را بررسی می‌کند.
- `/codex computer-use install` Plugin پیکربندی‌شده استفاده از رایانه را نصب می‌کند و سرورهای MCP را دوباره بارگذاری می‌کند.
- `/codex account` وضعیت حساب و محدودیت نرخ را نشان می‌دهد.
- `/codex mcp` وضعیت سرور MCP app-server Codex را فهرست می‌کند.
- `/codex skills` skills app-server Codex را فهرست می‌کند.

### گردش‌کار رایج عیب‌یابی

وقتی یک عامل پشتیبانی‌شده با Codex در Telegram، Discord، Slack،
یا کانال دیگری کاری غیرمنتظره انجام می‌دهد، از گفت‌وگویی شروع کنید که مشکل در آن رخ داده است:

1. `/diagnostics bad tool choice after image upload` یا یادداشت کوتاه دیگری را اجرا کنید
   که آنچه دیده‌اید را توصیف می‌کند.
2. درخواست تشخیص را یک‌بار تأیید کنید. این تأیید فایل zip تشخیصی محلی Gateway را
   ایجاد می‌کند و، چون نشست از harness Codex استفاده می‌کند، بسته بازخورد مرتبط
   Codex را نیز به سرورهای OpenAI ارسال می‌کند.
3. پاسخ تکمیل‌شده تشخیص را در گزارش اشکال یا نخ پشتیبانی کپی کنید.
   این پاسخ شامل مسیر بسته محلی، خلاصه حریم خصوصی، شناسه‌های نشست OpenClaw،
   شناسه‌های نخ Codex، و یک خط `Inspect locally` برای هر نخ Codex است.
4. اگر می‌خواهید خودتان اجرا را debug کنید، فرمان چاپ‌شده `Inspect locally`
   را در یک ترمینال اجرا کنید. این فرمان شبیه `codex resume <thread-id>` است و
   نخ بومی Codex را باز می‌کند تا بتوانید گفت‌وگو را بررسی کنید، آن را به‌صورت محلی ادامه دهید،
   یا از Codex بپرسید چرا ابزار یا برنامه مشخصی را انتخاب کرده است.

از `/codex diagnostics [note]` فقط زمانی استفاده کنید که به‌طور مشخص آپلود بازخورد Codex را برای رشته‌ی فعلاً پیوست‌شده می‌خواهید، بدون بسته‌ی کامل تشخیص Gateway در OpenClaw. برای بیشتر گزارش‌های پشتیبانی، `/diagnostics [note]` نقطه‌ی شروع بهتری است، چون وضعیت Gateway محلی و شناسه‌های رشته‌ی Codex را در یک پاسخ به هم وصل می‌کند. برای مدل کامل حریم خصوصی و رفتار در گفت‌وگوی گروهی، [خروجی تشخیص](/fa/gateway/diagnostics) را ببینید.

هسته‌ی OpenClaw همچنین `/diagnostics [note]` فقط برای مالک را به‌عنوان فرمان عمومی تشخیص Gateway ارائه می‌کند. اعلان تأیید آن مقدمه‌ی داده‌های حساس را نشان می‌دهد، به [خروجی تشخیص](/fa/gateway/diagnostics) پیوند می‌دهد، و هر بار از طریق تأیید صریح اجرا، درخواست `openclaw gateway diagnostics export --json` می‌کند. تشخیص را با قاعده‌ی اجازه‌ی همه‌جانبه تأیید نکنید. پس از تأیید، OpenClaw گزارشی قابل جای‌گذاری همراه با مسیر بسته‌ی محلی و خلاصه‌ی manifest می‌فرستد. وقتی نشست فعال OpenClaw از هارنس Codex استفاده می‌کند، همان تأیید همچنین اجازه می‌دهد بسته‌های بازخورد مرتبط Codex به سرورهای OpenAI ارسال شوند. اعلان تأیید می‌گوید که بازخورد Codex ارسال خواهد شد، اما پیش از تأیید، شناسه‌های نشست یا رشته‌ی Codex را فهرست نمی‌کند.

اگر `/diagnostics` توسط یک مالک در گفت‌وگوی گروهی فراخوانی شود، OpenClaw کانال مشترک را تمیز نگه می‌دارد: گروه فقط یک اعلان کوتاه دریافت می‌کند، در حالی که مقدمه‌ی تشخیص، اعلان‌های تأیید، و شناسه‌های نشست/رشته‌ی Codex از طریق مسیر خصوصی تأیید برای مالک ارسال می‌شوند. اگر مسیر خصوصی مالک وجود نداشته باشد، OpenClaw درخواست گروه را رد می‌کند و از مالک می‌خواهد آن را از یک پیام مستقیم اجرا کند.

آپلود تأییدشده‌ی Codex، `feedback/upload` در app-server مربوط به Codex را فراخوانی می‌کند و از app-server می‌خواهد در صورت موجود بودن، گزارش‌ها را برای هر رشته‌ی فهرست‌شده و زیررشته‌های Codex ایجادشده شامل کند. آپلود از مسیر عادی بازخورد Codex به سرورهای OpenAI عبور می‌کند؛ اگر بازخورد Codex در آن app-server غیرفعال باشد، فرمان خطای app-server را برمی‌گرداند. پاسخ کامل‌شده‌ی تشخیص، کانال‌ها، شناسه‌های نشست OpenClaw، شناسه‌های رشته‌ی Codex، و فرمان‌های محلی `codex resume <thread-id>` را برای رشته‌هایی که ارسال شده‌اند فهرست می‌کند. اگر تأیید را رد یا نادیده بگیرید، OpenClaw آن شناسه‌های Codex را چاپ نمی‌کند. این آپلود جایگزین خروجی تشخیص محلی Gateway نمی‌شود.

`/codex resume` همان فایل اتصال جانبی را می‌نویسد که هارنس برای نوبت‌های عادی استفاده می‌کند. در پیام بعدی، OpenClaw همان رشته‌ی Codex را از سر می‌گیرد، مدل فعلاً انتخاب‌شده‌ی OpenClaw را به app-server می‌فرستد، و تاریخچه‌ی گسترده را فعال نگه می‌دارد.

### بررسی یک رشته‌ی Codex از CLI

سریع‌ترین راه برای فهمیدن یک اجرای بد Codex اغلب این است که رشته‌ی بومی Codex را مستقیماً باز کنید:

```sh
codex resume <thread-id>
```

از این زمانی استفاده کنید که در یک مکالمه‌ی کانالی متوجه خطایی می‌شوید و می‌خواهید نشست مشکل‌دار Codex را بررسی کنید، آن را به‌صورت محلی ادامه دهید، یا از Codex بپرسید چرا یک انتخاب خاص برای ابزار یا استدلال انجام داده است. ساده‌ترین مسیر معمولاً این است که ابتدا `/diagnostics [note]` را اجرا کنید: پس از تأیید، گزارش کامل‌شده هر رشته‌ی Codex را فهرست می‌کند و یک فرمان `Inspect locally` چاپ می‌کند، برای مثال `codex resume <thread-id>`. می‌توانید آن فرمان را مستقیماً در ترمینال وارد کنید.

همچنین می‌توانید شناسه‌ی رشته را از `/codex binding` برای گفت‌وگوی فعلی یا از `/codex threads [filter]` برای رشته‌های اخیر app-server مربوط به Codex بگیرید، سپس همان فرمان `codex resume` را در shell خود اجرا کنید.

سطح فرمان به app-server مربوط به Codex نسخه‌ی `0.125.0` یا جدیدتر نیاز دارد. اگر یک app-server سفارشی یا آینده آن متد JSON-RPC را ارائه نکند، متدهای کنترلی جداگانه به‌صورت `unsupported by this Codex app-server` گزارش می‌شوند.

## مرزهای hook

هارنس Codex سه لایه‌ی hook دارد:

| لایه                                  | مالک                     | هدف                                                                 |
| ------------------------------------- | ------------------------ | ------------------------------------------------------------------- |
| hookهای Plugin در OpenClaw            | OpenClaw                 | سازگاری محصول/Plugin در هارنس‌های PI و Codex.                       |
| میان‌افزار افزونه‌ی app-server در Codex | Pluginهای همراه OpenClaw | رفتار آداپتور در هر نوبت پیرامون ابزارهای پویای OpenClaw.          |
| hookهای بومی Codex                    | Codex                    | چرخه‌ی حیات سطح پایین Codex و سیاست ابزار بومی از پیکربندی Codex.  |

OpenClaw از فایل‌های `hooks.json` پروژه یا سراسری Codex برای مسیردهی رفتار Pluginهای OpenClaw استفاده نمی‌کند. برای پل پشتیبانی‌شده‌ی ابزار بومی و مجوز، OpenClaw پیکربندی Codex را به‌ازای هر رشته برای `PreToolUse`، `PostToolUse`، `PermissionRequest`، و `Stop` تزریق می‌کند. hookهای دیگر Codex مانند `SessionStart` و `UserPromptSubmit` کنترل‌های سطح Codex باقی می‌مانند؛ آن‌ها در قرارداد v1 به‌عنوان hookهای Plugin در OpenClaw ارائه نمی‌شوند.

برای ابزارهای پویای OpenClaw، OpenClaw پس از درخواست فراخوانی توسط Codex ابزار را اجرا می‌کند، بنابراین OpenClaw رفتار Plugin و میان‌افزاری را که مالک آن است در آداپتور هارنس اجرا می‌کند. برای ابزارهای بومی Codex، خود Codex مالک رکورد رسمی ابزار است. OpenClaw می‌تواند رویدادهای منتخب را بازتاب دهد، اما نمی‌تواند رشته‌ی بومی Codex را بازنویسی کند، مگر آنکه Codex آن عملیات را از طریق app-server یا callbackهای hook بومی ارائه کند.

پرتاب‌های چرخه‌ی حیات Compaction و LLM از اعلان‌های app-server مربوط به Codex و وضعیت آداپتور OpenClaw می‌آیند، نه از فرمان‌های hook بومی Codex. رویدادهای `before_compaction`، `after_compaction`، `llm_input`، و `llm_output` در OpenClaw مشاهده‌های سطح آداپتور هستند، نه برداشت‌های بایت‌به‌بایت از درخواست داخلی یا payloadهای Compaction در Codex.

اعلان‌های app-server مربوط به hookهای بومی `hook/started` و `hook/completed` در Codex، به‌عنوان رویدادهای agent با نام `codex_app_server.hook` برای مسیر اجرا و اشکال‌زدایی بازتاب داده می‌شوند. آن‌ها hookهای Plugin در OpenClaw را فراخوانی نمی‌کنند.

## قرارداد پشتیبانی V1

حالت Codex همان PI با یک فراخوانی مدل متفاوت در زیر آن نیست. Codex بخش بیشتری از حلقه‌ی مدل بومی را مالک است، و OpenClaw سطوح Plugin و نشست خود را پیرامون آن مرز تطبیق می‌دهد.

پشتیبانی‌شده در runtime نسخه‌ی v1 مربوط به Codex:

| سطح                                          | پشتیبانی                                | دلیل                                                                                                                                                                                                 |
| -------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| حلقه‌ی مدل OpenAI از طریق Codex              | پشتیبانی‌شده                            | app-server مربوط به Codex مالک نوبت OpenAI، ازسرگیری رشته‌ی بومی، و ادامه‌ی ابزار بومی است.                                                                                                          |
| مسیردهی و تحویل کانال در OpenClaw            | پشتیبانی‌شده                            | Telegram، Discord، Slack، WhatsApp، iMessage، و کانال‌های دیگر بیرون از runtime مدل می‌مانند.                                                                                                        |
| ابزارهای پویای OpenClaw                      | پشتیبانی‌شده                            | Codex از OpenClaw می‌خواهد این ابزارها را اجرا کند، بنابراین OpenClaw در مسیر اجرا باقی می‌ماند.                                                                                                    |
| Pluginهای prompt و زمینه                     | پشتیبانی‌شده                            | OpenClaw overlayهای prompt را می‌سازد و پیش از شروع یا ازسرگیری رشته، زمینه را به نوبت Codex پرتاب می‌کند.                                                                                           |
| چرخه‌ی حیات موتور زمینه                      | پشتیبانی‌شده                            | مونتاژ، دریافت یا نگهداری پس از نوبت، و هماهنگی Compaction موتور زمینه برای نوبت‌های Codex اجرا می‌شوند.                                                                                            |
| hookهای ابزار پویا                           | پشتیبانی‌شده                            | `before_tool_call`، `after_tool_call`، و میان‌افزار نتیجه‌ی ابزار پیرامون ابزارهای پویای تحت مالکیت OpenClaw اجرا می‌شوند.                                                                           |
| hookهای چرخه‌ی حیات                          | به‌عنوان مشاهده‌های آداپتور پشتیبانی‌شده | `llm_input`، `llm_output`، `agent_end`، `before_compaction`، و `after_compaction` با payloadهای صادقانه‌ی حالت Codex اجرا می‌شوند.                                                                    |
| دروازه‌ی بازنگری پاسخ نهایی                  | از طریق رله‌ی hook بومی پشتیبانی‌شده    | `Stop` در Codex به `before_agent_finalize` رله می‌شود؛ `revise` از Codex یک گذر مدل دیگر پیش از نهایی‌سازی می‌خواهد.                                                                                |
| مسدودسازی یا مشاهده‌ی shell، patch، و MCP بومی | از طریق رله‌ی hook بومی پشتیبانی‌شده    | `PreToolUse` و `PostToolUse` در Codex برای سطوح ابزار بومی commit‌شده، از جمله payloadهای MCP روی app-server مربوط به Codex نسخه‌ی `0.125.0` یا جدیدتر، رله می‌شوند. مسدودسازی پشتیبانی می‌شود؛ بازنویسی آرگومان پشتیبانی نمی‌شود. |
| سیاست مجوز بومی                              | از طریق رله‌ی hook بومی پشتیبانی‌شده    | `PermissionRequest` در Codex می‌تواند در جایی که runtime آن را ارائه می‌کند از طریق سیاست OpenClaw مسیردهی شود. اگر OpenClaw تصمیمی برنگرداند، Codex از مسیر عادی guardian یا تأیید کاربر ادامه می‌دهد. |
| ثبت مسیر اجرای app-server                    | پشتیبانی‌شده                            | OpenClaw درخواستی را که به app-server فرستاده و اعلان‌هایی را که از app-server دریافت می‌کند ثبت می‌کند.                                                                                            |

پشتیبانی‌نشده در runtime نسخه‌ی v1 مربوط به Codex:

| سطح | مرز V1 | مسیر آینده |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| تغییر آرگومان ابزار بومی | قلاب‌های پیشاابزار بومی Codex می‌توانند مسدود کنند، اما OpenClaw آرگومان‌های ابزار بومی Codex را بازنویسی نمی‌کند. | به پشتیبانی قلاب/طرحواره Codex برای ورودی جایگزین ابزار نیاز دارد. |
| تاریخچه قابل ویرایش رونوشت بومی Codex | Codex مالک تاریخچه رشته بومی متعارف است. OpenClaw مالک یک آینه است و می‌تواند زمینه آینده را فرافکنی کند، اما نباید اجزای داخلی پشتیبانی‌نشده را تغییر دهد. | اگر جراحی رشته بومی لازم باشد، APIهای صریح سرور برنامه Codex اضافه شود. |
| `tool_result_persist` برای رکوردهای ابزار بومی Codex | آن قلاب نوشتن‌های رونوشت متعلق به OpenClaw را تبدیل می‌کند، نه رکوردهای ابزار بومی Codex. | می‌توان رکوردهای تبدیل‌شده را آینه کرد، اما بازنویسی متعارف به پشتیبانی Codex نیاز دارد. |
| فراداده غنی Compaction بومی | OpenClaw شروع و تکمیل Compaction را مشاهده می‌کند، اما فهرست پایدار نگه‌داشته/حذف‌شده، دلتا توکن، یا محتوای خلاصه دریافت نمی‌کند. | به رویدادهای غنی‌تر Compaction در Codex نیاز دارد. |
| مداخله در Compaction | قلاب‌های فعلی Compaction در OpenClaw در حالت Codex در سطح اعلان هستند. | اگر plugins نیاز به وتو یا بازنویسی Compaction بومی دارند، قلاب‌های پیش/پس Compaction در Codex اضافه شود. |
| ضبط درخواست API مدل به‌صورت بایت‌به‌بایت | OpenClaw می‌تواند درخواست‌ها و اعلان‌های سرور برنامه را ضبط کند، اما هسته Codex درخواست نهایی API OpenAI را به‌صورت داخلی می‌سازد. | به یک رویداد ردیابی درخواست مدل Codex یا API اشکال‌زدایی نیاز دارد. |

## ابزارها، رسانه و Compaction

مهار Codex فقط اجراکننده عامل تعبیه‌شده سطح پایین را تغییر می‌دهد.

OpenClaw همچنان فهرست ابزارها را می‌سازد و نتایج ابزار پویا را از مهار دریافت می‌کند. متن، تصاویر، ویدئو، موسیقی، TTS، تأییدها، و خروجی ابزارهای پیام‌رسانی همچنان از مسیر تحویل عادی OpenClaw عبور می‌کنند.

رله قلاب بومی عمداً عمومی است، اما قرارداد پشتیبانی v1 به مسیرهای ابزار و مجوز بومی Codex که OpenClaw آزمایش می‌کند محدود است. در زمان اجرای Codex، این شامل محتوای shell، patch و MCP `PreToolUse`، `PostToolUse` و `PermissionRequest` است. تا زمانی که قرارداد زمان اجرا آن را نام نبرده، فرض نکنید هر رویداد قلاب آینده Codex سطح Plugin در OpenClaw است.

برای `PermissionRequest`، OpenClaw فقط وقتی سیاست تصمیم بگیرد تصمیم‌های صریح اجازه یا رد را برمی‌گرداند. نتیجه بدون تصمیم، اجازه نیست. Codex آن را به‌عنوان نبود تصمیم قلاب در نظر می‌گیرد و به مسیر نگهبان خود یا تأیید کاربر سقوط می‌کند.

درخواست‌های تأیید ابزار MCP در Codex، وقتی Codex مقدار `_meta.codex_approval_kind` را `"mcp_tool_call"` علامت‌گذاری می‌کند، از طریق جریان تأیید Plugin در OpenClaw مسیریابی می‌شوند. اعلان‌های Codex `request_user_input` به چت مبدأ برگردانده می‌شوند، و پیام پیگیری بعدی در صف، به‌جای هدایت شدن به‌عنوان زمینه اضافی، به آن درخواست سرور بومی پاسخ می‌دهد. سایر درخواست‌های فراخوانی MCP همچنان به‌صورت بسته شکست می‌خورند.

هدایت صف اجرای فعال روی `turn/steer` سرور برنامه Codex نگاشت می‌شود. با مقدار پیش‌فرض `messages.queue.mode: "steer"`، OpenClaw پیام‌های چت صف‌شده را برای پنجره سکوت پیکربندی‌شده دسته‌بندی می‌کند و آن‌ها را به‌ترتیب ورود به‌عنوان یک درخواست `turn/steer` می‌فرستد. حالت قدیمی `queue` درخواست‌های جداگانه `turn/steer` می‌فرستد. نوبت‌های بازبینی Codex و Compaction دستی می‌توانند هدایت در همان نوبت را رد کنند، که در این حالت OpenClaw وقتی حالت انتخاب‌شده اجازه جایگزین می‌دهد از صف پیگیری استفاده می‌کند. [صف هدایت](/fa/concepts/queue-steering) را ببینید.

وقتی مدل انتخاب‌شده از مهار Codex استفاده می‌کند، Compaction رشته بومی به سرور برنامه Codex واگذار می‌شود. OpenClaw یک آینه رونوشت برای تاریخچه کانال، جست‌وجو، `/new`، `/reset` و تعویض آینده مدل یا مهار نگه می‌دارد. آینه شامل اعلان کاربر، متن نهایی دستیار، و رکوردهای سبک استدلال یا طرح Codex است وقتی سرور برنامه آن‌ها را منتشر کند. امروز، OpenClaw فقط سیگنال‌های شروع و تکمیل Compaction بومی را ثبت می‌کند. هنوز خلاصه قابل خواندن برای انسان یا فهرست قابل ممیزی از ورودی‌هایی که Codex پس از Compaction نگه داشته است را ارائه نمی‌کند.

چون Codex مالک رشته بومی متعارف است، `tool_result_persist` در حال حاضر رکوردهای نتیجه ابزار بومی Codex را بازنویسی نمی‌کند. فقط وقتی اعمال می‌شود که OpenClaw در حال نوشتن نتیجه ابزار رونوشت نشست متعلق به OpenClaw باشد.

تولید رسانه به PI نیاز ندارد. تصویر، ویدئو، موسیقی، PDF، TTS و درک رسانه همچنان از تنظیمات ارائه‌دهنده/مدل متناظر مانند `agents.defaults.imageGenerationModel`، `videoGenerationModel`، `pdfModel` و `messages.tts` استفاده می‌کنند.

## عیب‌یابی

**Codex به‌عنوان ارائه‌دهنده عادی `/model` ظاهر نمی‌شود:** این برای پیکربندی‌های جدید مورد انتظار است. یک مدل `openai/gpt-*` با `agentRuntime.id: "codex"` (یا یک ارجاع قدیمی `codex/*`) انتخاب کنید، `plugins.entries.codex.enabled` را فعال کنید، و بررسی کنید که آیا `plugins.allow` مقدار `codex` را مستثنی کرده است یا نه.

**OpenClaw به‌جای Codex از PI استفاده می‌کند:** `agentRuntime.id: "auto"` همچنان می‌تواند وقتی هیچ مهار Codex اجرای کار را نمی‌پذیرد، از PI به‌عنوان پس‌زمینه سازگاری استفاده کند. برای اجبار انتخاب Codex هنگام آزمایش، `agentRuntime.id: "codex"` را تنظیم کنید. زمان اجرای اجباری Codex اکنون به‌جای بازگشت به PI شکست می‌خورد مگر اینکه به‌صراحت `agentRuntime.fallback: "pi"` را تنظیم کنید. پس از انتخاب سرور برنامه Codex، شکست‌های آن مستقیماً بدون پیکربندی جایگزین اضافی نمایان می‌شوند.

**سرور برنامه رد می‌شود:** Codex را ارتقا دهید تا دست‌دهی سرور برنامه نسخه `0.125.0` یا جدیدتر را گزارش کند. پیش‌انتشارهای هم‌نسخه یا نسخه‌های دارای پسوند ساخت مانند `0.125.0-alpha.2` یا `0.125.0+custom` رد می‌شوند، چون کف پروتکل پایدار `0.125.0` همان چیزی است که OpenClaw آزمایش می‌کند.

**کشف مدل کند است:** مقدار `plugins.entries.codex.config.discovery.timeoutMs` را کاهش دهید یا کشف را غیرفعال کنید.

**انتقال WebSocket بلافاصله شکست می‌خورد:** `appServer.url`، `authToken` و اینکه سرور برنامه راه‌دور با همان نسخه پروتکل سرور برنامه Codex صحبت می‌کند را بررسی کنید.

**یک مدل غیر Codex از PI استفاده می‌کند:** این مورد انتظار است مگر اینکه برای آن عامل `agentRuntime.id: "codex"` را اجبار کرده باشید یا یک ارجاع قدیمی `codex/*` انتخاب کرده باشید. ارجاع‌های ساده `openai/gpt-*` و سایر ارائه‌دهنده‌ها در حالت `auto` روی مسیر عادی ارائه‌دهنده خود می‌مانند. اگر `agentRuntime.id: "codex"` را اجبار کنید، هر نوبت تعبیه‌شده برای آن عامل باید یک مدل OpenAI پشتیبانی‌شده توسط Codex باشد.

**Computer Use نصب است اما ابزارها اجرا نمی‌شوند:** از یک نشست تازه، `/codex computer-use status` را بررسی کنید. اگر ابزاری `Native hook relay unavailable` گزارش کرد، از `/new` یا `/reset` استفاده کنید؛ اگر ادامه داشت، Gateway را راه‌اندازی مجدد کنید تا ثبت‌نام‌های کهنه قلاب بومی پاک شوند. اگر `computer-use.list_apps` زمان‌بر شد، Codex Computer Use یا Codex Desktop را راه‌اندازی مجدد کنید و دوباره تلاش کنید.

## مرتبط

- [plugins مهار عامل](/fa/plugins/sdk-agent-harness)
- [زمان‌های اجرای عامل](/fa/concepts/agent-runtimes)
- [ارائه‌دهندگان مدل](/fa/concepts/model-providers)
- [ارائه‌دهنده OpenAI](/fa/providers/openai)
- [وضعیت](/fa/cli/status)
- [قلاب‌های Plugin](/fa/plugins/hooks)
- [مرجع پیکربندی](/fa/gateway/configuration-reference)
- [آزمایش](/fa/help/testing-live#live-codex-app-server-harness-smoke)
