---
read_when:
    - راه‌اندازی کنترل دسترسی پیام مستقیم
    - جفت‌سازی یک گره جدید iOS/Android
    - بررسی وضعیت امنیتی OpenClaw
summary: 'مرور کلی جفت‌سازی: تأیید کنید چه کسی می‌تواند به شما DM بدهد + کدام گره‌ها می‌توانند بپیوندند'
title: جفت‌سازی
x-i18n:
    generated_at: "2026-06-27T17:12:55Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 92870489b62aeec710f49ec92908f4b83c7d9ee2ce34174b42e283839748e549
    source_path: channels/pairing.md
    workflow: 16
---

«جفت‌سازی» مرحلهٔ تأیید صریح دسترسی در OpenClaw است.
در دو جا استفاده می‌شود:

1. **جفت‌سازی DM** (چه کسی مجاز است با ربات صحبت کند)
2. **جفت‌سازی Node** (کدام دستگاه‌ها/Nodeها مجازند به شبکهٔ Gateway بپیوندند)

زمینهٔ امنیتی: [امنیت](/fa/gateway/security)

## 1) جفت‌سازی DM (دسترسی چت ورودی)

وقتی یک کانال با سیاست DM برابر با `pairing` پیکربندی شود، فرستنده‌های ناشناس یک کد کوتاه دریافت می‌کنند و پیامشان تا زمانی که تأیید کنید **پردازش نمی‌شود**.

سیاست‌های پیش‌فرض DM در اینجا مستند شده‌اند: [امنیت](/fa/gateway/security)

`dmPolicy: "open"` فقط وقتی عمومی است که فهرست مجاز مؤثر DM شامل `"*"` باشد.
راه‌اندازی و اعتبارسنجی برای پیکربندی‌های عمومی-باز به آن wildcard نیاز دارند. اگر وضعیت موجود
شامل `open` همراه با ورودی‌های مشخص `allowFrom` باشد، runtime همچنان فقط همان
فرستنده‌ها را می‌پذیرد، و تأییدهای ذخیرهٔ جفت‌سازی دسترسی `open` را گسترده‌تر نمی‌کنند.

کدهای جفت‌سازی:

- ۸ نویسه، حروف بزرگ، بدون نویسه‌های مبهم (`0O1I`).
- **پس از ۱ ساعت منقضی می‌شوند**. ربات فقط وقتی پیام جفت‌سازی را می‌فرستد که یک درخواست جدید ساخته شود (تقریباً هر ساعت یک بار برای هر فرستنده).
- درخواست‌های در انتظار جفت‌سازی DM به‌طور پیش‌فرض به **۳ مورد برای هر کانال** محدود می‌شوند؛ درخواست‌های اضافی نادیده گرفته می‌شوند تا یکی منقضی یا تأیید شود.

### تأیید یک فرستنده

```bash
openclaw pairing list telegram
openclaw pairing approve telegram <CODE>
```

اگر هنوز مالک فرمانی پیکربندی نشده باشد، تأیید یک کد جفت‌سازی DM همچنین
`commands.ownerAllowFrom` را با فرستندهٔ تأییدشده راه‌اندازی اولیه می‌کند، مانند `telegram:123456789`.
این کار برای راه‌اندازی‌های نخستین یک مالک صریح برای فرمان‌های دارای امتیاز و درخواست‌های تأیید exec
فراهم می‌کند. پس از وجود یک مالک، تأییدهای جفت‌سازی بعدی فقط دسترسی DM
می‌دهند؛ مالک‌های بیشتری اضافه نمی‌کنند.

کانال‌های پشتیبانی‌شده: `discord`, `feishu`, `googlechat`, `imessage`, `irc`, `line`, `matrix`, `mattermost`, `msteams`, `nextcloud-talk`, `nostr`, `openclaw-weixin`, `signal`, `slack`, `synology-chat`, `telegram`, `twitch`, `whatsapp`, `zalo`, `zalouser`.

### گروه‌های فرستندهٔ قابل استفادهٔ مجدد

وقتی همان مجموعهٔ فرستنده‌های مورد اعتماد باید برای چند کانال پیام یا برای هر دو
فهرست مجاز DM و گروه اعمال شود، از `accessGroups` سطح بالا استفاده کنید.

گروه‌های ایستا از `type: "message.senders"` استفاده می‌کنند و از فهرست‌های مجاز کانال با
`accessGroup:<name>` ارجاع داده می‌شوند:

```json5
{
  accessGroups: {
    operators: {
      type: "message.senders",
      members: {
        discord: ["discord:123456789012345678"],
        telegram: ["987654321"],
        whatsapp: ["+15551234567"],
      },
    },
  },
  channels: {
    telegram: { dmPolicy: "allowlist", allowFrom: ["accessGroup:operators"] },
    whatsapp: { groupPolicy: "allowlist", groupAllowFrom: ["accessGroup:operators"] },
  },
}
```

گروه‌های دسترسی به‌تفصیل در اینجا مستند شده‌اند: [گروه‌های دسترسی](/fa/channels/access-groups)

### وضعیت کجا نگهداری می‌شود

زیر `~/.openclaw/credentials/` ذخیره می‌شود:

- درخواست‌های در انتظار: `<channel>-pairing.json`
- ذخیرهٔ فهرست مجاز تأییدشده:
  - حساب پیش‌فرض: `<channel>-allowFrom.json`
  - حساب غیرپیش‌فرض: `<channel>-<accountId>-allowFrom.json`

رفتار محدوده‌بندی حساب:

- حساب‌های غیرپیش‌فرض فقط فایل فهرست مجاز محدوده‌بندی‌شدهٔ خودشان را می‌خوانند/می‌نویسند.
- حساب پیش‌فرض از فایل فهرست مجاز بدون محدودهٔ کانال استفاده می‌کند.

با این موارد به‌عنوان دادهٔ حساس رفتار کنید (دسترسی به دستیار شما را کنترل می‌کنند).

<Note>
ذخیرهٔ فهرست مجاز جفت‌سازی برای دسترسی DM است. مجوزدهی گروه جداگانه است.
تأیید یک کد جفت‌سازی DM به‌طور خودکار به آن فرستنده اجازه نمی‌دهد فرمان‌های گروهی
را اجرا کند یا ربات را در گروه‌ها کنترل کند. راه‌اندازی اولیهٔ مالک نخستین وضعیت پیکربندی
جداگانه‌ای در `commands.ownerAllowFrom` است، و تحویل چت گروهی همچنان از فهرست‌های مجاز
گروهی کانال پیروی می‌کند (برای مثال `groupAllowFrom`، `groups`، یا overrideهای هر گروه
یا هر موضوع، بسته به کانال).
</Note>

## 2) جفت‌سازی دستگاه Node (iOS/Android/macOS/Nodeهای headless)

Nodeها به‌عنوان **دستگاه** با `role: node` به Gateway متصل می‌شوند. Gateway
یک درخواست جفت‌سازی دستگاه ایجاد می‌کند که باید تأیید شود.

### جفت‌سازی از طریق Telegram (برای iOS توصیه می‌شود)

اگر از Plugin `device-pair` استفاده می‌کنید، می‌توانید جفت‌سازی اولیهٔ دستگاه را کاملاً از Telegram انجام دهید:

1. در Telegram، به ربات خود پیام دهید: `/pair`
2. ربات با دو پیام پاسخ می‌دهد: یک پیام دستورالعمل و یک پیام جداگانهٔ **کد راه‌اندازی** (برای کپی/پیست در Telegram آسان است).
3. روی تلفن خود، برنامهٔ OpenClaw iOS را باز کنید ← Settings ← Gateway.
4. کد QR را اسکن کنید یا کد راه‌اندازی را بچسبانید و متصل شوید.
5. دوباره در Telegram: `/pair pending` (شناسه‌های درخواست، نقش و scopeها را بررسی کنید)، سپس تأیید کنید.

کد راه‌اندازی یک payload JSON کدگذاری‌شده با base64 است که شامل موارد زیر است:

- `url`: نشانی WebSocket مربوط به Gateway (`ws://...` یا `wss://...`)
- `bootstrapToken`: یک توکن bootstrap کوتاه‌عمر و تک‌دستگاهی که برای handshake اولیهٔ جفت‌سازی استفاده می‌شود

آن توکن bootstrap پروفایل bootstrap داخلی جفت‌سازی را حمل می‌کند:

- پروفایل راه‌اندازی داخلی فقط baseline تازهٔ QR/کد راه‌اندازی را مجاز می‌کند:
  `node` به‌همراه handoff محدود `operator`
- توکن `node` واگذارشده در `scopes: []` باقی می‌ماند
- توکن `operator` واگذارشده به `operator.approvals`،
  `operator.read` و `operator.write` محدود است
- `operator.admin` و `operator.pairing` از طریق bootstrap
  QR/کد راه‌اندازی اعطا نمی‌شوند؛ آن‌ها به یک جفت‌سازی یا جریان توکن operator جداگانه و تأییدشده نیاز دارند
- چرخش/ابطال بعدی توکن همچنان هم به قرارداد نقش تأییدشدهٔ دستگاه
  و هم به scopeهای operator در نشست فراخواننده محدود می‌ماند

تا زمانی که کد راه‌اندازی معتبر است، با آن مانند رمز عبور رفتار کنید.

برای جفت‌سازی موبایل از راه دور با Tailscale، عمومی، یا روش‌های دیگر، از Tailscale Serve/Funnel
یا یک نشانی Gateway دیگر با `wss://` استفاده کنید. کدهای راه‌اندازی متنی `ws://` فقط
برای loopback، نشانی‌های LAN خصوصی، میزبان‌های Bonjour با `.local`، و میزبان شبیه‌ساز Android
پذیرفته می‌شوند. نشانی‌های CGNAT مربوط به Tailnet، نام‌های `.ts.net`، و میزبان‌های عمومی همچنان
پیش از صدور QR/کد راه‌اندازی به‌صورت fail closed رد می‌شوند.

### تأیید یک دستگاه Node

```bash
openclaw devices list
openclaw devices approve <requestId>
openclaw devices reject <requestId>
```

وقتی یک تأیید صریح رد می‌شود چون نشست دستگاه جفت‌شدهٔ تأییدکننده
با scope فقط-جفت‌سازی باز شده بود، CLI همان درخواست را با
`operator.admin` دوباره امتحان می‌کند. این کار به یک دستگاه جفت‌شدهٔ موجود با توان admin اجازه می‌دهد
بدون ویرایش دستی `devices/paired.json` یک جفت‌سازی جدید Control UI/مرورگر را بازیابی کند. Gateway
همچنان اتصال دوباره‌امتحان‌شده را اعتبارسنجی می‌کند؛ توکن‌هایی که نتوانند
با `operator.admin` احراز هویت کنند مسدود می‌مانند.

اگر همان دستگاه با جزئیات احراز هویت متفاوت دوباره تلاش کند (برای مثال
نقش/scopeها/کلید عمومی متفاوت)، درخواست در انتظار قبلی جایگزین می‌شود و یک
`requestId` جدید ساخته می‌شود.

<Note>
یک دستگاهی که از قبل جفت شده است بی‌سروصدا دسترسی گسترده‌تری نمی‌گیرد. اگر دوباره وصل شود و scopeهای بیشتر یا نقش گسترده‌تری بخواهد، OpenClaw تأیید موجود را همان‌طور که هست نگه می‌دارد و یک درخواست ارتقای تازه در حالت انتظار ایجاد می‌کند. پیش از تأیید، از `openclaw devices list` برای مقایسهٔ دسترسی تأییدشدهٔ فعلی با دسترسی تازه‌درخواست‌شده استفاده کنید.
</Note>

### تأیید خودکار اختیاری Node با CIDR مورد اعتماد

جفت‌سازی دستگاه به‌طور پیش‌فرض دستی باقی می‌ماند. برای شبکه‌های Node به‌شدت کنترل‌شده،
می‌توانید با CIDRهای صریح یا IPهای دقیق، تأیید خودکار نخستین‌بار Node را فعال کنید:

```json5
{
  gateway: {
    nodes: {
      pairing: {
        autoApproveCidrs: ["192.168.1.0/24"],
      },
    },
  },
}
```

این فقط برای درخواست‌های تازهٔ جفت‌سازی `role: node` بدون scopeهای درخواستی اعمال می‌شود.
کلاینت‌های operator، مرورگر، Control UI و WebChat همچنان به تأیید دستی نیاز دارند.
تغییرات نقش، scope، فراداده و کلید عمومی همچنان به تأیید دستی نیاز دارند.

### ذخیره‌سازی وضعیت جفت‌سازی Node

زیر `~/.openclaw/devices/` ذخیره می‌شود:

- `pending.json` (کوتاه‌عمر؛ درخواست‌های در انتظار منقضی می‌شوند)
- `paired.json` (دستگاه‌های جفت‌شده + توکن‌ها)

### یادداشت‌ها

- API قدیمی `node.pair.*` (CLI: `openclaw nodes pending|approve|reject|remove|rename`) یک
  ذخیرهٔ جفت‌سازی جداگانه و متعلق به gateway است. Nodeهای WS همچنان به جفت‌سازی دستگاه نیاز دارند.
- رکورد جفت‌سازی منبع حقیقت پایدار برای نقش‌های تأییدشده است. توکن‌های فعال
  دستگاه به همان مجموعه نقش تأییدشده محدود می‌مانند؛ یک ورودی توکن پراکنده
  بیرون از نقش‌های تأییدشده دسترسی جدید ایجاد نمی‌کند.

## مستندات مرتبط

- مدل امنیتی + prompt injection: [امنیت](/fa/gateway/security)
- به‌روزرسانی ایمن (doctor را اجرا کنید): [به‌روزرسانی](/fa/install/updating)
- پیکربندی کانال‌ها:
  - Telegram: [Telegram](/fa/channels/telegram)
  - WhatsApp: [WhatsApp](/fa/channels/whatsapp)
  - Signal: [Signal](/fa/channels/signal)
  - iMessage: [iMessage](/fa/channels/imessage)
  - Discord: [Discord](/fa/channels/discord)
  - Slack: [Slack](/fa/channels/slack)
