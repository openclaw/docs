---
read_when:
    - پیاده‌سازی تأییدهای جفت‌سازی Node بدون رابط کاربری macOS
    - افزودن جریان‌های CLI برای تأیید گره‌های راه دور
    - گسترش پروتکل Gateway با مدیریت Node
summary: جفت‌سازی گره تحت مالکیت Gateway (گزینه B) برای iOS و دیگر گره‌های دوردست
title: جفت‌سازی تحت مالکیت Gateway
x-i18n:
    generated_at: "2026-04-29T22:54:51Z"
    model: gpt-5.5
    provider: openai
    source_hash: 5c662b8f5c1bb44cfc306d42ae19ba1c8bc36e0d96130d730b322ee07e02cad8
    source_path: gateway/pairing.md
    workflow: 16
---

در جفت‌سازی تحت مالکیت Gateway، **Gateway** منبع حقیقت برای تعیین این است که کدام Nodeها مجاز به پیوستن هستند. رابط‌های کاربری (برنامه macOS، کلاینت‌های آینده) فقط فرانت‌اندهایی هستند که درخواست‌های در انتظار را تایید یا رد می‌کنند.

**مهم:** Nodeهای WS هنگام `connect` از **جفت‌سازی دستگاه** (نقش `node`) استفاده می‌کنند. `node.pair.*` یک ذخیره‌گاه جفت‌سازی جداگانه است و دست‌دهی WS را کنترل **نمی‌کند**. فقط کلاینت‌هایی که صراحتا `node.pair.*` را فراخوانی می‌کنند از این جریان استفاده می‌کنند.

## مفاهیم

- **درخواست در انتظار**: یک Node درخواست پیوستن داده است؛ نیازمند تایید است.
- **Node جفت‌شده**: Node تاییدشده با توکن احراز هویت صادرشده.
- **انتقال**: نقطه پایانی WS در Gateway درخواست‌ها را فوروارد می‌کند اما درباره عضویت تصمیم نمی‌گیرد. (پشتیبانی از پل TCP قدیمی حذف شده است.)

## جفت‌سازی چگونه کار می‌کند

1. یک Node به WS در Gateway وصل می‌شود و درخواست جفت‌سازی می‌دهد.
2. Gateway یک **درخواست در انتظار** ذخیره می‌کند و `node.pair.requested` را منتشر می‌کند.
3. شما درخواست را تایید یا رد می‌کنید (CLI یا رابط کاربری).
4. در صورت تایید، Gateway یک **توکن جدید** صادر می‌کند (توکن‌ها هنگام جفت‌سازی دوباره چرخش داده می‌شوند).
5. Node با استفاده از توکن دوباره وصل می‌شود و اکنون «جفت‌شده» است.

درخواست‌های در انتظار پس از **5 دقیقه** به‌طور خودکار منقضی می‌شوند.

## گردش‌کار CLI (مناسب برای حالت بدون رابط)

```bash
openclaw nodes pending
openclaw nodes approve <requestId>
openclaw nodes reject <requestId>
openclaw nodes status
openclaw nodes remove --node <id|name|ip>
openclaw nodes rename --node <id|name|ip> --name "Living Room iPad"
```

`nodes status` Nodeهای جفت‌شده/متصل و قابلیت‌های آن‌ها را نشان می‌دهد.

## سطح API (پروتکل gateway)

رویدادها:

- `node.pair.requested` — وقتی یک درخواست در انتظار جدید ایجاد می‌شود منتشر می‌شود.
- `node.pair.resolved` — وقتی یک درخواست تایید/رد/منقضی می‌شود منتشر می‌شود.

متدها:

- `node.pair.request` — ایجاد یا استفاده مجدد از یک درخواست در انتظار.
- `node.pair.list` — فهرست Nodeهای در انتظار + جفت‌شده (`operator.pairing`).
- `node.pair.approve` — تایید یک درخواست در انتظار (توکن صادر می‌کند).
- `node.pair.reject` — رد یک درخواست در انتظار.
- `node.pair.remove` — حذف یک ورودی قدیمی Node جفت‌شده.
- `node.pair.verify` — تایید `{ nodeId, token }`.

نکته‌ها:

- `node.pair.request` برای هر Node هم‌توان است: فراخوانی‌های تکراری همان درخواست در انتظار را برمی‌گردانند.
- درخواست‌های تکراری برای همان Node در انتظار، فراداده ذخیره‌شده Node و تازه‌ترین اسنپ‌شات allowlistشده از فرمان‌های اعلام‌شده را نیز برای دید اپراتور تازه‌سازی می‌کنند.
- تایید **همیشه** یک توکن تازه تولید می‌کند؛ هیچ توکنی هرگز از `node.pair.request` برگردانده نمی‌شود.
- درخواست‌ها می‌توانند `silent: true` را به‌عنوان راهنمایی برای جریان‌های تایید خودکار شامل شوند.
- `node.pair.approve` از فرمان‌های اعلام‌شده درخواست در انتظار برای اعمال دامنه‌های تایید اضافی استفاده می‌کند:
  - درخواست بدون فرمان: `operator.pairing`
  - درخواست فرمان غیر exec: `operator.pairing` + `operator.write`
  - درخواست `system.run` / `system.run.prepare` / `system.which`:
    `operator.pairing` + `operator.admin`

<Warning>
جفت‌سازی Node یک جریان اعتماد و هویت به‌همراه صدور توکن است. این جریان سطح فرمان زنده Node را برای هر Node تثبیت **نمی‌کند**.

- فرمان‌های زنده Node از چیزی می‌آیند که Node پس از اعمال سیاست سراسری فرمان Node در gateway (`gateway.nodes.allowCommands` و `denyCommands`) هنگام اتصال اعلام می‌کند.
- سیاست allow و ask برای `system.run` در سطح هر Node روی خود Node در `exec.approvals.node.*` قرار دارد، نه در رکورد جفت‌سازی.

</Warning>

## کنترل فرمان Node (2026.3.31+)

<Warning>
**تغییر ناسازگار:** از `2026.3.31`، فرمان‌های Node تا زمانی که جفت‌سازی Node تایید نشود غیرفعال هستند. جفت‌سازی دستگاه به‌تنهایی دیگر برای در دسترس قرار دادن فرمان‌های اعلام‌شده Node کافی نیست.
</Warning>

وقتی یک Node برای نخستین بار وصل می‌شود، جفت‌سازی به‌طور خودکار درخواست می‌شود. تا زمانی که درخواست جفت‌سازی تایید نشود، همه فرمان‌های در انتظار Node از آن Node فیلتر می‌شوند و اجرا نخواهند شد. پس از اینکه اعتماد از طریق تایید جفت‌سازی برقرار شد، فرمان‌های اعلام‌شده Node با رعایت سیاست عادی فرمان در دسترس قرار می‌گیرند.

این یعنی:

- Nodeهایی که پیش‌تر برای ارائه فرمان‌ها فقط به جفت‌سازی دستگاه تکیه می‌کردند، اکنون باید جفت‌سازی Node را کامل کنند.
- فرمان‌هایی که پیش از تایید جفت‌سازی در صف قرار گرفته‌اند حذف می‌شوند، نه اینکه به تعویق بیفتند.

## مرزهای اعتماد رویداد Node (2026.3.31+)

<Warning>
**تغییر ناسازگار:** اجراهای منشاگرفته از Node اکنون روی سطح اعتماد کاهش‌یافته باقی می‌مانند.
</Warning>

خلاصه‌های منشاگرفته از Node و رویدادهای مرتبط نشست به سطح اعتماد موردنظر محدود می‌شوند. جریان‌های مبتنی بر اعلان یا فعال‌شده توسط Node که پیش‌تر به دسترسی گسترده‌تر ابزار میزبان یا نشست متکی بودند ممکن است نیازمند تنظیم باشند. این سخت‌سازی تضمین می‌کند که رویدادهای Node نتوانند فراتر از آنچه مرز اعتماد Node اجازه می‌دهد به دسترسی ابزار در سطح میزبان ارتقا یابند.

به‌روزرسانی‌های پایدار حضور Node نیز از همان مرز هویت پیروی می‌کنند. رویداد `node.presence.alive`
فقط از نشست‌های دستگاه Node احراز هویت‌شده پذیرفته می‌شود و فراداده جفت‌سازی را فقط زمانی به‌روزرسانی می‌کند که
هویت دستگاه/Node از قبل جفت شده باشد. مقادیر خوداظهارشده `client.id` برای نوشتن
وضعیت آخرین مشاهده کافی نیستند.

## تایید خودکار (برنامه macOS)

برنامه macOS می‌تواند به‌صورت اختیاری وقتی شرایط زیر برقرار باشد یک **تایید بی‌صدا** را امتحان کند:

- درخواست با `silent` علامت‌گذاری شده باشد، و
- برنامه بتواند اتصال SSH به میزبان gateway را با استفاده از همان کاربر تایید کند.

اگر تایید بی‌صدا شکست بخورد، به اعلان عادی «تایید/رد» برمی‌گردد.

## تایید خودکار دستگاه با CIDR مورد اعتماد

جفت‌سازی دستگاه WS برای `role: node` به‌طور پیش‌فرض همچنان دستی است. برای شبکه‌های خصوصی
Node که Gateway از قبل مسیر شبکه را قابل اعتماد می‌داند، اپراتورها می‌توانند با CIDRهای صریح یا IPهای دقیق
آن را فعال کنند:

```json5
{
  gateway: {
    nodes: {
      pairing: {
        autoApproveCidrs: ["192.168.1.0/24"],
      },
    },
  },
}
```

مرز امنیتی:

- وقتی `gateway.nodes.pairing.autoApproveCidrs` تنظیم نشده باشد غیرفعال است.
- هیچ حالت تایید خودکار کلی برای LAN یا شبکه خصوصی وجود ندارد.
- فقط جفت‌سازی دستگاه تازه با `role: node` و بدون دامنه‌های درخواست‌شده واجد شرایط است.
- کلاینت‌های اپراتور، مرورگر، Control UI و WebChat همچنان دستی می‌مانند.
- ارتقاهای نقش، دامنه، فراداده و کلید عمومی همچنان دستی می‌مانند.
- مسیرهای هدر trusted-proxy در local loopback همان میزبان واجد شرایط نیستند، چون این
  مسیر می‌تواند توسط فراخوان‌های محلی جعل شود.

## تایید خودکار ارتقای فراداده

وقتی یک دستگاه از قبل جفت‌شده فقط با تغییرات فراداده غیرحساس
(برای مثال، نام نمایشی یا راهنماهای پلتفرم کلاینت) دوباره وصل می‌شود، OpenClaw آن را
یک `metadata-upgrade` در نظر می‌گیرد. تایید خودکار بی‌صدا محدود است: فقط
برای اتصال‌های مجدد محلی غیرمرورگری قابل اعتماد اعمال می‌شود که از قبل مالکیت اعتبارنامه‌های محلی
یا مشترک را ثابت کرده‌اند، از جمله اتصال‌های مجدد برنامه بومی همان میزبان پس از تغییرات فراداده نسخه
سیستم‌عامل. کلاینت‌های مرورگر/Control UI و کلاینت‌های راه‌دور همچنان
از جریان تایید مجدد صریح استفاده می‌کنند. ارتقاهای دامنه (خواندن به نوشتن/ادمین) و
تغییرات کلید عمومی واجد شرایط تایید خودکار ارتقای فراداده **نیستند** —
آن‌ها به‌صورت درخواست‌های تایید مجدد صریح باقی می‌مانند.

## کمک‌کننده‌های جفت‌سازی QR

`/pair qr` محتوای جفت‌سازی را به‌عنوان رسانه ساخت‌یافته رندر می‌کند تا کلاینت‌های موبایل و
مرورگر بتوانند آن را مستقیم اسکن کنند.

حذف یک دستگاه همچنین هر درخواست جفت‌سازی در انتظار قدیمی برای آن
شناسه دستگاه را پاک می‌کند، بنابراین `nodes pending` پس از ابطال ردیف‌های یتیم را نشان نمی‌دهد.

## محلی‌بودن و هدرهای فورواردشده

جفت‌سازی Gateway فقط وقتی یک اتصال را loopback در نظر می‌گیرد که هم سوکت خام
و هم هر شواهد پروکسی بالادست با هم توافق داشته باشند. اگر درخواستی روی loopback برسد اما
هدرهای `X-Forwarded-For` / `X-Forwarded-Host` / `X-Forwarded-Proto` را همراه داشته باشد
که به مبدا غیرمحلی اشاره کنند، آن شواهد هدر فورواردشده ادعای محلی‌بودن
loopback را فاقد صلاحیت می‌کند. سپس مسیر جفت‌سازی به‌جای اینکه درخواست را بی‌صدا
به‌عنوان اتصال همان میزبان در نظر بگیرد، نیازمند تایید صریح می‌شود. برای قاعده معادل در
احراز هویت اپراتور، [احراز هویت پروکسی مورد اعتماد](/fa/gateway/trusted-proxy-auth) را ببینید.

## ذخیره‌سازی (محلی، خصوصی)

وضعیت جفت‌سازی زیر دایرکتوری وضعیت Gateway ذخیره می‌شود (پیش‌فرض `~/.openclaw`):

- `~/.openclaw/nodes/paired.json`
- `~/.openclaw/nodes/pending.json`

اگر `OPENCLAW_STATE_DIR` را بازنویسی کنید، پوشه `nodes/` نیز با آن جابه‌جا می‌شود.

نکته‌های امنیتی:

- توکن‌ها اسرار هستند؛ با `paired.json` به‌عنوان داده حساس رفتار کنید.
- چرخش یک توکن نیازمند تایید مجدد است (یا حذف ورودی Node).

## رفتار انتقال

- انتقال **بدون وضعیت** است؛ عضویت را ذخیره نمی‌کند.
- اگر Gateway آفلاین باشد یا جفت‌سازی غیرفعال باشد، Nodeها نمی‌توانند جفت شوند.
- اگر Gateway در حالت راه‌دور باشد، جفت‌سازی همچنان در برابر ذخیره‌گاه Gateway راه‌دور انجام می‌شود.

## مرتبط

- [جفت‌سازی کانال](/fa/channels/pairing)
- [Nodeها](/fa/nodes)
- [CLI دستگاه‌ها](/fa/cli/devices)
