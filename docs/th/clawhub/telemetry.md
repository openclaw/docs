---
read_when:
    - กำลังทำงานกับการควบคุมเทเลเมทรี / ความเป็นส่วนตัว
    - คำถามเกี่ยวกับข้อมูลที่ถูกรวบรวม
summary: ข้อมูลเทเลเมทรีการติดตั้งที่รวบรวมโดย ClawHub CLI และวิธีเลือกไม่เข้าร่วม
x-i18n:
    generated_at: "2026-07-02T17:50:12Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# เทเลเมทรี

ClawHub ใช้เทเลเมทรีของ CLI แบบน้อยที่สุดเพื่อคำนวณจำนวนการติดตั้งรวม

## เมื่อมีการเก็บเทเลเมทรี

เทเลเมทรีจะถูกส่งเฉพาะเมื่อ:

- คุณเข้าสู่ระบบใน CLI แล้ว
- คุณเรียกใช้ `clawhub install <slug>`
- เทเลเมทรี **ไม่ได้ถูกปิดใช้งาน** (ดู “วิธีปิดใช้งาน” ด้านล่าง)

หากคุณไม่ได้เข้าสู่ระบบ จะไม่มีการรายงานสิ่งใด

## สิ่งที่เราเก็บ

ในแต่ละ `clawhub install` ที่มีการรายงาน CLI จะส่งเหตุการณ์การติดตั้งหนึ่งรายการแบบพยายามให้ดีที่สุด

เหตุการณ์ประกอบด้วย:

- `slug`: slug ของ Skills ที่ติดตั้ง
- `version`: เวอร์ชันที่ติดตั้ง เมื่อทราบ

### สิ่งที่เรา _ไม่_ เก็บ

- ไม่มีพาธโฟลเดอร์หรือตัวระบุที่ได้มาจากโฟลเดอร์
- ไม่มีเนื้อหาไฟล์
- ไม่มีบันทึกต่อการรัน พรอมต์ หรือเอาต์พุตอื่นของ CLI

## จำนวนการติดตั้ง

ClawHub เก็บตัวนับรวมต่อ Skills:

- `installsAllTime`: ผู้ใช้ที่ไม่ซ้ำกันซึ่งรายงานการติดตั้ง CLI สำหรับ Skills อย่างน้อยหนึ่งครั้ง
- `installsCurrent`: ผู้ใช้ที่ไม่ซ้ำกันซึ่งรายงานการติดตั้งและยังไม่ได้ลบ
  เทเลเมทรีของตน

## ความโปร่งใส + การควบคุมของผู้ใช้

ทุกคนจะเห็นเฉพาะ **ตัวนับการติดตั้งแบบรวม** เท่านั้น

การลบบัญชีของคุณจะลบข้อมูลเทเลเมทรีของคุณด้วย

## วิธีปิดใช้งานเทเลเมทรี

ตั้งค่าตัวแปรสภาพแวดล้อม:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

เมื่อตั้งค่านี้แล้ว CLI จะไม่ส่งเทเลเมทรีการติดตั้ง
