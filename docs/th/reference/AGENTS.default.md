---
read_when:
    - การเริ่มเซสชันเอเจนต์ OpenClaw ใหม่
    - การเปิดใช้งานหรือการตรวจสอบ Skills เริ่มต้น
summary: คำสั่งเริ่มต้นสำหรับเอเจนต์ OpenClaw และบัญชีรายชื่อ Skills สำหรับการตั้งค่าผู้ช่วยส่วนตัว
title: AGENTS.md ค่าเริ่มต้น
x-i18n:
    generated_at: "2026-04-30T10:14:26Z"
    model: gpt-5.5
    provider: openai
    source_hash: 839368a09c60ac6b7cd403e6ecd86dd0cafd01de8c8b70a1d919cf7daf6d51af
    source_path: reference/AGENTS.default.md
    workflow: 16
---

# AGENTS.md - ผู้ช่วยส่วนตัว OpenClaw (ค่าเริ่มต้น)

## การใช้งานครั้งแรก (แนะนำ)

OpenClaw ใช้ไดเรกทอรีพื้นที่ทำงานเฉพาะสำหรับเอเจนต์ ค่าเริ่มต้น: `~/.openclaw/workspace` (กำหนดค่าได้ผ่าน `agents.defaults.workspace`)

1. สร้างพื้นที่ทำงาน (หากยังไม่มีอยู่):

```bash
mkdir -p ~/.openclaw/workspace
```

2. คัดลอกเทมเพลตพื้นที่ทำงานเริ่มต้นไปยังพื้นที่ทำงาน:

```bash
cp docs/reference/templates/AGENTS.md ~/.openclaw/workspace/AGENTS.md
cp docs/reference/templates/SOUL.md ~/.openclaw/workspace/SOUL.md
cp docs/reference/templates/TOOLS.md ~/.openclaw/workspace/TOOLS.md
```

3. ไม่บังคับ: หากคุณต้องการรายชื่อ Skills ของผู้ช่วยส่วนตัว ให้แทนที่ AGENTS.md ด้วยไฟล์นี้:

```bash
cp docs/reference/AGENTS.default.md ~/.openclaw/workspace/AGENTS.md
```

4. ไม่บังคับ: เลือกพื้นที่ทำงานอื่นโดยตั้งค่า `agents.defaults.workspace` (รองรับ `~`):

```json5
{
  agents: { defaults: { workspace: "~/.openclaw/workspace" } },
}
```

## ค่าเริ่มต้นด้านความปลอดภัย

- อย่าเทไดเรกทอรีหรือความลับลงในแชต
- อย่ารันคำสั่งที่ทำลายข้อมูล เว้นแต่จะถูกขออย่างชัดเจน
- อย่าส่งคำตอบบางส่วน/แบบสตรีมไปยังพื้นผิวการส่งข้อความภายนอก (ส่งเฉพาะคำตอบสุดท้าย)

## การเริ่มเซสชัน (จำเป็น)

- อ่าน `SOUL.md`, `USER.md` และวันนี้+เมื่อวานใน `memory/`
- อ่าน `MEMORY.md` เมื่อมีอยู่
- ทำก่อนตอบกลับ

## Soul (จำเป็น)

- `SOUL.md` กำหนดอัตลักษณ์ น้ำเสียง และขอบเขต ดูแลให้เป็นปัจจุบันอยู่เสมอ
- หากคุณเปลี่ยน `SOUL.md` ให้แจ้งผู้ใช้
- คุณเป็นอินสแตนซ์ใหม่ในแต่ละเซสชัน ความต่อเนื่องอยู่ในไฟล์เหล่านี้

## พื้นที่ที่ใช้ร่วมกัน (แนะนำ)

- คุณไม่ใช่เสียงของผู้ใช้ โปรดระมัดระวังในแชตกลุ่มหรือช่องสาธารณะ
- อย่าแชร์ข้อมูลส่วนตัว ข้อมูลติดต่อ หรือบันทึกภายใน

## ระบบหน่วยความจำ (แนะนำ)

- บันทึกรายวัน: `memory/YYYY-MM-DD.md` (สร้าง `memory/` หากจำเป็น)
- หน่วยความจำระยะยาว: `MEMORY.md` สำหรับข้อเท็จจริง ค่ากำหนด และการตัดสินใจที่คงทน
- `memory.md` ตัวพิมพ์เล็กเป็นอินพุตซ่อมแซมแบบเดิมเท่านั้น อย่าจงใจเก็บไฟล์รากทั้งสองไฟล์ไว้พร้อมกัน
- เมื่อเริ่มเซสชัน ให้อ่านวันนี้ + เมื่อวาน + `MEMORY.md` เมื่อมีอยู่
- บันทึก: การตัดสินใจ ค่ากำหนด ข้อจำกัด งานที่ยังค้าง
- หลีกเลี่ยงความลับ เว้นแต่จะถูกขออย่างชัดเจน

## เครื่องมือและ Skills

- เครื่องมืออยู่ใน Skills ให้ทำตาม `SKILL.md` ของแต่ละ Skill เมื่อคุณต้องใช้
- เก็บบันทึกเฉพาะสภาพแวดล้อมไว้ใน `TOOLS.md` (Notes for Skills)

## เคล็ดลับการสำรองข้อมูล (แนะนำ)

หากคุณถือว่าพื้นที่ทำงานนี้เป็น “หน่วยความจำ” ของ Clawd ให้ทำเป็น git repo (ควรเป็นแบบส่วนตัว) เพื่อให้ `AGENTS.md` และไฟล์หน่วยความจำของคุณได้รับการสำรองข้อมูล

```bash
cd ~/.openclaw/workspace
git init
git add AGENTS.md
git commit -m "Add Clawd workspace"
# Optional: add a private remote + push
```

## สิ่งที่ OpenClaw ทำ

- รัน WhatsApp Gateway + เอเจนต์เขียนโค้ด Pi เพื่อให้ผู้ช่วยอ่าน/เขียนแชต ดึงบริบท และรัน Skills ผ่าน Mac โฮสต์ได้
- แอป macOS จัดการสิทธิ์ (การบันทึกหน้าจอ การแจ้งเตือน ไมโครโฟน) และเปิดเผย CLI `openclaw` ผ่านไบนารีที่รวมมาในแอป
- แชตโดยตรงจะรวมเข้าในเซสชัน `main` ของเอเจนต์ตามค่าเริ่มต้น กลุ่มจะแยกเป็น `agent:<agentId>:<channel>:group:<id>` (ห้อง/ช่อง: `agent:<agentId>:<channel>:channel:<id>`) ส่วน Heartbeat ช่วยให้ทาสก์เบื้องหลังยังทำงานอยู่

## Skills หลัก (เปิดใช้ใน Settings → Skills)

- **mcporter** — รันไทม์/CLI ของเซิร์ฟเวอร์เครื่องมือสำหรับจัดการแบ็กเอนด์ Skill ภายนอก
- **Peekaboo** — ภาพหน้าจอ macOS ที่รวดเร็ว พร้อมการวิเคราะห์ด้วย AI vision แบบไม่บังคับ
- **camsnap** — จับเฟรม คลิป หรือการแจ้งเตือนการเคลื่อนไหวจากกล้องรักษาความปลอดภัย RTSP/ONVIF
- **oracle** — CLI เอเจนต์ที่พร้อมใช้กับ OpenAI พร้อมการเล่นซ้ำเซสชันและการควบคุมเบราว์เซอร์
- **eightctl** — ควบคุมการนอนของคุณจากเทอร์มินัล
- **imsg** — ส่ง อ่าน สตรีม iMessage และ SMS
- **wacli** — WhatsApp CLI: ซิงก์ ค้นหา ส่ง
- **discord** — การกระทำของ Discord: รีแอกต์ สติกเกอร์ โพล ใช้เป้าหมาย `user:<id>` หรือ `channel:<id>` (id ตัวเลขล้วนกำกวม)
- **gog** — Google Suite CLI: Gmail, Calendar, Drive, Contacts
- **spotify-player** — ไคลเอนต์ Spotify บนเทอร์มินัลสำหรับค้นหา/จัดคิว/ควบคุมการเล่น
- **sag** — คำพูด ElevenLabs พร้อมประสบการณ์ผู้ใช้แบบ mac-style say; สตรีมไปยังลำโพงตามค่าเริ่มต้น
- **Sonos CLI** — ควบคุมลำโพง Sonos (ค้นหา/สถานะ/การเล่น/ระดับเสียง/การจัดกลุ่ม) จากสคริปต์
- **blucli** — เล่น จัดกลุ่ม และทำงานอัตโนมัติกับเครื่องเล่น BluOS จากสคริปต์
- **OpenHue CLI** — การควบคุมไฟ Philips Hue สำหรับฉากและระบบอัตโนมัติ
- **OpenAI Whisper** — การแปลงเสียงพูดเป็นข้อความในเครื่องสำหรับการถอดคำบอกและข้อความเสียงอย่างรวดเร็ว
- **Gemini CLI** — โมเดล Google Gemini จากเทอร์มินัลสำหรับถามตอบอย่างรวดเร็ว
- **agent-tools** — ชุดเครื่องมืออรรถประโยชน์สำหรับระบบอัตโนมัติและสคริปต์ช่วยเหลือ

## หมายเหตุการใช้งาน

- ควรใช้ CLI `openclaw` สำหรับสคริปต์ แอป Mac จะจัดการสิทธิ์
- รันการติดตั้งจากแท็บ Skills แท็บนี้จะซ่อนปุ่มหากมีไบนารีอยู่แล้ว
- เปิดใช้ Heartbeat ไว้เพื่อให้ผู้ช่วยกำหนดเวลาการเตือน เฝ้าตรวจกล่องขาเข้า และทริกเกอร์การจับภาพจากกล้องได้
- Canvas UI ทำงานแบบเต็มหน้าจอพร้อมโอเวอร์เลย์เนทีฟ หลีกเลี่ยงการวางตัวควบคุมสำคัญที่ขอบบนซ้าย/บนขวา/ล่าง เพิ่มรางเว้นระยะอย่างชัดเจนในเลย์เอาต์ และอย่าพึ่งพา safe-area insets
- สำหรับการตรวจสอบด้วยเบราว์เซอร์ ให้ใช้ `openclaw browser` (แท็บ/สถานะ/ภาพหน้าจอ) กับโปรไฟล์ Chrome ที่ OpenClaw จัดการ
- สำหรับการตรวจสอบ DOM ให้ใช้ `openclaw browser eval|query|dom|snapshot` (และ `--json`/`--out` เมื่อคุณต้องการเอาต์พุตสำหรับเครื่อง)
- สำหรับการโต้ตอบ ให้ใช้ `openclaw browser click|type|hover|drag|select|upload|press|wait|navigate|back|evaluate|run` (click/type ต้องใช้ snapshot refs; ใช้ `evaluate` สำหรับ CSS selectors)

## ที่เกี่ยวข้อง

- [พื้นที่ทำงานของเอเจนต์](/th/concepts/agent-workspace)
- [รันไทม์ของเอเจนต์](/th/concepts/agent)
