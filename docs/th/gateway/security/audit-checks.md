---
read_when:
    - คุณเห็น `checkId` ที่เฉพาะเจาะจงในเอาต์พุตของ `openclaw security audit` และต้องการทราบว่ามันหมายถึงอะไร
    - คุณต้องการคีย์/พาธสำหรับการแก้ไขของผลการตรวจพบที่กำหนดไว้
    - คุณกำลังคัดแยกระดับความรุนแรงตลอดการรัน security audit
summary: แค็ตตาล็อกอ้างอิงของ checkId ที่ `openclaw security audit` ส่งออกมา
title: การตรวจสอบของ security audit
x-i18n:
    generated_at: "2026-04-26T11:31:50Z"
    model: gpt-5.4
    provider: openai
    source_hash: 7a5463bd1cec8382eb480cbbe1d8f8cceef0c15efc1f9124990df1e8f70b209a
    source_path: gateway/security/audit-checks.md
    workflow: 15
---

`openclaw security audit` จะส่งผลการตรวจพบแบบมีโครงสร้างโดยอ้างอิงด้วย `checkId` หน้านี้คือแค็ตตาล็อกอ้างอิงสำหรับ ID เหล่านั้น สำหรับโมเดลภัยคุกคามระดับสูงและแนวทางการเสริมความมั่นคงปลอดภัย โปรดดู [Security](/th/gateway/security)

ค่า `checkId` ที่มีสัญญาณสำคัญซึ่งคุณมีแนวโน้มจะพบมากที่สุดในการดีพลอยจริง (ไม่ใช่รายการทั้งหมด):

| `checkId`                                                     | ระดับความรุนแรง | เหตุใดจึงสำคัญ                                                                        | คีย์/พาธหลักสำหรับการแก้ไข                                                                         | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------- |
| `fs.state_dir.perms_world_writable`                           | critical         | ผู้ใช้/โปรเซสอื่นสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                              | สิทธิ์ของระบบไฟล์บน `~/.openclaw`                                                                    | ได้              |
| `fs.state_dir.perms_group_writable`                           | warn             | ผู้ใช้ในกลุ่มสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                                  | สิทธิ์ของระบบไฟล์บน `~/.openclaw`                                                                    | ได้              |
| `fs.state_dir.perms_readable`                                 | warn             | ผู้อื่นสามารถอ่าน state dir ได้                                                       | สิทธิ์ของระบบไฟล์บน `~/.openclaw`                                                                    | ได้              |
| `fs.state_dir.symlink`                                        | warn             | เป้าหมายของ state dir กลายเป็นขอบเขตความเชื่อถืออีกชั้นหนึ่ง                          | โครงสร้างระบบไฟล์ของ state dir                                                                      | ไม่ได้           |
| `fs.config.perms_writable`                                    | critical         | ผู้อื่นสามารถเปลี่ยนนโยบาย auth/tool/config ได้                                       | สิทธิ์ของระบบไฟล์บน `~/.openclaw/openclaw.json`                                                      | ได้              |
| `fs.config.symlink`                                           | warn             | ไฟล์คอนฟิกที่เป็น symlink ไม่รองรับการเขียน และเพิ่มขอบเขตความเชื่อถืออีกชั้นหนึ่ง     | แทนที่ด้วยไฟล์คอนฟิกปกติ หรือชี้ `OPENCLAW_CONFIG_PATH` ไปยังไฟล์จริง                               | ไม่ได้           |
| `fs.config.perms_group_readable`                              | warn             | ผู้ใช้ในกลุ่มสามารถอ่านโทเค็น/การตั้งค่าจากคอนฟิกได้                                  | สิทธิ์ของระบบไฟล์บนไฟล์คอนฟิก                                                                        | ได้              |
| `fs.config.perms_world_readable`                              | critical         | คอนฟิกอาจเปิดเผยโทเค็น/การตั้งค่า                                                      | สิทธิ์ของระบบไฟล์บนไฟล์คอนฟิก                                                                        | ได้              |
| `fs.config_include.perms_writable`                            | critical         | ผู้อื่นสามารถแก้ไขไฟล์ include ของคอนฟิกได้                                            | สิทธิ์ของไฟล์ include ที่อ้างถึงจาก `openclaw.json`                                                  | ได้              |
| `fs.config_include.perms_group_readable`                      | warn             | ผู้ใช้ในกลุ่มสามารถอ่านความลับ/การตั้งค่าที่ถูก include ได้                            | สิทธิ์ของไฟล์ include ที่อ้างถึงจาก `openclaw.json`                                                  | ได้              |
| `fs.config_include.perms_world_readable`                      | critical         | ความลับ/การตั้งค่าที่ถูก include สามารถอ่านได้โดยทุกคน                                | สิทธิ์ของไฟล์ include ที่อ้างถึงจาก `openclaw.json`                                                  | ได้              |
| `fs.auth_profiles.perms_writable`                             | critical         | ผู้อื่นสามารถแทรกหรือแทนที่ข้อมูลรับรองของโมเดลที่เก็บไว้ได้                           | สิทธิ์ของ `agents/<agentId>/agent/auth-profiles.json`                                                | ได้              |
| `fs.auth_profiles.perms_readable`                             | warn             | ผู้อื่นสามารถอ่าน API key และโทเค็น OAuth ได้                                          | สิทธิ์ของ `agents/<agentId>/agent/auth-profiles.json`                                                | ได้              |
| `fs.credentials_dir.perms_writable`                           | critical         | ผู้อื่นสามารถแก้ไขสถานะการจับคู่/ข้อมูลรับรองของแชนเนลได้                             | สิทธิ์ของระบบไฟล์บน `~/.openclaw/credentials`                                                        | ได้              |
| `fs.credentials_dir.perms_readable`                           | warn             | ผู้อื่นสามารถอ่านสถานะข้อมูลรับรองของแชนเนลได้                                        | สิทธิ์ของระบบไฟล์บน `~/.openclaw/credentials`                                                        | ได้              |
| `fs.sessions_store.perms_readable`                            | warn             | ผู้อื่นสามารถอ่านทรานสคริปต์/เมทาดาทาของเซสชันได้                                    | สิทธิ์ของ session store                                                                              | ได้              |
| `fs.log_file.perms_readable`                                  | warn             | ผู้อื่นสามารถอ่าน log ที่ผ่านการ redaction แล้วแต่ยังมีความละเอียดอ่อน                 | สิทธิ์ของไฟล์ log ของ gateway                                                                         | ได้              |
| `fs.synced_dir`                                               | warn             | การเก็บ state/config ไว้ใน iCloud/Dropbox/Drive ขยายการเปิดเผยโทเค็น/ทรานสคริปต์      | ย้าย config/state ออกจากโฟลเดอร์ที่ซิงก์                                                            | ไม่ได้           |
| `gateway.bind_no_auth`                                        | critical         | มีการ bind แบบ remote โดยไม่มี shared secret                                           | `gateway.bind`, `gateway.auth.*`                                                                     | ไม่ได้           |
| `gateway.loopback_no_auth`                                    | critical         | loopback ที่ผ่าน reverse proxy อาจกลายเป็นแบบไม่มีการยืนยันตัวตน                       | `gateway.auth.*`, การตั้งค่า proxy                                                                   | ไม่ได้           |
| `gateway.trusted_proxies_missing`                             | warn             | มี header จาก reverse proxy แต่ไม่ได้รับการเชื่อถือ                                    | `gateway.trustedProxies`                                                                             | ไม่ได้           |
| `gateway.http.no_auth`                                        | warn/critical    | เข้าถึง Gateway HTTP API ได้ด้วย `auth.mode="none"`                                    | `gateway.auth.mode`, `gateway.http.endpoints.*`                                                      | ไม่ได้           |
| `gateway.http.session_key_override_enabled`                   | info             | ผู้เรียก HTTP API สามารถเขียนทับ `sessionKey` ได้                                      | `gateway.http.allowSessionKeyOverride`                                                               | ไม่ได้           |
| `gateway.tools_invoke_http.dangerous_allow`                   | warn/critical    | เปิดใช้เครื่องมืออันตรายผ่าน HTTP API อีกครั้ง                                         | `gateway.tools.allow`                                                                                | ไม่ได้           |
| `gateway.nodes.allow_commands_dangerous`                      | warn/critical    | เปิดใช้คำสั่ง Node ที่มีผลกระทบสูง (กล้อง/หน้าจอ/รายชื่อ/ปฏิทิน/SMS)                 | `gateway.nodes.allowCommands`                                                                        | ไม่ได้           |
| `gateway.nodes.deny_commands_ineffective`                     | warn             | รายการ deny ที่มีลักษณะเหมือน pattern ไม่ตรงกับข้อความ shell หรือกลุ่ม                | `gateway.nodes.denyCommands`                                                                         | ไม่ได้           |
| `gateway.tailscale_funnel`                                    | critical         | เปิดเผยสู่สาธารณะบนอินเทอร์เน็ต                                                       | `gateway.tailscale.mode`                                                                             | ไม่ได้           |
| `gateway.tailscale_serve`                                     | info             | เปิดการเข้าถึงผ่าน Tailnet ด้วย Serve                                                  | `gateway.tailscale.mode`                                                                             | ไม่ได้           |
| `gateway.control_ui.allowed_origins_required`                 | critical         | Control UI ที่ไม่ใช่ loopback ไม่มี allowlist ของ browser-origin แบบชัดเจน              | `gateway.controlUi.allowedOrigins`                                                                   | ไม่ได้           |
| `gateway.control_ui.allowed_origins_wildcard`                 | warn/critical    | `allowedOrigins=["*"]` ปิดการใช้ allowlist ของ browser-origin                          | `gateway.controlUi.allowedOrigins`                                                                   | ไม่ได้           |
| `gateway.control_ui.host_header_origin_fallback`              | warn/critical    | เปิดใช้ Host-header origin fallback (ลดระดับการป้องกัน DNS rebinding)                  | `gateway.controlUi.dangerouslyAllowHostHeaderOriginFallback`                                         | ไม่ได้           |
| `gateway.control_ui.insecure_auth`                            | warn             | เปิดใช้ toggle ความเข้ากันได้ของ insecure-auth                                         | `gateway.controlUi.allowInsecureAuth`                                                                | ไม่ได้           |
| `gateway.control_ui.device_auth_disabled`                     | critical         | ปิดการตรวจสอบตัวตนอุปกรณ์                                                              | `gateway.controlUi.dangerouslyDisableDeviceAuth`                                                     | ไม่ได้           |
| `gateway.real_ip_fallback_enabled`                            | warn/critical    | การเชื่อถือ fallback ของ `X-Real-IP` อาจเปิดทางให้ปลอมแปลง source IP ผ่าน proxy ที่ตั้งค่าผิด | `gateway.allowRealIpFallback`, `gateway.trustedProxies`                                              | ไม่ได้           |
| `gateway.token_too_short`                                     | warn             | shared token ที่สั้นเกินไปเดาแบบ brute force ได้ง่ายกว่า                              | `gateway.auth.token`                                                                                 | ไม่ได้           |
| `gateway.auth_no_rate_limit`                                  | warn             | auth ที่เปิดเผยโดยไม่มี rate limiting เพิ่มความเสี่ยงจาก brute force                  | `gateway.auth.rateLimit`                                                                             | ไม่ได้           |
| `gateway.trusted_proxy_auth`                                  | critical         | ตัวตนของ proxy กลายเป็นขอบเขตการยืนยันตัวตน                                           | `gateway.auth.mode="trusted-proxy"`                                                                  | ไม่ได้           |
| `gateway.trusted_proxy_no_proxies`                            | critical         | trusted-proxy auth ที่ไม่มี trusted proxy IPs ไม่ปลอดภัย                               | `gateway.trustedProxies`                                                                             | ไม่ได้           |
| `gateway.trusted_proxy_no_user_header`                        | critical         | trusted-proxy auth ไม่สามารถ resolve ตัวตนผู้ใช้ได้อย่างปลอดภัย                        | `gateway.auth.trustedProxy.userHeader`                                                               | ไม่ได้           |
| `gateway.trusted_proxy_no_allowlist`                          | warn             | trusted-proxy auth ยอมรับผู้ใช้ต้นทางที่ยืนยันตัวตนแล้วทุกคน                           | `gateway.auth.trustedProxy.allowUsers`                                                               | ไม่ได้           |
| `checkId`                                                     | ระดับความรุนแรง | เหตุใดจึงสำคัญ                                                                        | คีย์/พาธหลักสำหรับการแก้ไข                                                                         | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------- |
| `gateway.probe_auth_secretref_unavailable`                    | warn             | deep probe ไม่สามารถ resolve SecretRef ของ auth ได้ในเส้นทางคำสั่งนี้                 | แหล่ง auth ของ deep-probe / ความพร้อมใช้งานของ SecretRef                                            | ไม่ได้           |
| `gateway.probe_failed`                                        | warn/critical    | การ probe Gateway แบบ live ล้มเหลว                                                    | การเข้าถึง/auth ของ gateway                                                                          | ไม่ได้           |
| `discovery.mdns_full_mode`                                    | warn/critical    | โหมด mDNS full ประกาศเมทาดาทา `cliPath`/`sshPort` บนเครือข่ายภายใน                  | `discovery.mdns.mode`, `gateway.bind`                                                                | ไม่ได้           |
| `config.insecure_or_dangerous_flags`                          | warn             | มีการเปิดใช้แฟล็ก debug ที่ไม่ปลอดภัย/อันตราย                                         | หลายคีย์ (ดูรายละเอียดของผลการตรวจพบ)                                                               | ไม่ได้           |
| `config.secrets.gateway_password_in_config`                   | warn             | รหัสผ่าน Gateway ถูกเก็บไว้ตรง ๆ ในคอนฟิก                                              | `gateway.auth.password`                                                                              | ไม่ได้           |
| `config.secrets.hooks_token_in_config`                        | warn             | Hook bearer token ถูกเก็บไว้ตรง ๆ ในคอนฟิก                                             | `hooks.token`                                                                                        | ไม่ได้           |
| `hooks.token_reuse_gateway_token`                             | critical         | โทเค็นสำหรับรับข้อมูลของ hooks สามารถปลดล็อก auth ของ Gateway ได้ด้วย                 | `hooks.token`, `gateway.auth.token`                                                                  | ไม่ได้           |
| `hooks.token_too_short`                                       | warn             | brute force กับทางเข้า hooks ได้ง่ายกว่า                                               | `hooks.token`                                                                                        | ไม่ได้           |
| `hooks.default_session_key_unset`                             | warn             | การรัน agent จาก hook กระจายออกเป็นเซสชันที่สร้างต่อคำขอ                              | `hooks.defaultSessionKey`                                                                            | ไม่ได้           |
| `hooks.allowed_agent_ids_unrestricted`                        | warn/critical    | ผู้เรียก hook ที่ผ่านการยืนยันตัวตนสามารถกำหนดเส้นทางไปยัง agent ใดก็ได้ที่กำหนดไว้     | `hooks.allowedAgentIds`                                                                              | ไม่ได้           |
| `hooks.request_session_key_enabled`                           | warn/critical    | ผู้เรียกภายนอกสามารถเลือก `sessionKey` ได้                                             | `hooks.allowRequestSessionKey`                                                                       | ไม่ได้           |
| `hooks.request_session_key_prefixes_missing`                  | warn/critical    | ไม่มีการจำกัดรูปแบบของ session key จากภายนอก                                          | `hooks.allowedSessionKeyPrefixes`                                                                    | ไม่ได้           |
| `hooks.path_root`                                             | critical         | path ของ hook คือ `/` ทำให้ทางเข้าชนกันหรือกำหนดเส้นทางผิดได้ง่าย                     | `hooks.path`                                                                                         | ไม่ได้           |
| `hooks.installs_unpinned_npm_specs`                           | warn             | ระเบียนการติดตั้ง hook ไม่ได้ pin ไว้กับ npm spec ที่ไม่เปลี่ยนรูป                      | เมทาดาทาการติดตั้ง hook                                                                              | ไม่ได้           |
| `hooks.installs_missing_integrity`                            | warn             | ระเบียนการติดตั้ง hook ไม่มีเมทาดาทา integrity                                          | เมทาดาทาการติดตั้ง hook                                                                              | ไม่ได้           |
| `hooks.installs_version_drift`                                | warn             | ระเบียนการติดตั้ง hook เบี่ยงเบนจากแพ็กเกจที่ติดตั้งอยู่                               | เมทาดาทาการติดตั้ง hook                                                                              | ไม่ได้           |
| `logging.redact_off`                                          | warn             | ค่าที่ละเอียดอ่อนรั่วไหลไปยัง logs/status                                               | `logging.redactSensitive`                                                                            | ได้              |
| `browser.control_invalid_config`                              | warn             | คอนฟิก browser control ไม่ถูกต้องตั้งแต่ก่อนรันไทม์                                     | `browser.*`                                                                                          | ไม่ได้           |
| `browser.control_no_auth`                                     | critical         | browser control ถูกเปิดเผยโดยไม่มี token/password auth                                  | `gateway.auth.*`                                                                                     | ไม่ได้           |
| `browser.remote_cdp_http`                                     | warn             | CDP ระยะไกลผ่าน HTTP ธรรมดาไม่มีการเข้ารหัสการขนส่ง                                    | `cdpUrl` ของ browser profile                                                                         | ไม่ได้           |
| `browser.remote_cdp_private_host`                             | warn             | CDP ระยะไกลกำหนดเป้าหมายไปยังโฮสต์ private/internal                                    | `cdpUrl` ของ browser profile, `browser.ssrfPolicy.*`                                                 | ไม่ได้           |
| `sandbox.docker_config_mode_off`                              | warn             | มีคอนฟิก Docker ของ sandbox แต่ไม่ได้เปิดใช้งาน                                         | `agents.*.sandbox.mode`                                                                              | ไม่ได้           |
| `sandbox.bind_mount_non_absolute`                             | warn             | bind mount แบบ relative อาจ resolve ได้อย่างคาดเดายาก                                  | `agents.*.sandbox.docker.binds[]`                                                                    | ไม่ได้           |
| `sandbox.dangerous_bind_mount`                                | critical         | bind mount ของ sandbox ชี้ไปยังพาธระบบ พาธข้อมูลรับรอง หรือ Docker socket ที่ถูกบล็อก   | `agents.*.sandbox.docker.binds[]`                                                                    | ไม่ได้           |
| `sandbox.dangerous_network_mode`                              | critical         | เครือข่าย Docker ของ sandbox ใช้โหมด `host` หรือ `container:*` ที่เข้าร่วม namespace   | `agents.*.sandbox.docker.network`                                                                    | ไม่ได้           |
| `sandbox.dangerous_seccomp_profile`                           | critical         | seccomp profile ของ sandbox ทำให้การแยกตัวของคอนเทนเนอร์อ่อนลง                         | `agents.*.sandbox.docker.securityOpt`                                                                | ไม่ได้           |
| `sandbox.dangerous_apparmor_profile`                          | critical         | AppArmor profile ของ sandbox ทำให้การแยกตัวของคอนเทนเนอร์อ่อนลง                        | `agents.*.sandbox.docker.securityOpt`                                                                | ไม่ได้           |
| `sandbox.browser_cdp_bridge_unrestricted`                     | warn             | browser bridge ของ sandbox ถูกเปิดเผยโดยไม่มีการจำกัดช่วงแหล่งที่มา                    | `sandbox.browser.cdpSourceRange`                                                                     | ไม่ได้           |
| `sandbox.browser_container.non_loopback_publish`              | critical         | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่เผยแพร่ CDP บนอินเทอร์เฟซที่ไม่ใช่ loopback            | คอนฟิกการเผยแพร่ของคอนเทนเนอร์เบราว์เซอร์ sandbox                                                  | ไม่ได้           |
| `sandbox.browser_container.hash_label_missing`                | warn             | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่เก่ากว่าป้ายกำกับ config-hash ปัจจุบัน                  | `openclaw sandbox recreate --browser --all`                                                          | ไม่ได้           |
| `sandbox.browser_container.hash_epoch_stale`                  | warn             | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่เก่ากว่า epoch ของคอนฟิกเบราว์เซอร์ปัจจุบัน             | `openclaw sandbox recreate --browser --all`                                                          | ไม่ได้           |
| `tools.exec.host_sandbox_no_sandbox_defaults`                 | warn             | `exec host=sandbox` จะ fail closed เมื่อ sandbox ปิดอยู่                                | `tools.exec.host`, `agents.defaults.sandbox.mode`                                                    | ไม่ได้           |
| `tools.exec.host_sandbox_no_sandbox_agents`                   | warn             | `exec host=sandbox` ราย agent จะ fail closed เมื่อ sandbox ปิดอยู่                      | `agents.list[].tools.exec.host`, `agents.list[].sandbox.mode`                                        | ไม่ได้           |
| `tools.exec.security_full_configured`                         | warn/critical    | host exec กำลังรันด้วย `security="full"`                                               | `tools.exec.security`, `agents.list[].tools.exec.security`                                           | ไม่ได้           |
| `tools.exec.auto_allow_skills_enabled`                        | warn             | การอนุมัติ exec เชื่อถือไบนารีของ Skills โดยปริยาย                                     | `~/.openclaw/exec-approvals.json`                                                                    | ไม่ได้           |
| `tools.exec.allowlist_interpreter_without_strict_inline_eval` | warn             | allowlist ของ interpreter อนุญาต inline eval โดยไม่มีการบังคับอนุมัติใหม่              | `tools.exec.strictInlineEval`, `agents.list[].tools.exec.strictInlineEval`, exec approvals allowlist | ไม่ได้           |
| `tools.exec.safe_bins_interpreter_unprofiled`                 | warn             | ไบนารี interpreter/runtime ใน `safeBins` ที่ไม่มีโปรไฟล์ชัดเจนขยายความเสี่ยงของ exec   | `tools.exec.safeBins`, `tools.exec.safeBinProfiles`, `agents.list[].tools.exec.*`                    | ไม่ได้           |
| `tools.exec.safe_bins_broad_behavior`                         | warn             | เครื่องมือที่มีพฤติกรรมกว้างใน `safeBins` ทำให้โมเดลความเชื่อถือแบบ stdin-filter ความเสี่ยงต่ำอ่อนลง | `tools.exec.safeBins`, `agents.list[].tools.exec.safeBins`                                           | ไม่ได้           |
| `tools.exec.safe_bin_trusted_dirs_risky`                      | warn             | `safeBinTrustedDirs` มีไดเรกทอรีที่แก้ไขได้หรือมีความเสี่ยง                             | `tools.exec.safeBinTrustedDirs`, `agents.list[].tools.exec.safeBinTrustedDirs`                       | ไม่ได้           |
| `skills.workspace.symlink_escape`                             | warn             | `skills/**/SKILL.md` ใน workspace resolve ออกนอก root ของ workspace (symlink-chain drift) | สถานะระบบไฟล์ของ `skills/**` ใน workspace                                                            | ไม่ได้           |
| `plugins.extensions_no_allowlist`                             | warn             | มีการติดตั้ง Plugins โดยไม่มี allowlist ของ Plugin แบบชัดเจน                            | `plugins.allowlist`                                                                                  | ไม่ได้           |
| `plugins.installs_unpinned_npm_specs`                         | warn             | ระเบียนดัชนี Plugin ไม่ได้ pin ไว้กับ npm spec ที่ไม่เปลี่ยนรูป                         | เมทาดาทาการติดตั้ง Plugin                                                                            | ไม่ได้           |
| `checkId`                                                     | ระดับความรุนแรง | เหตุใดจึงสำคัญ                                                                        | คีย์/พาธหลักสำหรับการแก้ไข                                                                         | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------- |
| `plugins.installs_missing_integrity`                          | warn             | ระเบียนดัชนี Plugin ไม่มีเมทาดาทา integrity                                          | เมทาดาทาการติดตั้ง Plugin                                                                            | ไม่ได้           |
| `plugins.installs_version_drift`                              | warn             | ระเบียนดัชนี Plugin เบี่ยงเบนจากแพ็กเกจที่ติดตั้งอยู่                                | เมทาดาทาการติดตั้ง Plugin                                                                            | ไม่ได้           |
| `plugins.code_safety`                                         | warn/critical    | การสแกนโค้ดของ Plugin พบรูปแบบที่น่าสงสัยหรืออันตราย                                 | โค้ดของ Plugin / แหล่งที่มาติดตั้ง                                                                   | ไม่ได้           |
| `plugins.code_safety.entry_path`                              | warn             | พาธ entry ของ Plugin ชี้เข้าไปยังตำแหน่งที่ซ่อนอยู่หรืออยู่ใน `node_modules`          | `entry` ใน manifest ของ Plugin                                                                       | ไม่ได้           |
| `plugins.code_safety.entry_escape`                            | critical         | entry ของ Plugin หลุดออกนอกไดเรกทอรีของ Plugin                                       | `entry` ใน manifest ของ Plugin                                                                       | ไม่ได้           |
| `plugins.code_safety.scan_failed`                             | warn             | การสแกนโค้ดของ Plugin ไม่สามารถทำงานจนเสร็จได้                                        | พาธของ Plugin / สภาพแวดล้อมการสแกน                                                                  | ไม่ได้           |
| `skills.code_safety`                                          | warn/critical    | เมทาดาทาหรือตัวโค้ดของตัวติดตั้ง Skill มีรูปแบบที่น่าสงสัยหรืออันตราย                 | แหล่งที่มาติดตั้ง Skill                                                                              | ไม่ได้           |
| `skills.code_safety.scan_failed`                              | warn             | การสแกนโค้ดของ Skill ไม่สามารถทำงานจนเสร็จได้                                         | สภาพแวดล้อมการสแกน Skill                                                                            | ไม่ได้           |
| `security.exposure.open_channels_with_exec`                   | warn/critical    | ห้องที่ใช้ร่วมกัน/สาธารณะสามารถเข้าถึง agent ที่เปิดใช้ exec ได้                       | `channels.*.dmPolicy`, `channels.*.groupPolicy`, `tools.exec.*`, `agents.list[].tools.exec.*`        | ไม่ได้           |
| `security.exposure.open_groups_with_elevated`                 | critical         | กลุ่มที่เปิดกว้าง + เครื่องมือ elevated สร้างเส้นทาง prompt injection ที่มีผลกระทบสูง | `channels.*.groupPolicy`, `tools.elevated.*`                                                         | ไม่ได้           |
| `security.exposure.open_groups_with_runtime_or_fs`            | critical/warn    | กลุ่มที่เปิดกว้างสามารถเข้าถึงเครื่องมือคำสั่ง/ไฟล์ได้โดยไม่มีตัวป้องกัน sandbox/workspace | `channels.*.groupPolicy`, `tools.profile/deny`, `tools.fs.workspaceOnly`, `agents.*.sandbox.mode`    | ไม่ได้           |
| `security.trust_model.multi_user_heuristic`                   | warn             | คอนฟิกดูเหมือนรองรับผู้ใช้หลายคน ขณะที่โมเดลความเชื่อถือของ gateway เป็นผู้ช่วยส่วนตัว | แยกขอบเขตความเชื่อถือ หรือเสริมความปลอดภัยสำหรับผู้ใช้หลายคน (`sandbox.mode`, การ deny tool/การกำหนดขอบเขต workspace) | ไม่ได้           |
| `tools.profile_minimal_overridden`                            | warn             | agent เขียนทับ minimal profile ระดับ global                                            | `agents.list[].tools.profile`                                                                        | ไม่ได้           |
| `plugins.tools_reachable_permissive_policy`                   | warn             | เครื่องมือของส่วนขยายเข้าถึงได้ในบริบทที่ผ่อนปรน                                      | `tools.profile` + การ allow/deny ของเครื่องมือ                                                       | ไม่ได้           |
| `models.legacy`                                               | warn             | ยังคงกำหนดค่าตระกูลโมเดลแบบเดิมอยู่                                                    | การเลือกโมเดล                                                                                       | ไม่ได้           |
| `models.weak_tier`                                            | warn             | โมเดลที่กำหนดค่าไว้ต่ำกว่าระดับที่แนะนำในปัจจุบัน                                      | การเลือกโมเดล                                                                                       | ไม่ได้           |
| `models.small_params`                                         | critical/info    | โมเดลขนาดเล็ก + พื้นผิวเครื่องมือที่ไม่ปลอดภัย เพิ่มความเสี่ยงจากการฉีดคำสั่ง          | การเลือกโมเดล + นโยบาย sandbox/tool                                                                 | ไม่ได้           |
| `summary.attack_surface`                                      | info             | สรุปภาพรวมของท่าทีด้าน auth, แชนเนล, เครื่องมือ และการเปิดเผยพื้นผิวการโจมตี          | หลายคีย์ (ดูรายละเอียดของผลการตรวจพบ)                                                               | ไม่ได้           |

## ที่เกี่ยวข้อง

- [Security](/th/gateway/security)
- [Configuration](/th/gateway/configuration)
- [Trusted proxy auth](/th/gateway/trusted-proxy-auth)
