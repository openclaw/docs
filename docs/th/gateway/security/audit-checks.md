---
read_when:
    - คุณเห็น `checkId` เฉพาะรายการหนึ่งในเอาต์พุต `openclaw security audit` และต้องการทราบว่าหมายถึงอะไร
    - คุณต้องใช้คีย์/พาธการแก้ไขสำหรับข้อค้นพบที่กำหนด
    - คุณกำลังคัดแยกระดับความรุนแรงในการรันการตรวจสอบความปลอดภัย
summary: แค็ตตาล็อกอ้างอิงของ checkIds ที่ส่งออกโดยการตรวจสอบความปลอดภัยของ openclaw
title: การตรวจสอบการตรวจประเมินความปลอดภัย
x-i18n:
    generated_at: "2026-07-01T08:44:26Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 5fd8811c0635461a3c5a8512ea4136588d1d8ef6872dd3299fb5a9892d8a9c2b
    source_path: gateway/security/audit-checks.md
    workflow: 16
---

`openclaw security audit` จะแสดงผลการตรวจพบแบบมีโครงสร้างที่ผูกกับ `checkId` หน้านี้คือแค็ตตาล็อกอ้างอิงสำหรับ ID เหล่านั้น สำหรับโมเดลภัยคุกคามระดับสูงและแนวทางการเพิ่มความแข็งแกร่งด้านความปลอดภัย โปรดดู [ความปลอดภัย](/th/gateway/security)

ค่า `checkId` ที่มีสัญญาณชัดเจนและคุณมีแนวโน้มจะพบมากที่สุดในการใช้งานจริง (ไม่ครอบคลุมทั้งหมด):

| `checkId`                                                     | ความรุนแรง      | เหตุผลที่สำคัญ                                                                       | คีย์/พาธหลักสำหรับการแก้ไข                                                                                 | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- | -------- |
| `fs.state_dir.perms_world_writable`                           | ร้ายแรง      | ผู้ใช้/โปรเซสอื่นสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                                 | สิทธิ์ระบบไฟล์บน `~/.openclaw`                                                                    | ใช่      |
| `fs.state_dir.perms_group_writable`                           | เตือน          | ผู้ใช้ในกลุ่มสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                                           | สิทธิ์ระบบไฟล์บน `~/.openclaw`                                                                    | ใช่      |
| `fs.state_dir.perms_readable`                                 | เตือน          | ไดเรกทอรีสถานะอ่านได้โดยผู้อื่น                                                      | สิทธิ์ระบบไฟล์บน `~/.openclaw`                                                                    | ใช่      |
| `fs.state_dir.symlink`                                        | เตือน          | เป้าหมายของไดเรกทอรีสถานะกลายเป็นอีกขอบเขตความเชื่อถือ                                      | เลย์เอาต์ระบบไฟล์ของไดเรกทอรีสถานะ                                                                          | ไม่       |
| `fs.config.perms_writable`                                    | ร้ายแรง      | ผู้อื่นสามารถเปลี่ยนนโยบาย/การกำหนดค่าสำหรับ auth/tool ได้                                            | สิทธิ์ระบบไฟล์บน `~/.openclaw/openclaw.json`                                                      | ใช่      |
| `fs.config.symlink`                                           | เตือน          | ไม่รองรับไฟล์การกำหนดค่าที่เป็น symlink สำหรับการเขียน และเพิ่มอีกขอบเขตความเชื่อถือ     | แทนที่ด้วยไฟล์การกำหนดค่าปกติ หรือชี้ `OPENCLAW_CONFIG_PATH` ไปยังไฟล์จริง                  | ไม่       |
| `fs.config.perms_group_readable`                              | เตือน          | ผู้ใช้ในกลุ่มสามารถอ่านโทเค็น/การตั้งค่าการกำหนดค่าได้                                          | สิทธิ์ระบบไฟล์บนไฟล์การกำหนดค่า                                                                      | ใช่      |
| `fs.config.perms_world_readable`                              | ร้ายแรง      | การกำหนดค่าอาจเปิดเผยโทเค็น/การตั้งค่า                                                    | สิทธิ์ระบบไฟล์บนไฟล์การกำหนดค่า                                                                      | ใช่      |
| `fs.config_include.perms_writable`                            | ร้ายแรง      | ไฟล์ include ของการกำหนดค่าอาจถูกแก้ไขโดยผู้อื่นได้                                        | สิทธิ์ไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                   | ใช่      |
| `fs.config_include.perms_group_readable`                      | เตือน          | ผู้ใช้ในกลุ่มสามารถอ่านความลับ/การตั้งค่าที่ include ไว้ได้                                       | สิทธิ์ไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                   | ใช่      |
| `fs.config_include.perms_world_readable`                      | ร้ายแรง      | ความลับ/การตั้งค่าที่ include ไว้อ่านได้โดยทุกคน                                         | สิทธิ์ไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                   | ใช่      |
| `fs.auth_profiles.perms_writable`                             | ร้ายแรง      | ผู้อื่นสามารถแทรกหรือแทนที่ข้อมูลประจำตัวโมเดลที่จัดเก็บไว้                                | สิทธิ์ `agents/<agentId>/agent/auth-profiles.json`                                                    | ใช่      |
| `fs.auth_profiles.perms_readable`                             | เตือน          | ผู้อื่นสามารถอ่าน API keys และ OAuth tokens ได้                                            | สิทธิ์ `agents/<agentId>/agent/auth-profiles.json`                                                    | ใช่      |
| `fs.credentials_dir.perms_writable`                           | ร้ายแรง      | ผู้อื่นสามารถแก้ไขสถานะการจับคู่/ข้อมูลประจำตัวของช่องทางได้                                   | สิทธิ์ระบบไฟล์บน `~/.openclaw/credentials`                                                        | ใช่      |
| `fs.credentials_dir.perms_readable`                           | เตือน          | ผู้อื่นสามารถอ่านสถานะข้อมูลประจำตัวของช่องทางได้                                             | สิทธิ์ระบบไฟล์บน `~/.openclaw/credentials`                                                        | ใช่      |
| `fs.sessions_store.perms_readable`                            | เตือน          | ผู้อื่นสามารถอ่าน transcript/metadata ของเซสชันได้                                         | สิทธิ์ของ session store                                                                                  | ใช่      |
| `fs.log_file.perms_readable`                                  | เตือน          | ผู้อื่นสามารถอ่านบันทึกที่ถูกปกปิดแล้วแต่ยังมีความอ่อนไหวได้                                    | สิทธิ์ของไฟล์บันทึก Gateway                                                                               | ใช่      |
| `fs.synced_dir`                                               | เตือน          | สถานะ/การกำหนดค่าใน iCloud/Dropbox/Drive ขยายการเปิดเผยโทเค็น/transcript              | ย้ายการกำหนดค่า/สถานะออกจากโฟลเดอร์ที่ซิงค์                                                                 | ไม่       |
| `gateway.bind_no_auth`                                        | ร้ายแรง      | การ bind ระยะไกลที่ไม่มี shared secret                                                    | `gateway.bind`, `gateway.auth.*`                                                                     | ไม่       |
| `gateway.loopback_no_auth`                                    | ร้ายแรง      | loopback ที่ผ่าน reverse proxy อาจกลายเป็นแบบไม่ผ่านการยืนยันตัวตน                                  | `gateway.auth.*`, การตั้งค่า proxy                                                                        | ไม่       |
| `gateway.trusted_proxies_missing`                             | เตือน          | มีส่วนหัว reverse-proxy อยู่แต่ยังไม่ได้เชื่อถือ                                    | `gateway.trustedProxies`                                                                             | ไม่       |
| `gateway.http.no_auth`                                        | เตือน/ร้ายแรง | Gateway HTTP APIs เข้าถึงได้ด้วย `auth.mode="none"`                                  | `gateway.auth.mode`, `gateway.http.endpoints.*`, `plugins.entries.admin-http-rpc`                    | ไม่       |
| `gateway.http.session_key_override_enabled`                   | ข้อมูล          | ผู้เรียก HTTP API สามารถ override `sessionKey` ได้                                           | `gateway.http.allowSessionKeyOverride`                                                               | ไม่       |
| `gateway.tools_invoke_http.dangerous_allow`                   | เตือน/ร้ายแรง | เปิดใช้งาน dangerous tools อีกครั้งผ่าน HTTP API สำหรับผู้เรียก owner/admin                     | `gateway.tools.allow`                                                                                | ไม่       |
| `gateway.nodes.allow_commands_dangerous`                      | เตือน/ร้ายแรง | เปิดใช้งานคำสั่งโหนดที่มีผลกระทบสูง (camera/screen/contacts/calendar/SMS)              | `gateway.nodes.allowCommands`                                                                        | ไม่       |
| `gateway.nodes.deny_commands_ineffective`                     | เตือน          | รายการ deny ที่คล้าย pattern ไม่ตรงกับข้อความ shell หรือกลุ่ม                          | `gateway.nodes.denyCommands`                                                                         | ไม่       |
| `gateway.tailscale_funnel`                                    | ร้ายแรง      | เปิดเผยต่ออินเทอร์เน็ตสาธารณะ                                                             | `gateway.tailscale.mode`                                                                             | ไม่       |
| `gateway.tailscale_serve`                                     | ข้อมูล          | เปิดใช้งานการเปิดเผยใน tailnet ผ่าน Serve                                                | `gateway.tailscale.mode`                                                                             | ไม่       |
| `gateway.control_ui.allowed_origins_required`                 | ร้ายแรง      | Control UI ที่ไม่ใช่ local loopback ไม่มี allowlist สำหรับ browser-origin ที่ระบุชัด                    | `gateway.controlUi.allowedOrigins`                                                                   | ไม่       |
| `gateway.control_ui.allowed_origins_wildcard`                 | เตือน/ร้ายแรง | `allowedOrigins=["*"]` ปิดใช้งาน allowlisting สำหรับ browser-origin                          | `gateway.controlUi.allowedOrigins`                                                                   | ไม่       |
| `gateway.control_ui.host_header_origin_fallback`              | เตือน/ร้ายแรง | เปิดใช้งาน Host-header origin fallback (ลดระดับการป้องกัน DNS rebinding)              | `gateway.controlUi.dangerouslyAllowHostHeaderOriginFallback`                                         | ไม่       |
| `gateway.control_ui.insecure_auth`                            | เตือน          | เปิดใช้งาน toggle ความเข้ากันได้ของ insecure-auth                                           | `gateway.controlUi.allowInsecureAuth`                                                                | ไม่       |
| `gateway.control_ui.device_auth_disabled`                     | ร้ายแรง      | ปิดใช้งานการตรวจสอบตัวตนอุปกรณ์                                                       | `gateway.controlUi.dangerouslyDisableDeviceAuth`                                                     | ไม่       |
| `gateway.real_ip_fallback_enabled`                            | เตือน/ร้ายแรง | การเชื่อถือ fallback `X-Real-IP` อาจทำให้ปลอม source-IP ผ่าน proxy ที่ตั้งค่าผิดได้      | `gateway.allowRealIpFallback`, `gateway.trustedProxies`                                              | ไม่       |
| `gateway.token_too_short`                                     | เตือน          | shared token สั้นทำให้ brute force ได้ง่ายขึ้น                                          | `gateway.auth.token`                                                                                 | ไม่       |
| `gateway.auth_no_rate_limit`                                  | เตือน          | auth ที่เปิดเผยโดยไม่มี rate limiting เพิ่มความเสี่ยง brute-force                        | `gateway.auth.rateLimit`                                                                             | ไม่       |
| `gateway.trusted_proxy_auth`                                  | ร้ายแรง      | ตอนนี้ตัวตนของ proxy กลายเป็นขอบเขต auth                                         | `gateway.auth.mode="trusted-proxy"`                                                                  | ไม่       |
| `gateway.trusted_proxy_no_proxies`                            | ร้ายแรง      | auth แบบ trusted-proxy ที่ไม่มี IP ของ trusted proxy ไม่ปลอดภัย                               | `gateway.trustedProxies`                                                                             | ไม่       |
| `gateway.trusted_proxy_no_user_header`                        | ร้ายแรง      | auth แบบ trusted-proxy ไม่สามารถระบุตัวตนผู้ใช้ได้อย่างปลอดภัย                               | `gateway.auth.trustedProxy.userHeader`                                                               | ไม่       |
| `gateway.trusted_proxy_no_allowlist`                          | เตือน          | auth แบบ trusted-proxy ยอมรับผู้ใช้ upstream ที่ผ่านการยืนยันตัวตนทุกคน                           | `gateway.auth.trustedProxy.allowUsers`                                                               | ไม่       |
| `gateway.trusted_proxy_allow_loopback`                        | warn          | การยืนยันตัวตนแบบพร็อกซีที่เชื่อถือได้ยอมรับแหล่งพร็อกซี loopback ที่อนุญาตไว้อย่างชัดเจน                 | `gateway.auth.trustedProxy.allowLoopback`                                                            | ไม่ใช่       |
| `gateway.probe_auth_secretref_unavailable`                    | warn          | การตรวจสอบเชิงลึกไม่สามารถแก้ SecretRefs สำหรับการยืนยันตัวตนในเส้นทางคำสั่งนี้                    | แหล่งการยืนยันตัวตนของ deep-probe / ความพร้อมใช้งานของ SecretRef                                                      | ไม่ใช่       |
| `gateway.probe_failed`                                        | warn/critical | การตรวจสอบ Gateway แบบสดล้มเหลว                                                            | การเข้าถึง/การยืนยันตัวตนของ gateway                                                                            | ไม่ใช่       |
| `discovery.mdns_full_mode`                                    | warn/critical | โหมดเต็มของ mDNS ประกาศเมตาดาต้า `cliPath`/`sshPort` บนเครือข่ายภายในเครื่อง              | `discovery.mdns.mode`, `gateway.bind`                                                                | ไม่ใช่       |
| `config.insecure_or_dangerous_flags`                          | warn          | เปิดใช้แฟล็กดีบักที่ไม่ปลอดภัย/อันตรายหนึ่งรายการ                                         | คีย์ที่ระบุในรายละเอียดของข้อค้นพบ                                                                          | ไม่ใช่       |
| `security.audit.suppressions.active`                          | info          | เอาต์พุตการตรวจสอบมีการกำหนดค่าการระงับไว้และอาจถูกกรอง                         | `security.audit.suppressions`                                                                        | ไม่ใช่       |
| `config.secrets.gateway_password_in_config`                   | warn          | รหัสผ่าน Gateway ถูกเก็บไว้โดยตรงในการกำหนดค่า                                        | `gateway.auth.password`                                                                              | ไม่ใช่       |
| `config.secrets.hooks_token_in_config`                        | warn          | โทเค็น bearer ของ Hook ถูกเก็บไว้โดยตรงในการกำหนดค่า                                       | `hooks.token`                                                                                        | ไม่ใช่       |
| `hooks.token_reuse_gateway_token`                             | critical      | โทเค็น ingress ของ Hook ยังปลดล็อกการยืนยันตัวตนของ Gateway ด้วย                                         | `hooks.token`, `gateway.auth.token`, `gateway.auth.password`                                         | ไม่ใช่       |
| `hooks.token_too_short`                                       | warn          | ทำให้การ brute force บน ingress ของ Hook ง่ายขึ้น                                                   | `hooks.token`                                                                                        | ไม่ใช่       |
| `hooks.default_session_key_unset`                             | warn          | การรันเอเจนต์ของ Hook กระจายออกไปยังเซสชันต่อคำขอที่สร้างขึ้น                          | `hooks.defaultSessionKey`                                                                            | ไม่ใช่       |
| `hooks.allowed_agent_ids_unrestricted`                        | warn/critical | ผู้เรียก Hook ที่ผ่านการยืนยันตัวตนอาจกำหนดเส้นทางไปยังเอเจนต์ใดก็ได้ที่กำหนดค่าไว้                         | `hooks.allowedAgentIds`                                                                              | ไม่ใช่       |
| `hooks.request_session_key_enabled`                           | warn/critical | ผู้เรียกภายนอกสามารถเลือก sessionKey ได้                                                | `hooks.allowRequestSessionKey`                                                                       | ไม่ใช่       |
| `hooks.request_session_key_prefixes_missing`                  | warn/critical | ไม่มีขอบเขตจำกัดรูปแบบของคีย์เซสชันภายนอก                                              | `hooks.allowedSessionKeyPrefixes`                                                                    | ไม่ใช่       |
| `hooks.path_root`                                             | critical      | เส้นทาง Hook คือ `/` ทำให้ ingress ชนกันหรือกำหนดเส้นทางผิดได้ง่ายขึ้น                       | `hooks.path`                                                                                         | ไม่ใช่       |
| `hooks.installs_unpinned_npm_specs`                           | warn          | ระเบียนการติดตั้ง Hook ไม่ได้ตรึงกับสเปก npm ที่เปลี่ยนไม่ได้                           | เมตาดาต้าการติดตั้ง Hook                                                                                | ไม่ใช่       |
| `hooks.installs_missing_integrity`                            | warn          | ระเบียนการติดตั้ง Hook ขาดเมตาดาต้า integrity                                         | เมตาดาต้าการติดตั้ง Hook                                                                                | ไม่ใช่       |
| `hooks.installs_version_drift`                                | warn          | ระเบียนการติดตั้ง Hook คลาดเคลื่อนจากแพ็กเกจที่ติดตั้งไว้                                   | เมตาดาต้าการติดตั้ง Hook                                                                                | ไม่ใช่       |
| `logging.redact_off`                                          | warn          | ค่าที่ละเอียดอ่อนรั่วไหลไปยังล็อก/สถานะ                                                 | `logging.redactSensitive`                                                                            | ใช่      |
| `browser.control_invalid_config`                              | warn          | การกำหนดค่าการควบคุมเบราว์เซอร์ไม่ถูกต้องก่อนรันไทม์                                     | `browser.*`                                                                                          | ไม่ใช่       |
| `browser.control_no_auth`                                     | critical      | การควบคุมเบราว์เซอร์ถูกเปิดเผยโดยไม่มีการยืนยันตัวตนด้วยโทเค็น/รหัสผ่าน                                  | `gateway.auth.*`                                                                                     | ไม่ใช่       |
| `browser.remote_cdp_http`                                     | warn          | CDP ระยะไกลผ่าน HTTP แบบไม่เข้ารหัสขาดการเข้ารหัสระดับการขนส่ง                                | โปรไฟล์เบราว์เซอร์ `cdpUrl`                                                                             | ไม่ใช่       |
| `browser.remote_cdp_private_host`                             | warn          | CDP ระยะไกลชี้ไปยังโฮสต์ส่วนตัว/ภายใน                                           | โปรไฟล์เบราว์เซอร์ `cdpUrl`, `browser.ssrfPolicy.*`                                                     | ไม่ใช่       |
| `sandbox.docker_config_mode_off`                              | warn          | มีการกำหนดค่า Docker ของ Sandbox อยู่แต่ไม่ได้ใช้งาน                                           | `agents.*.sandbox.mode`                                                                              | ไม่ใช่       |
| `sandbox.bind_mount_non_absolute`                             | warn          | bind mount แบบสัมพันธ์อาจถูกแก้เส้นทางอย่างคาดเดาไม่ได้                                       | `agents.*.sandbox.docker.binds[]`                                                                    | ไม่ใช่       |
| `sandbox.dangerous_bind_mount`                                | critical      | bind mount ของ Sandbox ชี้ไปยังเส้นทางระบบ ข้อมูลรับรอง หรือ Docker socket ที่ถูกบล็อก        | `agents.*.sandbox.docker.binds[]`                                                                    | ไม่ใช่       |
| `sandbox.dangerous_network_mode`                              | critical      | เครือข่าย Docker ของ Sandbox ใช้โหมด `host` หรือ `container:*` แบบเข้าร่วม namespace              | `agents.*.sandbox.docker.network`                                                                    | ไม่ใช่       |
| `sandbox.dangerous_seccomp_profile`                           | critical      | โปรไฟล์ seccomp ของ Sandbox ทำให้การแยกคอนเทนเนอร์อ่อนลง                                  | `agents.*.sandbox.docker.securityOpt`                                                                | ไม่ใช่       |
| `sandbox.dangerous_apparmor_profile`                          | critical      | โปรไฟล์ AppArmor ของ Sandbox ทำให้การแยกคอนเทนเนอร์อ่อนลง                                 | `agents.*.sandbox.docker.securityOpt`                                                                | ไม่ใช่       |
| `sandbox.browser_cdp_bridge_unrestricted`                     | warn          | บริดจ์เบราว์เซอร์ของ Sandbox ถูกเปิดเผยโดยไม่มีการจำกัดช่วงแหล่งที่มา                   | `sandbox.browser.cdpSourceRange`                                                                     | ไม่ใช่       |
| `sandbox.browser_container.non_loopback_publish`              | critical      | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่เผยแพร่ CDP บนอินเทอร์เฟซที่ไม่ใช่ loopback                  | การกำหนดค่าการเผยแพร่คอนเทนเนอร์ Sandbox ของเบราว์เซอร์                                                             | ไม่ใช่       |
| `sandbox.browser_container.hash_label_missing`                | warn          | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่มีมาก่อนป้ายกำกับแฮชการกำหนดค่าปัจจุบัน                       | `openclaw sandbox recreate --browser --all`                                                          | ไม่ใช่       |
| `sandbox.browser_container.hash_epoch_stale`                  | warn          | คอนเทนเนอร์เบราว์เซอร์ที่มีอยู่มีมาก่อน epoch การกำหนดค่าเบราว์เซอร์ปัจจุบัน                     | `openclaw sandbox recreate --browser --all`                                                          | ไม่ใช่       |
| `tools.exec.host_sandbox_no_sandbox_defaults`                 | warn          | `exec host=sandbox` ล้มเหลวแบบปิดเมื่อ Sandbox ปิดอยู่                                 | `tools.exec.host`, `agents.defaults.sandbox.mode`                                                    | ไม่ใช่       |
| `tools.exec.host_sandbox_no_sandbox_agents`                   | warn          | `exec host=sandbox` ต่อเอเจนต์ล้มเหลวแบบปิดเมื่อ Sandbox ปิดอยู่                       | `agents.list[].tools.exec.host`, `agents.list[].sandbox.mode`                                        | ไม่ใช่       |
| `tools.exec.security_full_configured`                         | warn/critical | การรันคำสั่งบนโฮสต์กำลังทำงานด้วย `security="full"`                                          | `tools.exec.security`, `agents.list[].tools.exec.security`                                           | ไม่ใช่       |
| `tools.exec.agent_skill_mcp_boundary_drift`                   | warn          | มี allowlist ของ skill เอเจนต์อยู่ขณะที่การรันคำสั่งบนโฮสต์สามารถเข้าถึงไคลเอนต์/รีจิสทรี MCP ได้  | `agents.list[].tools.exec.*`, การแยก Sandbox/OS, ข้อมูลรับรองของเซิร์ฟเวอร์ MCP                           | ไม่ใช่       |
| `tools.exec.fs_tools_disabled_but_exec_enabled`               | warn          | นโยบายเครื่องมือระบบไฟล์ไม่ได้ทำให้การรันเชลล์เป็นแบบอ่านอย่างเดียว                       | `tools.deny`, `agents.list[].tools.deny`, `agents.*.sandbox.workspaceAccess`                         | ไม่ใช่       |
| `tools.exec.auto_allow_skills_enabled`                        | warn          | การอนุมัติ Exec เชื่อถือไบนารีของ skill โดยปริยาย                                           | ไฟล์การอนุมัติของโฮสต์                                                                                  | ไม่ใช่       |
| `tools.exec.allowlist_interpreter_without_strict_inline_eval` | warn          | allowlist ของอินเทอร์พรีเตอร์อนุญาต inline eval โดยไม่บังคับให้ขออนุมัติซ้ำ                  | `tools.exec.strictInlineEval`, `agents.list[].tools.exec.strictInlineEval`, allowlist การอนุมัติ exec | ไม่ใช่       |
| `tools.exec.safe_bins_interpreter_unprofiled`                 | warn          | ไบนารีอินเทอร์พรีเตอร์/รันไทม์ใน `safeBins` ที่ไม่มีโปรไฟล์ชัดเจนทำให้ความเสี่ยงของ exec กว้างขึ้น   | `tools.exec.safeBins`, `tools.exec.safeBinProfiles`, `agents.list[].tools.exec.*`                    | ไม่ใช่       |
| `tools.exec.safe_bins_broad_behavior`                         | warn          | เครื่องมือที่มีพฤติกรรมกว้างใน `safeBins` ทำให้โมเดลความเชื่อถือ stdin-filter ความเสี่ยงต่ำอ่อนลง      | `tools.exec.safeBins`, `agents.list[].tools.exec.safeBins`                                           | ไม่ใช่       |
| `tools.exec.safe_bin_trusted_dirs_risky`                      | warn          | `safeBinTrustedDirs` มีไดเรกทอรีที่เปลี่ยนแปลงได้หรือมีความเสี่ยง                           | `tools.exec.safeBinTrustedDirs`, `agents.list[].tools.exec.safeBinTrustedDirs`                       | ไม่       |
| `skills.workspace.symlink_escape`                             | warn          | `skills/**/SKILL.md` ในเวิร์กสเปซ resolve ออกนอก root ของเวิร์กสเปซ (symlink-chain drift) | สถานะระบบไฟล์ของ workspace `skills/**`                                                               | ไม่       |
| `plugins.extensions_no_allowlist`                             | warn          | Plugin ถูกติดตั้งโดยไม่มี allowlist ของ plugin ที่ระบุชัดเจน                           | `plugins.allowlist`                                                                                  | ไม่       |
| `plugins.installs_unpinned_npm_specs`                         | warn          | ระเบียนดัชนี Plugin ไม่ได้ pin ไว้กับ npm specs ที่เปลี่ยนแปลงไม่ได้                           | metadata การติดตั้ง plugin                                                                              | ไม่       |
| `plugins.installs_missing_integrity`                          | warn          | ระเบียนดัชนี Plugin ไม่มี metadata ด้าน integrity                                         | metadata การติดตั้ง plugin                                                                              | ไม่       |
| `plugins.installs_version_drift`                              | warn          | ระเบียนดัชนี Plugin drift จากแพ็กเกจที่ติดตั้งไว้                                   | metadata การติดตั้ง plugin                                                                              | ไม่       |
| `plugins.code_safety`                                         | warn/critical | การสแกนโค้ด Plugin พบรูปแบบที่น่าสงสัยหรืออันตราย                              | โค้ด plugin / แหล่งติดตั้ง                                                                         | ไม่       |
| `plugins.code_safety.entry_path`                              | warn          | เส้นทาง entry ของ Plugin ชี้เข้าไปในตำแหน่งที่ซ่อนอยู่หรือ `node_modules`                     | manifest ของ plugin `entry`                                                                              | ไม่       |
| `plugins.code_safety.entry_escape`                            | critical      | entry ของ Plugin หลุดออกจากไดเรกทอรี plugin                                            | manifest ของ plugin `entry`                                                                              | ไม่       |
| `plugins.code_safety.scan_failed`                             | warn          | การสแกนโค้ด Plugin ไม่สามารถเสร็จสมบูรณ์ได้                                                  | เส้นทาง plugin / สภาพแวดล้อมการสแกน                                                                       | ไม่       |
| `skills.code_safety`                                          | warn/critical | metadata/โค้ดของตัวติดตั้ง Skill มีรูปแบบที่น่าสงสัยหรืออันตราย              | แหล่งติดตั้ง skill                                                                                 | ไม่       |
| `skills.code_safety.scan_failed`                              | warn          | การสแกนโค้ด Skill ไม่สามารถเสร็จสมบูรณ์ได้                                                   | สภาพแวดล้อมการสแกน skill                                                                               | ไม่       |
| `security.exposure.open_channels_with_exec`                   | warn/critical | ห้องแบบแชร์/สาธารณะสามารถเข้าถึงเอเจนต์ที่เปิดใช้ exec ได้                                    | `channels.*.dmPolicy`, `channels.*.groupPolicy`, `tools.exec.*`, `agents.list[].tools.exec.*`        | ไม่       |
| `security.exposure.open_groups_with_elevated`                 | critical      | DM/กลุ่มที่เปิดอยู่ + เครื่องมือที่ยกระดับสิทธิ์สร้างเส้นทาง prompt-injection ที่มีผลกระทบสูง           | เส้นทางนโยบาย DM ระดับบนสุดหรือแบบซ้อน, การ override บัญชี, `channels.*.groupPolicy`                     | ไม่       |
| `security.exposure.open_groups_with_runtime_or_fs`            | critical/warn | DM/กลุ่มที่เปิดอยู่สามารถเข้าถึงเครื่องมือคำสั่ง/ไฟล์ได้โดยไม่มีการ์ด sandbox/workspace        | เส้นทางนโยบาย DM/group, `tools.profile/deny`, `tools.fs.workspaceOnly`, `agents.*.sandbox.mode`       | ไม่       |
| `security.trust_model.multi_user_heuristic`                   | warn          | Config ดูเหมือนเป็นแบบหลายผู้ใช้ ขณะที่ trust model ของ gateway เป็นผู้ช่วยส่วนบุคคล              | แบ่งขอบเขตความไว้วางใจ หรือการ harden ผู้ใช้ร่วมกัน (`sandbox.mode`, การกำหนดขอบเขต tool deny/workspace)      | ไม่       |
| `tools.profile_minimal_overridden`                            | warn          | การ override ของเอเจนต์ข้าม profile แบบ minimal ระดับ global                                        | `agents.list[].tools.profile`                                                                        | ไม่       |
| `plugins.tools_reachable_permissive_policy`                   | warn          | เครื่องมือ Extension เข้าถึงได้ใน context ที่ผ่อนปรน                                     | `tools.profile` + tool allow/deny                                                                    | ไม่       |
| `models.legacy`                                               | warn          | ยังมีการกำหนดค่า family ของโมเดล legacy อยู่                                           | การเลือกโมเดล                                                                                      | ไม่       |
| `models.weak_tier`                                            | warn          | โมเดลที่กำหนดค่าไว้ต่ำกว่า tier ที่แนะนำในปัจจุบัน                                | การเลือกโมเดล                                                                                      | ไม่       |
| `models.small_params`                                         | critical/info | โมเดลขนาดเล็ก + พื้นผิวเครื่องมือที่ไม่ปลอดภัยเพิ่มความเสี่ยงจาก injection                             | การเลือกโมเดล + นโยบาย sandbox/tool                                                                   | ไม่       |
| `summary.attack_surface`                                      | info          | สรุปรวมสถานะ auth, channel, tool และ exposure                         | หลายคีย์ (ดูรายละเอียด finding)                                                                   | ไม่       |

## ที่เกี่ยวข้อง

- [ความปลอดภัย](/th/gateway/security)
- [การกำหนดค่า](/th/gateway/configuration)
- [การยืนยันตัวตนพร็อกซีที่เชื่อถือได้](/th/gateway/trusted-proxy-auth)
