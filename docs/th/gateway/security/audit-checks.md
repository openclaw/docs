---
read_when:
    - คุณเห็น `checkId` เฉพาะรายการหนึ่งในเอาต์พุตของ `openclaw security audit` และต้องการทราบว่ามันหมายถึงอะไร
    - คุณต้องการ fix key/path สำหรับผลการตรวจพบที่กำหนดไว้ კონკრეტely
    - คุณกำลังคัดกรองระดับความรุนแรงในระหว่างการรัน security audit
summary: แค็ตตาล็อกอ้างอิงของ checkIds ที่ถูกส่งออกโดย openclaw security audit
title: การตรวจสอบ security audit
x-i18n:
    generated_at: "2026-04-23T10:18:19Z"
    model: gpt-5.4
    provider: openai
    source_hash: b53a8dc798ecc8c37be43758b741c68772b10c92323ab3d26592d7b3cc2c795e
    source_path: gateway/security/audit-checks.md
    workflow: 15
---

# การตรวจสอบ security audit

`openclaw security audit` จะส่งออกผลการตรวจพบแบบมีโครงสร้างโดยอ้างอิงด้วย `checkId` หน้านี้คือแค็ตตาล็อกอ้างอิงสำหรับ ID เหล่านั้น สำหรับ threat model ระดับสูงและคำแนะนำด้านการเสริมความปลอดภัย ดู [Security](/th/gateway/security)

ค่า `checkId` ที่มีสัญญาณชัดเจนซึ่งคุณมีแนวโน้มจะพบมากที่สุดในการติดตั้งใช้งานจริง (ไม่ใช่รายการครบถ้วน):

| `checkId`                                                     | ระดับความรุนแรง | เหตุผลที่สำคัญ                                                                       | fix key/path หลัก                                                                                   | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | -------------- |
| `fs.state_dir.perms_world_writable`                           | critical        | ผู้ใช้/โปรเซสอื่นสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                              | สิทธิ์ของ filesystem บน `~/.openclaw`                                                               | ได้            |
| `fs.state_dir.perms_group_writable`                           | warn            | ผู้ใช้ในกลุ่มสามารถแก้ไขสถานะทั้งหมดของ OpenClaw ได้                                  | สิทธิ์ของ filesystem บน `~/.openclaw`                                                               | ได้            |
| `fs.state_dir.perms_readable`                                 | warn            | ไดเรกทอรีสถานะสามารถถูกอ่านได้โดยผู้อื่น                                              | สิทธิ์ของ filesystem บน `~/.openclaw`                                                               | ได้            |
| `fs.state_dir.symlink`                                        | warn            | เป้าหมายของไดเรกทอรีสถานะกลายเป็นขอบเขตความเชื่อถืออีกชั้นหนึ่ง                       | โครงร่าง filesystem ของไดเรกทอรีสถานะ                                                              | ไม่ได้         |
| `fs.config.perms_writable`                                    | critical        | ผู้อื่นสามารถเปลี่ยนนโยบาย auth/tool/config ได้                                      | สิทธิ์ของ filesystem บน `~/.openclaw/openclaw.json`                                                 | ได้            |
| `fs.config.symlink`                                           | warn            | ไม่รองรับการเขียนไปยังไฟล์ config ที่เป็น symlink และเพิ่มขอบเขตความเชื่อถืออีกชั้นหนึ่ง | แทนที่ด้วยไฟล์ config ปกติ หรือชี้ `OPENCLAW_CONFIG_PATH` ไปยังไฟล์จริง                            | ไม่ได้         |
| `fs.config.perms_group_readable`                              | warn            | ผู้ใช้ในกลุ่มสามารถอ่าน tokens/การตั้งค่าจาก config ได้                               | สิทธิ์ของ filesystem บนไฟล์ config                                                                  | ได้            |
| `fs.config.perms_world_readable`                              | critical        | config อาจเปิดเผย tokens/การตั้งค่า                                                  | สิทธิ์ของ filesystem บนไฟล์ config                                                                  | ได้            |
| `fs.config_include.perms_writable`                            | critical        | ไฟล์ include ของ config สามารถถูกแก้ไขโดยผู้อื่นได้                                   | สิทธิ์ของไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                | ได้            |
| `fs.config_include.perms_group_readable`                      | warn            | ผู้ใช้ในกลุ่มสามารถอ่าน secrets/การตั้งค่าที่ include ไว้ได้                          | สิทธิ์ของไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                | ได้            |
| `fs.config_include.perms_world_readable`                      | critical        | secrets/การตั้งค่าที่ include ไว้สามารถถูกอ่านได้โดยทุกคน                             | สิทธิ์ของไฟล์ include ที่อ้างอิงจาก `openclaw.json`                                                | ได้            |
| `fs.auth_profiles.perms_writable`                             | critical        | ผู้อื่นสามารถแทรกหรือแทนที่ข้อมูลรับรอง model ที่จัดเก็บไว้ได้                         | สิทธิ์ของ `agents/<agentId>/agent/auth-profiles.json`                                               | ได้            |
| `fs.auth_profiles.perms_readable`                             | warn            | ผู้อื่นสามารถอ่าน API keys และ OAuth tokens ได้                                       | สิทธิ์ของ `agents/<agentId>/agent/auth-profiles.json`                                               | ได้            |
| `fs.credentials_dir.perms_writable`                           | critical        | ผู้อื่นสามารถแก้ไขสถานะ pairing/credential ของ channel ได้                            | สิทธิ์ของ filesystem บน `~/.openclaw/credentials`                                                   | ได้            |
| `fs.credentials_dir.perms_readable`                           | warn            | ผู้อื่นสามารถอ่านสถานะ credential ของ channel ได้                                     | สิทธิ์ของ filesystem บน `~/.openclaw/credentials`                                                   | ได้            |
| `fs.sessions_store.perms_readable`                            | warn            | ผู้อื่นสามารถอ่าน session transcripts/metadata ได้                                     | สิทธิ์ของ session store                                                                             | ได้            |
| `fs.log_file.perms_readable`                                  | warn            | ผู้อื่นสามารถอ่าน logs ที่แม้จะปกปิดแล้วแต่ยังมีความอ่อนไหวอยู่                         | สิทธิ์ของไฟล์ log ของ Gateway                                                                       | ได้            |
| `fs.synced_dir`                                               | warn            | การวาง state/config ใน iCloud/Dropbox/Drive ทำให้ความเสี่ยงการเปิดเผย token/transcript กว้างขึ้น | ย้าย config/state ออกจากโฟลเดอร์ที่ซิงก์                                                            | ไม่ได้         |
| `gateway.bind_no_auth`                                        | critical        | bind กับเครือข่ายภายนอกโดยไม่มี shared secret                                        | `gateway.bind`, `gateway.auth.*`                                                                    | ไม่ได้         |
| `gateway.loopback_no_auth`                                    | critical        | loopback ที่ถูก reverse-proxy อาจกลายเป็นแบบไม่มี auth                                | `gateway.auth.*`, การตั้งค่า proxy                                                                  | ไม่ได้         |
| `gateway.trusted_proxies_missing`                             | warn            | มี reverse-proxy headers แต่ยังไม่ได้กำหนดให้เป็น trusted                              | `gateway.trustedProxies`                                                                            | ไม่ได้         |
| `gateway.http.no_auth`                                        | warn/critical   | Gateway HTTP APIs เข้าถึงได้เมื่อ `auth.mode="none"`                                  | `gateway.auth.mode`, `gateway.http.endpoints.*`                                                     | ไม่ได้         |
| `gateway.http.session_key_override_enabled`                   | info            | ผู้เรียก HTTP API สามารถ override `sessionKey` ได้                                    | `gateway.http.allowSessionKeyOverride`                                                              | ไม่ได้         |
| `gateway.tools_invoke_http.dangerous_allow`                   | warn/critical   | เปิดใช้เครื่องมืออันตรายผ่าน HTTP API อีกครั้ง                                        | `gateway.tools.allow`                                                                               | ไม่ได้         |
| `gateway.nodes.allow_commands_dangerous`                      | warn/critical   | เปิดใช้คำสั่ง Node ที่มีผลกระทบสูง (camera/screen/contacts/calendar/SMS)              | `gateway.nodes.allowCommands`                                                                       | ไม่ได้         |
| `gateway.nodes.deny_commands_ineffective`                     | warn            | รายการ deny แบบลักษณะคล้าย pattern ไม่ตรงกับข้อความ shell หรือกลุ่ม                  | `gateway.nodes.denyCommands`                                                                        | ไม่ได้         |
| `gateway.tailscale_funnel`                                    | critical        | เปิดเผยสู่ public internet                                                            | `gateway.tailscale.mode`                                                                            | ไม่ได้         |
| `gateway.tailscale_serve`                                     | info            | เปิดใช้งานการเปิดเผยผ่าน tailnet ด้วย Serve                                           | `gateway.tailscale.mode`                                                                            | ไม่ได้         |
| `gateway.control_ui.allowed_origins_required`                 | critical        | Control UI ที่ไม่ใช่ loopback โดยไม่มี allowlist ของ browser-origin แบบชัดเจน          | `gateway.controlUi.allowedOrigins`                                                                  | ไม่ได้         |
| `gateway.control_ui.allowed_origins_wildcard`                 | warn/critical   | `allowedOrigins=["*"]` ปิดการทำ allowlist ของ browser-origin                           | `gateway.controlUi.allowedOrigins`                                                                  | ไม่ได้         |
| `gateway.control_ui.host_header_origin_fallback`              | warn/critical   | เปิดใช้ Host-header origin fallback (ลดระดับการป้องกัน DNS rebinding)                 | `gateway.controlUi.dangerouslyAllowHostHeaderOriginFallback`                                        | ไม่ได้         |
| `gateway.control_ui.insecure_auth`                            | warn            | เปิดใช้ compatibility toggle สำหรับ insecure auth                                     | `gateway.controlUi.allowInsecureAuth`                                                               | ไม่ได้         |
| `gateway.control_ui.device_auth_disabled`                     | critical        | ปิดการตรวจสอบ device identity                                                        | `gateway.controlUi.dangerouslyDisableDeviceAuth`                                                    | ไม่ได้         |
| `gateway.real_ip_fallback_enabled`                            | warn/critical   | การเชื่อถือ fallback ของ `X-Real-IP` อาจเปิดให้ปลอมแปลง source IP ผ่าน proxy misconfig | `gateway.allowRealIpFallback`, `gateway.trustedProxies`                                             | ไม่ได้         |
| `gateway.token_too_short`                                     | warn            | shared token ที่สั้นมีโอกาสถูก brute force ได้ง่ายกว่า                               | `gateway.auth.token`                                                                                | ไม่ได้         |
| `gateway.auth_no_rate_limit`                                  | warn            | auth ที่เปิดเผยโดยไม่มี rate limiting เพิ่มความเสี่ยงจาก brute force                  | `gateway.auth.rateLimit`                                                                            | ไม่ได้         |
| `gateway.trusted_proxy_auth`                                  | critical        | ตอนนี้ตัวตนของ proxy กลายเป็นขอบเขต auth                                            | `gateway.auth.mode="trusted-proxy"`                                                                 | ไม่ได้         |
| `gateway.trusted_proxy_no_proxies`                            | critical        | trusted-proxy auth โดยไม่มี IP ของ trusted proxy นั้นไม่ปลอดภัย                       | `gateway.trustedProxies`                                                                            | ไม่ได้         |
| `gateway.trusted_proxy_no_user_header`                        | critical        | trusted-proxy auth ไม่สามารถ resolve ตัวตนผู้ใช้ได้อย่างปลอดภัย                       | `gateway.auth.trustedProxy.userHeader`                                                              | ไม่ได้         |
| `gateway.trusted_proxy_no_allowlist`                          | warn            | trusted-proxy auth ยอมรับผู้ใช้ upstream ที่ยืนยันตัวตนแล้วทุกคน                       | `gateway.auth.trustedProxy.allowUsers`                                                              | ไม่ได้         |
| `checkId`                                                     | ระดับความรุนแรง | เหตุผลที่สำคัญ                                                                       | fix key/path หลัก                                                                                   | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | -------------- |
| `gateway.probe_auth_secretref_unavailable`                    | warn            | Deep probe ไม่สามารถ resolve auth SecretRefs ได้ในเส้นทางคำสั่งนี้                  | แหล่ง auth ของ deep-probe / ความพร้อมใช้งานของ SecretRef                                           | ไม่ได้         |
| `gateway.probe_failed`                                        | warn/critical   | การ probe Gateway แบบสดล้มเหลว                                                     | การเข้าถึง/auth ของ Gateway                                                                         | ไม่ได้         |
| `discovery.mdns_full_mode`                                    | warn/critical   | โหมดเต็มของ mDNS ประกาศเมทาดาทา `cliPath`/`sshPort` บนเครือข่ายภายในเครื่อง        | `discovery.mdns.mode`, `gateway.bind`                                                               | ไม่ได้         |
| `config.insecure_or_dangerous_flags`                          | warn            | มีการเปิดใช้แฟล็กดีบักที่ไม่ปลอดภัย/อันตรายใด ๆ                                    | หลายคีย์ (ดูรายละเอียดในผลการตรวจพบ)                                                              | ไม่ได้         |
| `config.secrets.gateway_password_in_config`                   | warn            | รหัสผ่าน Gateway ถูกเก็บไว้โดยตรงใน config                                          | `gateway.auth.password`                                                                             | ไม่ได้         |
| `config.secrets.hooks_token_in_config`                        | warn            | hook bearer token ถูกเก็บไว้โดยตรงใน config                                         | `hooks.token`                                                                                       | ไม่ได้         |
| `hooks.token_reuse_gateway_token`                             | critical        | hook ingress token สามารถใช้ปลดล็อก auth ของ Gateway ได้ด้วย                        | `hooks.token`, `gateway.auth.token`                                                                 | ไม่ได้         |
| `hooks.token_too_short`                                       | warn            | เพิ่มความง่ายต่อการ brute force บน hook ingress                                     | `hooks.token`                                                                                       | ไม่ได้         |
| `hooks.default_session_key_unset`                             | warn            | การรัน agent ผ่าน hook กระจายออกเป็น sessions ต่อคำขอที่ถูกสร้างขึ้นเอง             | `hooks.defaultSessionKey`                                                                           | ไม่ได้         |
| `hooks.allowed_agent_ids_unrestricted`                        | warn/critical   | ผู้เรียก hook ที่ยืนยันตัวตนแล้วอาจกำหนดเส้นทางไปยัง agent ใดก็ได้ที่ตั้งค่าไว้      | `hooks.allowedAgentIds`                                                                             | ไม่ได้         |
| `hooks.request_session_key_enabled`                           | warn/critical   | ผู้เรียกจากภายนอกสามารถเลือก `sessionKey` ได้                                       | `hooks.allowRequestSessionKey`                                                                      | ไม่ได้         |
| `hooks.request_session_key_prefixes_missing`                  | warn/critical   | ไม่มีขอบเขตจำกัดรูปแบบของ session key จากภายนอก                                     | `hooks.allowedSessionKeyPrefixes`                                                                   | ไม่ได้         |
| `hooks.path_root`                                             | critical        | พาธของ hook คือ `/` ทำให้การรับคำขอชนกันหรือกำหนดเส้นทางผิดได้ง่ายขึ้น              | `hooks.path`                                                                                        | ไม่ได้         |
| `hooks.installs_unpinned_npm_specs`                           | warn            | ระเบียนการติดตั้ง hook ไม่ได้ตรึงไว้กับ npm specs ที่เปลี่ยนแปลงไม่ได้               | เมทาดาทาการติดตั้ง hook                                                                            | ไม่ได้         |
| `hooks.installs_missing_integrity`                            | warn            | ระเบียนการติดตั้ง hook ไม่มีเมทาดาทา integrity                                       | เมทาดาทาการติดตั้ง hook                                                                            | ไม่ได้         |
| `hooks.installs_version_drift`                                | warn            | ระเบียนการติดตั้ง hook คลาดเคลื่อนจากแพ็กเกจที่ติดตั้งจริง                           | เมทาดาทาการติดตั้ง hook                                                                            | ไม่ได้         |
| `logging.redact_off`                                          | warn            | ค่าที่อ่อนไหวรั่วไหลไปยัง logs/status                                               | `logging.redactSensitive`                                                                           | ได้            |
| `browser.control_invalid_config`                              | warn            | config ของ browser control ไม่ถูกต้องก่อนเริ่ม runtime                               | `browser.*`                                                                                         | ไม่ได้         |
| `browser.control_no_auth`                                     | critical        | browser control ถูกเปิดเผยโดยไม่มี token/password auth                               | `gateway.auth.*`                                                                                    | ไม่ได้         |
| `browser.remote_cdp_http`                                     | warn            | Remote CDP ผ่าน HTTP แบบไม่เข้ารหัสไม่มีการเข้ารหัสระดับ transport                   | `cdpUrl` ของ browser profile                                                                        | ไม่ได้         |
| `browser.remote_cdp_private_host`                             | warn            | Remote CDP ชี้ไปยังโฮสต์ private/internal                                            | `cdpUrl` ของ browser profile, `browser.ssrfPolicy.*`                                                | ไม่ได้         |
| `sandbox.docker_config_mode_off`                              | warn            | มี config ของ Sandbox Docker แต่ไม่ได้เปิดใช้งาน                                     | `agents.*.sandbox.mode`                                                                             | ไม่ได้         |
| `sandbox.bind_mount_non_absolute`                             | warn            | bind mounts แบบ relative อาจ resolve ได้ไม่แน่นอน                                    | `agents.*.sandbox.docker.binds[]`                                                                   | ไม่ได้         |
| `sandbox.dangerous_bind_mount`                                | critical        | target ของ Sandbox bind mount ชี้ไปยังพาธระบบ ข้อมูลรับรอง หรือ Docker socket ที่ถูกบล็อก | `agents.*.sandbox.docker.binds[]`                                                                   | ไม่ได้         |
| `sandbox.dangerous_network_mode`                              | critical        | เครือข่าย Docker ของ Sandbox ใช้โหมด `host` หรือ `container:*` แบบรวม namespace     | `agents.*.sandbox.docker.network`                                                                   | ไม่ได้         |
| `sandbox.dangerous_seccomp_profile`                           | critical        | โปรไฟล์ seccomp ของ Sandbox ทำให้การแยกตัวของคอนเทนเนอร์อ่อนแอลง                    | `agents.*.sandbox.docker.securityOpt`                                                               | ไม่ได้         |
| `sandbox.dangerous_apparmor_profile`                          | critical        | โปรไฟล์ AppArmor ของ Sandbox ทำให้การแยกตัวของคอนเทนเนอร์อ่อนแอลง                   | `agents.*.sandbox.docker.securityOpt`                                                               | ไม่ได้         |
| `sandbox.browser_cdp_bridge_unrestricted`                     | warn            | Sandbox browser bridge ถูกเปิดเผยโดยไม่มีข้อจำกัดช่วงแหล่งที่มา                      | `sandbox.browser.cdpSourceRange`                                                                    | ไม่ได้         |
| `sandbox.browser_container.non_loopback_publish`              | critical        | คอนเทนเนอร์ browser ที่มีอยู่เผยแพร่ CDP บนอินเทอร์เฟซที่ไม่ใช่ loopback            | config การ publish ของคอนเทนเนอร์ browser sandbox                                                  | ไม่ได้         |
| `sandbox.browser_container.hash_label_missing`                | warn            | คอนเทนเนอร์ browser ที่มีอยู่สร้างก่อน labels ของ config-hash รุ่นปัจจุบัน          | `openclaw sandbox recreate --browser --all`                                                         | ไม่ได้         |
| `sandbox.browser_container.hash_epoch_stale`                  | warn            | คอนเทนเนอร์ browser ที่มีอยู่สร้างก่อน browser config epoch ปัจจุบัน                 | `openclaw sandbox recreate --browser --all`                                                         | ไม่ได้         |
| `tools.exec.host_sandbox_no_sandbox_defaults`                 | warn            | `exec host=sandbox` จะล้มเหลวแบบปิดปลอดภัยเมื่อปิด sandbox                          | `tools.exec.host`, `agents.defaults.sandbox.mode`                                                   | ไม่ได้         |
| `tools.exec.host_sandbox_no_sandbox_agents`                   | warn            | `exec host=sandbox` ระดับต่อ agent จะล้มเหลวแบบปิดปลอดภัยเมื่อปิด sandbox           | `agents.list[].tools.exec.host`, `agents.list[].sandbox.mode`                                       | ไม่ได้         |
| `tools.exec.security_full_configured`                         | warn/critical   | Host exec กำลังรันด้วย `security="full"`                                             | `tools.exec.security`, `agents.list[].tools.exec.security`                                          | ไม่ได้         |
| `tools.exec.auto_allow_skills_enabled`                        | warn            | การอนุมัติ Exec เชื่อถือ skill bins โดยปริยาย                                      | `~/.openclaw/exec-approvals.json`                                                                   | ไม่ได้         |
| `tools.exec.allowlist_interpreter_without_strict_inline_eval` | warn            | allowlists ของ interpreter อนุญาต inline eval โดยไม่บังคับให้ขออนุมัติใหม่          | `tools.exec.strictInlineEval`, `agents.list[].tools.exec.strictInlineEval`, exec approvals allowlist | ไม่ได้         |
| `tools.exec.safe_bins_interpreter_unprofiled`                 | warn            | bins ของ interpreter/runtime ใน `safeBins` ที่ไม่มีโปรไฟล์ชัดเจนเพิ่มความเสี่ยงของ exec | `tools.exec.safeBins`, `tools.exec.safeBinProfiles`, `agents.list[].tools.exec.*`                  | ไม่ได้         |
| `tools.exec.safe_bins_broad_behavior`                         | warn            | เครื่องมือที่มีพฤติกรรมกว้างใน `safeBins` ทำให้ trust model แบบกรอง stdin ความเสี่ยงต่ำอ่อนลง | `tools.exec.safeBins`, `agents.list[].tools.exec.safeBins`                                          | ไม่ได้         |
| `tools.exec.safe_bin_trusted_dirs_risky`                      | warn            | `safeBinTrustedDirs` รวมไดเรกทอรีที่แก้ไขได้หรือมีความเสี่ยง                         | `tools.exec.safeBinTrustedDirs`, `agents.list[].tools.exec.safeBinTrustedDirs`                      | ไม่ได้         |
| `skills.workspace.symlink_escape`                             | warn            | `skills/**/SKILL.md` ใน workspace resolve ออกนอก workspace root (symlink-chain drift) | สถานะ filesystem ของ workspace `skills/**`                                                          | ไม่ได้         |
| `plugins.extensions_no_allowlist`                             | warn            | มีการติดตั้ง plugins โดยไม่มี plugin allowlist แบบชัดเจน                             | `plugins.allowlist`                                                                                 | ไม่ได้         |
| `plugins.installs_unpinned_npm_specs`                         | warn            | ระเบียนการติดตั้ง Plugin ไม่ได้ตรึงไว้กับ npm specs ที่เปลี่ยนแปลงไม่ได้             | เมทาดาทาการติดตั้ง plugin                                                                           | ไม่ได้         |
| `checkId`                                                     | ระดับความรุนแรง | เหตุผลที่สำคัญ                                                                       | fix key/path หลัก                                                                                   | แก้ไขอัตโนมัติ |
| ------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | -------------- |
| `plugins.installs_missing_integrity`                          | warn            | ระเบียนการติดตั้ง Plugin ไม่มีเมทาดาทา integrity                                     | เมทาดาทาการติดตั้ง plugin                                                                           | ไม่ได้         |
| `plugins.installs_version_drift`                              | warn            | ระเบียนการติดตั้ง Plugin คลาดเคลื่อนจากแพ็กเกจที่ติดตั้งจริง                         | เมทาดาทาการติดตั้ง plugin                                                                           | ไม่ได้         |
| `plugins.code_safety`                                         | warn/critical   | การสแกนโค้ด Plugin พบรูปแบบที่น่าสงสัยหรืออันตราย                                    | โค้ด plugin / แหล่งที่มาของการติดตั้ง                                                               | ไม่ได้         |
| `plugins.code_safety.entry_path`                              | warn            | พาธ entry ของ Plugin ชี้เข้าไปยังตำแหน่งที่ซ่อนอยู่หรือ `node_modules`               | `entry` ใน manifest ของ plugin                                                                      | ไม่ได้         |
| `plugins.code_safety.entry_escape`                            | critical        | entry ของ Plugin หลุดออกนอกไดเรกทอรีของ plugin                                      | `entry` ใน manifest ของ plugin                                                                      | ไม่ได้         |
| `plugins.code_safety.scan_failed`                             | warn            | การสแกนโค้ด Plugin ไม่สามารถทำให้เสร็จสมบูรณ์ได้                                     | พาธ plugin / สภาพแวดล้อมสำหรับการสแกน                                                             | ไม่ได้         |
| `skills.code_safety`                                          | warn/critical   | เมทาดาทา/โค้ดของตัวติดตั้ง skill มีรูปแบบที่น่าสงสัยหรืออันตราย                      | แหล่งที่มาของการติดตั้ง skill                                                                       | ไม่ได้         |
| `skills.code_safety.scan_failed`                              | warn            | การสแกนโค้ด skill ไม่สามารถทำให้เสร็จสมบูรณ์ได้                                      | สภาพแวดล้อมสำหรับการสแกน skill                                                                     | ไม่ได้         |
| `security.exposure.open_channels_with_exec`                   | warn/critical   | ห้องที่ใช้ร่วมกัน/สาธารณะสามารถเข้าถึง agents ที่เปิดใช้ exec                        | `channels.*.dmPolicy`, `channels.*.groupPolicy`, `tools.exec.*`, `agents.list[].tools.exec.*`      | ไม่ได้         |
| `security.exposure.open_groups_with_elevated`                 | critical        | groups แบบเปิดร่วมกับเครื่องมือสิทธิ์ยกระดับสร้างเส้นทาง prompt-injection ที่มีผลกระทบสูง | `channels.*.groupPolicy`, `tools.elevated.*`                                                        | ไม่ได้         |
| `security.exposure.open_groups_with_runtime_or_fs`            | critical/warn   | groups แบบเปิดสามารถเข้าถึงเครื่องมือคำสั่ง/ไฟล์ได้โดยไม่มีการป้องกันจาก sandbox/workspace | `channels.*.groupPolicy`, `tools.profile/deny`, `tools.fs.workspaceOnly`, `agents.*.sandbox.mode`  | ไม่ได้         |
| `security.trust_model.multi_user_heuristic`                   | warn            | config ดูเหมือนใช้งานหลายผู้ใช้ ขณะที่ trust model ของ gateway เป็นผู้ช่วยส่วนบุคคล   | แยกขอบเขตความเชื่อถือ หรือใช้การเสริมความปลอดภัยสำหรับผู้ใช้ร่วมกัน (`sandbox.mode`, tool deny/workspace scoping) | ไม่ได้         |
| `tools.profile_minimal_overridden`                            | warn            | การ override ของ agent ข้าม global minimal profile                                  | `agents.list[].tools.profile`                                                                        | ไม่ได้         |
| `plugins.tools_reachable_permissive_policy`                   | warn            | เครื่องมือของ extension เข้าถึงได้ในบริบทที่มีนโยบายผ่อนปรน                         | `tools.profile` + การ allow/deny เครื่องมือ                                                        | ไม่ได้         |
| `models.legacy`                                               | warn            | ยังมีการตั้งค่าตระกูล model แบบเดิมอยู่                                              | การเลือก model                                                                                      | ไม่ได้         |
| `models.weak_tier`                                            | warn            | models ที่ตั้งค่าไว้ต่ำกว่าระดับที่แนะนำในปัจจุบัน                                    | การเลือก model                                                                                      | ไม่ได้         |
| `models.small_params`                                         | critical/info   | models ขนาดเล็กร่วมกับพื้นผิวเครื่องมือที่ไม่ปลอดภัยเพิ่มความเสี่ยงจาก injection     | การเลือก model + นโยบาย sandbox/เครื่องมือ                                                         | ไม่ได้         |
| `summary.attack_surface`                                      | info            | สรุปภาพรวมของสถานะด้าน auth, channel, tool และการเปิดเผย                             | หลายคีย์ (ดูรายละเอียดในผลการตรวจพบ)                                                              | ไม่ได้         |

## ที่เกี่ยวข้อง

- [Security](/th/gateway/security)
- [Configuration](/th/gateway/configuration)
- [Trusted proxy auth](/th/gateway/trusted-proxy-auth)
