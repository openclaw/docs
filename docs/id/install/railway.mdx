---
read_when:
    - Men-deploy OpenClaw ke Railway
    - Anda menginginkan deployment cloud sekali klik dengan Control UI berbasis browser
summary: Deploy OpenClaw di Railway dengan template sekali klik
title: Railway
x-i18n:
    generated_at: "2026-04-05T13:58:43Z"
    model: gpt-5.4
    provider: openai
    source_hash: f59699aa4e9d27d50018acc2e89ed1bbb1b1a5c85e3fd52f2afe5b2c97b21720
    source_path: install/railway.mdx
    workflow: 15
---

Deploy OpenClaw di Railway dengan template sekali klik dan akses melalui web Control UI.
Ini adalah jalur termudah "tanpa terminal di server": Railway menjalankan Gateway untuk Anda.

## Daftar periksa cepat (pengguna baru)

1. Klik **Deploy on Railway** (di bawah).
2. Tambahkan **Volume** yang di-mount di `/data`.
3. Setel **Variables** yang diperlukan (setidaknya `OPENCLAW_GATEWAY_PORT` dan `OPENCLAW_GATEWAY_TOKEN`).
4. Aktifkan **HTTP Proxy** pada port `8080`.
5. Buka `https://<your-railway-domain>/openclaw` lalu hubungkan menggunakan shared secret yang telah dikonfigurasi. Template ini menggunakan `OPENCLAW_GATEWAY_TOKEN` secara default; jika Anda menggantinya dengan auth berbasis password, gunakan password tersebut.

## Deployment sekali klik

<a href="https://railway.com/deploy/clawdbot-railway-template" target="_blank" rel="noreferrer">
  Deploy on Railway
</a>

Setelah deployment, temukan URL publik Anda di **Railway → layanan Anda → Settings → Domains**.

Railway akan:

- memberi Anda domain yang dibuat otomatis (sering kali `https://<something>.up.railway.app`), atau
- menggunakan domain kustom Anda jika Anda menambahkannya.

Lalu buka:

- `https://<your-railway-domain>/openclaw` — Control UI

## Yang Anda dapatkan

- OpenClaw Gateway + Control UI yang di-host
- Penyimpanan persisten melalui Railway Volume (`/data`) sehingga `openclaw.json`,
  `auth-profiles.json` per-agent, state channel/provider, sesi, dan
  workspace tetap ada setelah redeploy

## Pengaturan Railway yang diperlukan

### Public Networking

Aktifkan **HTTP Proxy** untuk layanan.

- Port: `8080`

### Volume (wajib)

Pasang volume yang di-mount di:

- `/data`

### Variables

Setel variabel ini pada layanan:

- `OPENCLAW_GATEWAY_PORT=8080` (wajib — harus cocok dengan port di Public Networking)
- `OPENCLAW_GATEWAY_TOKEN` (wajib; perlakukan sebagai secret admin)
- `OPENCLAW_STATE_DIR=/data/.openclaw` (direkomendasikan)
- `OPENCLAW_WORKSPACE_DIR=/data/workspace` (direkomendasikan)

## Hubungkan channel

Gunakan Control UI di `/openclaw` atau jalankan `openclaw onboard` melalui shell Railway untuk petunjuk penyiapan channel:

- [Telegram](/id/channels/telegram) (paling cepat — hanya perlu token bot)
- [Discord](/id/channels/discord)
- [Semua channel](/id/channels)

## Backup & migrasi

Ekspor state, konfigurasi, auth profile, dan workspace Anda:

```bash
openclaw backup create
```

Ini membuat arsip backup portabel dengan state OpenClaw plus workspace
yang telah dikonfigurasi. Lihat [Backup](/cli/backup) untuk detailnya.

## Langkah selanjutnya

- Siapkan channel pesan: [Channels](/id/channels)
- Konfigurasikan Gateway: [Konfigurasi Gateway](/id/gateway/configuration)
- Jaga OpenClaw tetap terbaru: [Memperbarui](/install/updating)
