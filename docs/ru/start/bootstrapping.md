---
read_when:
    - Понимание того, что происходит при первом запуске агента
    - Объяснение, где находятся файлы начальной настройки
    - Отладка настройки идентификации при онбординге
sidebarTitle: Bootstrapping
summary: Ритуал начальной подготовки агента, который заполняет рабочую область и файлы идентификации
title: Инициализация агента
x-i18n:
    generated_at: "2026-06-28T23:47:46Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: e25f05ca47184068b87f0bf8b7dea1c427f4ed48edde170a74888d586b8a606d
    source_path: start/bootstrapping.md
    workflow: 16
---

Начальная подготовка — это ритуал **первого запуска**, который подготавливает рабочее пространство агента и
собирает сведения об идентичности. Она выполняется после первичной настройки, когда агент запускается
в первый раз.

## Что делает начальная подготовка

При первом запуске агента OpenClaw подготавливает рабочее пространство (по умолчанию
`~/.openclaw/workspace`):

- Создает начальные версии `AGENTS.md`, `BOOTSTRAP.md`, `IDENTITY.md`, `USER.md`.
- Запускает короткий ритуал вопросов и ответов (по одному вопросу за раз).
- Записывает идентичность и предпочтения в `IDENTITY.md`, `USER.md`, `SOUL.md`.
- Удаляет `BOOTSTRAP.md` после завершения, чтобы подготовка выполнялась только один раз.

Для встроенных/локальных запусков моделей OpenClaw не помещает `BOOTSTRAP.md` в
привилегированный системный контекст. При основном интерактивном первом запуске он все равно передает
содержимое файла в пользовательском промпте, чтобы модели, которые не всегда надежно вызывают инструмент
`read`, могли завершить ритуал. Если текущий запуск не может безопасно получить доступ к
рабочему пространству, агент получает ограниченную заметку о начальной подготовке вместо общего приветствия.

## Пропуск начальной подготовки

Чтобы пропустить это для заранее подготовленного рабочего пространства, выполните `openclaw onboard --skip-bootstrap`.

## Где она выполняется

Начальная подготовка всегда выполняется на **хосте Gateway**. Если приложение macOS подключается к
удаленному Gateway, рабочее пространство и файлы начальной подготовки находятся на этой удаленной
машине.

<Note>
Когда Gateway работает на другой машине, редактируйте файлы рабочего пространства на хосте Gateway
(например, `user@gateway-host:~/.openclaw/workspace`).
</Note>

## Связанная документация

- Первичная настройка приложения macOS: [Первичная настройка](/ru/start/onboarding)
- Структура рабочего пространства: [Рабочее пространство агента](/ru/concepts/agent-workspace)
