---
read_when:
    - Работа над элементами управления телеметрией / конфиденциальностью
    - Вопросы о том, какие данные собираются
summary: Установка телеметрии, собираемой ClawHub CLI, и как отказаться от нее.
x-i18n:
    generated_at: "2026-07-01T18:17:18Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: 906be32778baaf89e77c5350cd33ff3b975df66d8152a33fdf20c24b5c8286ce
    source_path: clawhub/telemetry.md
    workflow: 16
---

# Телеметрия

ClawHub использует минимальную телеметрию CLI для подсчета агрегированного числа установок.

## Когда собирается телеметрия

Телеметрия отправляется только когда:

- Вы вошли в систему в CLI.
- Вы запускаете `clawhub install <slug>`.
- Телеметрия **не отключена** (см. «Как отключить» ниже).

Если вы не вошли в систему, ничего не отправляется.

## Что мы собираем

При каждом учитываемом `clawhub install` CLI отправляет одно событие установки по принципу best-effort.

Событие включает:

- `slug`: slug установленного skill.
- `version`: установленная версия, если она известна.

### Что мы _не_ собираем

- Никаких путей к папкам или идентификаторов, производных от папок.
- Никакого содержимого файлов.
- Никаких журналов отдельных запусков, промптов или другого вывода CLI.

## Счетчики установок

ClawHub поддерживает агрегированные счетчики по каждому skill:

- `installsAllTime`: уникальные пользователи, которые сообщили хотя бы об одной установке skill через CLI.
- `installsCurrent`: уникальные пользователи, которые сообщили об установке и не удалили свои
  данные телеметрии.

## Прозрачность и пользовательские настройки

Все видят только **агрегированные счетчики установок**.

Удаление учетной записи также удаляет ваши данные телеметрии.

## Как отключить телеметрию

Задайте переменную окружения:

```bash
export CLAWHUB_DISABLE_TELEMETRY=1
```

Когда она задана, CLI не будет отправлять телеметрию установок.
