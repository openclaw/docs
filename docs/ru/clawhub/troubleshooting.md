---
read_when:
    - Команды CLI ClawHub или реестра OpenClaw завершаются ошибкой
    - Пакет не может быть установлен, опубликован или обновлён
summary: Устранение неполадок входа, установки, публикации, обновления и проблем API в ClawHub.
x-i18n:
    generated_at: "2026-07-01T15:29:16Z"
    model: gpt-5.5
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: fc789fcc891cf8c44b5d1a10d38a4e6dd4dec9474d8d13f8058ea1c3392a9f91
    source_path: clawhub/troubleshooting.md
    workflow: 16
---

# Устранение неполадок

## `clawhub login` открывает браузер, но не завершается

CLI запускает кратковременный локальный сервер обратного вызова во время входа через браузер.

- Убедитесь, что браузер может открыть `http://127.0.0.1:<port>/callback`.
- Проверьте правила локального брандмауэра, VPN и прокси, если обратный вызов не приходит.
- В средах без графического интерфейса создайте API-токен в веб-интерфейсе ClawHub и выполните:

```bash
clawhub login --token clh_...
```

## `whoami` или `publish` возвращает `Unauthorized` (401)

- Войдите снова с помощью `clawhub login`.
- Если вы используете пользовательский путь к конфигурации, убедитесь, что `CLAWHUB_CONFIG_PATH` указывает на
  файл, содержащий ваш текущий токен.
- Если вы используете API-токен, убедитесь, что он не был отозван в веб-интерфейсе.

## Поиск или установка возвращает `Rate limit exceeded` (429)

Прочитайте информацию о повторной попытке в ответе:

- `Retry-After`: сколько секунд ждать перед повторной попыткой.
- `RateLimit-Limit`: лимит, примененный к этому запросу.
- `RateLimit-Remaining`: ваш точный оставшийся лимит, если заголовок присутствует. При `429` он равен `0`.
- `RateLimit-Reset` или `X-RateLimit-Reset`: время сброса.

Если много пользователей используют один исходящий IP-адрес, анонимные IP-лимиты могут быть исчерпаны, даже если каждый
человек отправляет лишь несколько запросов. По возможности войдите в систему и повторите попытку после
указанной задержки.

## Поиск или установка завершается ошибкой за прокси

CLI учитывает стандартные переменные прокси:

```bash
export HTTPS_PROXY=http://proxy.example.com:3128
clawhub search "my query"
```

Поддерживаемые имена включают `HTTPS_PROXY`, `HTTP_PROXY`, `https_proxy` и
`http_proxy`.

## Skill не появляется в поиске

- Проверьте точный slug или страницу владельца, если она вам известна.
- Убедитесь, что релиз публичный и не удерживается проверкой или модерацией.
- Если skill принадлежит вам, войдите и просмотрите его:

```bash
clawhub inspect @openclaw/demo
```

Диагностика, видимая владельцу, может объяснить состояние проверки, блокировки загрузки или модерации.

## Публикация завершается ошибкой из-за отсутствия обязательных метаданных

Для Skills проверьте frontmatter в `SKILL.md`. Обязательные переменные окружения и
инструменты должны быть объявлены, чтобы пользователи и сканеры могли понять пакет.

Для плагинов проверьте метаданные совместимости в `package.json`. Публикации code-plugin
требуют полей совместимости OpenClaw, таких как `openclaw.compat.pluginApi` и
`openclaw.build.openclawVersion`.

Сначала просмотрите полезную нагрузку публикации:

```bash
clawhub package publish <source> --family code-plugin --dry-run
```

## Публикация завершается ошибкой владельца GitHub или источника

ClawHub использует идентичность GitHub и атрибуцию источника, чтобы связывать пакеты с их
издателями.

- Убедитесь, что вы вошли под учетной записью GitHub, которая владеет пакетом или может его публиковать.
- Проверьте, что URL источника публичный или доступен ClawHub.
- Для источников GitHub используйте `owner/repo`, `owner/repo@ref` или полный URL GitHub.

## Публикация завершается ошибкой, потому что пространство имен занято или зарезервировано

Если публикация завершается ошибкой, потому что handle владельца, пространство имен организации, scope пакета, slug skill
или имя пакета уже заняты или зарезервированы, сначала убедитесь, что вы
публикуете от имени владельца, соответствующего этому пространству имен. Для пакетов плагинов
scoped-имена, такие как `@example-org/example-plugin`, должны публиковаться от имени
соответствующего владельца `example-org`.

Если вы считаете, что ваша организация, проект или бренд является законным владельцем пространства имен, но
вы не можете управлять текущим владельцем ClawHub, откройте
[issue для заявки на организацию / пространство имен](https://github.com/openclaw/clawhub/issues/new?template=org-namespace-claim.yml)
с публичными, нечувствительными доказательствами. См.
[Заявки на организации и пространства имен](/clawhub/namespace-claims), чтобы узнать, какие доказательства нужны и что
не следует включать в публичные issue.

## `sync` сообщает, что Skills не найдены

`sync` ищет папки, содержащие `SKILL.md` или `skill.md`.

Укажите корни, которые нужно просканировать:

```bash
clawhub sync --root /path/to/skills
```

Сначала выполните предварительный просмотр, если не уверены, что будет опубликовано:

```bash
clawhub sync --all --dry-run --no-input
```

## `update` отказывается выполняться из-за локальных изменений

Локальные файлы не соответствуют ни одной версии, известной ClawHub. Выберите один вариант:

- Сохранить локальные правки и пропустить обновление.
- Перезаписать опубликованной версией:

```bash
clawhub update @openclaw/demo --force
```

- Опубликовать отредактированную копию с новым slug или как fork.

## Установка плагина в OpenClaw завершается ошибкой

- Используйте явный источник ClawHub:

```bash
openclaw plugins install clawhub:<package>
```

- Проверьте страницу сведений о пакете, чтобы увидеть статус проверки и метаданные совместимости.
- Убедитесь, что ваша версия OpenClaw удовлетворяет заявленному пакетом
  диапазону совместимости.
- Если пакет скрыт, удерживается или заблокирован, его может быть невозможно установить, пока
  владелец не устранит проблему.

## Запросы к публичному API завершаются ошибкой

- Соблюдайте заголовки повторной попытки `429` и кэшируйте публичные ответы списков/поиска.
- Направляйте пользователей обратно к канонической странице ClawHub.
- Не зеркалируйте скрытый, приватный, удерживаемый или заблокированный модерацией контент за пределами
  поверхности публичного API.

См. [HTTP API](/clawhub/http-api) для подробностей об эндпоинтах.
