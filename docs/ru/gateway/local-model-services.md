---
read_when:
    - Вы хотите, чтобы OpenClaw запускал локальный сервер модели, только когда выбран соответствующий поставщик модели или эмбеддингов
    - Вы используете ds4, inferrs, vLLM, llama.cpp, MLX или другой локальный сервер, совместимый с OpenAI
    - Необходимо управлять холодным запуском, готовностью и завершением работы локальных провайдеров при простое
summary: Запускайте локальные серверы моделей по требованию перед запросами OpenClaw к моделям и для создания эмбеддингов
title: Локальные службы моделей
x-i18n:
    generated_at: "2026-07-12T11:24:30Z"
    model: gpt-5.6
    postprocess_version: locale-links-v1
    provider: openai
    source_hash: a761113dd591fed0394379b2bad173165efc5e284565c652493e73d1e724529d
    source_path: gateway/local-model-services.md
    workflow: 16
---

`models.providers.<id>.localService` запускает по требованию локальный сервер моделей, управляемый провайдером. Когда запрос к модели или на создание эмбеддингов выбирает этого провайдера, OpenClaw проверяет конечную точку состояния, запускает процесс, если сервер не работает, ожидает готовности, а затем отправляет запрос. Используйте эту возможность, чтобы не держать ресурсоёмкие локальные серверы запущенными весь день.

## Как это работает

1. Для запроса к модели или на создание эмбеддингов определяется настроенный провайдер.
2. Если у этого провайдера есть `localService`, OpenClaw проверяет `healthUrl`.
3. При успешной проверке OpenClaw использует уже запущенный сервер.
4. При неудачной проверке OpenClaw запускает `command` с `args`.
5. OpenClaw опрашивает конечную точку состояния до истечения `readyTimeoutMs`.
6. Запрос проходит через обычный транспорт модели или эмбеддингов.
7. Если OpenClaw запустил процесс и задано значение `idleStopMs`, он останавливает процесс после того, как с момента завершения последнего активного запроса проходит указанное время простоя.

OpenClaw не устанавливает для этого launchd, systemd, Docker или какие-либо демоны. Сервер является обычным дочерним процессом того процесса OpenClaw, которому он впервые потребовался.

Запуск сериализуется для каждой комбинации настроенного провайдера, команды, аргументов и переменных окружения, поэтому одновременные запросы чата и эмбеддингов к одной службе не создают дублирующиеся серверы. Каждый запрос удерживает собственную аренду до завершения обработки ответа, поэтому остановка при простое ожидает завершения всех активных запросов к моделям и на создание эмбеддингов. Настроенные псевдонимы провайдеров остаются отдельными: два псевдонима могут указывать на разные узлы с GPU, не объединяясь под одним идентификатором адаптера Ollama, LM Studio или совместимого с OpenAI.

Если у другого процесса OpenClaw уже есть исправный сервер по тому же адресу `healthUrl`, текущий процесс повторно использует его, но не принимает под своё управление (каждый процесс управляет только тем дочерним процессом, который запустил сам). Журналы запуска и завершения содержат ограниченные по объёму, отредактированные окончания вывода дочернего процесса, а также сведения о времени выполнения и завершении; настроенные значения переменных окружения никогда не выводятся.

## Структура конфигурации

```json5
{
  models: {
    providers: {
      local: {
        baseUrl: "http://127.0.0.1:8000/v1",
        apiKey: "local-model",
        api: "openai-completions",
        timeoutSeconds: 300,
        localService: {
          command: "/absolute/path/to/server",
          args: ["--host", "127.0.0.1", "--port", "8000"],
          cwd: "/absolute/path/to/working-dir",
          env: { LOCAL_MODEL_CACHE: "/absolute/path/to/cache" },
          healthUrl: "http://127.0.0.1:8000/v1/models",
          readyTimeoutMs: 180000,
          idleStopMs: 0,
        },
        models: [
          {
            id: "my-local-model",
            name: "My Local Model",
            reasoning: false,
            input: ["text"],
            cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
            contextWindow: 131072,
            maxTokens: 8192,
          },
        ],
      },
    },
  },
}
```

Задайте `timeoutSeconds` в записи провайдера (не в `localService`), чтобы медленный холодный запуск и длительная генерация не достигали стандартного тайм-аута запроса к модели. Явно задавайте `healthUrl`, если ваш сервер предоставляет сведения о готовности не по пути `/models` относительно базового URL.

## Поля

| Поле             | Обязательно | Описание                                                                                                                                    |
| ---------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `command`        | да          | Абсолютный путь к исполняемому файлу. Поиск через shell PATH не выполняется.                                                                |
| `args`           | нет         | Аргументы процесса. Подстановка оболочки, каналы, шаблоны имён файлов и обработка кавычек не поддерживаются.                                 |
| `cwd`            | нет         | Рабочий каталог процесса.                                                                                                                   |
| `env`            | нет         | Переменные окружения, объединяемые с окружением процесса OpenClaw с приоритетом заданных значений.                                           |
| `healthUrl`      | нет         | URL проверки готовности. По умолчанию к `baseUrl` добавляется `/models` (`http://127.0.0.1:8000/v1` превращается в `http://127.0.0.1:8000/v1/models`). |
| `readyTimeoutMs` | нет         | Крайний срок ожидания готовности при запуске. По умолчанию: `120000`.                                                                        |
| `idleStopMs`     | нет         | Задержка остановки запущенного OpenClaw процесса при простое. Значение `0` или отсутствие поля сохраняет процесс запущенным до завершения OpenClaw. |

## Пример Inferrs

Inferrs — пользовательский бэкенд `/v1`, совместимый с OpenAI, поэтому тот же API `localService` работает с записью провайдера `inferrs`:

```json5
{
  agents: {
    defaults: {
      model: { primary: "inferrs/google/gemma-4-E2B-it" },
    },
  },
  models: {
    mode: "merge",
    providers: {
      inferrs: {
        baseUrl: "http://127.0.0.1:8080/v1",
        apiKey: "inferrs-local",
        api: "openai-completions",
        timeoutSeconds: 300,
        localService: {
          command: "/opt/homebrew/bin/inferrs",
          args: [
            "serve",
            "google/gemma-4-E2B-it",
            "--host",
            "127.0.0.1",
            "--port",
            "8080",
            "--device",
            "metal",
          ],
          healthUrl: "http://127.0.0.1:8080/v1/models",
          readyTimeoutMs: 180000,
          idleStopMs: 0,
        },
        models: [
          {
            id: "google/gemma-4-E2B-it",
            name: "Gemma 4 E2B (inferrs)",
            reasoning: false,
            input: ["text"],
            cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
            contextWindow: 131072,
            maxTokens: 4096,
            compat: { requiresStringContent: true },
          },
        ],
      },
    },
  },
}
```

Замените `command` результатом выполнения `which inferrs` на компьютере, где работает OpenClaw. Полная настройка inferrs: [Inferrs](/ru/providers/inferrs).

## Пример ds4

```json5
{
  models: {
    providers: {
      ds4: {
        baseUrl: "http://127.0.0.1:18000/v1",
        apiKey: "ds4-local",
        api: "openai-completions",
        timeoutSeconds: 300,
        localService: {
          command: "<DS4_DIR>/ds4-server",
          args: [
            "--model",
            "<DS4_DIR>/ds4flash.gguf",
            "--host",
            "127.0.0.1",
            "--port",
            "18000",
            "--ctx",
            "32768",
            "--tokens",
            "128",
          ],
          cwd: "<DS4_DIR>",
          healthUrl: "http://127.0.0.1:18000/v1/models",
          readyTimeoutMs: 300000,
          idleStopMs: 0,
        },
        models: [],
      },
    },
  },
}
```

Полная настройка, выбор размера контекста и команды проверки: [ds4](/ru/providers/ds4).

## Связанные материалы

<CardGroup cols={2}>
  <Card title="Локальные модели" href="/ru/gateway/local-models" icon="server">
    Настройка локальных моделей, выбор провайдера и рекомендации по безопасности.
  </Card>
  <Card title="Inferrs" href="/ru/providers/inferrs" icon="cpu">
    Запуск OpenClaw через локальный сервер inferrs, совместимый с OpenAI.
  </Card>
</CardGroup>
